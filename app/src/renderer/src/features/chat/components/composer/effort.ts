import type { EffortLevel } from '../../../../../../shared/ipc'

export const EFFORT_LABELS: Record<EffortLevel, string> = {
  low: '낮음',
  medium: '중간',
  high: '높음',
  xhigh: '매우 높음',
  max: '최대'
}
