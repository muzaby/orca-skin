// Plugin is the UX umbrella term (meaning A). See docs/GLOSSARY.md §Plugin.
import { ExtensionsCatalogView } from '../features/skills'

export function PluginsPage(): React.JSX.Element {
  return <ExtensionsCatalogView />
}
