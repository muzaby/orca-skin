-- 0149 — 턴별 모델의 컨텍스트 윈도우(SDK 실측) 영속.
--
-- 0134 가 도넛 분모의 1차 소스를 SDK 실측(telemetry.contextWindow)으로 바꿨지만 그 값을
-- 저장하지 않아, 세션 재로드 경로는 렌더러의 모델명 하드코딩 목록으로 되돌아갔다. 그 목록은
-- 새 모델이 나올 때마다 손으로 갱신해야 했고, 갱신 전까지 재로드된 세션의 분모가 틀렸다.
-- 실측값을 여기 남기면 복원 경로가 라이브와 같은 숫자를 쓴다.
--
-- NULL 허용 = 이 마이그레이션 이전 행(실측 미보유)은 종전 폴백으로 자연 강등된다.
ALTER TABLE turn_model_usage ADD COLUMN context_window INTEGER;
