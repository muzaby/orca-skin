# Plan — <slug>

> 템플릿. `docs/handoff/<NNNN-slug>/plan.md` 로 복사해 작성한다.
> **작성 절차·검증 가능성 게이트는 [`SKILL.md`](SKILL.md)** (같은 디렉토리), 협업 규칙·상태 머신은
> [`docs/handoff/AGENTS.md`](../../../docs/handoff/AGENTS.md).
> 흐름: **의도 → 조사 → 설계 → 리스크**. 각 섹션의 작성 유도 질문을 채우고, 안 쓰는 줄은 지운다.

## 메타

| 항목 | 값 |
|---|---|
| slug | `<NNNN-slug>` |
| 작성자 | Claude Code |
| 일자 | YYYY-MM-DD |
| 매핑 | PHASES 행 / PR (있으면) |
| 상태 | DRAFT → READY |

## 사용자 의도 / 요구 출처 (Intent & Provenance)

> "사용자가 말한 것"과 "내가 해석한 것"을 **가른다**.

| 구분 | 내용 | 출처 |
|---|---|---|
| 명시 요구 | 사용자가 직접 말한 것 | `@chats/chatN.md` 결정 키워드("확정"/"A로 진행") / **라이브 세션 요청**(영속 파일 없으면 요청 요약을 인용) |
| 추론 의도 | 위에서 내가 *해석*한 것 (추론임을 표기) | (근거) |

## Context (왜)

이 작업이 필요한 배경 — 해결하려는 문제/요구, 의도한 결과.

## 요구 비판적 검토 (수석 엔지니어 관점)

> 설계 전에 요구 자체를 따진 결과(SKILL.md 관문 0). **동의도 적는다** — "따져봤고 타당하다" 가
> 정보다. 이견이 있어도 **요구 범위는 줄이지 않는다**; 우려를 적고 그대로 진행한다.
> 문서에선 결론이 앞에 오지만 **채우는 순서는 아래 §자료조사 다음**이다 — 인상으로 판단하지 않고
> 근거 칸을 `파일:라인` 으로 채운다.

| 질문 | 판단 | 근거 (`파일:라인` · `@docs/…`) |
|---|---|---|
| 이 요구가 진짜 문제를 겨냥하는가 (증상 ↔ 원인) | 타당 / 전제 정정 / 이견 | … |
| 이미 있는 것 아닌가 (기존 코드로 충족되나) | … | … |
| 더 작은 해법이 있는가 (구조 변경 없이 되나) | … | … |
| 인용 자료가 요구를 부풀리지 않았나 | … | … |
| 기존 채택 결정을 뒤집는가 | … | (아래 §기존 결정·규칙과의 관계) |

- **사용자에게 올릴 것**(단독 결정 불가): … (없으면 "없음")

## 자료조사 (Research)

> 인수 기준·설계가 *이 조사에 근거*하도록. **모든 발견에 레퍼런스를 명시한다. 레퍼런스 없는 주장은 적지 않는다**(추측은 리스크/Open Question 으로).

| 발견 / 제약 | 레퍼런스 |
|---|---|
| … | 내부 문서 `@docs/TRD.md §6` · `@docs/arch/backend/…` · `@chats/chat2.md` |
| … | 코드 `app/src/…:line` |
| … | 외부 **웹 URL**(SDK 문서·이슈·블로그) 또는 원문 미러 `@docs/spec/…` |

## 인수 기준 (Acceptance Criteria)

> verify 가 1:1 로 대조한다. **`검증 수단` 칸을 비워둔 채 READY 로 넘기지 않는다** — 채울 수
> 없으면 기준이 잘못 쓰인 것이다(SKILL.md 관문 2). 기계 검증이 불가능하면 "런타임 실기 — 사람"
> 이라고 *명시*한다. 부정형("~불변") 금지 — **양성 단언**으로 쓴다.

| # | 인수 기준 | 검증 수단 (`파일::케이스` 또는 "사람 실기 — 실행 경로") | 프로덕션 도달 경로 |
|---|---|---|---|
| 1 | … | `app/src/…/x.test.ts::"…"` | 이 동작에 닿는 진입점 (예: `handlers/x.ts` → `Y.method`) |
| 2 | … | … | … |

> `프로덕션 도달 경로` 칸이 "테스트뿐" 이면 그 AC 는 기능을 보장하지 않는다 — 테스트는 통과하는데
> 실제 경로가 그 코드를 우회할 수 있다(SKILL.md 관문 2 규칙 1-b).

## 범위 / 비범위

- **범위**: …
- **비범위**: … (Open Question 소관·후속 핸드오프로 미룰 것)

| 미룬 항목 | 나중에 하면 더 비싼가 (일방향인가) |
|---|---|
| … | 아니오 — 되돌릴 수 있음 / **예 — 이름·식별자·스키마가 걸림 → 지금 사용자에게 물음** |

## 의존 기술 / 전제 (Dependencies & Assumptions)

- 구현이 기댈 SDK 옵션·라이브러리·기존 모듈: …
- 전제 조건: …
- **신규 의존성** (있으면): … → **사용자 승인 필요** (단독 결정 금지, Open Question 연결).

## 설계

- 접근 방법.
- **재사용할 기존 함수·유틸·파일 경로** (예: `src/main/ipc/router.ts`, `features/chat/hooks/useChat.ts`).
- 레이어 경계(`app`/`pages`/`features`/`shared`, main DAG) 준수 방법 — 신규 타입·모듈마다
  **어느 레이어에 두고 누가 import 하는가**.

| 신규 모듈 | 책임 | 레이어 | 테스트 방법 |
|---|---|---|---|
| … | … | … | 순수 단위 / electron·DB 의존 시 **떼어낼 순수부** 명시 |

> "테스트 불가" 로 적으려면 *무엇을 떼면 가능한가* 를 먼저 답한다(SKILL.md 관문 3).

## 기존 결정·규칙과의 관계

> 이번 변경이 **뒤집는** 채택 결정과, 설계 입력으로 읽은 **저장소 규칙**을 적는다.
> 없으면 `N/A`. (SKILL.md 관문 4 · P10 · 관문 3 P7)
>
> **이 표는 본문(§설계·§파생 UX·§범위)을 다 쓴 뒤 마지막에 채운다.** 의도 기준으로 먼저 채우면
> 나중에 쓴 절이 표 밖에서 결정을 뒤집는다. 각 행마다 **본문 어느 문장이 이 결정을 건드리는지**
> 찾아 그 문장 기준으로 판정하라. 출처에는 문서뿐 아니라 **코드 주석·계약 헤더**도 포함된다.

| 기존 결정 / 규칙 | 출처 | 본문에서 건드리는 문장 | 이번 변경 |
|---|---|---|---|
| … | `git log`·AGENTS.md·`docs/arch/…`·`eslint.config.mjs`·**코드 주석/계약 헤더**·위생 테스트 | §… 의 "…" | 유지 / 뒤집음(근거) |

## 파생 UX / 엣지케이스 (Derived UX & Edge Cases)

> 사용자가 직접 말하지 않았지만 요구에서 *따라오는* 것. **관련 없으면 `N/A` 로 적고, 아래 예시를 그대로 복붙하지 말 것**(이 작업에 실제로 해당하는 것만).

- 상태: 로딩 · 에러 · 빈 상태 · 동시성/멀티세션 · …
- 상호작용: 키보드 · 접근성(a11y) · 테마 3종 · …
- 엣지케이스: …

## 리스크 / 트레이드오프 (Risks & Trade-offs)

> 이 작업 고유의 리스크만. 일반론·예시 복붙 금지 — 없으면 `N/A`.

| 리스크 / 트레이드오프 | 완화책 / 결정 |
|---|---|
| … | … |

- 되돌리기 어려운 결정: …
- **단독 결정 금지 항목(Open Question)** → 사용자에게: …

## 영향 받는 파일

- `app/...`
- …

## 참고 문서

- `docs/TRD.md §…`
- `docs/arch/…`
- IPC 변경 시: `docs/IPC_CONTRACT.md` (§6 변경 절차 — **반드시 동시 갱신**)

## 게이트

- 통과 필요: `cd app && npm run lint && npm run typecheck && npm test`.
- 신규 테스트 요구: … (어댑터 정규화 / reducer / IPC 스키마 / 순수 변환기).

## 설계 self-review 체크리스트 (READY 전)

> 모두 ✅ 가 된 뒤에야 `plan/READY` 로 표기한다.

- [ ] 사용자 의도 — 명시 요구를 출처(`@chats/…` 또는 라이브 세션 요청)로 인용했고, 추론은 추론으로 표기했다.
- [ ] 자료조사 — 모든 발견에 레퍼런스(`@docs/…`·`파일:라인`·웹 URL)를 붙였다.
- [ ] 의존 기술 — 의존·전제를 식별했고, 신규 의존성은 사용자 승인 항목으로 표기했다.
- [ ] 파생 UX — 라이프사이클(quit/crash)·취소·전이 등 이 작업에 실제로 해당하는 엣지케이스를 펼쳤다.
- [ ] 리스크 — 트레이드오프·되돌리기 어려운 결정을 적고, Open Question 은 사용자로 분리했다.

**기계적으로 확인 가능한 것 — 반드시 본다** (SKILL.md §READY 전 self-review):

- [ ] **요구 비판적 검토** 표의 다섯 질문에 답했고, 이견을 적었더라도 **요구 범위를 임의로 줄이지 않았다**
- [ ] 인수 기준의 **`검증 수단` 칸이 하나도 비어 있지 않다** (불가 항목은 "사람 실기" 로 *명시*)
- [ ] 부정형/"불변" 기준 **0개** (전부 양성 단언)
- [ ] **AC 끼리 모순 없음** — 각 AC 를 다른 AC 와 짝지어 훑었다 (자기 산출물이 자기 AC 를 위반하지 않는지 포함)
- [ ] 인용 수치를 **이번 세션에서 직접 측정**했다 (승계한 숫자 0개, 내역 합 = 총계 검산)
- [ ] 신규 모듈마다 테스트 방법이 있고, electron/DB 의존이면 **순수부 seam** 이 설계에 있다
- [ ] 전수 조사 대상에 **N 수치**가 적혀 있다
- [ ] 각 AC 에 **프로덕션 도달 경로**가 있다 (유일한 호출자가 테스트인 AC 0개)
- [ ] "사람 실기" AC 마다 **실행 경로**가 있고, 그 경로가 자기 비범위에 막혀 있지 않다
- [ ] 선택적 필드로 판정하는 곳마다 **미지정 케이스 AC** 가 있다
- [ ] 소비하는 계약의 제약 필드마다 **강제 지점**(누가·언제)이 설계에 있다
- [ ] 참조 구현을 입력으로 썼다면 계약의 **union/enum 전수** 대비 커버리지를 표시했다
- [ ] 미룬 항목마다 **일방향 여부**에 답했다 (§범위의 유예 표)
- [ ] **관문 4 를 본문 완성 후 돌렸다** — 기존 결정 표를 본문 훑으며 채웠고, 인용 경로가 실제로
      열리며, 아래 `[구현자 기입]`·`[검증자 기입]` 블록이 문서에 남아 있다
- [ ] "확정돼 있다"·"채택 결정이다" 로 쓴 것마다 **문서 앵커(`§표제어`)를 grep 해 존재를 확인**했다

---

> **[구현자 기입]** 이하는 구현 턴에서 채운다 (Codex=기능 / Claude=비기능). 설계자(Claude)는 위쪽을 쓰고, 구현자는 이 블록만 추가한다(공유 파일 충돌 회피).

## [구현자 기입] 설계 리뷰 (비판적)

> 설계를 **실무 관점에서 비판적으로** 검토한 결과. 동의만 적지 말고 *현실성·구멍*을 짚는다. plan 섹션을 인용(예: "의존 기술 §…").

- 동의 / 그대로 진행: …
- 이견 / 우려: … (어느 섹션의 무엇이 비현실적인가)

## [구현자 기입] 놓친 잠재 문제 + 대응 (선조치 후보고)

> 설계가 다루지 못한 문제와 구현 중 취한 대응. **선조치 경계**: 구현 세부·놓친 엣지케이스·명백한 누락/버그는 **구현하고 보고**(✅). Open Question·신규 의존성·제품 의도·인수 기준(설계) 변경은 **보고만**(⚠️) 하고 사용자/설계자 결정에 맡긴다. **의심되면 ⚠️ 보고만**(보수적 기본값).

| # | 놓친 문제 | 대응 | 근거 |
|---|---|---|---|
| 1 | … | ✅ 구현함 / ⚠️ 보고만·**결정 필요** | … |

## [구현자 기입] 구현 체크리스트

- [ ] …
- [ ] …

## [구현자 기입] 구현 보고

| 항목 | 내용 |
|---|---|
| 변경 파일 | … |
| 실행 명령 | `npm run lint` / `typecheck` / `test` |
| 게이트 결과 | lint ✅ / typecheck ✅ / test ✅ (N passed) |
| 블로커 / 역질문 | (없으면 "없음") |
| 대상 커밋 | `<hash>` |

---

## [검증자 기입] 파생 이슈 (Derived Issues)

> verify 가 **미해결**로 판정한 문제를 라운드를 넘겨 추적하는 챕터. 기본은 비어 있고, `verify/FAIL` 시 **검증자(Claude)가** 신설해 채운다(plan.md 는 검증자가 쓴다). 항목은 구현자 코멘트를 참조하거나, 사용자 결정이 필요하면 **검증자가 사용자 결정을 대리 기록**한다 → 다음 구현 턴이 여기서 이어간다.

| # | 이슈 | 출처 | 대응 방향 | 상태 |
|---|---|---|---|---|
| D1 | … | 구현자 코멘트 §… / 사용자 / verify r<N> | … | open / 구현중 / 해결 |
