# Verify — 0189-0188-post-audit

> 검증 절차는 [`.agents/skills/handoff-verify/SKILL.md`](../../../.agents/skills/handoff-verify/SKILL.md), 협업/상태 머신은 [`../AGENTS.md`](../AGENTS.md).
>
> **이 문서는 r2 검증이다.** r1 검증(FAIL) 전문은 아래 [부록 — r1 검증 (FAIL)](#부록--r1-검증-fail) 에 보존한다 — 판정을 조용히 덮지 않는다(D-009 취지).

## 메타

| 항목 | 값 |
|---|---|
| slug | `0189-0188-post-audit` |
| 검증자 | Claude Code |
| 일자 | 2026-08-16 |
| 대상 커밋/range | `702cd3c..5a4176d` (`19baf21` r2 재구현 · `5a4176d` 보드 커밋 해시) |
| 구현 전 plan 기준 | `702cd3c` (verify FAIL r1 커밋) — **이번 라운드는 기준선 잠금이 diff 로 작동한다** |
| 라운드 | 2 |
| 상태 | **PASS** |
| 자기 검증 여부 | **그렇다.** 0188 구현 · 0189 설계 · 0189 구현(r1·r2) · 0189 검증(r1·r2) 이 전부 Claude Code. 이 한계는 감사 자신이 `audit.md` 머리에 적었고 이 문서도 승계한다 |

**한 줄 판정**: r1 FAIL 3건(D1·D2·D3)이 **전부 닫혔고**, 권고였던 D4·D5 도 함께 정리됐다. AC **15/15**, §10 강제 지점 **23/23 지점 · 8/8 행**(구현자 소관 22 + verify 몫 1). 이번 라운드의 자기보고(`Criteria-Met: 15/15` · 강제 지점 22/22)는 **재측정에서 과대가 아니었다** — r1·0187 r1 에서 두 번 반복된 과대 보고 증상이 재발하지 않았다. 남은 3건은 전부 **비차단 문서 위생**이며 어떤 판정도 뒤집지 않는다.

---

## 0. 기준선 / plan 변경 확인

r1 은 `9a2980a` 가 plan 과 audit 을 **같은 커밋**에 만들어 §0 의 자기 증명 방지 장치가 작동하지 않았다. **이번 라운드는 다르다** — 구현 전 상태가 `702cd3c` 로 고정돼 있어 diff 로 채점할 수 있다.

- **AC 변경**: **없음.** `§7 Acceptance Criteria` 절 전체가 `702cd3c` 와 **바이트 동일**(`diff` 빈 출력). 구현자가 자기 산출에 맞춰 AC 를 완화·재작성하지 않았다.
- **Decision Ledger 변경**: **없음.** `§3` 절 전체가 `702cd3c` 와 바이트 동일. D-001~D-009 전부 ACTIVE, SUPERSEDED 0.
- **Product/UX Contract 변경**: 없음(§1~§6 무변경).
- **구현자가 고친 plan 은 3종이고 전부 provenance 가 있다**:

| plan 변경 | 방향 | 판정 |
|---|---|---|
| §10 에 8번째 강제 지점 신설(결론 사본의 정합) | **계약을 조인다** | ✅ 정당. verify r1 §5 가 "표에 없는데 같은 불변식이 필요한 지점 1건" 으로 지목한 바로 그 자리다 |
| §14 인용 상한에 "인과 사슬" 예외 신설 | **자기 상한을 푼다** | ✅ 절차상 정당(verify r1 D5② 가 "상한 문언을 고치는 쪽을 권한다" 로 명시). **다만 푼 문언이 산출을 여전히 못 덮는다** → [D-B](#13-파생-이슈-비차단) |
| 수치 2건 정정(63→62 · 22→20) 5곳 | 사실 정정 | ✅ 검증자 지적을 구현자가 **재측정으로 재현**한 뒤 고쳤다. 나도 다시 셌다(§7) |

- **감사 대상(0188)의 Ledger/AC 변경**: **없음.** `git diff --stat 9a2980a^..HEAD -- docs/handoff/0188-*/` **빈 diff**.
- **채점에 사용할 원 기준**: `plan.md §7` AC1~AC15 **원문** + `§10` 강제 지점 표(r2 기준 8행). AC 가 왜 그 모양인지는 채점하지 않는다.

## 1. Product & UX / ACTIVE Decision 요약

산출물이 문서라 런타임 소비자가 없다. 소비자는 **다음 유지보수자**이고 흐름은 `INDEX 보드 → audit.md → 판정 4줄 → 표 → 파일:줄 대조 → 0190 결정` 이다.

| Decision | 기대 결과 | 실제 (r2) |
|---|---|---|
| D-001 (산출물 전용, `app/**` 0) | 코드 무변경 | ✅ 전 범위 `9a2980a^..HEAD` app diff **빈 출력** |
| D-002 (볼륨 축 포함) | 경량화가 볼륨으로도 채점됨 | ✅ 축 4 6행 수치표 — 11종 전부 재측정 일치(§7) |
| D-003·D-005 (이탈 3건 = 시정 필요, 실행은 범위 밖) | 문제·원인·방안 3열, 코드는 그대로 | ✅ |
| D-004 (U1·U3 기록만) | 조치 없이 기록 | ✅ 회귀표 방안 열 머리가 `(D-004: 이번엔 기록만)` |
| **D-006 (실측/연역 구분)** | 연역 항목이 그렇게 표기됨 | ✅ **r1 ❌ → r2 해소.** §근거의 두 종류 + 연역 전수표 5행 + 규칙("표에 없으면 실측"). 전칭 문구 1건은 [D-A](#13-파생-이슈-비차단) |
| D-007 (0188 D39/D40 중복 명시) | 매핑 문장 존재 | ✅ 0188 `verify.md:238`(D39)·`:239`(D40) 원문과 대조 일치 |
| **D-008 (3층 채점)** | 축 1·3 이 3층을 거치고 층이 밝혀짐 | ✅ **r1 ⚠️ → r2 해소.** 축 1 `:85` · 축 3 `:196` 둘 다 `층` 열 |
| **D-009 (조용히 덮지 않음)** | 철회·정정이 초안 주장과 함께 남음 | ✅ **r1 ⚠️ → r2 해소.** `audit.md` 정정 이력 4개 절 + **보드에도 "무엇을 왜 지웠는가" 를 남겼다** |

### end-to-end 흐름

```text
다음 유지보수자
  → docs/handoff/INDEX.md 0189 행            ✅ 철회본 0벌 · 정정 note 존재 · 대상 커밋 해시
  → docs/handoff/0189-.../audit.md
  → 축별 판정 4줄 (75·130·175·216)            ✅
  → 근거의 두 종류 (실측/연역) 표             ✅ r2 신설
  → 축별 표 → 파일:줄                         ✅ 51/51 내용 대조
  → 부록 A (36불릿 전수 매핑)                  ✅ r2 신설 · F4 ↔ 부록 양방향 링크
  → 정정 이력 (철회2·정정3·강화3 + 수치2)      ✅
  → 0190 시정 결정
```

**r1 이 "이 흐름의 첫 칸이 어긋난다" 고 적은 자리가 이번에 맞춰졌다.**

## 2. 구현 결과 비판적 검토 — AC 전에

| 질문 | 판정 | 근거 |
|---|---|---|
| 실환경 실패 방식 | 인용 부정확 1종뿐 | 문서 산출물. 저장소 쓰기·동시성·부분 실패 없음 |
| **false success 가능성** | **이번엔 나지 않았다** | r1 은 자기보고 ✅ 가 문서 표식 부재를 덮었다. r2 는 §10 전수표에 `재현 명령` 열을 만들고 행마다 관측값을 적었다 — 나도 8행을 전부 다시 실행해 재현했다(§5) |
| partial failure / 두 사본 정합 | **해소** | 결론이 `audit.md`·INDEX 두 곳에 사는 구조는 그대로지만, r2 가 이것을 §10 #8 강제 지점으로 올리고 4종 문서를 전수 조사했다. 나도 저장소 전체에서 옛 수치(`63건`·`22행`)의 잔존 사본을 훑어 **0건** 확인 |
| Product/UX 의 A 가 아닌 B | 아니다 | r1 이 "형식을 요구했는데 내용만 산문에 담았다" 로 지적한 두 건이 **형식으로** 올라왔다(연역 표·층 열) |
| 증상만 제거하고 상태가 남았는가 | **아니다** | 뒷벌 삭제가 정보 손실이 되지 않도록 뒷벌 전용 사실 1건(`deployment/` 529줄 중 실행 130 · `runtime-config.ts` 266줄 도달 불가)을 앞벌로 **옮긴 뒤** 지웠다 — 보드에서 2회 등장 실측 확인 |
| 정정이 판정을 흔들었는가 | 아니다 | 63→62·22→20 은 "전부 미위반/전부 승계" 결론의 **분모**다. 20행 전부 미위반 · Decision 대조 결과 동일을 내가 다시 확인(§7) |
| 출력 상한 | **초과 3건(비차단)** | §14 는 발견당 인용 ≤3(인과 사슬 예외)인데 F1 **5** · F2 **5** · U1 **5**. 초과분이 전부 2·3층 근거라 독자에게 이롭고 판정 영향 0 → [D-B](#13-파생-이슈-비차단) |

## 3. 역방향 탐색

```bash
$ bash .agents/skills/handoff-verify/scripts/scan-surface.sh 702cd3c..HEAD
변경된 소스 파일이 없습니다 (범위: 702cd3c..HEAD, 루트: app/src)
```

코드 diff 가 0 이라 스크립트 축은 비대상이다. 문서 산출물의 등가 표면 — **새로 만든 것에 소비자가 있는가 · 옛 사본이 살아남았는가 · 링크가 닿는가** — 를 직접 걷었다.

| 후보 | 판정 | 근거 |
|---|---|---|
| **부록 A(신설)의 소비자** | 정상 | `audit.md:114` F4 본문이 부록을 가리키고, 부록 `:325`·`:386` 이 역방향으로 F4·F2 를 가리킨다. 양방향 도달 |
| **연역표(신설)의 소비자** | 정상 | §근거의 두 종류가 규칙을 세우고 P1·P2·P3~P6·U1·U3 본문이 그 표의 항목이다 |
| **옛 판정 사본의 잔존** | **0건** | `grep -rn "ACTIVE Decision 63\|Decision 63건\|금지표 22행"` on `docs/`·`.agents/` → verify r1 문서(검증자 소유 이력) 외 **0건** |
| **보드 철회본** | **0벌** | `부분 회귀 3건` 0 · `금지표 22행` 0 · `ACTIVE Decision 63` 0. `제안 비범위 침범`·`정보성` 2건씩 남지만 **전부 "r2 가 철회했다" 를 서술하는 문맥**이고 현재형 주장이 아니다(육안 확인) |
| **문서 내 앵커 링크** | **11/11 해결** | `audit.md`·`plan.md` 의 `#anchor` 11개를 헤딩에서 slug 생성해 대조 — **미해결 0**. r2 신설 4개(`#부록-a…`·`#f4…`·`#채점은-3층이다…`·`#정정-이력`) 포함. `check-doc-inventory` 는 상대 **파일** 링크만 보고 앵커는 보지 않으므로 게이트가 못 보는 축이다 |
| **INDEX-history 의 0189 언급** | 사본 아님 | 0188 행이 자기 리뷰 노이즈("다른 plan 초안 · 0189/0190 분할")를 적은 것뿐. 0189 결론의 복제 아님 — 구현자 주장과 일치 |
| **plan 자기 재진술 3곳** | 정합 | §갱신 메모(`:55`)·§16(`:370`)·놓친 문제 #5(`:537`) 전부 62건으로 정정됨. §9.2(`:195`)·§9.5(`:297`)도 20행 |

**역방향에서 새로 나온 판정 뒤집기**: **0건.** r2 가 새로 실측한 주장(U1 3층 baseline)도 내가 baseline 코드에서 독립 확인했다(§4).

## 4. 기존 테스트 / semantic 검증 확인

- 코드를 만들지 않아 단위 테스트 축이 없다. plan §11 이 **AC2 의 인용 전수 재확인**을 기계 검증의 대체물로 지정했다.
- **구현자의 재확인 방법은 AC 문언보다 약하다.** §10 #1 의 `재현 명령` 은 "파일 해결 → 해당 줄 **비어있지 않음** 확인" 인데, AC2 는 "그 줄이 **실제로 해당 내용을 가리킨다**" 를 요구한다. 이것은 structural proxy 다 — 그래서 **검증자가 51건의 줄 내용을 전부 읽어 주장과 대조했다**(§7).
- **r2 가 새로 실측했다고 한 것을 baseline 코드에서 재현**:

| r2 의 새 주장 | 독립 확인 | 결과 |
|---|---|---|
| `ad10f6c:features/providers/auth/login.ts:114` 의 `revoke()` 가 **실패를 삼키고** 상태를 바꿔 행을 '연결 안 됨' 으로 떨어뜨렸다 | 3홉 추적: `login.revoke()` 는 `store.revoke()` 반환을 **읽지 않고** `onChange?.()` 를 부른다 → `store.revoke()` 는 `grants.delete`·`verified.delete` **뒤에** `flush()` → `createRecordPersistence.save()` 가 쓰기 실패를 `catch` 해 `onUnavailable` 만 부르고 **되던지지 않는다**(`ad10f6c:store-file.ts:159-163`) | ✅ **성립.** 메모리 상태는 이미 바뀌었고 실패는 어디에도 표면화되지 않는다 |
| 따라서 U1 의 **실패 경로 자체가 0188 이 만든 것**(3층 신규) | 현재 `login.ts:213-228` 주석이 같은 사실을 반대편에서 적는다("r8 은 `store.revoke()` 의 영속 결과를 버리고 무조건 `revoked` 를 냈다"). `D-061` 이유 칸도 동일 | ✅ **성립** |
| U1 의 2층 승인 = `D-061`, 출처 `r8 재리뷰 §2 + 실측 재현` | `0188/plan.md` D-061 행 **출처 칸 원문과 문자 일치** · 상태 ACTIVE | ✅ **성립** (AC14) |
| U2 의 2건이 외부 리뷰 → 수용(D36) | `0188/plan.md:775` D36 행 = "PR #336 리뷰 P2 + `GLOSSARY.md:70` 실측 / ✅ **해결** — 가드 메시지 2건은 Harness 어휘로" | ✅ 성립 |

- **structural proxy 만으로 통과한 AC**: **없다.** AC3·AC12 는 이번에 형식(표·열)과 내용(층 판단의 근거)이 **둘 다** 있고, 각 층 라벨을 코드/Ledger 에서 되짚어 확인했다.

## 5. 요구사항 충족 매트릭스

> 자기보고 `Criteria-Met: 15/15` 는 증거로 쓰지 않았다. 아래는 검증자가 다시 센 결과 — **15 ✅ / 0 ❌**. (r1 은 자기보고 15/15 에 재측정 13/15 였다.)

| # | 기준 | 결과 | 검증 증거 |
|---|---|---|---|
| AC1 | 축별 한 줄 판정 4건 | ✅ | `grep '^\*\*판정'` → **75·130·175·216** |
| AC2 | 인용 `파일:줄` 이 현재 트리에서 실제 내용을 가리킴 | ✅ | 고유 인용 **51건**(검증자 독립 추출도 51) 전수 해결 → **줄 내용을 claim 과 대조 · 어긋남 0**. 자동 해결 48 + 수동 4(`0188/plan.md:452`·`:415` · `ad10f6c:provider-config.ts:11-14` · `ad10f6c:model-resolve.ts:51` · `ad10f6c:login.ts:114`) 는 `git show`/직접 열람으로 확인. 느슨한 2건은 블록 내부라 재현 가능(`prepared-config.ts:153` 주석 머리, 근거 문장은 `:155-157` / `useProviderGate.ts:48` 은 `run()` 본문 첫 줄) |
| AC3 | 실측/연역 구분 표기 + 폐쇄망 조건 명시 | ✅ | **r1 ❌ → 해소.** §근거의 두 종류(`:20`) + **연역 전수표 5행**(P1·P2·P3~P6·U1·U3) + 규칙("표에 없으면 실측"). 발견 13건 중 런타임 추론 항목 **누락 0**. 폐쇄망 조건은 표 `조건` 열(P2 "폐쇄망 형상 한정" · P6 "동적 배포 한정") + 본문 `:133`·`:149`. 전칭 문구 1건은 비차단 [D-A](#13-파생-이슈-비차단) |
| AC4 | 이탈 3건 문제·원인·방안 3열 + 코드 무변경 | ✅ | 축 1 표 헤더 `\| # \| 층 \| 문제 \| 원인 \| 방안 \|`(`:85`) 3행 + app diff 빈 출력 |
| AC5 | 낭비형/대가형 2표 + 대가형에 되살아나는 결함 병기 | ✅ | `### 낭비형`(`:144`) · `### 대가형`(`:151`). 대가형 4행 전부 `맞바꾼 것` 열 + 승인 Decision ID(D-020·D-060·D-056·D-038) |
| AC6 | 경량화 미달 + 수치 + **반대 논거** | ✅ | 판정 `:216` + 6행 수치표(전부 재측정 일치) + `### 반대 논거`(`:233`) 2항목이 실질(강건화 5종 · LOC 목표 부재) |
| AC7 | 0188 D39·D40 중복 명시 | ✅ | 종합 절 표 — D39↔U1 "동일 항목의 재확인" · D40 "범위 밖". 0188 `verify.md:238`·`:239` 원문 대조 |
| AC8 | `app/**` 무변경 | ✅ | `git diff --stat 9a2980a^..HEAD -- app/` **빈 출력** · `git status --short -- app/` **빈 출력** |
| AC9 | INDEX 0189 행이 실제 상태·다음 주체와 일치 | ✅ | `impl` / `IMPL_DONE (r2)` / `Claude (검증 r2)` / 라운드 `2` — 커밋 시점 상태와 일치. 대상 커밋 열에 `19baf21`(r2) 추가됨(자기 해시 `5a4176d` 부재는 구조적으로 불가피) |
| AC10 | doc-inventory 통과 | ✅ | **검증자 재실행**: `generated doc ok (9 items, 76 channels)` · `prose ok` · `links ok` · EXIT=0 |
| AC11 | 36불릿 ↔ AC1~25 **전수 매핑** + 미대응 목록 | ✅ | **r1 ⚠️ → 해소.** 부록 A 36행 실측(구조 **11** · 동작·성능 **17** · 보안·호환성 **8**) = 원본 `proposal.md §수용 기준` 불릿 카운트와 **일치**. 집계 매핑됨 **30** + AC 없음 **5** + 부분 **1** = 36 재측정 일치. 매핑 표본 대조(구1→AC1 · 구3·구4→AC3 · 구8→AC9 · 동5·동6→AC11 · 보4→AC22 · 보5→AC8 · 보7·보8→AC24) **전부 0188 AC 원문과 부합**. 잔여 6건이 본문 F4 표 6행과 동일 |
| AC12 | 축 1·3 이 3층을 거치고 **항목별 `층` 열** | ✅ | **r1 ❌ → 해소.** `grep '^\| # \| 층 \|'` → **:85(축 1) · :196(축 3)** · 부수표 `:119` · F4 표 2·3층 열. 라벨 자체도 검증: F3 "2층에서 악화"(D-017 ACTIVE·대체 `—` 실측) · U1 "3층 신규·2층 부분 승인"(baseline + D-061 실측) · U2 "2층 2건 해소·1층 잔여 1"(D36 실측) · U3 "2층에서 악화"(D-054 원문) |
| AC13 | 정정 이력 = 철회 2·정정 3·강화 3 | ✅ | 데이터 행 실측 **2 / 3 / 3** + `### 수치 정정 2건` 절 신설(이번 라운드 정정도 초안 값과 함께 남김 — D-009 를 자기 자신에게 적용) |
| AC14 | Decision 인용에 ID + 출처 | ✅ | r2 신규 인용 `D-061` 출처를 0188 원문 출처 칸과 대조해 **문자 일치**. 인용된 0188 Decision **26종 전부** ledger 에 실재(`D-053` 만 0건 = 결번 주장 자체가 근거). 0189 자기 Decision 2곳의 정본 표기 누락은 비차단 [D-C](#13-파생-이슈-비차단) |
| AC15 | §9 하위절 5개 + 발견 ID 13건 좌표 | ✅ | `### 9.1~9.5` 5절 · 발견 ID **13/13** 이 §9.2~§9.3 구간에 등장(검증자 독립 grep: F1~F4 각 1 · P1 3 · P2 1 · P3 1 · P4 1 · P5 2 · P6 2 · U1 2 · U2 1 · U3 2) |

### plan §10 강제 지점 표 — AC 와 별개로 걷는다

> 구현자 보고는 **22/22 지점 · 8/8 행**(23 중 verify 몫 1 제외). 검증자가 `언제 강제` 칸에서 지점을 다시 세고 각 지점을 재현했다 — **23/23**.

| # | 계약 | plan 이 적은 강제 지점 | 검증자가 확인한 것 | 결과 |
|---|---|---|---|---|
| 1 | 인용 `파일:줄` 정확성 | 작성 시 + **verify** (2) | 작성 시 51/51(구현자) / **verify 51/51 내용 대조(검증자, 이번 문서 §7)** | ✅ **2/2** |
| 2 | 실측/연역 구분 | 작성 시 (1) | §근거의 두 종류 + 연역 5행 표 + 규칙. **r1 의 `grep 연역` 0 → 7줄** | ✅ **1/1** |
| 3 | `app/**` 무변경 | 커밋 시 (1) | 전 범위 diff 0 | ✅ **1/1** |
| 4 | 0188 Decision/AC 무변경 | 커밋 시 (1) | `docs/handoff/0188-*/` diff 0 | ✅ **1/1** |
| 5 | 3층 채점 적용 | 축별 판정 시 (축 1·3 = 2) | 두 표 모두 `층` 열 + 라벨을 코드/Ledger 로 역검증 | ✅ **2/2** |
| 6 | 발견의 구조 좌표 | 발견 신설 시 (13) | 검증자 독립 grep **13/13** | ✅ **13/13** |
| 7 | 수치의 커밋 범위 명시 | 작성 시 (1) | `audit.md:6-18` "이 문서의 성격" 표 + inventory 관계 문단 | ✅ **1/1** |
| 8 | **결론 사본의 정합** (r2 신설) | 판정 변경 시 — `audit.md` + INDEX 비고 (2) | 두 사본 모두 정정값(62·ACTIVE 61·SUPERSEDED 1·D-053 결번·20행) 보유 · 철회본 0벌 · 저장소 전체에서 옛 값 잔존 0 | ✅ **2/2** |

- **표에 없는데 같은 불변식이 필요한 지점**: **새로 발견한 것 없다.** r1 이 지목한 1건은 #8 로 승격됐다. 다만 **문서 내 앵커 유효성**은 어느 게이트도 강제하지 않는 축이고(§3), 이번엔 11/11 이 해결돼 결함이 아니다 — 관측만 남긴다.

## 6. 외부 포트 / 문서 계약

해당 없음 — 외부가 구현하는 port/schema/config 를 만들지 않는다.

## 7. 숫자 / 음성 기준 / 상한 재측정

전부 검증자가 독립 재측정했다. **r2 가 고친 2건을 포함해 어긋난 값 0건.**

| 감사의 수치 | 재측정 | 결과 |
|---|---|---|
| main 프로덕션 `.ts` `24,249 → 26,480` | 현재 **26,480**(207파일) · `ad10f6c` **24,249**(200파일) | ✅ 정확 |
| 내역 합 = 총계 (`+1,325 + 906 = +2,231`) | `26480 − 24249 = 2231` | ✅ 검산 일치 |
| 구 `features/providers/` 35파일 5,068줄 | **35 / 5,068** | ✅ 정확 |
| 신 4슬라이스 34파일 6,171줄 | **34 / 6,171** | ✅ 정확 |
| `app/deployment/` 529 · `runtime-config.ts` 266 · `login.ts` 795 · `store.ts` 437 · `harness-runtime.ts` 163 | 529 / 266 / 795 / 437 / 163 | ✅ 전부 정확 |
| **(r2 정정) 0188 Decision 총 62 · ACTIVE 61 · SUPERSEDED 1 · D-053 결번** | 고유 ID **62** · 결번 스캔 → **D-053 유일** · 상태 tally **ACTIVE 61 / SUPERSEDED 1**(D-021 → D-038) | ✅ **정정이 정확하다** |
| **(r2 정정) 제안 §금지표 데이터 20행** | `proposal.md:1300` 표 데이터 행 **20**(헤더+구분선 포함 22) | ✅ **정정이 정확하다** |
| 수용기준 36불릿 (구조 11 · 동작·성능 17 · 보안·호환성 8) | 원문 불릿 카운트 **11 + 17 + 8 = 36** | ✅ 정확 |
| 0188 AC 25건 | 고유 AC ID **25** | ✅ 정확 |
| 부록 A 집계 30 + 5 + 1 | 표 실측 **30 매핑됨 / 5 없음 / 1 부분** = 36 | ✅ 정확 |
| 인용 51건 (자동 48 + 수동 3) | 검증자 독립 추출 **51** · 자동 해결 48 · 수동 확인 3~4 | ✅ 정확 |
| `audit.md` 283 → 387줄 | `wc -l` = **387** | ✅ 정확 |

**전칭 주장 재확인**(r2 가 건드리지 않았지만 판정의 뼈대): `app/src/shared/i18n/` diff **0파일** · renderer diff **2파일**(`providerCatalog.ts`·`useProviders.ts`) · 새 migration **0** · `ProviderKind = 'gate' | 'llm' | 'service'` 무변경 · `grep -rn characterization app/src` **0건**(F3) · `TurnRequest` 에 fingerprint 필드 **부재**(P1). 전부 ✅.

- **0건 게이트의 정당한 예외 보존**: "구현체 0개" 를 `deployment-wiring.test.ts` 가상 배포 예외와 함께 적었다. 유지 ✅
- **총량 임계의 분해**: `+2,231` 을 강건화(r4~r10) vs 간접층으로 갈라 반대 논거에 실었다. 유지 ✅
- **자기 상한**: §14 의 "발견당 인용 ≤3(인과 사슬 예외)" 를 **F1 5 · F2 5 · U1 5** 가 넘는다. U1 은 4홉 예외 + baseline 1, F1·F2 는 2·3층 근거 때문이다 → 비차단 [D-B](#13-파생-이슈-비차단)

## 8. 테스트 가능한 핸들 탐색 후 남은 사람 실기

"문서라서 기계 검증이 안 된다" 를 받지 않았다. 이번 검증도 거의 전부가 기계 검증됐다.

| 항목 | 기계 검증한 범위 | 남은 사람 실기 |
|---|---|---|
| 인용 정확성 | 51건 추출·해결·**내용 대조** | 없음 |
| 규모 수치 | `wc -l`/`git ls-tree` 전수 재계산 12종 | 없음 |
| Decision 대조 | 62 ID 실재·상태 tally·출처 칸 대조 | 없음 |
| 제안서 대조 | 불릿 카운트·금지표 행수·매핑 표본 8건 | 없음 |
| baseline 주장 | `git show ad10f6c:` 3홉 추적(U1) | 없음 |
| 보드 정합 | 철회본 탐색·정정값 대조·저장소 전역 잔존 스캔 | 없음 |
| 문서 링크·앵커 | `check-doc-inventory` + 앵커 11건 slug 대조 | 없음 |
| **네 축 판정의 시정 여부** | 근거는 전부 재현했다 | **F1~F3 · P1~P2 · U2~U3 시정 결정** — 제품 결정이라 사용자 몫(→ 0190) |

## 9. 게이트 재실행

`app/**` 변경이 0 이므로 `app/AGENTS.md §better-sqlite3 ABI · 제약 환경 게이트 가이드` 의 lint/typecheck/vitest 는 **대상이 아니다**(이 환경은 `app/node_modules` 부재로 실행도 불가). 문서 게이트를 재실행했다.

```text
$ cd app && node scripts/check-doc-inventory.mjs --check
[doc-inventory] generated doc ok (9 items, 76 channels)
[doc-inventory] prose ok: no inventory counts restated in current-state docs
[doc-inventory] links ok: every relative markdown link resolves
EXIT=0

$ git diff --check           → 출력 없음 (EXIT=0)
$ git status --short -- app/ → 빈 출력
$ bash .agents/skills/handoff-verify/scripts/scan-surface.sh 702cd3c..HEAD
변경된 소스 파일이 없습니다 (범위: 702cd3c..HEAD, 루트: app/src)
```

- **관측한 실행 산출(exit code 아님)**: doc-inventory **검사 3항목 전부 ok** — generated **9 items / 76 channels**, prose 위반 **0**, 미해결 상대 링크 **0**. `git diff --check` 출력 라인 **0**. scan-surface 대상 소스 파일 **0**.
- **`npm test` 미사용** — DB 동작 검증 필요 없음(코드 무변경).
- **환경 기인 실패 분리**: 이번 범위에 실패 0. `node_modules` 부재는 비대상 축이라 판정에 영향 없음.
- **게이트가 작업 트리를 바꿨는가**: **아니다.** `--check` 는 읽기 전용이고 실행 후 `git status --short` 가 `verify.md` 등 검증자 산출 외에 **빈 출력**.
- **검증 중 실행한 명령의 잔여물**: **없음.** 인용 추출 스크립트·중간 파일은 저장소 밖 scratchpad 에만 썼고 `git status` 에 미추적 파일 0.

## 10. 검증 책임 분리 — 사람 vs 에이전트

| 항목 | 에이전트 | 사람 | 결과 |
|---|---|---|---|
| doc-inventory / 링크 / 앵커 | 실행·산출 관측 | — | ✅ |
| AC ↔ 산출물 1:1 | 15건 대조 | — | **15 ✅** |
| §10 강제 지점 | 8행 23지점 재현 | — | **23/23** |
| 인용·수치 재측정 | 51 인용 + 12 수치 재계산 | — | 어긋남 **0** |
| 감사가 주장한 코드/baseline 사실 | 역방향 재확인 | — | 뒤집힌 것 **0건** |
| INDEX / trailer 정합 | 스캔·대조 | — | ✅ |
| **F1~F3·P1~P2·U2~U3 시정 여부** | 근거 제공 | **결정** | → 0190 |
| **감사의 독립성 한계** | 사실 기록 | **판단** | 자기 검증 5중(§메타) |

## 11. Repository operation checks

### AGENTS.md 위생

이번 범위(`702cd3c..HEAD`)에서 `AGENTS.md` 변경 **0건** — 비대상.

### INDEX 보드 정합성

- **상태 / 다음 주체 / 라운드**: `impl` · `IMPL_DONE (r2)` · `Claude (검증 r2)` · `2` — 커밋 시점 실제와 **일치** ✅
- **대상 커밋 열**: `9a2980a`·`57f75bd`·`6172bc1`·`ba016c5`·`dddd05b`·`702cd3c`·**`19baf21`** — r1 이 지적한 `(구현자 산출)` 자리표시자가 해소됐고 r2 구현 커밋 해시가 들어왔다 ✅
- **비고 본문 (r1 D1 의 자리)**: ✅ **해소.**
  - 철회본 검색 → `③**부분 회귀 3건**` **0** · `금지표 22행` **0** · `ACTIVE Decision 63` **0**.
  - `제안 비범위 침범`·`정보성` 은 각 2회 남지만 **둘 다 "r2 가 이미 철회한 판정" 을 서술하는 문맥**(보드 정정 note · verify FAIL 요약)이고 현재형 주장이 아니다.
  - **철회 사실이 보드에도 남았다** — `**(보드 정정 r2)**` 구절이 무엇이 왜 지워졌는지와 전문 위치(`audit.md §정정 이력`)를 적는다. D-009 를 보드에서도 지켰다.
  - **뒷벌 전용 사실 1건이 앞벌로 이관**됐다(`529줄 중 실행 130` · `runtime-config.ts 266줄 도달 불가` — 보드에서 2회 등장 실측).
  - 정정값 3종(`ACTIVE 61`·`SUPERSEDED 1`·`D-053 결번`)과 `20행` 이 보드에도 들어왔다 — §10 #8 불변식의 두 번째 사본 ✅
- **PASS 처리**: 이 검증으로 `verify/PASS` 이므로 0189 행을 [`../archive/handoffs/INDEX-history.md`](../../archive/handoffs/INDEX-history.md) 로 이동한다(보드 머리 규칙).

### Commit / reference 정합성

- **trailer**: `19baf21`·`5a4176d` 둘 다 `Agent: claude` · `Handoff: docs/handoff/0189-0188-post-audit/` · `Status: implemented` · `Criteria-Met: 15/15` · `Verified-By: pending`. root `AGENTS.md` 허용값을 따르고 **trailer 블록 내부에 빈 줄 없음**(`git interpret-trailers --parse` 가 7개 키를 전부 파싱) ✅
- **`Criteria-Met: 15/15`**: **이번엔 과대가 아니다.** 검증자 재측정도 15/15 다. r1(`15/15` → 13/15)·0187 r1(`16/16` → 13✅/2⚠️/1❌)에서 두 번 반복된 증상이 **재발하지 않았다** — §14 에 사실로 기록.
- **reference / script**: 이동·삭제 0건. `verify.template.md`·`scan-surface.sh` 정상 소비 ✅

## 12. 구현자 코멘트 / 선조치 경계

| 구현자 코멘트 | 검증자 판단 | 반영 |
|---|---|---|
| **되먹임 ② 갈래 2건** — §10 8행 신설 · §14 상한 예외 | **타당.** 전자는 verify r1 §5 가 지목한 자리이고 계약을 **조인다**. 후자는 verify r1 D5② 의 권고를 그대로 이행했다. 둘 다 AC·Decision 을 건드리지 않았고 provenance 를 남겼다 | 유지 |
| **③ 갈래 0건 — "AC 는 한 글자도 바꾸지 않았다"** | **사실.** §7 절이 `702cd3c` 와 바이트 동일함을 diff 로 확인 | 유지 |
| D5① 을 "AC 를 좁히는 대신 산출로 채웠다" | **타당.** AC11 문언 완화는 사용자 결정이므로 산출물을 AC 에 맞춘 선택이 맞다. 부록 A 를 원본 불릿과 대조해 36/36 확인 | 유지 |
| "인용 상한은 산출이 아니라 문언이 틀렸다" | **부분 타당.** 방향은 맞으나 고친 문언(인과 사슬 = 홉 수)이 F1·F2·U1 의 실제 인용 수를 여전히 못 덮는다 | [D-B](#13-파생-이슈-비차단) |
| §10 #1 재현 명령 = "줄이 비어있지 않음 확인" | **불충분.** AC2 는 줄 **내용**을 요구한다. 검증자가 51건 내용을 직접 대조해 메웠다(어긋남 0) | §4 에 기록 |
| 놓친 문제 #8 — "뒷벌 삭제 전에 두 벌을 실제로 대조했다" | **타당하고 중요하다.** 이관된 사실이 보드에 실재함을 확인 | 유지 |

## 13. 파생 이슈 (비차단)

> **PASS 판정을 막지 않는다.** 전부 문서 위생이고 어떤 판정·수치·코드 사실도 바꾸지 않는다.
> 0190 또는 다음 문서 손질 때 함께 정리하면 된다. `plan.md` 의 `[검증자 기입] 파생 이슈` 로 이관한다.

- [ ] **D-A — 연역 전수표의 전칭 문구가 발견 밖에서 성립하지 않는다 (낮음).** `audit.md:28` 이 "아래가 이 문서의 **연역 항목 전부**이고, 표에 없는 발견은 전부 실측이다" 라고 적는다. **발견 13건 축에서는 참**이다(누락 0 — AC3 ✅). 그러나 발견 번호가 없는 서술 중 최소 3건이 런타임 추론이다: `:132-133` "정적·기본 배포에서는 턴당 파일 I/O 증가 0 · vault 접근 −1" · `:204-206` "읽기 IPC 한 번이 push 방송과 도구 회수를 유발할 수 있다" · `:207-208` "재인증 실패 시 row 가 '연결됨' 으로 남는다". 조치: 전칭을 "**발견** 항목의 연역 전부" 로 좁히거나 세 문장에 표기를 단다.
- [ ] **D-B — §14 인용 상한 개정이 산출을 여전히 못 덮는다 (낮음).** r2 가 "인과 사슬은 홉 수만큼" 예외를 넣었으나 실제 인용 수는 **F1 5 · F2 5 · U1 5** 다(U1 은 4홉 + baseline 1, F1·F2 는 인과 사슬이 아니라 **2·3층 근거**). 초과분이 전부 검증에 쓰인 정확한 인용이라 해가 없다 — 상한이 3층 채점(D-008)이 요구하는 근거량을 반영하지 못한 것이다. 조치: 예외를 "층별 근거 1건씩 추가 허용" 으로 넓히거나 상한을 발견 유형별로 나눈다.
- [ ] **D-C — 0189 자신의 Decision 인용 2곳에 정본 표기가 없다 (낮음).** `audit.md:20` "(D-006)" · `:313` "D-009 대로" 는 **0189 plan** 의 Decision 인데, 같은 문서가 0188 Decision 26종을 인용하고 **0188 에도 D-006·D-009 가 실재**한다(각각 `AuthDefinition` 계약 · 재인증 보존 — 내용이 전혀 다르다). 같은 문서 `:217` 은 `[plan.md](plan.md) D-002` 로 정본을 밝히는 관례를 이미 갖고 있다. 조치: 두 곳에 같은 관례를 적용한다.

## 14. Review Signals — 사실만

> 원인 분류(A~F)와 SKILL 변경은 `handoff-review` 몫이다.

- **이전 라운드와 동일/유사 증상**: **없다 — 반복이 끊겼다.** r1 의 FAIL 2건과 0187 r1 의 1건은 모두 "자기보고가 형식 요건 충족을 주장했는데 문서/코드에 그 표식이 없다" 였다. 이번 라운드는 §10 전수표에 `재현 명령` 열을 만들고 행마다 관측값을 적었으며, 검증자 재측정에서 **자기보고와 재측정이 일치**했다(15/15 · 23/23).
- **관련 plan 지침/AC 의 존재 여부**: r1 시점에 AC3·AC12 는 이미 기계 판정 가능한 문언이었다(지침 부재가 아니라 자기 대조 누락). **반면 §10 에는 실제로 빠진 지점이 있었고**(결론의 두 번째 사본) r2 가 8번째 행으로 신설했다 — 지침 결함이 맞았고 닫혔다.
- **사용자 결정 변경 근거**: 없음. D-001~D-009 전부 ACTIVE 유지, AC1~15 무변경(diff 로 확인).
- **구조적 사실 — 게이트가 볼 수 없는 계약**: r1 FAIL 3건은 `check-doc-inventory` 초록 상태에서 났다. 이번에 새로 확인된 같은 형태의 사각지대가 하나 더 있다 — **문서 내 앵커 유효성**은 어느 게이트도 보지 않는다(상대 파일 링크만 검사). 이번엔 11/11 이 해결돼 결함은 없었다.
- **구조적 사실 — 설계·산출 분리**: r1 은 `9a2980a` 가 plan 과 audit 을 같은 커밋에 만들어 §0 기준선 잠금이 작동하지 않았다. **r2 는 구현 커밋이 verify 커밋 뒤에 와서 diff 로 AC 무변경을 증명할 수 있었다** — 문서 전용 handoff 도 라운드가 갈리면 §0 이 정상 작동한다.
- **반복된 검증 환경 한계**: `app/node_modules` 부재로 lint/typecheck/vitest 실행 불가 — 0188 verify(ABI)·0189 impl r1·verify r1·impl r2 에 이어 **5회째**. 이번에도 `app/**` 미변경이라 판정에 영향 없다.
- **자기 검증의 겹수**: 0188 구현 · 0189 설계 · 0189 구현(r1·r2) · 0189 검증(r1·r2) 이 전부 Claude Code. 이 검증은 기준을 AC 문언·제안서 원문·baseline 코드로 잠가 완화했으나 **독립 감사자가 아니다**.
- **라운드**: impl 라운드 **2** — 3을 넘지 않았으므로 `handoff-review` 진입 조건 아님.

## 15. 결론

- **상태: PASS** (라운드 2)
- **Product/UX 및 ACTIVE Decision 충족**: D-001~D-009 **전부 ✅**. r1 이 ❌/⚠️ 를 준 D-006·D-008·D-009 세 건이 모두 닫혔다.
- **AC 충족**: **15 ✅ / 0 ❌**. 자기보고 `15/15` 가 재측정과 일치한다.
- **§10 강제 지점**: **23/23 지점 · 8/8 행**(구현자 소관 22 + verify 몫 1). 표에 없는 새 지점 발견 0건.
- **기준 밖 결함**: 비차단 3건(D-A 연역 전칭 · D-B 인용 상한 · D-C 자기 Decision 정본 표기). **판정·수치·코드 사실을 바꾸는 것은 0건.**
- **감사 내용 자체의 신뢰도**: **높다.** 인용 **51/51** 이 줄 내용까지 일치하고, 규모 수치 **12종**과 정정 수치 **2종**이 전부 재측정에서 정확하며, 전칭 주장 6종이 성립하고, r2 가 새로 실측한 baseline 주장(U1 3층)도 `git show ad10f6c:` 3홉 추적으로 확인됐다. **역방향 탐색에서 뒤집힌 판정 0건.**
- **repository operation checks**: INDEX 보드 ✅(철회본 0벌 · 정정 note · 대상 커밋 해시) · trailer ✅ · reference ✅.
- **남은 사람 확인**: F1~F3 · P1~P2 · U2~U3 의 **시정 여부 결정**(→ 0190). 이 감사는 코드를 바꾸지 않았고 바꾸지 않는 것이 D-001 이다.
- **다음 단계**: INDEX 를 `verify/PASS` 로 올리고 0189 행을 archive history 로 이동한다. 시정은 사용자 결정 후 별도 핸드오프(0190).

---

## 부록 — r1 검증 (FAIL)

> 2026-08-16 · 대상 `21a46ba..dddd05b` · 라운드 1 · **FAIL**. r2 가 D1·D2·D3 을 닫고 D4·D5 를 함께 정리해 위 본문이 그 결과를 채점했다. 아래는 그때의 판정 요약이며 **현재 상태가 아니다**.

- **AC 13 ✅ / 2 ❌** (자기보고 `15/15` 는 과대) · **§10 강제 지점 5/7 충족 · 2 부분**(자기보고 7/7 은 과대).
- **감사 내용 자체는 뒤집히지 않았다** — 인용 40/40 재현, 규모 수치 11종 정확, 전칭 주장 8종 성립, 철회 2건 모두 baseline 대조에서 정당.
- **FAIL 사유 3건**:
  - **D1** — INDEX 21행 비고의 `②`·`④` 블록이 각각 2벌이고 **뒷벌이 r2 가 철회한 주장을 현재형으로** 실었다(U2 "제안 비범위 침범" · U3 "정보성"). 유입 커밋 `6172bc1`. 보드가 감사 결론의 두 번째 사본이자 독자 흐름의 첫 칸이라 **D-009 위반**.
  - **D2** — AC3 미충족: `audit.md` 에 "연역" 표기 **0회**. (전역 `unhandledrejection` 리스너는 `renderer/src/shared/logging.ts:43` 에 있으나 로그만 남겨 U1 결론 자체는 성립.)
  - **D3** — AC12 미충족: 축 3 회귀표에 `층` 열 부재(축 1·부수·F4 는 보유).
- **부가**: **D4** 수치 2건 과다("ACTIVE Decision 63건" → 총 62·ACTIVE 61·SUPERSEDED 1 · "금지표 22행" → 데이터 20행, 판정 불변) · **D5** AC11 매핑 미수록 + §14 인용 상한 초과(낮음).
- **표에 없는데 같은 불변식이 필요한 지점 1건 발견**: 결론이 `audit.md` 와 INDEX 두 곳에 사는데 §10 이 전자에만 강제 지점을 걸었다 → r2 가 §10 8번째 행으로 신설.
- **게이트(r1)**: `check-doc-inventory --check` 3항목 ok(exit 0) · `git diff -- app/` 빈 diff · scan-surface "변경된 소스 파일 없음".
