# Verify — 0189-0188-post-audit

> 검증 절차는 [`.agents/skills/handoff-verify/SKILL.md`](../../../.agents/skills/handoff-verify/SKILL.md), 협업/상태 머신은 [`../AGENTS.md`](../AGENTS.md).

## 메타

| 항목 | 값 |
|---|---|
| slug | `0189-0188-post-audit` |
| 검증자 | Claude Code |
| 일자 | 2026-08-16 |
| 대상 커밋/range | `21a46ba..dddd05b` (`9a2980a` · `57f75bd` · `6172bc1` · `ba016c5` · `dddd05b`) |
| 구현 전 plan 기준 | `9a2980a` 에서 plan 과 audit 이 **같은 커밋**에 신설됐다 — 사전 기준선이 따로 없다(§0) |
| 라운드 | 1 |
| 상태 | **FAIL** |
| 자기 검증 여부 | **그렇다.** 0188 구현 · 0189 설계 · 0189 구현 · 이 검증이 전부 Claude Code. 이 한계는 감사 자신이 `audit.md` 머리에 적었고 이 문서도 승계한다 |

**한 줄 판정**: 감사의 **실질 내용은 견고하다** — 인용 40/40 과 모든 규모 수치가 독립 재측정에서 정확히 일치했고, F3·U1·U3 의 코드 주장을 역방향으로 확인해도 성립한다. FAIL 사유는 **판정 내용이 아니라 계약 이행**이다: AC3·AC12 가 자기보고 ✅ 인데 미충족이고, 보드가 **철회된 주장을 여전히 현재형으로** 싣고 있다.

---

## 0. 기준선 / plan 변경 확인

- **구현 커밋이 `plan.md` 를 변경했는가**: 변경이 아니라 **신설**이다. `9a2980a` 가 `plan.md`·`audit.md` 를 함께 만들었고, 이후 `57f75bd`·`6172bc1`·`ba016c5`·`dddd05b` 가 둘을 함께 개정했다.
- **따라서 "구현자가 자기 코드에 맞춰 AC 를 완화했는가" 를 diff 로 가를 수 없다.** 설계와 산출이 한 커밋에 들어온 handoff 는 §0 의 자기 증명 방지 장치가 구조적으로 작동하지 않는다. 이 검증은 그 대신 **AC 문언을 그대로 읽고 산출물이 그 문언을 만족하는지** 만 본다 — AC 가 왜 그 모양인지는 채점하지 않는다.
- **Decision Ledger 변경**: 없음. D-001~D-009 전부 ACTIVE 이고 SUPERSEDED 0건. D-008·D-009 는 r2 에서 **사용자 지적을 받아 설계자 자격으로 추가**됐고 출처 칸에 그 provenance 가 있다 — 무단 변경이 아니다.
- **감사 대상(0188)의 Ledger/AC 변경**: **없음.** `git diff 21a46ba..dddd05b -- docs/handoff/0188-*/` 빈 diff. 강제 지점 #4 성립.
- **채점에 사용할 원 기준**: `plan.md §7` 의 AC1~AC15 문언 + `§10` 강제 지점 7행.

## 1. Product & UX / ACTIVE Decision 요약

산출물이 문서라 런타임 소비자가 없다. 소비자는 **다음 유지보수자**이고 흐름은 `INDEX 보드 → audit.md → 판정 4줄 → 표 → 파일:줄 대조 → 0190 결정` 이다.

| Decision | 기대 결과 | 실제 |
|---|---|---|
| D-001 (산출물 전용, `app/**` 0) | 코드 무변경 | ✅ `git diff --stat 21a46ba..dddd05b -- app/` **빈 diff** |
| D-002 (볼륨 축 포함) | 경량화가 볼륨으로도 채점됨 | ✅ 축 4 에 6행 수치표 |
| D-003·D-005 (이탈 3건 = 시정 필요, 실행은 범위 밖) | 문제·원인·방안 3열, 코드는 그대로 | ✅ |
| D-004 (U1·U3 기록만) | 조치 없이 기록 | ✅ 방안 열이 "이번엔 기록만" 으로 명시 |
| D-006 (실측/연역 구분) | 연역 항목이 그렇게 표기됨 | ❌ **"연역" 이 audit.md 에 0회** — §5 D2 |
| D-007 (0188 D39/D40 중복 명시) | 매핑 문장 존재 | ✅ 종합 절 표 |
| D-008 (3층 채점) | 축 1·3 이 3층을 거치고 층이 밝혀짐 | ⚠️ 축 1 ✅ / **축 3 층 열 없음** — §5 D3 |
| D-009 (조용히 덮지 않음) | 철회·정정이 초안 주장과 함께 남음 | ⚠️ `audit.md` 는 ✅ / **보드는 철회본을 그대로 실음** — §11 D1 |

### end-to-end 흐름

```text
다음 유지보수자
  → docs/handoff/INDEX.md 0189 행          ◄ D1: ②③④ 블록이 2벌, 뒷벌은 철회 전 주장
  → docs/handoff/0189-.../audit.md
  → 축별 판정 4줄 (line 56·108·153·191)     ✅
  → 축별 표 → 파일:줄                        ✅ 40/40 재현
  → 정정 이력 절                             ✅ 철회2·정정3·강화3
  → 0190 시정 결정
```

**보드와 보고서가 어긋나는 지점이 이 흐름의 첫 칸이다.**

## 2. 구현 결과 비판적 검토 — AC 전에

| 질문 | 판정 | 근거 |
|---|---|---|
| 실환경 실패 방식 | 인용 부정확 1종뿐 | 문서 산출물. 저장소 쓰기·동시성·부분 실패 없음 |
| **false success 가능성** | **있었고 실제로 났다** | 자기보고 AC 대조가 AC3·AC12 를 ✅ 로 적었으나 문언 미충족. 게이트(doc-inventory)는 이것을 볼 수 없다 |
| partial failure/rollback | 해당 없음 | — |
| **Product/UX 의 A 가 아닌 B** | 부분적으로 | AC3·AC12 는 *표기 형식*(연역 표시·층 열)을 요구했는데 산출은 *내용*(연역 판단·층 추론)을 산문에 담았다. 내용은 맞으나 계약한 형식이 아니다 |
| 증상만 제거하고 상태가 남았는가 | **그렇다 — D1** | r2 가 audit 본문을 3층으로 재작성하면서 보드 비고에는 **새 블록을 덧붙이고 옛 블록을 지우지 않았다** |
| 최적화가 잃은 관측 | 해당 없음 | — |
| 출력 상한 | 지켜짐 | plan §14 "축당 표 1~2개" — audit.md 283줄, 0188 문서 3종(2,488줄) 재요약 없음 |

## 3. 역방향 탐색

```bash
$ bash .agents/skills/handoff-verify/scripts/scan-surface.sh 21a46ba..dddd05b
변경된 소스 파일이 없습니다 (범위: 21a46ba..dddd05b, 루트: app/src)
```

코드 diff 가 0 이라 스크립트 축은 비대상이다. 대신 **감사가 주장한 코드 사실을 역방향으로 되짚었다** — 보고서의 결론이 아니라 코드에서 출발했다.

| 감사의 주장 | 독립 확인 | 결과 |
|---|---|---|
| U1 — 해제 실패가 화면에서 "아무 일도 안 일어남" | `login.ts:228` throw → `handlers/providers.ts:67` `'reject'` → `useProviders.ts` revoke 에 try/catch **없음** → `ExtensionsCatalogView.tsx:142` `void providers.revoke(...)` | ✅ 성립. **추가 실측**: `renderer/src/shared/logging.ts:43` 에 전역 `unhandledrejection` 리스너가 있으나 `rendererLog.error` 로 **로그만** 남긴다 — 사용자 표면 0. 감사가 "화면에서" 로 한정한 것이 정확하다 |
| U3 — D-054 가드가 2곳 중 1곳만 | `useProviders.ts:57` `requestSeq` + `applyIfCurrent` 존재 / `useProviderGate.ts:47-55` `run()` 에 순서 가드 **없음**(전문 확인) | ✅ 성립. D-054 원문도 "Renderer 도 자기보다 뒤에 시작된 요청이 있으면 invoke 응답을 버린다" 로 renderer 를 명시 |
| U3 실피해 없음 | `login.ts:176` `supersededStep()` 이 현재값 반환 | ✅ 성립 |
| F3 — characterization test 0건 | `grep -rn "characterization" app/src` **0건**. `options.env` 를 만지는 테스트 3종(`prepared-config`·`respawn-policy`·`claude-adapt`)은 전부 **Orca 자체 병합 로직**을 단언하고 SDK 의 두 채널 충돌 동작은 아무도 고정하지 않는다 | ✅ 성립. 감사의 "grep 0건" 은 표현이 거칠지만 결론은 맞다 |
| F3 — D-017 ACTIVE·대체관계 없음 | `0188/plan.md` D-017 행: `ACTIVE` · 마지막 칸 `—`. D-042 행도 `ACTIVE` · `—` | ✅ 성립 |
| F2 — deps 가 `AuthRuntime` 전체 | `deployment/harness-runtime.ts:97` `HarnessConfigApiDeps` · `plugins.ts:67` `PluginDeploymentDeps` · `usage-fetcher.ts:40` `UsageDeploymentDeps`, 주입 `bootstrap.ts:441` | ✅ 성립. 제안 4곳(`:523`·`:750`·`:1135`·`:1378`)이 `BoundAuth` 를 명시하는 것도 원문 확인 |
| 금지표 미위반 | `ProviderPlatformV2`·`HarnessModelProviderDefinition`·`PluginHost`·`ConnectorRegistry` 심볼 **0건**(`deployment/plugins.ts` 의 "만들지 않는다" 주석 2줄만 매치) · 새 migration diff 0 | ✅ 성립 |
| UI 불변식 | `i18n/` diff **0** · renderer diff **2파일**(`providerCatalog.ts`·`useProviders.ts`) · `shared/ipc.ts` diff 는 주석 경로 1줄 | ✅ 성립. `ProviderKind` 무변경 |
| 철회 2건이 정당한가 | `git show ad10f6c:adapters/provider-config.ts:11-14` = `ResolvedProviderSettings{providerKey, provider, settings}` · `ad10f6c:features/providers/model-resolve.ts:51` = `toAgentEnvironments` | ✅ **두 철회 모두 정당.** 3층 채점이 실제로 오판을 잡았다 |

**역방향에서 새로 나온 결함**: 없다. 감사가 코드에 대해 한 판정 중 뒤집힌 것은 0건이다.

## 4. 기존 테스트 / semantic 검증 확인

- 이 handoff 는 코드를 만들지 않아 단위 테스트 축이 없다. plan §11 이 그것을 인정하고 **AC2 의 인용 전수 재확인을 기계 검증의 대체물**로 지정했다.
- **그 대체물은 유효하다** — 검증자가 같은 방법(`sed -n <n>p`)으로 40건을 독립 재현했고 전부 일치했다(§7).
- **structural proxy 로 통과한 AC**: **AC3·AC12** 가 그 형태다. 자기보고는 "표기했다"·"층 열" 이라고 적었는데 실제 문서에는 그 표식이 없다 — 보고를 증거로 받지 않은 것이 이 두 건을 잡았다(SKILL §4).

## 5. 요구사항 충족 매트릭스

> `Criteria-Met: 15/15` 는 자기보고이며 증거로 쓰지 않았다. 아래는 검증자가 다시 센 결과 — **13 ✅ / 2 ❌**.

| # | 기준 | 결과 | 검증 증거 |
|---|---|---|---|
| AC1 | 축별 한 줄 판정 4건 | ✅ | `grep '^\*\*판정'` → line 56·108·153·191 |
| AC2 | 인용 `파일:줄` 이 현재 트리에서 실제 내용을 가리킴 | ✅ | 고유 인용 **40건 전수** `sed -n <n>p` 재현, **40/40 일치**. 가장 느슨한 것이 `prepared-config.ts:153` (주석 블록 머리를 가리키고 근거 문장은 `:155-157`) 이나 블록 내부라 재현 가능 |
| AC3 | 실측/연역 구분 표기 + 폐쇄망 조건 명시 | ❌ | **"연역" 이 `audit.md` 에 0회**(`grep -n '연역'`). U1 은 `void`+catch 부재로부터의 연역인데 단정문으로 적혀 있다. 후반부(폐쇄망 조건)는 충족 — P2 "폐쇄망 표준 형상에서만 발생" · P6 "기본/정적 배포에서는 발생하지 않는다" |
| AC4 | 이탈 3건 문제·원인·방안 3열 + 코드 무변경 | ✅ | 축 1 표 헤더 `\| # \| 층 \| 문제 \| 원인 \| 방안 \|` + app diff 빈 diff |
| AC5 | 낭비형/대가형 2표 + 대가형에 되살아나는 결함 병기 | ✅ | line 122 · 129. 대가형 4행 전부 `맞바꾼 것` 열 존재 |
| AC6 | 경량화 미달 + 수치 + **반대 논거** | ✅ | 6행 수치표 + line 208 `### 반대 논거` 2항목. 논거가 형식이 아니라 실질(강건화 5종 · LOC 목표 부재)이다 |
| AC7 | 0188 D39·D40 중복 명시 | ✅ | 종합 절 표 — D39↔U1 "동일 항목의 재확인", D40 "범위 밖" |
| AC8 | `app/**` 무변경 | ✅ | `git diff --stat 21a46ba..dddd05b -- app/` 빈 출력 |
| AC9 | INDEX 0189 행이 실제 상태·다음 주체와 일치 | ⚠️→**❌ 아님** | 상태·다음 주체·라운드는 일치(`impl`/`IMPL_DONE`/`Claude(검증)`/`1`). **문언 자체는 충족**이므로 ✅ 로 둔다. 단 같은 행의 비고에 중복·철회본이 있어 §11 D1 로 별도 이관 |
| AC10 | doc-inventory 통과 | ✅ | **검증자 재실행**: `generated doc ok (9 items, 76 channels)` · `prose ok` · `links ok` · exit 0 |
| AC11 | 36불릿 ↔ AC1~25 **전수 매핑** + 미대응 목록 | ⚠️ | 후반부 ✅(F4 표 6행). **전수 매핑 자체는 산출물에 없다** — line 92 가 "수동 1:1 매핑" 을 *방법*으로만 적어 독자가 어느 불릿이 어느 AC 인지 재현할 수 없다. 실측: 불릿 **36**(구조 11·동작성능 17·보안호환성 8) · AC 고유 ID **25** — 두 수는 맞다. §13 D5 |
| AC12 | 축 1·3 이 3층을 거치고 **항목별 `층` 열** | ❌ | 축 1 ✅(line 65 헤더에 `층`) · 축 1 부수 ✅(line 97) · F4 표 ✅(2층/3층 열) · **축 3 회귀표 ❌** — line 171 헤더는 `\| # \| 문제 \| 원인 \| 방안 \|` 로 `층` 열이 없다. 층 판단은 산문(line 155, U2·U3 본문)에 있으나 AC 가 요구한 형식이 아니다 |
| AC13 | 정정 이력 절 = 철회 2·정정 3·강화 3 | ✅ | 데이터 행 2/3/3 실측 |
| AC14 | Decision 인용에 ID + 출처 | ✅ | D-017(제안서)·D-048(r5 리뷰)·D-051(r6 리뷰)·D-054(r6~r7 리뷰)·D-030(제안서) 등 전수 확인. 0188 원문 출처 칸과 대조해 **불일치 0건** |
| AC15 | §9 하위절 5개 + 발견 ID 13건 좌표 | ✅ | §9.1~9.5 존재 · F1~F4·P1~P6·U1~U3 **13/13** 이 §9 본문에 최소 1회 등장(실측 최소 1, 최대 4) |

### plan §10 강제 지점 표 — AC 와 별개로 걷는다

> 구현자 보고는 **7/7** 이다. 검증자가 다시 셌다 — **5/7 충족 · 2 부분**.

| # | 계약 | plan 이 적은 강제 지점 | 코드/문서에서 확인 | 결과 |
|---|---|---|---|---|
| 1 | 인용 `파일:줄` 정확성 | 작성 시 + **verify** (2) | 작성 시 40건 정정 4 / verify 40건 재현 **40/40** | ✅ **2/2** |
| 2 | 실측/연역 구분 | 작성 시 (1) | 구분 표식이 문서에 **없다**. 구현자가 "그렇게 표기했다" 고 보고했으나 미표기 | ❌ **0/1** → D2 |
| 3 | `app/**` 무변경 | 커밋 시 (1) | 5커밋 전 범위 diff 0 | ✅ **1/1** |
| 4 | 0188 Decision/AC 무변경 | 커밋 시 (1) | `0188/` diff 0 | ✅ **1/1** |
| 5 | 3층 채점 적용 | **축별 판정 시** (축 1·3 = 2) | 축 1 ✅ / 축 3 은 층 열 없이 산문만 | ⚠️ **1.5/2** → D3 |
| 6 | 발견의 구조 좌표 | 발견 신설 시 (13) | §9 grep 13/13 | ✅ **13/13** |
| 7 | 수치의 커밋 범위 명시 | 작성 시 (1) | `audit.md` 머리 "이 문서의 성격" 표 + inventory 관계 1문단 | ✅ **1/1** |

**표에 없는데 같은 불변식이 필요한 지점 — 1건 발견**: §10 의 1행("인용의 정확성")은 `audit.md` 만 대상으로 읽히지만, **같은 주장이 `INDEX.md` 비고에도 복제된다.** 보드 비고는 감사 결론의 두 번째 사본인데 §10 에 강제 지점이 없어 아무도 걷지 않았고, 실제로 그 자리에서만 철회본이 살아남았다(D1). 설계가 못 본 지점이다.

## 6. 외부 포트 / 문서 계약

해당 없음 — 외부가 구현하는 port/schema/config 를 만들지 않는다.

## 7. 숫자 / 음성 기준 / 상한 재측정

전부 검증자가 독립 재측정했다.

| 감사의 수치 | 재측정 | 결과 |
|---|---|---|
| main 프로덕션 `.ts` `24,249 → 26,480` | `26480` (현재) · `24249` (`ad10f6c` 전 파일 합) | ✅ **정확** |
| 구 `features/providers/` 35파일 5,068줄 | 35 / 5068 | ✅ **정확** |
| 신 4슬라이스 34파일 6,171줄 | 34 / 6171 | ✅ **정확** |
| `app/deployment/` 529줄 | 529 (6파일) | ✅ **정확** |
| `runtime-config.ts` 266 · `login.ts` 795 · `store.ts` 437 · `harness-runtime.ts` 163 | 266 / 795 / 437 / 163 | ✅ **전부 정확** |
| 내역 합 = 총계 (`+1,325 + 906 = +2,231`) | `26480 − 24249 = 2231` | ✅ **검산 일치** |
| 수용기준 **36불릿** (구조 11 · 동작성능 17 · 보안호환성 8) | 36 = 11+17+8 | ✅ **정확** |
| 0188 AC **25건** | 고유 AC ID 25 | ✅ **정확** |
| 인용 **40건** | 고유 `파일:줄` 40 | ✅ **정확** |
| 발견 **13건** (F4+P6+U3) | 4+6+3 = 13 | ✅ **정확** |
| **제안 §금지표 "22행"** | 표 데이터 행 **20**. 22 는 헤더 + `\|---\|` 구분선을 포함한 마크다운 줄 수 | ❌ **2 과다** → D4 |
| **"ACTIVE Decision 63건"** | 총 **62건**(D-053 **결번**) · 그중 **ACTIVE 61 · SUPERSEDED 1**(D-021). 최대 ID 만 D-063 | ❌ **총계 1 과다 · ACTIVE 2 과다** → D4 |

- **0건 게이트의 정당한 예외 보존**: 감사가 "구현체 0개" 를 단정하지 않고 "**기본 배포 기준**" 으로 한정한 뒤 `deployment-wiring.test.ts` 가 가상 배포로 그 경로를 태운다는 예외를 병기했다 — 정당한 예외를 지우지 않았다. ✅
- **총량 임계의 제거/허용 분해**: 축 4 의 `+2,231` 을 "강건화(r4~r10) vs 간접층" 으로 갈라 반대 논거에 실었다. 섞지 않았다. ✅
- **상한**: plan §14 의 "발견마다 인용 최소 1 최대 3" — 최다 인용 항목은 U1 의 4건(`login.ts:228`·`handlers/providers.ts:67`·`useProviders.ts:91-96`·`ExtensionsCatalogView.tsx:142`)으로 **상한 1 초과**. 인과 사슬 4홉을 보이려면 불가피해 보이고 실질 해는 없으나, 자기 상한을 넘긴 사실은 기록한다(D5 에 병합).

## 8. 테스트 가능한 핸들 탐색 후 남은 사람 실기

"문서라서 기계 검증이 안 된다" 를 받지 않았다. 실제로 이 검증의 **거의 전부가 기계 검증됐다**.

| 항목 | 기계 검증한 범위 | 남은 사람 실기 |
|---|---|---|
| 인용 정확성 | `sed -n` 40/40 | 없음 |
| 규모 수치 | `wc -l` / `git ls-tree` 전수 재계산 11종 | 없음 |
| 전칭("0건"·"무변경") | `grep` / `git diff` 8종 | 없음 |
| Decision 인용 | `0188/plan.md` 행 대조 | 없음 |
| U1·U3 코드 사실 | 소비처 전문 읽기 + 전역 리스너 탐색 | 없음 |
| 문서 링크·인벤토리 | `check-doc-inventory.mjs` | 없음 |
| **네 축 판정의 타당성** | 근거는 전부 재현했다 | **시정 여부 결정**(F1~F3·P1~P2·U2~U3) — 제품 결정이라 사용자 몫 |

## 9. 게이트 재실행

`app/**` 변경이 0 이므로 `app/AGENTS.md §better-sqlite3 ABI · 제약 환경 게이트 가이드` 의 lint/typecheck/vitest 는 **대상이 아니다**(이 환경은 `app/node_modules` 부재로 실행도 불가). 문서 게이트만 재실행했다.

```text
$ cd app && node scripts/check-doc-inventory.mjs --check
[doc-inventory] generated doc ok (9 items, 76 channels)
[doc-inventory] prose ok: no inventory counts restated in current-state docs
[doc-inventory] links ok: every relative markdown link resolves
EXIT=0
```

- **관측한 실행 산출(exit code 아님)**: 검사 **3항목 전부 ok** — generated doc **9 items / 76 channels**, prose 위반 **0**, 상대 링크 미해결 **0**. 구현자 보고와 일치한다.
- `npm test` 미사용 — DB 동작 검증 필요 없음.
- **환경 기인 실패 분리**: 이번 범위에 실패가 없다. `node_modules` 부재는 비대상 축이라 판정에 영향 없음.
- **게이트가 작업 트리를 바꿨는가**: 없음. `check-doc-inventory.mjs --check` 는 읽기 전용이고 실행 후 `git status --short` 에 `verify.md` 외 변경 없음.
- **검증 중 실행한 명령의 잔여물**: 없음.

## 10. 검증 책임 분리 — 사람 vs 에이전트

| 항목 | 에이전트 | 사람 | 결과 |
|---|---|---|---|
| doc-inventory / 링크 | 실행·산출 관측 | — | ✅ |
| AC ↔ 산출물 1:1 | 15건 대조 | — | 13 ✅ / 2 ❌ |
| 인용·수치 재측정 | 51종 재계산 | — | 49 ✅ / 2 ❌ |
| 감사가 주장한 코드 사실 | 역방향 재확인 | — | 뒤집힌 것 0건 |
| INDEX / trailer 정합 | 스캔·대조 | — | trailer ✅ / **보드 ❌** |
| **F1~F3·P1~P2·U2~U3 시정 여부** | 근거 제공 | **결정** | 0190 |
| **감사의 독립성 한계** | 사실 기록 | **판단** | 자기 검증 4중(§메타) |

## 11. Repository operation checks

### AGENTS.md 위생

이번 범위에서 `AGENTS.md` 변경 **0건** — 비대상. (직전 `21a46ba`·`4b9e4fc` 등 handoff 인프라 커밋은 이 검증 범위 밖이다.)

### INDEX 보드 정합성

- **상태 / 다음 주체 / 라운드**: `impl` · `IMPL_DONE` · `Claude (검증)` · `1` — **실제와 일치** ✅
- **대상 커밋 열**: `9a2980a` · `57f75bd` · `6172bc1` · `ba016c5` · **`(구현자 산출)`** — 마지막 커밋이 해시가 아니라 자리표시자로 남았다. 실제 해시는 `dddd05b`. ⚠️
- **비고 본문**: ❌ **D1** — 아래.

**D1 — 보드가 철회된 판정을 현재형으로 싣고 있다.**

INDEX 21행의 비고에 **`②0187 개선 4건 전부 보존` 블록이 2벌, `④미달` 블록이 2벌** 있다. `git show` 로 유입 시점을 특정했다:

| 커밋 | `④미달` 블록 수 |
|---|---|
| `57f75bd` (r1+F4) | 1 |
| **`6172bc1` (r2 3층 개정)** | **2** ← 여기서 유입 |
| `ba016c5` · `dddd05b` | 2 (그대로) |

r2 가 `audit.md` 본문은 3층으로 다시 썼는데 **보드에는 새 블록을 덧붙이고 옛 블록을 지우지 않았다.** 그래서 남은 뒷벌이 r2 가 **명시적으로 철회한 주장**을 그대로 말한다:

> `③부분 회귀 3건 — … U2 사용자 문구 3건 변경(**제안 비범위 침범**, plan r10 은 2건만 기록) · U3 시퀀스 가드가 소비처 2곳 중 1곳에만`

`audit.md` 의 [정정 이력] 은 이 문장을 이렇게 뒤집었다 — "U2 '문구 3건이 비범위 침범' → `settings-write` 2건은 **외부 리뷰 지적 → 사용자 결정으로 수용**(D36)". 즉 **같은 보드 행이 U2 를 한 문단에서는 '2건 승인됨' 으로, 다른 문단에서는 '비범위 침범' 으로 말한다.** U3 도 앞벌은 "D-054 가 요구한" 으로, 뒷벌은 "정보성" 시절 표현으로 갈린다.

이것이 단순 중복이 아닌 이유: **보드는 감사 결론의 두 번째 사본이자 다음 에이전트가 가장 먼저 읽는 자리**다(plan §5 흐름의 첫 칸). D-009 는 "이미 push 된 문서이므로 조용히 덮어쓰지 않는다" 를 요구했는데, 보드에서는 정반대로 **철회 사실 없이 철회 전 주장이 살아남았다**. `audit.md` 안에서만 정직하고 보드에서는 그렇지 않다.

### Commit / reference 정합성

- **trailer**: 5커밋 전부 `Agent: claude` · `Handoff: docs/handoff/0189-0188-post-audit/` · `Status: implemented` · `Criteria-Met: N/N` · `Verified-By: pending`. root `AGENTS.md` 허용값을 따르고 trailer 블록 내부에 빈 줄 없음. ✅
  - `Criteria-Met` 이 root 표에 "구현 커밋(Codex)만" 으로 적혀 있으나, root §협업 워크플로우가 "비기능 작업은 Claude 가 직접 구현하며 이때도 구현 커밋 trailer 형식은 동일하게 따른다" 고 정하므로 **위반이 아니다**.
  - 다만 `dddd05b` 의 `Criteria-Met: 15/15` 는 **과대 보고**다 — 검증 결과 13/15(§5). 0187 r1 의 `16/16` 과 **같은 증상**이다(§14).
- **reference / script**: 이동·삭제 0건. `verify.template.md`·`scan-surface.sh` 정상 소비. ✅

## 12. 구현자 코멘트 / 선조치 경계

| 구현자 코멘트 | 검증자 판단 | 반영 |
|---|---|---|
| 놓친 문제 #1 — `createConnectionSources` 의 `harness`/`usage` 미호출을 "결함" 으로 본 조사 에이전트 보고를 **기각** | **타당.** `deployment/connections.ts` 헤더가 확장점 설계를 명시하고 `deployment-wiring.test.ts` 가 두 category 를 태운다. 기각 근거가 코드에 있다 | 유지 |
| 놓친 문제 #3 — `shared/ipc.ts` 2줄 diff 를 **무해 판정** | **타당.** 검증자 재확인 결과 주석의 경로 표기 1줄(`contracts/provider.ts`→`contracts/auth.ts`)뿐이고 `ProviderKind` 등 계약은 무변경 | 유지 |
| 되먹임 3분기 — "전부 첫째 갈래, 둘째·셋째 0건" | **타당.** D-008·D-009·AC11~15 는 사용자 지적 후 설계자 자격 추가이고 provenance 가 Ledger 출처 칸에 있다. 무단 AC 완화 아님 | 유지 |
| 강제 지점 **7/7** · AC **15/15** | **과대 보고.** 재측정 5/7(2 부분) · 13/15 | D2·D3 로 이관 |
| "r1 보고는 5행에 멈춰 있었다 — 게이트 초록이 전수를 뜻하지 않는다" | **자기 관찰은 정확한데 같은 실수가 이번 턴에도 났다.** #2·#5 를 닫았다고 보고했으나 문서에 표식이 없다 | D2·D3 |

## 13. 파생 이슈

> `plan.md` 의 `[검증자 기입] 파생 이슈` 로 이관한다. **D1~D3 이 재구현 대상, D4~D5 는 함께 정리 권장.**

- [ ] **D1 — INDEX 21행 비고의 중복 블록 제거.** `②③④` 가 2벌이고 뒷벌이 r2 가 철회한 주장(U2 "제안 비범위 침범", U3 "정보성" 시절 표현)을 현재형으로 싣는다. 유입 커밋 `6172bc1`. 뒷벌을 지우고, D-009 취지대로 **보드에도 "r2 에서 철회" 사실을 한 구절로 남긴다.** 같은 행의 대상 커밋 열 `(구현자 산출)` → `dddd05b` 로 교체.
- [ ] **D2 — AC3 미충족: 연역 표기 0건.** `audit.md` 에 "연역" 이 한 번도 없다. U1(`void`+catch 부재 → unhandled rejection)은 실행 관측이 아니라 연역이므로 그렇게 표기한다. 표기 후 §10 강제 지점 #2 를 다시 센다.
- [ ] **D3 — AC12 미충족: 축 3 회귀표에 `층` 열 부재.** line 171 헤더가 `# / 문제 / 원인 / 방안` 이다. 축 1(line 65)·부수(line 97)·F4(line 78)와 같은 축으로 `층` 열을 넣는다 — 층 판단 자체는 이미 산문에 있으므로 옮기면 된다.
- [ ] **D4 — 수치 2건 정정.** ① "ACTIVE Decision **63건**" → 실제 **총 62건(D-053 결번) · ACTIVE 61 · SUPERSEDED 1(D-021)**. `audit.md:36`·`plan.md:55`·`plan.md:364`·`plan.md:460`·INDEX 21행. 범위 표현 `D-001~D-063` 은 그대로 유효하다. ② "제안 §금지표 **22행**" → **데이터 20행**(22 는 헤더+구분선 포함 마크다운 줄 수). `audit.md:58`·`plan.md:195`·`plan.md:297`·INDEX 21행. **두 오차 모두 판정을 바꾸지 않는다** — 20행 전부 미위반이고 Decision 대조 결론도 동일하다.
- [ ] **D5 — AC11 부분 · 자기 상한 초과(낮음).** ① 36불릿 ↔ AC1~25 "전수 매핑" 이 *방법*으로만 서술되고(line 92) 매핑 자체가 산출물에 없어 독자가 재현할 수 없다 — 잔여 6건만 남았다. 부록 표로 싣거나 AC11 문언을 "잔여 목록" 으로 좁힌다(후자는 사용자 결정). ② plan §14 의 "발견당 인용 최대 3" 을 U1 이 4건으로 넘겼다 — 인과 사슬 4홉이라 불가피해 보이므로 상한 문언을 고치는 쪽을 권한다.

## 14. Review Signals — 사실만

> 원인 분류(A~F)와 SKILL 변경은 `handoff-review` 몫이다.

- **이전 라운드와 동일/유사 증상**: **있다.** `Criteria-Met` 과대 보고가 0187 r1(`16/16` → 실제 13✅/2⚠️/1❌)에 이어 0189 r1(`15/15` → 13/15)에서 반복됐다. 두 번 다 **자기보고 AC 대조가 형식 요건을 통과했다고 적었고 검증자가 문서/코드에서 그 형식을 찾지 못했다.**
- **같은 handoff 안에서의 반복**: 구현자 스스로 "r1 전수표가 5행에 멈춰 있었다 — 게이트 초록이 전수를 뜻하지 않는다" 를 기록했는데, **같은 턴에 닫았다고 보고한 #2(실측/연역)·#5(3층)가 실제로는 닫히지 않았다.** 관찰과 적용 사이가 벌어졌다.
- **관련 plan 지침/AC 의 존재 여부**: **있었다.** AC3·AC12 가 검증 수단을 "표기 누락 0건"·"항목별 `층` 열 존재" 로 **이미 기계적으로** 적어 뒀다. 지침 부재가 아니라 **자기 대조에서 그 문언을 다시 읽지 않은 것**이다.
- **구조적 사실 — 설계와 산출이 한 커밋**: `9a2980a` 가 plan 과 audit 을 함께 만들어 SKILL §0("구현 전 plan 기준선을 잠근다")이 이 handoff 에서는 diff 로 작동하지 않았다. 문서 전용 handoff 의 일반형일 수 있다.
- **구조적 사실 — 결론의 두 번째 사본**: 감사 결론이 `audit.md` 와 INDEX 비고 **두 곳**에 산다. plan §10 은 인용 정확성의 강제 지점을 전자에만 걸었고 후자에서만 철회본이 살아남았다(D1). 같은 형태가 다른 handoff 의 긴 INDEX 비고에도 있을 수 있다.
- **사용자 결정 변경 근거**: 없음. D-001~D-009 전부 ACTIVE 유지.
- **반복된 검증 환경 한계**: `app/node_modules` 부재로 lint/typecheck/vitest 실행 불가 — 0188 verify(ABI)·0189 impl 에 이어 3회째. **이번엔 `app/**` 미변경이라 판정에 영향 없다.**
- **자기 검증 4중**: 0188 구현 · 0189 설계 · 0189 구현 · 0189 검증이 모두 Claude Code. 이 검증은 기준을 AC 문언과 코드로 잠가 완화했으나 독립 감사자가 아니다.

## 15. 결론

- **상태: FAIL** (라운드 1)
- **Product/UX 및 ACTIVE Decision 충족**: D-001~D-005·D-007 ✅ / **D-006 ❌**(연역 표기) · **D-008 ⚠️**(축 3) · **D-009 ⚠️**(보드에서 철회본 생존).
- **AC 충족**: **13 ✅ / 2 ❌** (AC3 · AC12). 자기보고 `15/15` 는 과대.
- **§10 강제 지점**: **5/7 충족 · 2 부분**. 자기보고 `7/7` 은 과대. 표에 없는 지점 1건 발견(보드 = 결론의 두 번째 사본).
- **기준 밖 결함**: 수치 2건 과다(D4) · AC11 매핑 미수록(D5). **판정을 뒤집는 것은 0건.**
- **repository operation checks**: trailer ✅ / **INDEX 비고 ❌**(D1) · 대상 커밋 열 자리표시자 ⚠️.
- **감사 내용 자체의 신뢰도**: **높다.** 인용 40/40, 규모 수치 11종 전부 정확, 전칭 주장 8종 성립, 철회 2건 모두 baseline 대조에서 정당함이 확인됐다. **역방향 탐색에서 뒤집힌 판정은 0건이다.** FAIL 은 감사가 틀렸다는 뜻이 아니라 **계약한 표기·보드 정합이 닫히지 않았다**는 뜻이다.
- **남은 사람 확인**: F1~F3 · P1~P2 · U2~U3 의 시정 여부(→ 0190). AC11 문언을 좁힐지 부록을 실을지(D5①).
- **다음 단계**: **재구현 r2 — 구현자(Claude).** D1·D2·D3 을 닫고 D4 를 함께 정정한 뒤 `impl/IMPL_DONE` 으로 되돌린다. 전부 문서 수정이며 `app/**` 는 계속 건드리지 않는다. 재구현 시 §10 전수표와 AC 대조를 **문언을 다시 읽고** 채운다 — 이번 FAIL 2건은 둘 다 "닫았다고 적었으나 문서에 표식이 없는" 형태다.
