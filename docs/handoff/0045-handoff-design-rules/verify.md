# Verify — 0045-handoff-design-rules

> 정본 규칙은 [`../AGENTS.md`](../AGENTS.md) §3.

## 메타

| 항목 | 값 |
|---|---|
| slug | `0045-handoff-design-rules` |
| 검증자 | Claude Code |
| 일자 | 2026-06-25 |
| 대상 커밋 | `9b82b19` (구현) · `4c3b73a`(설계) · 회고 `49fbed9`·`0b896af`·`0d81844` |
| 라운드 | 1 |
| 상태 | PASS |

## 구현자 코멘트 확인 (매트릭스 전 선행)

| 구현자 코멘트 | 검증자 판단 | 반영 |
|---|---|---|
| 이견: B1/B3 는 문구로 강제 불가, 자가 점검 약점 잔존 | 타당 — C1 계열 한계, plan 리스크 §에 수용 기재됨 | 비범위로 인정(verify 자기 리뷰에 재기록) |
| 선조치 #1: 리스크 섹션 blockquote 신설(B3 자리) | 타당 — 템플릿 일관, 설계 변경 아님 | AC6 증거로 채택 |
| 선조치 #2: 디렉토리 구조 번호 표기도 정합화 | 타당 — AC7 정신에 부합(한 규칙 두 표기 방지) | AC7 증거로 채택 |

## 요구사항 충족 매트릭스

| # | 인수 기준 | 충족 | 증거 |
|---|---|---|---|
| 1 | A1 트리비얼/메타 카브아웃 (root+handoff) | ✅ | `AGENTS.md:43` "예외(핸드오프 생략…)" · `docs/handoff/AGENTS.md:26` 카브아웃 |
| 2 | A3 라이브 출처 (템플릿+self-review) | ✅ | `plan.template.md` 의도 표 "라이브 세션 요청" + self-review "라이브 세션 요청" |
| 3 | A4 파생 이슈 주체 → 검증자, 대리 기록 | ✅ | `plan.template.md:146` `[검증자 기입]` · `AGENTS.md §3` "검증자가 사용자 결정을 대리 기록" |
| 4 | B1 분류 휴리스틱 | ✅ | `docs/handoff/AGENTS.md:25` "변경 산출물→구현, 답/설명→자료조사…수정 넘어가는 순간 트리거" |
| 5 | B2 선조치 tie-breaker | ✅ | `docs/handoff/AGENTS.md:112` Tie-breaker · `plan.template.md:123` "의심되면 ⚠️ 보고만" |
| 6 | B3 체크박스 가드 (UX+리스크) | ✅ | `plan.template.md:66` "복붙하지 말 것" · `:74` "복붙 금지 — 없으면 N/A" |
| 7 | 번호 max(번호)+1 | ✅ | `AGENTS.md:43` · `handoff/AGENTS.md:31,56` · `INDEX.md:92` 모두 `max(번호)+1` |
| 8 | 정식화 (plan/verify/INDEX) | ✅ | `0045/plan.md`·본 `verify.md`·INDEX 0045행. **PHASES 미승격**(아래 — 0001 선례) |

**8/8 충족.**

## 검증 책임 분리 (사람 vs 에이전트)

| 항목 | 에이전트(Claude) | 사람(사용자) | 결과 |
|---|---|---|---|
| 문서 형식/링크/한국어 | ✅ | — | 일관 |
| 3자 정합(plan↔AGENTS↔템플릿) | ✅ grep | — | 7항목 일치 |
| AGENTS.md 위생 스캔 | ✅ grep | ✅ 최종 | 비밀/PII 0 |
| 규칙 타당성(카브아웃 남용 우려 등) | ✖ 보조 | ✅ 결정 | 사람 확인 대기 |
| 회고적 정식화 수용 | ✖ | ✅ | 사람 확인 대기 |

## 게이트 재실행 결과

```
문서 전용 변경 — 코드 게이트(lint/typecheck/test) N/A.
대체 게이트: grep 3자 정합 = A1/A3/A4/B1/B2/B3/번호 7항목 전부 존재·일관 ✅
```

## 위생 검토 (AGENTS.md 변경 시)

- 키/토큰/이메일/IP 패턴: 없음(규칙 문구만).
- 변동성/일회성/장문 코드설명서 혼입: 없음. 카브아웃·휴리스틱은 안정 규칙.

## PHASES.md 정합성

- **PHASES 미승격이 정답.** PHASES.md 는 *제품 페이즈 이력*(Phase 1~3++)이고, 핸드오프 인프라/메타 작업은 선례 `0001-handoff-bootstrap` 도 PHASES 에 없다(`grep -c` = 0). 0045 도 동일 — INDEX 행으로만 추적, PHASES 미기재. (plan AC8 의 "PHASES 승격" 문구는 본 verify 가 정정.)

## 검증 자기 리뷰 (무엇이 부족했나)

- 설계 단계: 회고적 정식화라 AC 가 *이미 한 일*과 *새 보정*을 한 묶음으로 섞었다 — 본래 둘을 별 핸드오프로 나눴으면 더 깔끔. 차회 교훈.
- 구현 단계: B1/B3 는 문구 가드일 뿐 *강제력 없음*(자가 점검 의존, C1). 본 핸드오프가 메우지 못하는 구조적 한계로 남는다.
- 검증 단계: 문서 정합은 grep 으로 충분히 보였으나, **실효성(에이전트가 실제로 규칙을 지키는지)** 은 향후 핸드오프들이 새 템플릿을 쓰는지로만 사후 확인 가능 — 이번 verify 범위 밖.

## [FAIL 시] 미충족 요구사항 (구현자 액션 아이템)

- 해당 없음 (PASS).

## 결론 / 다음 단계

- 상태: **PASS** → INDEX `verify/PASS`(PHASES 미승격 — 0001 선례). 세션 6커밋(`49fbed9`→verify)으로 핸드오프 3턴 규칙 + 진입 트리거 + 자기검토 보정이 `0045` 로 정식화됨.
- 사람 확인 대기: 카브아웃 남용 경계·회고적 정식화 수용·(요청 시) PR 머지.
