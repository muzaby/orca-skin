# Verify — 0192-doc-gate-baselines-as-data

> 절차 정본은 [`handoff-verify/SKILL.md`](../../../.agents/skills/handoff-verify/SKILL.md).

## 메타

| 항목 | 값 |
|---|---|
| slug | `0192-doc-gate-baselines-as-data` |
| 검증자 | Claude Code |
| 일자 | 2026-08-19 |
| 대상 커밋/range | `caf0006..61834d4` (구현 `3d9cf9e` · 해시 기입 `61834d4`) |
| 구현 전 plan 기준 | `caf0006` |
| 라운드 | 1 |
| 상태 | **PASS** |
| 자기 검증 여부 | **예 — 설계·구현·검증 동일 에이전트.** 완화는 §0 기준선 diff + 전 판정의 독립 재측정 |

## 0. 기준선 / plan 변경 확인

**기준선이 diff 로 성립한다.** 설계(`caf0006`)와 구현(`3d9cf9e`)이 갈린 커밋이라 §0 의 자기 증명 방지 장치가 작동한다.

- 구현 커밋의 `plan.md` diff: `+91` 줄 전부 `[구현자 기입]` 절과 메타 `상태` 한 줄. 재현: `git diff caf0006..3d9cf9e -- .../plan.md`.
- **AC 변경 없음** — §7 의 AC1~AC13 행에 hunk 0. 구현자가 자기 코드에 맞춰 완화한 흔적 없다.
- **Decision Ledger 변경 없음** — D-001~D-008·D-101~D-103 에 hunk 0. `SUPERSEDED` 신규 0건.
- **Product/UX Contract 변경 없음** — §1·§5·§6 에 hunk 0.
- 채점 기준 = `caf0006` 시점 AC 13행 + ACTIVE Decision 11건.

## 1. Product & UX / ACTIVE Decision 요약

| Decision | 기대 결과 | 실제 production path |
|---|---|---|
| D-002 완결성은 기계가 차집합으로 | 사람이 `미분류 0` 을 적지 않는다 | `check` → `comm -23`/`comm -13` → `emit_diff` → exit code |
| D-004 후행 주석까지 실재 판정 | `code // Sym` 이 코드로 안 셈 | `doc-gate.sh:74` sed → `Query` 4사이트 산출 |
| D-005 비범위는 사이트 경로로 | PRD 인용이 범위 내 버킷에 안 샘 | `is_out_of_scope_site` → `inscope` → `required` |
| D-003·D-101 CI·`app/**` 미변경 | 코드 diff 0 | `git diff --quiet caf0006..61834d4 -- app/ .github/` → **0파일** |
| D-007 0191 수치 미변경 | 정본이 두 벌 안 됨 | 0191 `plan.md` hunk 1개·5 insertions·0 deletions |

### end-to-end 흐름 (재현)

```text
저장소 트리 → doc-gate.sh check → 3축 스윕 → baselines/*.tsv 양방향 차집합
  → 축별 산출 수 + 버킷별 사이트 수 + exit 0
  ↘ 어느 차집합이든 비지 않음 → 어긋난 줄을 이름·위치로 + exit 1
  ↘ SCOPE/CORPUS/baseline 부재 → exit 2 (빈 산출이 "통과" 로 안 읽힌다)
```

세 종단을 전부 관측했다(§2·§5).

## 2. 구현 결과 비판적 검토 — AC 전에

| 질문 | 판정 | 근거/후속 |
|---|---|---|
| false success 가능성 | **없다 — 실측** | 3축 baseline 을 각각 **전부 비우고** 재실행: `미분류(사이트) 287` · `미등재 20` · `미판정 133`. 산출 전건이 그대로 떠 join·comm 이 한 행도 안 흘린다 |
| 눈이 없는 장치의 0건 | **막힌다** | CORPUS 1경로·baseline 1파일·비범위 문서를 각각 제거 → 전부 **exit 2**, 스윕 전에 죽는다 |
| 경보만 울리는 장치인가 | **아니다 — 참 음성 확인** | 실재하는 신규 심볼(`ZzVerifyRealProbe`, `index.ts` 에 실제 선언)을 문서에 인용 → **산출 0행**. 없는 심볼(`ZzVerifyAbsentProbe`)만 `ABSENT … GLOSSARY.md:98` |
| partial failure / 상태 잔여 | 없다 | `sweep`·`check` 읽기 전용. `regen` 은 임시파일 → `mv`. 검증 중 트리 상태 매번 복원 확인 |
| A 가 아니라 B 를 구현했는가 | A 다 | §5 상태 전이 4행이 전부 실제 산출로 대응된다 |
| 최적화가 잃은 관측 | 없다 | `SYMBOL_CACHE` 는 같은 스윕의 재사용이다. 캐시 유무로 산출이 갈리지 않음을 §5 AC6 재측정에서 확인 |
| worst-case 상한 | 재계산 일치 | 후보 쌍 **2013** · 고유 심볼 **922** → `grep` 호출 상한 922. `check` 실측 **13.7초** |

## 3. 역방향 탐색

`scan-surface.sh caf0006..61834d4` → "변경된 소스 파일이 없습니다" (코드 diff 0, D-101 과 정합).

| 후보 | 판정 | 근거 |
|---|---|---|
| 신규 표면의 프로덕션 소비처 | **⚠️ 파생 D3** | `doc-gate` 를 가리키는 곳 전수 4건이 **전부 `docs/handoff/` 안**이다 |
| 테스트 전용 참조 | 해당 없음 | 테스트 코드가 없다 — 검증 수단이 결함 심기다 |
| 형제 정책 비대칭 | 없다 | 3축이 같은 `scope_files`·같은 `emit_diff`·같은 `comm` 쌍을 쓴다 |
| producer ↔ consumer 파생 불일치 | 없다 | baseline 161행 ↔ 산출 167심볼의 차 6 = PRD 전용 6심볼(§5 AC7)로 전액 설명된다 |
| 동일 규칙 중복 구현 | **SSOT 유지** | 0191 §19 의 bash 블록은 살아 있으나 승계 포인터가 "현재 정본은 TSV" 를 명시(`0191/plan.md:323`) |
| 정리되지 않는 산출물 | **⚠️ 파생 D2** | `check` 1회당 `/tmp/tmp.*.m` 1개가 남는다 |

## 4. 기존 테스트 / semantic 검증 확인

- 기존 테스트 재사용: 없음(코드 diff 0). AC13 만 기존 가드를 회귀 수단으로 쓴다 — 실제 실행해 3항목 ok 관측.
- **structural proxy 로만 통과한 AC: 없다.** AC2·AC3·AC8·AC9 의 관측이 전부 `comm` 출력이고, 합계·총계 일치를 근거로 쓴 행이 없다.
- `12/12`·`13/13` 자기보고를 증거로 쓰지 않았다 — §5 에서 전부 다시 셌다.

## 5. 요구사항 충족 매트릭스

| # | 기준 | 결과 | 검증 증거 (검증자 재측정) |
|---|---|---|---|
| AC1 | baseline 1행 삭제 → 이름으로 짚고 exit≠0 | ✅ | 3축을 **전부 비워** 상한을 봤다: `287`·`20`·`133` — 전건이 이름·위치와 함께 출력. 복원 후 exit 0 |
| AC2 | 심볼 축 미분류 0 | ✅ | `미분류(사이트): 0`. 근거는 `comm -23` 출력이지 합계가 아니다 |
| AC3 | 심볼 축 잔류 0 | ✅ | `잔류(심볼): 0`. N1 3심볼 각각 baseline **0행** · 스윕 **0행** |
| AC4 | 버킷 6행 내역이 산문에 없다 | ✅ | 0192 `plan.md`·`INDEX.md` 정규식 스캔 → 버킷명+수치 나열 **0건**(hit 2건은 AC5 의 버킷 *이름*과 N2 의 "여기 적지 않는다" 문장) |
| AC5 | `filterEscalatingDefaultMode` = `외부` | ✅ | `symbol-buckets.tsv:129` 정확히 `filterEscalatingDefaultMode\t외부`. **L1 종결** |
| AC6 | 후행 주석을 주석으로 센다 | ✅ | `Query` 4사이트 전부 산출에 있다(`overview.md:209`·`provider-runtime.md:184`·`:188`·`:202`). 좁은 판정 대비 역차집합 **0** |
| AC7 | 비범위가 사이트 경로로 파생 | ✅ | PRD 사이트 **9** 전부 비범위. PRD 전용 6심볼 baseline **0행**. PRD·범위 양쪽에 사는 3심볼(`ChatEvent`·`SDKSystemMessage`·`feat-pretty-ui`)은 baseline 행을 **유지**하고 PRD 사이트만 분모에서 빠진다 |
| AC8 | 경로 축 양방향 0 | ✅ | `미등재 0`·`잔류 0` (산출 20줄). 가짜 행 주입 → `잔류 1` |
| AC9 | 시제 축 미판정 0 | ✅ | `미판정 0`·`잔류 0`. 133행 전부 판정 `참`, 고유 줄 **96** |
| AC10 | 판정 지점마다 심은 결함이 잡힌다 | ✅ | **12/12 재확인** — 아래 강제 지점 표 |
| AC11 | 두 행 비고 5문장 이내 · 0191 다음 주체 `—` | ✅ | 재측정 0192 **4문장/314자** · 0191 **3문장/245자** · `다음 주체` = `**—** (0192 승계)` |
| AC12 | 0191 §19 승계 포인터, 수치 줄 미변경 | ✅ | hunk **`@@ -322,0 +323,5 @@` 하나**, 5 insertions / 0 deletions |
| AC13 | 인벤토리 가드 3종 통과 | ✅ | `generated doc ok (9 items, 76 channels)` · `prose ok` · `links ok` · exit 0 |

- **합계 재측정**: `✅ 13 · ⚠️ 0 · ❌ 0 = 총 13`. 분모 13 을 plan §7 표에서 직접 세었다 — 자기보고 `13/13` 과 **일치**.
- **합계 사본 대조**: 본문 `13/13` ↔ trailer `Criteria-Met: 13/13`(`3d9cf9e`·`61834d4` 둘 다) ↔ INDEX 비고 `AC 13/13` — **세 곳 일치**(0190 r1 이 갈렸던 축).
- AC11 만 자기보고(5/4문장)와 재측정(4/3문장)이 다르다. 세는 법의 차이(제목 문단 분리)이고 **두 계산 모두 상한 5 이하**라 판정은 같다.

### plan §10 강제 지점 표 — AC 와 별개로 걸었다

**12/12 + 계약 6/6.** 자기보고를 출발점으로 쓰되 전 지점을 직접 다시 심었다.

| 지점 | 검증자가 심은 결함 | 관측 |
|---|---|---|
| 1·3·4 추출(백틱·bold·호출식) | `ZzVerifyAbsentProbe` 등 | `ABSENT ZzVerifyAbsentProbe docs/GLOSSARY.md:98` |
| 2 추출(하이픈) | 엄격 경계 재측정으로 대체 | 아래 T2 |
| 5·6 대상 집합(`*.test.*`·`*.md`) | — | 재현 확인, 구현자 관측과 일치 |
| 7·8 실재(줄머리·**후행** 주석) | `ZzVerifyHashProbe`(후행 `#`) | `#` 는 안 잡힌다 = 등재된 한계(#3)의 실증. `//` 는 잡는다 |
| 9 단어 경계 | 엄격 경계 스윕 전량 재실행 | 아래 T2 — **차집합 1** |
| 10 사이트 단위 | — | 재현 확인 |
| 11 비범위 파생 | PRD 사이트 전수 대조 | 9사이트 · 6심볼 baseline 0행 |
| 12 preflight | CORPUS 1경로 · baseline 1파일 · 비범위 문서 각각 제거 | **셋 다 exit 2** |
| 계약 형식·허용값 | 잘못된 버킷 · 빈 칸 · `UNCLASSIFIED` · `UNJUDGED` 동시 주입 | `형식오류 4` + `형식오류 1` · exit 1 |
| 양방향 | 3축에 가짜 행 주입 | `잔류(심볼) 1` · 경로 `잔류 1` · 시제 `잔류 1` · exit 1 |

- **표에 없는데 같은 불변식이 필요한 지점: 없다.** 구현자가 "join 키는 지점으로 세지 않았다" 고 적은 자리를 §2 의 baseline 전량 비우기로 대신 닫았다 — join 이 눈멀면 `287` 이 아니라 더 작은 수가 나온다.

### 재현이 아니라 엄격화 — 판정 기준을 한 단계 올린 차집합

게이트를 만든 턴과 검증하는 턴이 같으므로 같은 명령의 재실행은 증거가 아니다. 판정 기준을 각각 한 단계 좁혀 차집합을 봤다.

| 엄격화 | 차집합 | 판정 |
|---|---|---|
| **T1** 후행 `#` 주석도 자른다 | **0** (296 → 296) | 구현자 보고 #3("`#` 로만 존재하는 심볼 0건")이 **독립 확인**됐다 |
| **T2** `-wF` → `(?<![A-Za-z0-9_-])…(?![A-Za-z0-9_-])` | **+1** (296 → 297) | 실재 결함이다 — 아래 D5 |
| **T3** 블록 주석 내부 줄을 주석으로 | 후보 4 | **전부 내 엄격화의 오탐**. 4건 다 실제 코드 줄이었다(`index.ts:128 function createWindow`) — 내 검출기가 `adapters/*` 글로브의 `/*` 를 주석 시작으로 읽었다. **실제 차집합 0** |

## 6. 외부 포트 / 문서 계약

| 계약 | shape | semantics | 결과 |
|---|---|---|---|
| `symbol-buckets.tsv` | `awk NF!=2` + 허용값 5종 | 허용 밖 버킷·빈 칸·`UNCLASSIFIED` 전부 거부 | ✅ |
| `path-exceptions.tsv` | `awk NF!=4` + 축 `[ABC]` | ✅ | ✅ |
| `tense-sites.tsv` | `awk NF!=4` + 판정 2종 | `UNJUDGED` 거부 | ✅ |
| `regen` 승계 계약 | — | **멱등 재확인** — 실행 후 3파일 `git diff` **0줄**, 이어진 `check` 통과 | ✅ |

## 7. 숫자 / 음성 기준 / 상한 재측정

- 후보 쌍 **2013** · 고유 심볼 **922** — plan §8 과 일치(추출 4축을 직접 재실행).
- 산출 **296사이트 / 167심볼** · 범위 내 **287** · 비범위 **9** — `287 + 9 = 296` ✓.
- baseline 행 **161** = 167 − PRD 전용 6 ✓ (구현자의 "설계 대비 차이 2" 와 일치).
- 실재 판정 = 존재 로 갈린 심볼 **755**, `755 + 167 = 922` ✓.
- 경로 **20줄** · 시제 **133사이트**(고유 줄 96).
- **0건 게이트가 정당한 예외를 지우지 않는가**: 지우지 않는다. 경로 축 20줄은 삭제가 아니라 사유와 함께 등재돼 있고, `잔류` 축이 등재를 임의 삭제하는 것을 막는다.
- **버킷별 사이트 수는 이 문서에 적지 않는다**(AC4). 정본은 `doc-gate.sh check` 산출이다.

### 시제 축 육안 판정의 표본 재검

기계가 만든 목록에 사람이 붙인 값이라 구현자 보고를 그대로 받지 않았다. **최고위험 표본** — 버킷이 `설계어휘`/`future` 이면서 줄에 `현행` 또는 `✅` 가 있는 15사이트를 뽑아 그중 7건의 원문·표 머리·코드 실재를 대조했다.

- `rendering.md:84`~`:86` — 열 제목이 `현행 대응` 이고 `✅` 가 가리키는 `BashBody`·`FileBody`·`DiffBody` 는 코드에 실재(각 2·2·3파일). **참**.
- `state.md:91` — `#### 4.4.4 대안 비교` 의 미채택 옵션 행. `✅` 는 옵션의 성질이지 구현 단언이 아니다. **참**.
- `provider-runtime.md:407` — 표 제목이 `정규화 전 이름 → 목표명 매핑표 (rename 범위 밖 — 문서로만)`. `PermissionBridge` 는 **목표명** 열이다. **참**.
- `GLOSSARY.md:70`·`standardization.md:85` — `BackendAdapter` 를 각각 "과거 설계 논거"·"3번째 엔진까지 미룬다" 로 서술. **참**.

표본 7/7 이 `참` 으로 유지된다. 남은 126사이트는 재판정하지 않았다 — **못 본 것으로 적는다**.

## 8. 테스트 가능한 핸들 탐색 후 남은 사람 실기

| 항목 | 기계 검증한 범위 | 남은 사람 실기 |
|---|---|---|
| 시제 축 | 목록 생성 · 미판정/잔류 검출 · 형식·허용값 · 표본 7건 대조 | 나머지 126사이트의 자연어 사실성 |

"UI/SDK/electron 이라서" 로 넘긴 순수 로직은 없다. 스크립트는 POSIX 도구뿐이라 전량 기계 검증했다.

## 9. 게이트 재실행

`app/**` diff 0 이므로 `app/AGENTS.md` 의 ABI 게이트는 비적용이다(plan §19 판단 **타당**). `npm run lint`·`typecheck`·`vitest` 를 돌리지 않은 것은 환경 제약이 아니라 **대상 부재**다.

- 실행: `bash docs/handoff/0192-.../doc-gate.sh check` · 같은 스크립트 `regen`·`sweep` · `cd app && node scripts/check-doc-inventory.mjs --check`.
- **관측한 실행 산출(exit code 아님)**: `check` — 심볼 296사이트/167심볼 · 경로 20줄 · 시제 133사이트 · **차집합 9종 전부 0** · **13.7초**. 인벤토리 — `generated doc ok (9 items, 76 channels)` · `prose ok` · `links ok` **3줄**.
- **게이트가 작업 트리를 바꿨는가**: `check`·`sweep` 은 읽기 전용. `regen` 은 쓰기지만 실행 결과 **0줄 변경**(멱등). autofix·formatter 없음.
- **검증 중 실행한 명령의 잔여물**: 결함 심기·baseline 조작·파일 이동을 매 단계 복원했고 최종 `git status --short --untracked-files=all` **빈 출력**. 변종 스크립트 3종은 scratchpad 밖으로 안 나갔다.

## 10. 검증 책임 분리

| 항목 | 결과 |
|---|---|
| 정적 가드 / 게이트 실행 | 에이전트 — §9 산출 관측 |
| AC ↔ production path | 에이전트 — §5 1:1, 전건 재측정 |
| 문서 형식·링크 | 에이전트 — `links ok` |
| 제품 결정(도구 위치 · 보드 좌표 정책) | **사람** — D1·D3 |
| 시제 축 자연어 사실성 | **사람** — 126사이트 미판정 |

## 11. Repository operation checks

### AGENTS.md 위생

해당 없음 — 이번 범위에 `AGENTS.md` 변경 0파일.

### INDEX 보드 정합성

- 단계 `impl` · 상태 `IMPL_DONE (r1)` · 다음 주체 `Claude (검증)` · 라운드 `1` — 실제 상태와 **일치**.
- 0192 대상 커밋 `3d9cf9e` — **실재**(`git cat-file -e` 통과).
- 비고 5줄 이내 — 재측정 4문장·3문장 ✓.
- **0191 대상 커밋 `fa09594` — 실재하지 않는다.** → **D1** (아래).
- PASS archive 이동: **보류** — D-008 의 실행이 D3 와 얽힌다(§13).

### Commit / reference 정합성

- trailer 3커밋 전부 `git interpret-trailers --parse` 로 파싱 확인. `Agent: claude` · `Status: implemented` · `Criteria-Met: 13/13` · `Verified-By: pending`, `Next-Action` 없음 — `docs/git-template.md` 의 **구현 커밋** 규칙과 일치. 빈 줄로 끊긴 trailer 없음.
- 승계 포인터가 가리키는 `../0192-doc-gate-baselines-as-data/plan.md` 실재 ✓ (`links ok` 가 재확인).

## 12. 구현자 코멘트 / 선조치 경계

| 구현자 코멘트 | 검증자 판단 | 반영 |
|---|---|---|
| 선조치 — `://` 보호 sed | **타당** | T1 재측정으로 산출 동치 독립 확인 |
| 선조치 — `SYMBOL_CACHE` 1회 계산 | **타당** | 관측 없앰 없음(§2). 13.7초 실측 |
| 선조치 — 출력에 사이트 부착, `미분류(사이트)`/`잔류(심볼)` 라벨 | **타당** — plan §5 계약을 지킨 것이다 | AC1 재측정에서 위치가 실제로 나온다 |
| 보고 #3 후행 `#` 한계 | **타당 · 관측도 정확** | T1 차집합 0 으로 독립 확인 |
| 보고 #4 `-wF` 하이픈 한계 | **타당하나 관측이 없다** | 수확을 안 셌다 → **D5** 로 수치를 붙인다 |
| "설계 대비 차이" 2건(단위=사이트 · baseline 161 vs 167) | **타당** — AC 완화가 아니라 분모 명시 | §5 AC2·AC7 에서 확인 |

## 13. 파생 이슈

PASS 를 막지 않는다. D1·D3 은 **사용자 결정**이 필요해 해결안으로 위장하지 않는다.

| # | 이슈 | 관측 | 대응 방향 |
|---|---|---|---|
| **D1** | 보드가 인용한 커밋 좌표가 **저장소 전역에서 죽어 있다** | `docs/handoff/**/*.md` 인용 고유 해시 **429개 중 411개가 `git cat-file -e` 실패**. 살아남은 18개는 전부 현재 이력(53커밋, root `ee9cb9e` 2026-08-15) 이후 것이다. 0191 `verify.md:147` 은 `fa09594`·`4ef4ffd`·`404a32f`·`5b1fd22` 를 "전부 통과" 로 적었고 지금은 **4개 다 없다** | **이력 재작성의 결과지 0192 의 오기가 아니다.** 0192 는 죽은 좌표를 옮겨 적었을 뿐이다. 보드의 `대상 커밋` 열을 어떻게 할지는 정책 결정 — 사용자에게 묻는다. 참고로 0191 r6 구현의 현재 해시는 `5436419`, 해시 기입은 `abc9f2e` 다 |
| **D2** | `check` 1회당 `/tmp/tmp.*.m` 1개가 남는다 | `doc-gate.sh:150` 이 `"$stale.m"` 에 쓰는데 `:162` 의 `rm -f` 목록에 없다. 실행 전후 `/tmp/tmp.*` 12 → 13 | `rm -f` 에 `"$stale.m"` 추가. 1줄 |
| **D3** | 게이트를 가리키는 좌표가 **전부 "기본 세션에서 읽지 않는" 구역**에 있다 | `doc-gate` 참조 전수 4건 = 0191 `plan.md:323`·`:324` + `INDEX.md` 두 행. 루트 `AGENTS.md:33` 은 `docs/handoff/<NNNN-slug>/` 를 기본 세션 비독서로 지정. **D-008 이 두 행을 archive 로 옮기면 보드 쪽 포인터가 0 이 된다** | plan §1 은 "다음 세션이 명령 하나를 돌려" 를 성공 문장으로 뒀다. 도구 위치·진입점(`docs/INDEX.md` 라우팅 · `app/scripts/` 이설 · CI 승격)은 **사용자 결정**(plan §17 이 예고). **결정 전까지 D-008 archive 이동을 실행하지 않는다** |
| **D4** | AC9 의 `프로덕션 도달 경로` 칸이 조건을 뒤집어 적었다 | "심볼은 실재하는데 그 줄의 시제 단언이 거짓인 경로" — 실제 정의는 `sweep_tense` 가 **ABSENT/COMMENT_ONLY 사이트만** 걸러 현재형 술어를 보는 것이다(0191 §19 승계) | 구현은 0191 정의를 정확히 따랐다 — **plan 문장만 정정**. 채점에는 검증 수단 칸을 썼다 |
| **D5** | 한계 #4(`-wF` 하이픈)의 수확이 안 세어져 있었다 | 엄격 경계 재측정 차집합 **1** — `resize-handle`(`ux-domains.md:69`)이 `app-frame-resize-handle`(`Sidebar.tsx:170`) 의 부분 문자열로 통과 중이다. 문서는 "aside 내부는 `resize-handle`" 이라는 명명 규칙을 단언하는데 그 이름은 코드에 없다 | 정의 확장은 plan §6 비범위. **수치를 한계 기록에 붙인다**(넓히지 않는 판단의 근거가 된다) |

## 14. Review Signals — 사실만

- **이전 라운드와 동일 증상: 없다.** 0191 이 여섯 라운드 반복한 "합계로 완결성을 보고" 축은 이번에 발생하지 않았다 — AC2·AC3 의 관측이 전부 차집합이고, 검증자 재측정도 차집합으로 했다.
- **관련 plan 지침의 존재**: 있었고 작동했다. `handoff-impl` §8(`554b572`, 완결성 = 차집합)과 §3(`a2616f5`, 판정 지점마다 결함)이 AC2·AC3·AC10 의 형태를 정했다.
- **사용자 결정 변경 근거: 없다.** SUPERSEDED 0건.
- **반복된 검증 환경 한계**: `app/node_modules` 부재. 이번 라운드는 `app/**` diff 0 이라 게이트 자체가 비적용이었다 — 0191 r1~r6 과 달리 회피가 아니다.
- **새로 드러난 환경 사실**: git 이력이 재작성돼 handoff 문서의 커밋 좌표 411/429 가 죽었다(D1). 다음 라운드의 §0 기준선 고정에 영향을 준다.

## 15. 결론

**PASS (r1).**

- **Product/UX 및 ACTIVE Decision**: 충족. D-002·D-004·D-005 가 production path 로 도달하고, D-003·D-101(코드·CI 미변경)은 `app/**`·`.github/**` diff **0파일**로 지켜졌다.
- **AC**: **13/13**. 전건을 자기보고와 독립적으로 재측정했고 합계·분모를 따로 세어 본문·trailer·INDEX 세 곳 일치를 확인했다.
- **강제 지점**: **12/12 + 계약 6/6**. 재현이 아니라 **엄격화 차집합**으로 확인했다 — T1 0 · T3 실질 0 · T2 1(D5 로 등재).
- **기준 밖 결함**: 중대한 것 없음. D2(임시파일 1개)는 1줄 수정이고 판정에 영향이 없다.
- **repository operation**: 0192 자신의 좌표·trailer·비고·링크는 전부 정합. D1 은 이번 턴이 만든 것이 아니라 이력 재작성의 저장소 전역 결과다.
- **남은 사람 확인**: ① 도구 위치와 진입점(D3) ② 보드 `대상 커밋` 열 정책(D1) ③ 시제 축 126사이트의 자연어 사실성.
- **다음 단계**: `verify/PASS`. **archive 이동은 D3 결정까지 보류** — 지금 옮기면 게이트의 마지막 보드 포인터가 사라진다. D2·D4·D5 는 다음 접촉 시 함께 닫는다.
