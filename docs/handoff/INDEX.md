# 디스패치 보드 (Claude Code ↔ Codex)

> **두 에이전트 모두 착수 전 이 표를 가장 먼저 읽고, 작업 후 갱신한다.** "지금 누구 차례인가"의 단일 진실원.
> 상태 머신·절차 정본은 [`AGENTS.md`](AGENTS.md).
>
> **이 보드에는 미완료 작업만 둔다.** `verify/PASS` 로 끝난 행은
> [`../archive/handoffs/INDEX-history.md`](../archive/handoffs/INDEX-history.md) 로 옮긴다 —
> 완료 이력의 정본은 `git log` 이고, 보드가 이력까지 안으면 "지금 누구 차례인가" 가 묻힌다.
> 각 작업의 상세는 `<NNNN-slug>/{plan,verify}.md` 원본이 갖는다(제자리 보존).

## 단계 / 상태 범례

- **단계**: `plan` → `impl` → `verify`
- **상태**: `DRAFT` · `READY` · `IN_PROGRESS` · `IMPL_DONE` · `PASS` · `FAIL`
- **다음 주체**: `Claude` (설계/검증) · `Codex` (구현) · `—` (종료)

## 미완료 작업

| slug | 단계 | 상태 | 다음 주체 | 대상 커밋 | 라운드 | 비고 |
| --- | --- | --- | --- | --- | --- | --- |
| `0188-auth-harness-plugin-lightweight` | impl | **IMPL_DONE** (r3) | **Claude** (검증) | `2bebd67`(A) · `2b274ef`(B) · `110a1a9`(C) · `d197f0d`(r2) · `511ad32`(r3) | 3 | **인증·Harness·Plugin 경량화 리팩터링 (2026-08-14).** 사용자 요청: 첨부 제안서(`0188-.../proposal.md`, 저장소 보존)를 토대로 구현. **비기능 = Claude 직접 구현.** 핵심: `features/providers` 한 슬라이스가 ① 인증 lifecycle ② Harness+ModelProvider 설정 ③ Confluence Plugin ④ 범용 소비 표면을 `Provider` 계약 하나에 모은 집적을 푼다 — `Provider.kind/llm/tools`·`ProviderApi.materialize()`·`ProviderPlatform` 제거, `features/{auth,gate,harnesses,plugins}` + `app/deployment/` + `app/connection-views.ts` 로 재배치. 두 번째 축: `llm.envKey` 가 credential **한 값**만 표현해 "OAuth 로 config API 를 불러 URL·모델·실행 token 을 한꺼번에 싣는" 폐쇄망 요구를 못 담았다 → 기존 settings SSOT 위에 optional `RuntimeConfigAugmenter` + `HarnessRuntimeConfigService`(generation fence·single-flight·expiry) + `PreparedHarnessConfig.runtimeConfigFingerprint`(env 포함 respawn 판정). **불변식**: UI/DOM/IPC 채널/DTO/DB/vault key/`AuthId` 전부 동결(compat boundary), 부팅 방송 상한 `1 + K`(0187 D2) 유지, 신규 의존성 0, 새 migration 0. AC 24건. **[r3 · PR #336 REQUEST CHANGES 반영]** 이번 리뷰는 **이 브랜치의 실제 코드(`d197f0d`)를 대상**으로 왔고, 지적 5건이 **전부 코드에서 사실로 확인**돼 모두 고쳤다. ⓐ **배포 확장점이 실제로는 배선되지 않았다** — `createPluginBindings()`·`createRuntimeConfigAugmenters()`·`createUsageFetcher()` 가 인자를 받지 않아 배포가 자기 선언을 채우려면 범용 `bootstrap.ts` 를 열어야 했다(D-035 위반). 셋 + 신설 `createConnectionSources()` 에 deps 인터페이스를 두고 Bootstrap 이 `auth`·`registry`·`secretFor`·`gateMembers`·`plugins` 를 주입(**D-044**) ⓑ **`harness`·`usage` 카탈로그 row 를 만들 자리가 없었다** — 그 두 인증을 선언한 배포는 연결 탭에 행이 없어 **로그인 자체가 불가능**했다. `app/deployment/connections.ts` 신설(`gateRows`/`pluginRows` 조각 노출) + 가이드 레시피 B 에 row 추가 단계 명시 ⓒ **env 우선순위가 거꾸로 구현돼 있었다** — 계약은 `runtimeEnv > settings env > app env > process env` 인데 app 을 settings 뒤에 얹어 전역 폴백이 ModelProvider 전용 설정을 덮었고, **테스트가 그 뒤집힌 순서를 고정**하고 있었다. spread 순서 정정(**D-041**) + settings 의 `env` 블록을 **통째로** hoist(**D-042** — 충돌 키만 지우면 두 채널에 같은 키가 남아 SDK 우선순위에 결과가 달린다) ⓓ **시간 만료가 request·resume 에서 정착하지 않았고 세대도 안 올랐다** — r1 이 `snapshot()` 한 곳만 걸었고, 1회성을 `markExpired()` 조기 반환에 기대 `credentialRevision` 증가까지 건너뛰어 `credentialChanged:true` 인데 세대는 그대로인 change 가 나갔다. 정착 집합(`expirySettled`)으로 1회성을 분리하고 `authenticated-request`·`login.resume()` 에도 `settleExpiry()` 배치 ⓔ **fingerprint 가 env 평문을 세션 수명 내내 보존**(Security) → 프로세스 수명 랜덤 키의 **HMAC-SHA256 digest**(**D-043**). **필수 수정 5: 비어 있지 않은 가상 배포 fixture** — `app/deployment/deployment-wiring.test.ts` 신설(**AC25**/**D-045**): gate·harness·plugin·usage 4종 Auth 로 Bootstrap→Plugin 도구 등록/회수→augmenter 2방식→`UsageFetcher`→카탈로그 4행(`id`·`kind`·`tools.length` 순서 단언)을 끝까지 태운다. 기본 선언이 `[]` 인 동안 CI green 은 배포 경로를 전혀 검증하지 않았고, r2 가 그 상태로 ⓐⓑ 를 통과시켰다. 테스트 1,841 → **1,854**(파일 200 → 201). 문서: `arch/backend/auth.md` §3·§6.4·§9 개정 + 폐쇄망 가이드 §1.1 factory 시그니처 표·레시피 B 8단계·usage/cron 예제의 0188 이전 어휘(`providers.api.request`) 정리. | **[impl r1] Phase A/B/C 3커밋, 각 독립 green.** **설계가 못 본 5건을 구현이 정정**: ⓐ **Phase A 가 lint 를 통과할 수 없었다** — `auth`·`gate` 가 다른 슬라이스로 나간 순간 남은 `platform.ts`·`llm/`·`service/index.ts`·`declarations/` 가 feature 교차 import 가 된다. 넷을 Phase A 에서 컴포지션 루트로 당겨 `features/providers/` 를 A 에서 이미 없앴다(D-034 를 더 강하게 만족) ⓑ **AC4·AC23 이 electron 안에 갇혔다** — gate fail-closed 와 resume 순서를 순수 모듈(`features/gate/selectGateMembers`·`app/auth-resume.ts`)로 뽑아 순서·방송 횟수를 vitest 로 관측 ⓒ **fingerprint 가 feature 교차를 만든다**(조립부 harnesses ↔ spawn 기록부 sessions) → `adapters/harness-config.ts` 로 승격 ⓓ **재인증 실패가 이전 자격증명을 파괴하고 있었다** — D-009/AC7 은 "보존" 인데 0181 은 probe 전에 vault 를 덮고 실패 시 지웠다(구 주석이 그 사실을 인정하고 있었다). `captureForRollback/rollback` + vault 값 복구로 실제 보존하게 하고 `credentialRevision` 도 되돌린다 ⓔ **시간 만료가 정착되지 않았다** — `status()` 가 순수 조회라 `verified` 가 남고 revision 도 그대로여서 gate 가 열린 채, cache 가 죽은 토큰을 warm hit 로 준다. `settleExpiry()` 를 관측 지점(snapshot)에서 1회 호출(polling 미추가). **설계 대비 명시 차이 2건**: Phase A 가 이동 모듈의 심볼 renaming 까지 흡수(계약 diff 와 분리) · `options.settings.env` vs `options.env` 우선순위는 **실측 대신 fail-safe 분기** 채택(충돌 키를 settings 사본에서 제거 → 어느 쪽이 이기든 결과가 하나). **문서**: `arch/backend/providers.md` → **`auth.md` 재작성**(책임 지도·AuthChange 분류표·게이트 이중 강제·Harness cache 규칙·두 주입 채널·부팅 순서) + GLOSSARY 표제어 7종 신설/개정 + 폐쇄망 가이드 레시피 A·B·C 를 `app/deployment/` 기준으로 개정 + IPC compat 명시 + ADR-004 후속 절 + AGENTS 2종·overview·runtime-ipc·security·layers. **게이트**: typecheck 3/3 · lint 0 error/1 warn(기존) · vitest **200 파일 / 1,832 테스트 전부 green**(baseline 198/1,788 → **+44**) · script 49/49 · doc-inventory 3종 · migrations append-only. egress 가 열려 있어 **DB suite 포함 신규 red 0**. **인수 24/24**(사람 실기 잔여 = 폐쇄망 실서버 gate 로그인·Plugin 인증·Harness turn·Usage refresh — 기본 빌드는 선언이 비어 그 경로가 돌지 않으므로 전부 단위 테스트로 닫았다). **[r2 · 외부 리뷰 반영]** 리뷰 다수가 **다른 plan 초안**(slug `0188-providers-slice-split` · 0189/0190 분할 · 다른 AC 번호)을 대상으로 작성돼 그 항목들은 이 구현에 해당하지 않는다(`provider-compat` 임시 소유 위치 = Phase B 에서 이미 삭제 · AC5 test-count 동일 주장 = 한 적 없음 · Gate 4행 축소 = 진리표 전체 유지 · 0189/0190 INDEX 선등록 = 존재하지 않음). **실제로 유효한 4건은 반영**: ⓐ **respawn 판정 겹침 + 0125 null 보수성 훼손**(진짜 결함) — r1 fingerprint 가 `{settings, env}` 를 합쳐 settings 변화를 두 입력이 동시에 말했고, **settings loader 가 일시 실패한 턴마다 채널을 내리고 settings 없이 respawn** 했다. fingerprint 를 **env-only** 로 좁혀 두 축을 분리(D-021 → **D-038**), 회귀 2건 신설 ⓑ **삭제 테스트 단언 감사** — 4파일 전수 대조 후 미대체 3건(`unknown_provider` 회귀 · `unknown` 상태 도구 회수 · 미인증 fail-closed) 신설 ⓒ **Usage 후보 집합 변경을 명시**(D-039 + 코드 주석 — `Provider.llm` 삭제로 호환 경로가 없고 `supports()` 가 fetch 이전 게이트임을 근거로) ⓓ **`claude-user-skills-plugin.ts` 이동 누락** → `harness-plugins/claude-user-skills.ts`(D-040). **기록만**: plan-only 커밋의 `Status: implemented` 오기(푸시된 이력이라 fix-forward). 테스트 1,832 → **1,841**. |
| `0187-simplify-176-186-cleanup` | impl | **IMPL_DONE** (r2) | **Claude** (검증) | `49a1f20`(r1) · (r2 구현) | 2 | **0176~0186 /simplify 정리 (2026-08-13).** 사용자 요청 `/simplify 핸드오프 176~186` + 제약 **"uiux/db/성능 유지 혹은 개선. 절대 성능저하 안됨"**. 리뷰 구간 `f539326d..HEAD`(345 파일 · +13,353/−19,686 · 커밋 50). 스킬 절차대로 **4관점 리뷰 에이전트 병렬 기동** → 37건 → dedup 후 14건 적용·7건 기각/이월. **이 구간의 성격 = '큰 것을 지우고 새로 세운 자리의 이음매'**(0180 이 `auth-platform`·connectors 를 걷어내고 0181 이 `providers` 를 세운 뒤 0186 이 사용량 정본을 얹었다): ⓐ 지워진 쪽의 잔해 ⓑ 갈라 놓은 자리의 복사 ⓒ 새 플랫폼의 뜨거운 경로. **성능(요구의 핵심)**: `conf` 의 `store` getter 가 **호출마다 `readFileSync`+`JSON.parse` 를 다시 한다(캐시 없음, 실측)** — 그 위에 `safeStorage` 복호화가 얹힌다. 0181 이 `buildTurnEnv`→`llmEnvFor` 를 얹어 provider secret 이 **턴마다** 읽히기 시작했는데 `secret()` 이 `status()` 를 거쳐 같은 vault 를 **2~3회** 읽었다 → **1회**(만료는 읽기 전에 판정). `ProviderApi` 는 redirect 홉마다 secret·presentation 을 다시 풀었다(3홉이면 파일 읽기 ~9회) → **요청당 1회**(`Carrier`). 부팅 `sweepPlugins` 는 provider 를 **순차** probe(`PROBE_TIMEOUT_MS` 15초×N 직렬) + 루프 **안**에서 `onChange` (provider 마다 전체 상태 재생성·방송 = 파일 읽기 N²) → **`Promise.all` + 통지 1회**. OAuth pending 스토어는 DB 보다 앞으로 **일부러 당겨 둔** provider 부팅 단계에서 생성자 동기 파일 읽기를 했다 → **지연 개방**(실패는 메모리 폴백). **한 벌로 접기**: 창 크롬 45줄 복사(`GateFrame`↔`BootFailureFrame`) → `FullFrameShell`(DOM 불변) · 인증 칩 열 → `shared/ui/AuthKindChoices`(클래스 문자열 그대로) · probe 의 origin 판정 → `isAllowedOrigin`(`policy.ts` 주석이 "두 벌이면 갈린다"고 요구하던 바로 그 규칙) · whoami/exchange 요청 → `getJson`(**실패 문장은 호출부가 정한다** — 로그용/사용자용이 달라 `JsonReadFailure` 사유로 넘긴다) · `SessionGroupPolicy` **3선언→1**(하나는 `allowIntegratedAuthDomains` 누락 상태였다) · `<id>-tools` → `providerToolServerId`+`ProviderToolContext.serverId` · `mcp__…` **3벌→`runtimeToolFullName`** · grant/OAuth 영속 3쌍 → `createRecordPersistence<T>` · `messageOf`×2+인라인×4 → `errorMessage`. **깊이 교정**: `settings:set` 의 feature 별 `if (key === …)` → `SettingsStore.onPatch` + `app/settings-reactions.ts`. **bootstrap 인라인이 아니다** — 그쪽은 electron 을 물어 vitest 대상이 아니라 옮기면 회귀 테스트를 잃는다(`features/usage/jobs.ts` 선례). 한도 쓰기 → `UsageTracker.setProviderLimit`(사용량 authority 안). `usage-recompute` 등록도 `registerUsageJobs` 로. **UI 개선 1건**: `text-red` 는 **토큰이 없어**(`--color-bad` 만 존재) 오류 문구 3곳이 상속색으로 렌더되고 있었다 → `text-bad`. **죽은 표면**: `shared/connector-address.ts`(소비자 4개 전부 0180 에 삭제됨) · `createNamespacedSecretFacade`/`providerSecretPrefix`(파일은 `secret-store-port.ts` 로 개명 — 남은 것은 포트뿐) · `clearAll` · `globalUpdatedAt` · `providerKind` i18n · `composeGlobalUsage` 패스스루 · `SSO_PROVIDER|null` → `GATE_PROVIDERS[]`(소비자 전부가 N 을 전제). **기각 4건**: `UsageLimitBar.source` 제거(테스트가 의미를 고정하는 IPC 뷰모델 필드) · `SessionRunner.login` 의 중복 register(부팅 등록 실패 시 **회복 경로**) · `authChoices` 제거(순서 규칙의 문서화 지점) · reauth 채널 흡수(IPC 삭제라 AC16 충돌). **이월 3건**: `allowIntegratedAuthDomains` 세입자 없음(살릴지 지울지 = 설계 결정) · `GateLogin` 의 게이트 pending 재파생(IPC 필드 필요) · provider 상태 3중 구독 → `providerStore`(상태관리 재편, UX 무회귀 확인 필요). **게이트**: lint **0 error**(warn 1 = 0102 베이스라인) · typecheck **3/3** · vitest **1780/1780** · scripts **49/49** · `check-doc-inventory` 차이 0. **신규 의존성 0 · IPC 채널/스키마 무변경 · i18n 표시 문구 무변경 · DB 마이그레이션·쿼리 0줄.** 다음 = Claude 검증. plan `docs/handoff/0187-simplify-176-186-cleanup/plan.md`. | **▶ 검증 FAIL r1 (2026-08-13)** — 인수 **13 ✅ / 2 ⚠️ / 1 ❌** (커밋 trailer 의 `Criteria-Met: 16/16` 은 과대 보고). 게이트는 실측 재확인: lint 0 error · typecheck 3/3 · **vitest 1780/1780**(`npm rebuild better-sqlite3` 후. 미실행 축은 `scripts/*.test.mjs`·`check-doc-inventory` — verify §게이트에 명시). FAIL 사유는 **D1 하나**다: **AC2 의 `Carrier` 최적화가 redirect 홉 사이의 grant 변경(revoke·markExpired·자연만료) 차단 의미를 함께 지웠다** — 변경 전엔 홉마다 `store.secret()` 재평가가 그 차단을 했다. 보안 경계 유출은 아니나(값형 allowlist = `[provider.origin]`) 해제 후에도 진행 중 체인이 성공한다. **AC3 은 ❌ 이지만 기준 결함이다** — "통지 1회" 는 달성 불가(`onChange` 를 `ProviderApiImpl`·`LoginService` 가 공유해 실제 `1+K`)이고, 코드로 맞추면 `serviceTools.sync()` 까지 억제돼 **만료 provider 의 도구 회수가 지연되는 새 회귀**가 난다(`bootstrap.ts:355` 는 fire-and-forget). 변경 전이 `N+K` 였으므로 현재 `1+K` 는 항상 개선 → **사용자 결정으로 AC3 원문 폐기, 코드 무변경**(D2 에 대리 기록). 나머지: D3 동시성 테스트 부재(`probeApi` 스텁이 401 side effect 를 구조적으로 못 탄다 — 실제 `ProviderApiImpl` 배선 필요) · D4 `isConnected` 오명 → `canManageAuth` + 중복 단언 · D5 `AuthKindChoices` 부채 확대(이월). 자기 리뷰 → `failure-patterns.md` **P37**(semantic 목표를 structural proxy 로만 검증). **다음 = Claude 재구현 r2**. verify `docs/handoff/0187-simplify-176-186-cleanup/verify.md`, 파생 이슈는 같은 디렉토리 `plan.md` §파생 이슈. **▶ 재구현 r2 (2026-08-13)** — verify FAIL 의 D1·D3·D4 를 닫았다. **D1**: `Carrier` 가 해석 시점 `Grant` 객체를 함께 싣고, `send()` 가 **다음 홉 전송 직전에만** 그 참조를 재확인한다 — `ProviderStore.isCurrentGrant`(세션: identity) / `isCurrentUnexpiredGrant`(값형: identity+만료) **둘 다 메모리 판정, vault 접근 0회**. carrier 를 가른 이유는 0187 이전 홉당 동작이 달랐기 때문이다(세션은 만료를 안 봤다) — 양쪽에 만료를 넣으면 없던 정책이 새로 생긴다. generation 카운터는 만들지 않았다: `revoke` 삭제 · `markExpired` spread · 재인증 `put` 새 리터럴이라 **객체 참조가 이미 generation** 이고, 그 근거(=`put` 자체의 불변식이 아님)를 주석에 남겼다. `SessionGrant`/`ValueGrant` 로 갈라 불가능 상태를 컴파일 타임에 막았다(런타임 비용 0). **복원 범위 명시**: 앱 내부 grant lifecycle 만 — vault 계층 중간 변화는 요청당 1회 snapshot 과 맞바꾼 부분이라 보지 않는다. **D2**: 사용자 결정대로 **코드 무변경**, `login.ts` 주석만 `1+K` 로 교정(억제하면 `serviceTools.sync` 까지 막혀 도구 회수가 지연된다). **D3**: 회귀 6건 — 값형 revoke/강등/**자연만료**/정상완주 · 세션 revoke · sweep 병렬성. **병렬성 테스트는 deferred fetch 로 "셋이 동시에 떠 있음"(`started===3`)을 관측**하고 `unauthorized=3`·`onChange=4` 로 `1+K` 를 고정한다 — `Promise.all` 을 순차 루프로 바꿔 **실제로 실패하는 것을 확인**했다(`expected 1 to be 3`). `markExpired` 횟수는 단언하지 않는다(provider 당 2회가 정상). **D4**: `isConnected` → **`canManageAuth`**(레코드 유무와 어긋나 `hasAuthRecord` 도 부정확 — 이 판정이 정하는 건 버튼 노출뿐) + 중복 단언 제거. **D5** 이월. **게이트(r2 실측)**: lint **0 error**(warn 1 = 0102 베이스라인) · typecheck **3/3** · vitest **1786/1786**(1780+6) · scripts **49/49** · `check-doc-inventory` **차이 0**. 남은 1 suite(`chat-turn.continuity`)는 electron 바이너리 미설치로 **0건 수집** — 환경 기인. **신규 의존성 0 · IPC/스키마/마이그레이션 0줄 · 공개 포트 인자 0개 · 새 abstraction 0개.** 다음 = Claude 재검증(r2).
| `0183-usage-into-declarations` | impl | `IMPL_DONE` (r2) | Claude (검증) | `87c23b7`(r1, 취소) · `328b754`(재설계) · (r2 구현) | 2 | **사용량을 선언으로 접고 `static/` 을 제거한다 (2026-08-10).** 사용자 질의에서 파생 — "usage 를 반환하는 SP 를 service 에 선언해 api 로 부를 수 있다면 static 을 따로 둘 필요가 있나", "SP 구현을 declarations 에 다 할 수 있는데 별도 폴더링이 필요한가". **결정: `providers/declarations` 외 모두 제거 + SP API 호출·주기 호출(cron) 가이드 보완.** **진단(실측)**: 활성 모듈 **0개** · 3경로 중 `config`·`provider` 는 `secret.set(` 호출이 main 전체 **0곳**이라 인증 endpoint 도달 불가 · `adapter`/`provider` 좌표가 `Provider.llm` 과 **중복** · 선언은 이미 함수를 담는다(`Provider.tools`). 결과적으로 SP 하나가 **선언·모듈·설정 디렉토리 세 곳**에 흩어져 문자열 조인 2개로 이어졌고, 어긋나면 `refreshAll` 의 filter 가 **침묵**했다. **설계**: `Provider.usage{providerKey?,operation,params?,map}` — **선언한 provider 가 곧 호출 대상**이라 `sourceId` 조인 소멸, `providerKey` 는 `llm` 좌표에서 파생돼 두 번째 조인도 소멸(조인 **2→0**, 배포 파일 **3→1**). 침묵 제거: 대상 미확정은 `usage.spec.no-provider-key`, 설정 열거와 어긋나면 경고 로그. **삭제**: `static/`(7파일) · `http-usage-report`(+test) · `usage-feed`(+test) · `UsageReportConfig` · `createSecretFacade` · 계약 3종. **구현이 설계를 3건 정정**: ⓐ `UsageSpecEntry` 를 서비스 쪽에 **구조적으로** 선언(슬라이스 교차 금지) ⓑ `UsageFeed`·표본 dedupe 는 존재 이유가 사라져 삭제 ⓒ `standardization.md` 가 구 opt-in 절차를 서술 중이었다. **가이드**: §5-b 레시피 E 재작성(`Provider.usage`) + **§5-c 주기 실행(cron) 신설**(1분·5초 타임아웃·in-flight 병합·stale 폴백·정본 표). **게이트**: lint 0 error/1 warn · typecheck 3/3 · vitest **193 파일(188/5) · 1,697 테스트(1,658/39)** — DB ABI 베이스라인과 동일해 **신규 red 0**. **[verify FAIL r1 → r2 재설계 (2026-08-11)] 요구 오독.** 사용자 정정: "다른 feature 에서 sp 의 api 를 사용할 수 있다면 파편화된 구현(providers/declarations 외 구현)을 **모두 제거**하라고 했는데, 에이전트가 해당 경로에 **모두 조인**했다. **조인을 취소하고 제거하라. usage 스펙 구현을 취소하라.**" r1 은 `static/` 을 지운 것까지는 맞았으나 같은 능력을 `Provider.usage`(`UsageSpec` + `usage-specs.ts` + bootstrap 배선)로 **선언에 다시 달았다** — 제거가 아니라 **이동**이다. 요구 문장의 조건절("api 를 쓸 수 있다면")이 이미 대안을 지정했는데 배경으로 읽었고, 구현 보고의 성과 지표("조인 2→0, 배포 파일 3→1")까지 *이동*의 성과였다. **설계 실패 패턴 `P34` 로 축적**(`.agents/skills/handoff-plan/references/failure-patterns.md`). **사용자 결정 3건**: ⓐ **원격 사용량 경로 전면 제거**(조인 취소 + `ExternalUsageService`·`app/usage-source.ts`·`contracts/usage-{report,source}.ts`·cron 잡·IPC `cost:refreshProviderUsageReport`·`ExternalUsageReport`/`effectiveLimit` 타입·0111 renderer 정합까지. 사용량 = 로컬 집계만) ⓑ **새 번호 대신 0183 r2 로 재작성** ⓒ **구현 후 사용 가이드 보완 필수 — "sp api 의 주기적 호출 설정 방법 포함(cron 설정, 위치 등)"**. **실측**: `Provider.usage` 를 선언한 provider **0개**(슬롯만 있고 세입자가 없어 제거 비용 0) · 삭제 8파일 **863줄** · 소비자 N=1(usageSpecs)/3(ExternalUsageService)/2(UsageSourcePort)/4(effectiveLimit) · 채널 **77→76**(cost 6→5). **마이그레이션 0014 는 무수정**(append-only) — 테이블은 고아로 남기고 접근자만 제거. AC **15건**(14 기계 + AC15 사람 실기), 그중 **AC10~14 가 문서**(가이드 §5-b 주기 호출 레시피 7항목·§1.7 죽은 좌표 정정·§9 3행·구조 문서). **비범위**: `service/confluence/` 에 같은 잣대를 적용할지는 제품 결정(D2, 사용자 확인 필요) · DB 테이블 물리 삭제 · 로컬 집계·사용량 통계(0112). | **[impl r2] 구현 주체 = Claude**(비기능 = 구조 제거). **37파일 · +169/−1,403** — 삭제 8 · 코드 15 · 문서 14. `ProviderUsageEntry` 를 3필드로 줄인 것이 **typecheck 를 강제 검출기로** 만들어 남은 소비자를 전수 지목했다(전수 `rg` 로 못 찾은 `protocol.send.test.ts` 도 컴파일이 잡았다). **구현이 설계를 4건 정정(D1~D4)**: ⓐ `RefreshProviderUsageReportSchema` 소비자가 핸들러만이 아니었다(전용 describe 블록) ⓑ 0111 정합 제거로 `limits.ts` import 2개가 죽었다 ⓒ `misc-split.test.ts` 는 채널 수를 **제목·헤더 주석에도** 박아둔다(26→25) ⓓ `AGENTS.md`·`security.md §1.9` 의 fetch 포트 예시가 삭제 대상(`ExternalUsageService.fetchImpl`)을 인용 중이었다. **문서가 인수 기준이다(AC10~14)** — 가이드 **§5-b 를 "SP API 를 주기적으로 부르기(cron)" 로 재작성**(등록 위치=컴포지션 루트 · `register`/`schedule` API · cron 두 층(설정 노출형 `Settings.scheduler`↔`applySettings` / 코어 고정형) · 잘못된 식은 등록 시 throw · in-app 발화·`skipped`·`stopAll` · `ProviderApi.request` 호출부 · 좁힌 포트 주입) + **§1.7 죽은 좌표 정정**(선례로 인용하던 2파일이 이번에 삭제 — "살아 있는 선례 없음"을 명시) + §9 주기 실행 3행. **예제는 실제 `bootstrap.ts` 에 붙여 typecheck 3/3 을 통과시킨 뒤 되돌려** 검증했다(0181 5단계-e · 0182 AC11 선례). 문서 10건 동기화(contracts **7→5모듈** · IPC **77→76** · 소비 표면 **4→3종**). **게이트**: lint 0 error/1 warn · typecheck 3/3 · vitest **191 파일(186/5) · 1,677 테스트(1,639/38)** · scripts 28/28 — 실패 5파일이 DB ABI 베이스라인과 일치하고 39→38 은 이번에 삭제한 캐시 왕복 테스트 1건만큼이라 **신규 red 0**. **마이그레이션 0014 무수정**(`git diff` migrations 0파일) — 테이블은 고아로 남기고 접근자만 제거. **인수 14/15**(AC15 사람 실기만 — GUI 기동). |
| `0182-gate-principal-and-session-registration` | impl | `IMPL_DONE` | Claude (검증) | `b464cd3`·(구현) | 1 | **게이트 신원(principal) 표시 + 세션 group 부팅 등록 (2026-08-10).** 0181 후속. 사용자 요구 4건: ① nav 하단에 `developer` 대신 email ② 폐쇄망 공통 세션쿠키와 **다른 레이어에서의 SP 사용**을 가이드에 반영 ③ "첫 질문도 사실상 SP 질문"(두 요구가 한 축임을 사용자가 지목) ④ **보완 완료 시 가이드 문서도 그에 따라 반영**. **사용자 결정 3건**: ⓐ 범위 = 부팅 등록 + 신원 표시 + 문서(세션 공유·사용량·PAT 내장 MCP·SP API 소비 레시피는 후속) ⓑ 문서 = **기존 가이드 재구성**(0181 의 신설 금지 결정 유지) ⓒ whoami 실값 미상 → **파라미터화**. **결함 1건을 먼저 닫는다**: `sessions.register()` 호출부가 **2곳뿐**(로그인 실행부·OAuth 창, 전수 grep)이라 재시작하면 쿠키·grant 는 살아 있는데 group 이 미등록이라 `acquire()` 가 raw throw — `ProviderPolicyError` 가 아니라 401 강등도 안 타고 재인증 지점이 뜨지 않는다. 도구는 부팅 시 등록되므로 **보이는데 부르면 죽는** 형태. **설계 축**: SP 호출 4순간(probe·whoami·exchange·로그인 후 API) 중 **전송은 이미 한 벌**(`transport()` 가 세션 grant 를 `sessions.send()` 로 위임)이고 비어 있는 것은 **whoami 하나**. probe 는 판정 전용(본문 폐기 + 체인 최종 홉)이라 계약을 건드리지 않고 **기존 `sessions.send()`+`pickPath()` 재사용**. 로그인 중에는 `api.request()` 불가(`grantStatus!=='valid'` 거부, 닭·달걀) → 로그인 중 `send()` / 로그인 후 `request()` 비대칭을 문서에 명시. **일방향 결정 3건 지금 확정**: `whoami`·`valuePath`·`principalPath` (선언 스키마라 나중 개명이 배포된 선언을 깬다). AC **15건**(14 기계 + AC15 사람 실기) — 그중 **AC10~14 가 문서**다(가이드 §2 필드표·§6 확인 절차·§9 트러블슈팅 · auth.md §4·§7 · GLOSSARY `principal` 표제어 **현재 0건**). 0181 이 5단계-c·d 에서 동작을 바꾸고 절차 문서를 따라가지 않아 정본이 구 동작을 서술하던 실패를 반복하지 않으려 **문서를 AC 안에** 뒀다. **선행 아티팩트(플레이북)의 결함 4건을 근거에서 제외**: WIA 동작 미검증 추론 · `UsageSubscription.sourceId` optional 오독 · 설계 선택(세션 재검증 없음)을 결함으로 분류 · 실재하지 않는 심볼 예제. **비범위**: WIA 필드(Electron 의미 미종결 — `node_modules` 부재 + egress 차단) · 부팅 재검증 · 케이스 B~E 레시피. 신규 의존성 0 · IPC 무변경. | **[impl r1] 구현 주체 = Claude**(비기능). **부팅 등록**: `sessionPolicies`/`registerDeclaredSessions` 순수 seam 신설 — `Pick<BrowserSessionPort,'register'>` 를 받아 **fake 포트로 호출을 단언**한다(설계 R5). 입력이 `registry.list()` 라 거부된 선언의 jar 는 안 만들어지고, 등록 실패는 로그만 남기고 부팅을 막지 않는다. **신원**: `SessionLookup{path,valuePath}` + `whoami` + `exchange.principalPath`(있으면 **왕복 0**). probe 재사용 대신 `sessions.send()` — probe 는 본문을 버리고 체인 마지막 홉이라 신원 보장이 없다. **실패는 로그인을 실패시키지 않는다**(5케이스 회귀). **구현이 설계를 4건 정정(D1~D4)**: ⓐ `SessionGroupPolicy` 가 electron 파일에 있어 **구조적 재선언**(typecheck TS2305 가 잡음) ⓑ 점 경로 결과가 문자열이 아닐 수 있어 `pickPrincipal()` 좁힘(`[object Object]` 방지) ⓒ **`api.test.ts` 가 아예 없었다**(0181 미작성) → 신설, `fetchImpl` 을 던지게 해 경로 오염까지 검출 ⓓ `infra/browser-session.ts` 주석이 삭제된 `features/auth-platform/api.ts` 를 현재형으로 가리킴 → 정정. **문서가 인수 기준이다(AC10~14)** — 가이드 §1.6(세션 그룹)·§1.7(SP 4순간 + 소비 3패턴)·§2(`whoami` 행·표기 비대칭)·§5-b(레시피 E, **`sourceId` optional 정정**)·§6.3(신원 확인 절차)·§9(3행) · `auth.md` §4.1·§7.1 · `GLOSSARY` **`Principal`·`whoami` 신설**(실측 0건이었다). **AC11 은 예제를 실제 `declarations/sso.ts` 에 채워 typecheck 3/3 을 통과시킨 뒤 되돌려** 검증했다(0181 5단계-e 선례). **죽은 좌표 5곳 정리**: `static/modules/AGENTS.md`·`_example/provider-subscription.ts`(2)·`contracts/usage-source.ts`(4)·`external-usage-service.ts`·`browser-session.ts` — 전부 0180 이 지운 `auth-platform`/`PluginHost`/`USAGE_CONNECTORS` 를 현재형으로 가리켰다. `runtime-tools.ts` 의 소비자 0 타입 2종(`RuntimeToolContribution`·`PluginToolContext`)에 경고 주석. **게이트**: lint 0 error/1 warn · typecheck 3/3 · vitest **195 파일(190/5) · 1,706 테스트(1,667/39)** — 실패가 DB ABI 베이스라인 5파일/39건과 정확히 일치해 **신규 red 0**. 테스트 **+30건**. **인수 14/15**(AC15 사람 실기만 — OQ1 실값 부재 + egress 차단). **⚠️ WIA(P0-2) 조사 부분 종결**: `electron.d.ts` 를 열었으나 `allowNTLMCredentialsForDomains` docstring 이 `''` 의미·기본값을 서술하지 않고 공식 문서는 egress 차단 → **비범위 유지**(잘못된 필드가 나가면 일방향). |
| `0181-provider-platform` | impl | `IMPL_DONE` | Claude (검증) | `8b66f90`·`f3b8798`·`da5865b`·`be9887c`·`1c2ac66`·`5247080`·`80728d7`·`f7ccce9` | 1 | **auth·플러그인 재작성 — 2단계 중 2단계 (2026-08-10).** 0180 이 걷어낸 자리에 `Provider` 축을 세운다. **사용자 2차 결정 3건**: ① **ADFS 는 "둘 다"** — 게이트는 브라우저 세션(WIA 쿠키), 토큰이 필요한 곳은 그 세션으로 사내 API 를 불러 교환 ② **LLM 인증은 api key·SSO 토큰 발급 둘 다이고 선택 주체가 둘**(빌트인 구현자 선언 + 사용자 GUI 선택) ③ **먼저 plan 문서만**. **설계 축**: 폴더링을 프로토콜이 아니라 **관계**로 가른다(아티팩트 `d801bbaf` — opencode 엔 `idp/`·`oauth/`·`sso/`·`saml/` 폴더가 하나도 없다) → `kind: 'gate'|'llm'|'service'`. 상대가 전부 고정이므로 **RFC 8414 discovery·RFC 7591 동적 등록은 비범위**(아티팩트 `c5b48b30`). **결정 2건을 뒤집는다**: ⓐ 0180 의 "한 provider = 한 인증 방법" → `auth: readonly AuthSpec[]`(사용자 결정 ②). 단 **id 참조가 아니라 인라인 선언**이라 `acceptedMethods` 교차·`validateCrossReferences` 는 되살아나지 않는다 ⓑ 0028 의 "LLM settings 토큰 주입 폐지" → `Options.env` 레이어에만 주입(settings.json 은 verbatim 유지). **실측이 승인된 계획의 전제를 정정했다** — LLM settings 는 `${VAR}` 확장을 **하지 않는다**(`claude-settings.ts:4-6`, 0028 이 폐지). 실제 seam 은 `app/chat-turn/turn-setup.ts:79-87` 의 `buildTurnEnv` 로, 이미 vault 를 읽는다. 다만 선택 provider 를 모르므로 `providerKey` 인자 추가가 필요(R5). **새로 쓰지 않는 것 약 1,900줄** — `browser-session-store` 367 · `authenticated-fetch` 188 · `credential.ts` 178 · `browser-session.ts` 166 · `session-policy` 134 · `policy.test` 128 · `credential-vault` 108 · `policy` 105 · `binding-store-file` 38 + confluence 순수 576 (전부 `git show 8965fa7:…` 실측). AC **15건**(14 기계 + AC13 사람 실기). 채널 71→**77**, `ipc-documentation.test.ts` 가 문서 3곳과 교차 검증. **일방향 결정 3건을 지금 확정**: 채널명 6개 · `Provider.id` 규약(vault 네임스페이스이자 `${BINDING:}` 참조 대상) · vault 키 형식 `provider:<id>:<kind>`. **Open Question 4건**(착수는 막지 않음, 전부 파라미터화): ADFS 실값 · 토큰 교환 endpoint · 서비스 provider 인벤토리 · **OQ4 = 표준 OAuth code→token 을 실제로 쓰는 대상이 있는가** — 사용자 답변 경로(게이트=쿠키 + 토큰=사내 API 교환)에는 표준 OAuth 가 등장하지 않아, 답에 따라 PKCE·루프백(약 270줄)의 필요 여부와 AC3·AC4 이월이 갈린다. | **[impl r1] 4단계 독립 green 커밋.** 구현 주체 = **Claude**(환경에 Codex 부재, 사용자 지시 — 0160·0162·0163·0176·0179·0180 선례). **착수 전 OQ4 가 해결됐다** — 사용자 답변 "추후 사용 예정. 구현하라" 로 **AC3·AC4 를 이월하지 않고 구현**했다(PKCE S256 · `state` 파일 보관/대조 · redirect 3분기 loopback/window/manual · 루프백 리스너는 실제 포트 바인딩으로 검증). 단계: ①계약·코어 3종·IPC 6채널·카탈로그 연결 탭 ②OAuth ③게이트·요청 정책·`ProviderApi` ④소비 표면 4종(LLM `Options.env` · MCP `${BINDING:}` · usage 표본 · service 도구 + Confluence 복원). **게이트**: lint **0 error/1 warn** · typecheck **3/3** · vitest **190 파일(185/5) · 1,670 테스트(1,631/39)** — 실패 파일이 착수 전과 동일한 **DB ABI 5종**이고 실패 테스트 수도 39 로 같아 **신규 red 0**. 테스트 **+253건**. **인수 14/15** (AC13 사람 실기만 미충족 — OQ1 ADFS 실값 부재 + egress 차단으로 `npm run dev` 불가, 0180 AC9 선례). **구현이 설계를 11건 정정(D1~D11)**: ⓐ **AC 와 §7 표가 경로를 두 곳으로 갈랐다**(레지스트리·renderer) → AC 를 따르되 renderer 는 책임별로 셋으로 갈랐다(`app/GateFrame` · `features/providers/hooks/useProviderGate` · `features/skills/lib/providerRows`) — `GateFrame` 이 `WinControls`(app)를 쓰므로 feature 에 두면 boundaries 역방향 ⓑ **0180 이 `IPC_CONTRACT.md` 의 auth/plugin 절을 안 지웠다**(도메인 목록 23개 + §2.13-c/d 가 현재형 서술, 총계만 71 로 맞춰져 테스트는 통과) → provider 1절로 치환 + 22개로 정정 ⓒ **`ProviderRequest` 4필드로는 Confluence 복원이 불가**(query·responseType·maxBytes 필요) → 계약 확장 ⓓ **전송 조각이 infra↔feature 에 끼었다** → `infra/net/transport.ts`(전송·상한) + `features/.../present.ts`(Presentation 적용)로 분해 ⓔ **`RuntimeToolRegistry` 동등성이 handler identity 를 본다** → 조립 결과 캐시(안 하면 sync 마다 revision 이 올라 런타임 재spawn) ⓕ `expiresAt` 을 `GrantBase` 로 승격(401 강등이 secret 방식에서도 성립) ⓖ `orca:provider:state` 를 **양방향 1채널**로(invoke=스냅샷, send=변화 — 구 auth 의 status/stateEvent 2벌 동기화 버그 회피) ⓗ **게이트 판정 전에는 통과시키지 않는다**(fail-closed — main 이 잠깐 응답 못하는 사이 무인증 진입 방지). **문서 9건 동기화**: `security.md`(§1.4-b 를 3계층 + **raw secret 노출 3곳** 경계표로 재작성 — `Options.env` 가 세 번째 · §1.7 게이트 · §1.8 Chromium 파일 2→**3** · §1.9 인벤토리) · **`closed-network-extensions.md` 전면 재작성**(배포가 고치는 파일은 `declarations/` 셋뿐) · `GLOSSARY`(Provider 표제어 5종 신설 + **LLM Provider 금지어와의 충돌을 명시적으로 가름**) · arch overview 2종 · `PHASES` · AGENTS 3종. **[5단계-b · 범위 추가]** 사용자 요청("어떻게 제거·구현했고 어떻게 쓰고 등록하는지 문서화")으로 **`docs/arch/backend/auth.md` 신설** — 답이 6개 문서 + plan 2건에 흩어져 **읽는 순서가 없던** 문제를 구조 SSOT 하나로 모았다(제거 진단·인벤토리 → 축 → 모듈 지도 → 라이프사이클 → 등록 → 소비 표면 4종 → 게이트 → 뒤집으면 안 되는 결정 → 비범위). 기존 문서는 링크만 하도록 라우팅 6곳 정리(`ARCHITECTURE.md`·`docs/AGENTS.md`·`guides/AGENTS.md` **stale 행 정정**(사라진 계약 3종을 정본으로 가리키고 있었다)·`closed-network-extensions.md`·`app/src/main/AGENTS.md`·`declarations/index.ts`). 문서 내 수치는 **실측 대조**(모듈 13종 줄수·confluence 8모듈·IPC 6채널·0180 diffstat 145파일/−16,971). **[5단계-c · 사용자 보고 버그수정]** **게이트 우회 토글이 사라진 채였다**(D12) — `Settings.authBypass` 는 스키마·main 판정에 다 있었는데 **UI 가 없어 켤 수 없었다**(0180 이 `AuthDebugSection` 삭제 → 0181 이 main 쪽만 복원). K6 의 완화책("dev 경로로 되살린다")이 실제로는 작동하지 않는 상태였다. 셋을 함께 고쳤다: ⓐ `ProviderDebugSection` + `DebugPanel.providerSection` 슬롯 복원 ⓑ **`GateFrame` 에서도 디버그 패널 마운트** — 메인 셸에만 두면 *우회가 필요한 상황이 곧 스위치에 도달할 수 없는 상황*이 된다(구 `LoginFrame` 이 같은 배선을 갖고 있었다) ⓒ `settings:set` 이 `authBypass` 변경 시 provider 상태를 **push**(안 하면 설정만 저장되고 화면은 재시작 전까지 옛 판정). 회귀 **9건** 신설. **설계 self-review 의 빈틈**: 값·판정·소비자는 확인했으나 *"토글의 도달 가능성"* 은 질문 목록에 없었다. **[5단계-d · 사용자 보고 버그수정]** **우회 토글을 꺼도 로그인 페이지가 뜨지 않았다**(D13) — 기본 빌드는 게이트 선언이 0개라 `evaluateGate` 가 항상 `required:false` 를 줬고, **우회할 게이트가 없어 토글이 무의미했으며 로그인 화면에 도달할 방법 자체가 없었다.** 구 코드는 `DEV ? bypass||authenticated : !required||authenticated` 로 **DEV 게이트가 항상 켜져** 있었는데(0089→0130) 0181 이 그 분기를 prod 규칙 하나로 접으면서 사라진 것이다. 조치: ⓐ `evaluateGate.alwaysRequired`(호출부가 `import.meta.env.DEV` 주입 — 순수 모듈이 빌드 모드를 읽지 않게) ⓑ **빈 멤버 배열이 "전부 valid" 로 접히지 않게** 멤버 수 병행 검사(`[].every`=true 함정) ⓒ **구 `AuthView` 를 `GateLogin` 으로 복원** — Orca 제목·오르카 이미지·입력 카드·검정 로그인 버튼(화면이 0180 이전과 같아졌다) ⓓ 방식 선택 규칙을 `shared/config/providerAuth.ts` 로 승격(게이트 화면 ↔ 카탈로그가 같은 구현). **중간 산출물이던 dev 더미 게이트는 폐기** — 사용자 정정("dev-gate 가 아니라 원래 운영하던 로그인 페이지"). 회귀 **5건** 추가. **[5단계-e · 범위 추가]** 사용자 요청("180~181 최근 커밋을 반영해 로그인 게이트·플러그인 추가 방법을 문서화, **타 에이전트가 보고 구현할 수 있게 단계별로**")으로 **절차 문서를 재구성**했다. 5단계-c·d 가 *동작* 을 바꿨는데 동기화가 `arch/` 3곳에서 멈춰 **절차 정본 `guides/closed-network-extensions.md` 가 구 동작(선언 0 → 항상 통과)을 서술**하던 상태였다. ⓐ 가이드 전면 재구성 — §0 라우팅(**"플러그인" 은 0180 이후 코드 개념이 아니다** → provider 선언/MCP 로 번역) · §1 공통(**`features/providers/` 두 세입자 경고 신설** — 구 LLM 설정 슬라이스와 0181 인증 플랫폼이 같은 디렉토리에 공존해 오배치가 나기 쉽다) · **레시피 4종**(§2 게이트 · §3 LLM · §4 서비스 도구 · §5 MCP, 각 "단계 표 → 예제 → 필드/실수 표") · **§6 개발 중 확인 신설**(DEV `alwaysRequired` · 우회 토글이 게이트 화면·메인 셸 양쪽 · `settings:set` push · 게이트 화면 파일 지도 8종) · §8 검증 명령(ABI 가이드대로 `npm test` 금지, `./node_modules/.bin/vitest run`) · **§9 트러블슈팅 12행 신설** ⓑ **드리프트 정정 4건** — `GLOSSARY` 로그인 게이트 행(DEV 누락) · `IPC_CONTRACT` §2.13-c 도입부·`provider:state` 행·`Settings.authBypass` 주석("앱 시작 시" → 즉시 반영)·`settings:set` 부수효과 ⓒ **유실 콘텐츠 복원** — 5단계의 전면 재작성이 구 §4 **"폐쇄망 빌드/자동 업데이트 피드"**(0130/0133 — 사내 npm 미러 빌드 · `orca.json` `update` 4분기 · 익명 GET 주의)를 통째로 떨어뜨렸고 `release-operations.md:62` 가 **없는 절을 가리키고** 있었다. 스키마가 코드와 여전히 일치함을 확인하고 §10 으로 복원 + 참조 교정. ⓓ 라우팅 3곳(`auth.md` §3·§5 · `docs/AGENTS.md` · `guides/AGENTS.md`). **사용자 결정 2건**: 새 핸드오프(0182) 대신 **0181 흡수** · 새 howto 문서 신설 금지(**기존 문서 재구성**). ⓔ **선언 파일 주석 예제 2건 정정**(사용자 지시 — 보고 후 수정 요청). 가이드가 "여기서 시작하라" 고 가리키는 파일인데 **예제가 구현자를 오도**했다: `sso.ts` 의 `origin` 이 IdP(`adfs.…`)로 잡혀 있었으나 `exchange.path` 가 `Provider.origin` 기준 상대 경로라 probe·교환 호스트여야 하고(`portal.…` 로 정정 + 이유 주석), `service.ts` 의 `createConfluenceToolServer(api, {providerId, contextPath})` 는 **실재하지 않는 시그니처**였다(실제 `(providerId, connectorLabel, runtime, ctx)` + `createConfluenceRuntime` 선행 — 그대로 쓰면 컴파일 불가). **검증**: 두 예제를 실제 선언 파일에 채워 넣어 typecheck 3/3 + lint 0 을 통과시킨 뒤 되돌렸고, import 경로까지 실측으로 확정해 주석·가이드 §4 에 실었다 — **눈으로 읽지 않고 컴파일해 본 것이 두 번째 결함을 잡았다**. **남은 것**: AC13 사람 실기(SSO 실값을 채운 빌드로 로그인 왕복) · verify 턴. |
| `0180-auth-plugin-teardown` | impl | `IMPL_DONE` | Claude (검증) | `762a525` | 1 | **auth·플러그인 전면 제거 — 재작성 2단계 중 1단계 (2026-08-10).** 사용자 요청: "auth 및 플러그인 기능을 모두 제거 후 다시 재작성할것이다. opencode 사례를 확인하고 어떻게 구조화할지 우선 검토" + "**어설픈 재사용코드 플랫폼화 금지**" + "모든 플러그인 서비스는 gui 노출 · 재인증 가능". 환경 = 윈도우·폐쇄망·ADFS. **구현 주체 = Claude**(환경에 Codex 부재 — 0160·0162·0163·0176 선례. 사용자 지시). **사용자 결정 4건**: ① 작업 분할 = **제거 → 재작성 2단계**(0180/0181) ② LLM provider = **통합**(같은 목록·같은 인증 경로) ③ 로그인 게이트 = **강제** ④ 플러그인 GUI = **기존 skills 카탈로그 유지**. 참조는 **아티팩트 4건**(`c865512e` 프로바이더 로그인 플로우 · `d801bbaf` 인증 디렉토리 지도 · `c5b48b30` MCP OAuth IdP/SP · `024a4677` MCP 서버 없음) — 사용자 정정 "study 경로가 아니다. artifact 도구를 사용하라" 에 따라 `docs/etc/study/` 를 근거에서 **배제**했다. **진단**: 0178 이 1,603줄을 지웠는데도 같은 불만이 나온 이유는 남은 복잡도가 양이 아니라 **4개 축의 교차**(`AuthMethod` × `ConnectorRuntime` × `Binding` × `PluginHost`/`ConnectionRegistry`/`TransactionStore`/`loginChain`)이고, 그 교차가 계약에 박혀 뺄셈으로 안 닫히기 때문 — `InternalApi` **17파일**·`ConnectorRuntime` **16파일**에 걸려 있다. **실사용은 거의 0**: `SSO_CONFIG=null` 이라 ADFS 미등록(게이트가 동작한 적 없음) · OAuth code→token 코어 구현 없음 · **PKCE 0건** · 자동 refresh 없음 · connector 서버 목록 2종 빈 배열. **제거 규모(실측)**: `auth-platform` prod **4,569**(검산 2065+416+2088 ✓) · `connectors` 768 · `infra/auth` 1,114 · 계약 3종 297 · handlers 262 · renderer auth 445 · 테스트 **35파일 삭제**. IPC **82→71 채널**(auth 7 + plugin 4 제거). **절대 지우면 안 되는 것 4종을 명시**: `infra/auth/net-{fetch,request,response}.ts`+`no-node-fetch.test.ts`(이름만 auth, 실은 **main 전체의 유일한 원격 전송 스택** — `infra/net/` 으로 **이설**. 가드가 파일명 기준이고 스캔 루트가 `src/main` 이라 이설 안전) · runtime-tool 포트(기여자만 0) · `contracts/usage-source.ts`(`sources?` 가 **optional** 이라 주입만 끊으면 provider/config 폴백으로 생존) · `adapters/error-classifier.ts` 의 `auth_error`(이름만 auth, 코드 경로 무관). **Confluence 순수 변환기 576줄은 이동이 아니라 삭제** — 옮겨두면 소비자 0인 코드가 남아 이번 작업이 없애려는 냄새를 만든다(관문 2 규칙 1-b). 0181 이 `git show 8965fa7:…` 로 복원(좌표를 plan 에 기록). AC **9건**(전부 `검증 수단`+`프로덕션 도달 경로`, AC9 만 사람 실기). **베이스라인 실측(2회 재현)**: lint 0 error/1 warn · typecheck exit 0 · vitest **206 파일(201/5) · 1,914 테스트(1,875/39)**, red 5 = `better_sqlite3.node` **Module did not self-register**(환경 제약, 변경 무관). `node_modules` 부재는 조사 제약이 아니었다 — `ELECTRON_SKIP_BINARY_DOWNLOAD=1 npm ci` 가 exit 0(P12 선례 0150). **문서 stale 2건 적발**: `IPC_CONTRACT.md:119-120` 이 코드에 **0건**인 `connectorInstances`·`pluginAddEnabled` 를 서술 · `GLOSSARY.md` **4행**이 0178 에서 삭제된 심볼 인용 → AC7 로 정리. `app/AGENTS.md` 의 "DB red 6파일" 도 실측 **5파일**(0178 R15 가 지적했으나 미반영). **이견 1건(기록)**: Stage 1 종료 시점에 게이트 없음 + Confluence·usage 중단 = 릴리스 형상 아님 → **0180~0181 사이 `v*` 태그 금지**(K1). 신규 의존성 0 · Open Question 0(0181 소관 3건은 범위 밖). **[impl r1] 145파일 · −16,971 / +330 줄.** 게이트: lint **0 error/1 warn**(boundaries 위반 0) · typecheck **3/3** · vitest **171 파일(166/5) · 1,417 테스트(1,378/39)** — 실패 파일이 착수 전과 **동일한 DB ABI 5종**이고 실패 테스트 수도 39 로 같아 **신규 red 0**. 파일 −35 는 R17 의 삭제 예정 수와 정확히 일치. 채널 **71** 로 `ipc-documentation.test.ts` 가 문서 3곳과 교차 검증(AC1). **구현이 설계를 3건 정정(D1~D3)**: ⓐ `LoginFrame` 은 로그인 게이트 **와 부팅 실패 화면**을 겸하고 있었다 — 통째 삭제는 부트 실패 UI 를 없앤다. `BootFailureFrame` 으로 개명해 auth 부분만 걷어냈다. ⓑ AC6 이 사이드바 nav 까지 줄이라고 읽히는데, nav 4번째가 **카탈로그 모달의 유일한 입구**(`path:null` → `onOpenPlugins()`)라 지우면 도달 불가 — nav·라벨은 그대로 두고 카탈로그 탭만 3→2 로 줄였다(0181 이 같은 카탈로그에 provider 를 되돌리므로 어휘를 두 번 뒤집지 않는다). ⓒ AC7 의 grep 술어가 **P30 함정**(이력 문서와 자기 변경 이력 문장까지 잡아 게이트 통과가 문서 손상을 유인)이라 "라이브 스키마·타입·현재형 서술에 없을 것"으로 좁혔다 — 기계 검사는 `rg <심볼> app/src`=0 + 스키마 표 행 형태=0. 추가 발견 4건: **settings 키가 20 이 아니라 18**(죽은 2키를 문서 4곳이 살아 있는 키로 서술) · `GLOSSARY` 의 `Connector 인스턴스` 표제어 삭제 + 3행 개정 · `docs/etc/study/orca/` 를 인벤토리에서 **⚠️ 폐기** 표기(사용자 근거 배제 지시) · `cheerio`·`turndown` 3종이 소비자 0 이 되어 "0181 이 복원하므로 지우지 마라"를 `app/AGENTS.md` 에 박았다. 문서 12건 동기화. **AC9(앱 기동 실기)만 사람 몫** — egress 차단 환경에서 `npm run dev` 는 Electron ABI 재빌드에 막힌다. |
| `0178-auth-entry-collapse` | impl | `IMPL_DONE` (전 단계 완료) | Claude (검증) | `3ea8f45`·`33742c2`·`64b43a6`·(3b~5) | 2 | **인증 플랫폼 붕괴 — 진입점만 남긴다 (2026-08-06).** 사용자 요청: "플랫폼화로 어중간한 재사용성으로 복잡도가 너무 올라가있다 … 제약이 많은 재사용은 쓰레기다 … 내부 api로 시야를 바꿔보자". 리팩토링(비기능) = **Claude 가 plan→impl→verify 직접** (환경에 Codex 부재). 사용자 결정 4건: 폐쇄망 "코어 무수정" 요건 **포기** · 절단 깊이 **전면 붕괴** · UI 커넥터 추가 경로 **제거** · SSO→MCP 는 **사내 API 토큰 교환**. 보정 2건: "**진입점까지만** 남길 것(dd 의 fops 처럼)" · "dd 는 **예시**이니 **TypeScript 모듈화 형태**로". **실측 진단** — 확장점 **1,603줄**(`manifest` 187 + `registry` 448 + `conformance` 238 + `plugin-host` 348 + 계약 3종 382) 이 하는 일이 *타입 시스템이 컴파일 타임에 이미 하는 일*이다. 등록 패키지 2종의 서버 목록이 **둘 다 빈 배열** → connector 0개. **죽은 표면**: `refresh` 3/3 `not_supported` · `AuthExec` 호출자 0 · 미구현 메커니즘 4종(`oauth_browser`·`oauth_device_code`·`external_secret`·`device_code`) 생산자 0. **가장 무거운 발견**: 사용자가 지목한 실사용 4종 중 **3종이 동작하지 않는다** — ADFS/WIA(SSO) provider 는 구현체(191줄)가 있으나 **미등록 예제 `_example/` 에서만 참조**되어 어떤 패키지에도 배선이 없고(SSO→REST·SSO→MCP 미동작), SSO→MCP 는 `broker.ts:454-458` 이 **코드로 차단**한다. 즉 1,603줄 확장점이 실사용을 못 덮었다 → 이번 작업은 *삭제* + **SSO 배선 완성**을 함께 한다. 목표 구조: 진입점 2개 ~80줄(`contracts/auth-method.ts` 4함수 `authenticate`(재진입, 구 begin/continue 흡수)·`status`·`revoke`·`issueToken` + `contracts/internal-api.ts` `request`/`token`), 등록은 `satisfies` 배럴 한 줄, 런타임 검증은 **id 중복 1건뿐**. 동봉 모듈(confluence·usage)은 **위치 유지**, 플러그인 껍데기만 제거. AC **26건 전부 기계 검증**(사람 실기는 AC 아닌 §리스크 K3·K4 로 분리). 베이스라인 실측: lint 0 error/1 warn · typecheck 3/3 · vitest **215 파일(210/5) · 2045 테스트(2006/39)**, red 5 = `better_sqlite3.node` ABI(변경 무관). Open Question 1건: **사내 토큰 교환 endpoint 실값 미상** → `tokenExchange`(`path`·`valuePath`·`expiresAtPath`)로 파라미터화하고 실기에서 확정. **[impl r1 — 부분]** 1단계(죽은 표면: `refresh` 전량·`AuthExec`·미구현 메커니즘 4종·conformance 하네스·fixtures) · 2단계(UI 서버 추가: 템플릿/인스턴스 18파일·IPC 3채널·설정 2키·i18n) · 3a(등록 검증 붕괴: `manifest.ts`·`declare.ts`·`registry.test.ts` 삭제, registry 448→210, 배럴 `satisfies`) · 6단계(문서 — `modules/AGENTS.md` 재작성 · `closed-network-extensions.md` §1 개정 · 요구명세 폐기 표기 · `app`/`src/main` AGENTS 수치). **실측 삭감 prod -2,548 / test -2,248 (약 -4,800 LOC).** 게이트: lint 0 error/1 warn · typecheck 3/3 · vitest 202 파일(197/5) 1876 테스트(1837/39) — red 5 = `better_sqlite3.node` ABI, **베이스라인 동일**. 구현 중 발견 5건(plan §놓친 문제): **D1 (해소)** — `${BINDING:}` resolver 가 **동기** 계약(`infra/vars.ts:6`)이라 SSO 토큰 교환(HTTP 왕복)을 렌더 시점에 할 수 없다. 이 제약을 도메인 상황("MCP 도구는 별도 프로세스라 토큰이 뜨는 순간 고정되는 사진 한 장이 된다")으로 되짚은 결과 사용자가 **SSO→MCP 미지원**으로 결정(2026-08-06) → 토큰 교환·`issueToken`·`tokenExchange`·배포 freshness 변경이 전부 범위에서 빠지고 **진입점은 3함수로 축소**. AC15·16·17 은 15'·16'·17'(반출 안 됨 + credential 은 됨 + SSO 의 REST 는 정상)로 대체. 코드는 차단을 *의도*로 명시했다. **D2**: `refresh` 제거 후 `'expired'` 전이가 0개인데, 조사 결과 **제거 전에도 실질 0**이었다(3/3 `not_supported` 라 도달 불가) — 만료가 처음부터 동작한 적 없다. `status()` 판정을 binding 에 반영하는 배선도 부재(3b 범위). **D3**: `PluginConnectorInfo.source`·진단 `'instance'` 가 인스턴스와 함께 죽어 제거. **D4**: `ipc-documentation.test.ts` 가 채널 수 하드코딩(86→85→82 동시 갱신). **D5**: `app/AGENTS.md` 의 DB red 스위트 "6파일" 이 실측 5파일(범위 밖). **미완**: 3b(진입점 `auth-method`/`internal-api` 신설 + broker 재구성) · 4단계(SSO 배선 + 토큰 교환, D1 선행) · 5단계(모듈 껍데기). 각 단계가 독립 green 이라 현 상태는 정합하며 반쯤 적용된 지점이 없다. **[impl r2 — 3b·4·5 완료]** **3b**: 진입점 2계약 신설(`contracts/auth-method.ts` **3함수** `authenticate`(재진입 — `begin`/`continue` 두 IPC 가 같은 함수로 들어간다)·`status`·`revoke` / `contracts/internal-api.ts` `request`·`token`), 구 `auth-plugin.ts`·`connector-plugin.ts` 삭제. broker 785줄 → `login.ts`(505, lifecycle·체인·게이트) + `api.ts`(215, 호출 표면) + `broker.ts`(204, 조립·복원). **호출 표면이 대상 이름 하나를 받는다** — `bindingId`·`connectorId` 를 들고 다니지 않고, `${BINDING:<대상>}` MCP 참조의 해석 키도 무작위 binding id → 대상 이름으로 바뀌었다(**D9** — 무작위 id 는 사람이 적을 수 없는 참조였다). **만료 배선(D2 잔여 해소)**: `revalidate`/`revalidateAll` 이 방식의 `status()` 를 묻고 **레코드에 반영 + 브로드캐스트**하며, 부팅 복원은 재연결 **전에** 돈다. 요청 경로의 **401 도 만료로 반영**(403 은 제외 — 권한 문제를 만료로 접으면 멀쩡한 연결이 끊긴다), 판정이 던지면 `unknown`(사내망 밖을 만료로 단정하지 않는다). **D6(신규 발견)**: SSO 대상의 REST 요청이 **probe 판정만 돌려주고 본문이 빈 문자열**이었다 — 실사용 "SSO 인증 REST" 는 배선이 아니라 구현 자체가 없었다(plan §R3~R8 이 놓침). `BrowserSessionCapability.send` 신설로 해소. **4**: `methods/sso.ts`(배포가 채우는 한 파일) + `methods/index.ts` 내장 목록 — 설정이 있을 때만 등록한다(미설정 등록은 빈 주소로 창을 열고 게이트를 켠다). **5**: 위키·사용량 모듈이 **자기 인증 provider 를 만들지 않는다**(둘이 글자까지 같은 PAT/ID·비밀번호 구현을 한 벌씩 들고 있었다) — `acceptedMethods` 로 내장 방식을 고를 뿐이다. 함께 제거: `pluginId`(패키지 개념) · `capabilities`(분기 0, **D8**) · `AuthStepInfo.browser`·`AuthLogoutOutcome.not_supported`(생산자 0) · `ctx.fetch`/`ctx.store`/`ctx.env`(소비자 0, **D7**). 확장점 총량 1,603 → **480줄**. 신규 회귀 2파일(`revalidate.test.ts` 11건 — 만료 반영·401·SSO REST 본문·MCP 미반출 / `methods/index.test.ts` 7건 — SSO 배선·게이트·중복 거부). 게이트: lint 0 error/1 warn · typecheck 3/3 · vitest **202 파일(197/5) · 1874 테스트(1835/39)** — red 5 = `better_sqlite3.node` ABI, **베이스라인 동일**. |
| `0174-sso-redirect-semantics` | impl | **IMPL_DONE** | **Claude** | 작업 트리 | 1 | **SSO 로그인 실패 3건 — Electron 리다이렉트 의미 차이 (2026-08-05).** 사용자 실기 보고: ⓐ probe 가 리다이렉트에서 **cancelled 예외** ⓑ `openLoginWindow` 가 **ERR_ABORTED(-3)** 로 창을 못 띄움 ⓒ 넘겨도 bind 단계 probe 실패로 로그인 성공 처리 안 됨. **뿌리는 하나** — Electron 의 `redirect:'manual'` 은 웹 fetch 규약과 달리 **3xx 에서 요청을 취소**한다(`electron.d.ts:19836-19841` 1차 출처). `net.fetch`/`Session.fetch` 는 `redirect` 이벤트를 노출하지 않아 3xx 를 받을 방법이 없다. **0173 이 같은 함정을 커넥터 경로에도 심어** `sendFollowingRedirects`(0160)가 끊긴 상태였다 — 첨부 다운로드 파손, 0173 파생 D1 로 기록하고 여기서 복구. 설계: `net.request` 의 `'redirect'` 이벤트로 **3xx 를 재구성**하는 `sendOnce`(신규 `net-request.ts`, electron 경계) + 순수 변환부 `net-response.ts` · `netFetch` 의 manual 을 그리로 우회 · probe 는 **홉을 우리가 돌며** 매 홉 allowlist 검사. **0157 D1('3xx = 미인증')을 뒤집는다** — 사용자 실측상 **인증에 성공해도 probe 가 302 로 로그인 URL 을 가리켜** 그 규칙으로는 성공 판정이 영원히 불가능하다. 판별자를 **최종 origin**(probe origin 으로 돌아와 2xx = 인증 / IdP 에 머물면 미인증)으로 바꿔 D1 의 *목적* 은 지키고 *수단* 만 교체. 추가로 **probe 실패의 방향을 자리별로 분리** — 창 *앞*(세션 재사용)은 판정 신뢰(오판 대가 = 창 한 번 더), 창 *뒤*(확인)는 **강등**(오판 대가 = 로그인 불가). 사용자 실측 "probe 가 ok:true 면 체인이 이어진다" 가 probe 가 유일한 게이트임을 확증. `ERR_ABORTED` 는 비치명 처리 + 실패 진단 로그(status·hops·finalOrigin·blockedOrigin, **경로·쿼리 미포함**). 게이트: lint **0 error** · typecheck **3/3** · vitest **2001/2001**(+26). **신규 의존성 0 · IPC 무변경.** **남은 것 = 사람 실기 AC7·8**(사내망 SSO 완주 · 첨부 다운로드 — `net.request` 는 Electron 런타임 전용). 다음 = Claude 검증. plan `docs/handoff/0174-sso-redirect-semantics/plan.md`. |
| `0175-simplify-156-174-cleanup` | impl | **IMPL_DONE** | **Claude** | 작업 트리 | 2 | **0156~0174 /simplify 정리 (2026-08-05).** 사용자 요청 `/simplify 핸드오프 156~174`. 리뷰 구간 `887292b9..HEAD`(261 파일 · +26,951/−2,793 · 커밋 100). 스킬 절차대로 **4관점 리뷰 에이전트 병렬 기동**(재사용·단순화·효율·altitude) → 47건 → dedup 후 적용. **이 구간의 지배적 결함은 '같은 규칙이 두세 벌'** 이고 하필 그 규칙들이 *credential 이 어디로 나가는지* 를 정한다: origin allowlist **4벌** · 리다이렉트 Location 해석 **2벌** · origin/apiBasePath 스키마 **각 3벌** · lease 논리 키 encode/decode 가 **다른 feature 에 손으로**. 두 자리는 파일 자신의 주석이 "두 벌이면 갈린다" 고 경고하면서 바로 아래에서 두 벌을 만들고 있었다(`policy.ts` 0157 verify r1 · `manifest.ts` 0158 verify r1 D4). 조치: 규칙마다 정본 1개 + 나머지는 그리로 · `shared/lease-key.ts`·`shared/connector-address.ts` 신설(둘 다 순수, zod 비의존 — renderer 에 zod 안 딸려가게) · `locationOf` 를 **헤더 대소문자 무시 + trim** 으로 고쳐 주입 전송 포트 casing 에 안 깨지게 · `settleApplicationGate`(게이트 리셋 4벌) · `checkPairing`(선언↔구현 대조 3벌) · 죽은 표면 4종 제거(`onInterruptReceipt` 계약 필드 포함). 효율: `updater.patch` 무변화 broadcast 억제(턴당 5~8회) · Turndown 페이지당 신규 생성 → 싱글턴(최대 50회/호출) · `Buffer.concat` 이중 사본 제거 · `broker.restore` 부팅마다 무조건 파일 쓰기 → 변화 시에만 · `StatusLine` 5Hz 재파생 → memo. **기각 1건**: `stopByBinding` 의 인라인 filter 는 중복이 아니라 **레이스 가드**(재연결이 갈아끼운 연결 보호) — 제안을 기각하고 미사용 `removeByBinding` 쪽을 삭제. 게이트: lint **0 error**(warning 1 = 0102 베이스라인) · typecheck **3/3** · vitest **2002/2002**. **신규 의존성 0 · IPC/i18n 무변경.** **별도 핸드오프로 이월**(설계 결정 필요라 정리 범위 밖): 런타임 registry↔lease **이중 소유** · `attemptId`≡`uuid` 계약 정리 · **connector health enum 이 IPC 에서 소실되고 renderer 가 한국어 산문을 정규식 파싱**(문구를 영어로 쓰면 전 실패가 unknown) · `browser_session` binding 의 빈 200 응답 · template `fields` 미사용 · scheduler job 이름 열거. 다음 = Claude 검증. plan `docs/handoff/0175-simplify-156-174-cleanup/plan.md`. |
| `0173-electron-net-fetch` | impl | **IMPL_DONE** | **Claude** | 작업 트리 | 1 | **main 의 원격 요청을 Chromium 스택(`net.fetch`)으로 (2026-08-05).** 사용자 질의 "node fetch 쓰는 코드가 있으면 모두 electron.net.fetch 로" → **전수 조사 결과 3곳이 Node(undici) 전역 fetch** 였다: `authenticated-fetch.ts`(Confluence REST — 검색·본문·첨부) · `broker.ts` `ctx.fetch`(provider probe) · `external-usage-service.ts`(usage 보고서). axios·http/https·renderer 는 **0건**. 저장소는 이 구분을 이미 알고 있었다(`browser-session-store.ts` 는 `ses.fetch`, `index.ts` 는 `net.fetch`) — 인증·사용량 경로만 빠졌다. **왜 중요한가**: undici 는 OS 프록시·PAC 와 OS 인증서 저장소를 안 봐서, 사내 프록시 뒤 **사설 CA 서버로 못 나간다**(브라우저로는 열리는데 앱만 안 되는 증상). 설계: electron 경계를 **파일로** 긋고(`infra/auth/net-fetch.ts` — electron 을 무는 유일한 네트워크 파일, P29 준수) 세 소비자는 `typeof fetch` 포트로 **주입**받되 **기본값을 없앤다**(기본값 = 조용한 Node 스택 복귀). 타입체커가 주입 누락 **14곳**을 즉시 집어냈고 그 목록이 곧 '원격으로 나갈 수 있는 지점 전수' 였다. 재발 방지로 **위생 테스트**(`no-node-fetch.test.ts`) 신설 — 스캔 **197 파일**, 변경 전 소스는 잡고 변경 후는 안 잡음을 실측했다(측정력 증명). 정책 유지 확인: `credentials:'omit'`·`redirect:'manual'`·origin allowlist 강제 지점 전부 그대로(테스트로 고정). 게이트: lint **0 error** · typecheck **3/3** · vitest **1975/1975**. **신규 의존성 0 · IPC 무변경.** **남은 것 = 사람 실기 AC6**(사내망 프록시·사설 CA 환경에서 Confluence 검색·첨부 다운로드 — `net.fetch` 는 Electron 런타임 전용이라 이 샌드박스에서 검증 불가). 다음 = Claude 검증. plan `docs/handoff/0173-electron-net-fetch/plan.md`. |
| `0172-multi-provider-login-chain` | impl | **IMPL_DONE** | **Claude** | 작업 트리 | 1 | **[impl r1 · main 리베이스]** `03c45b2`(0170 auth-binding-restore 포함) 위로 리베이스했다 — **0168→0172 재번호**(main 이 0168~0171 을 선점). 0170 이 binding 을 영속화했으므로 `createMany` 가 그 계층을 물려받는다: id 뽑기 `mintId()`(복원 레코드·형제 멤버와 충돌 회피) + `flush()` **배치 끝 1회**(멤버마다 부르면 실패한 로그인이 반쯤 저장된다). 회귀 2건 신설 — 체인 커밋 저장 스냅샷 · **application 체인 레코드 미복원**(0170 의 '앱 게이트 자동 통과 금지' 가 멤버 N개에서도 유효한지). 설계대로 구현했다 — registry `loginChainFor`(manifest 선언 순서) · transaction `key` 고정 + `ChainState`·`advance`(멤버별 타임아웃 재시작) · `bindings.createMany`(root/child·같은 target 전량 축출·**축출 vault 정리** = 0157 부터의 누수 동시 해소) · broker stage→advance→commit / rollback(역순 `logout`, **새 AbortController**) · 취소·타임아웃도 롤백 · `authenticated` = application binding 전부 valid. **구현이 잡은 4건**: ⓐ 기존 broker fixture 가 한 패키지에 application provider 2개를 담고 있어(pat+adfs) 새 의미에선 체인이 된다 → **패키지를 둘로 나눠** 각 테스트의 원래 의도를 보존했다(**단언 무변경**, AC11 의 의미는 지키고 문구는 못 지킴 — 이 사실 자체가 "한 패키지 다중 application provider" 가 지금까지 무의미한 조합이었다는 방증) ⓑ 폼 초기화를 effect+setState 로 하면 `react-hooks/set-state-in-effect` **lint error** → `key={stepKey}` remount 로 전환 ⓒ AC10 은 static credential 의 `refresh:not_supported` 때문에 만료를 만들 수 없어 테스트 멤버를 `reauth_required` 로 감쌌다 ⓓ 기존 배포 영향 없음(이전엔 두 번째 provider 가 앱 로그인에서 **도달 불가**했고, 동봉 Confluence 는 application provider 0개 — 실측). 게이트: lint **0 error** · typecheck **3/3** · vitest **1908/1908**(신규 27건). 환경 실패 1파일(`chat-turn.continuity` — Electron 바이너리 미설치, `git stash` 로 변경 무관 확인). **IPC 채널 82 불변 · 신규 의존성 0.** 다음 = Claude 검증. / **한 패키지의 application provider 다수를 하나의 로그인 체인으로 (설계 2026-08-05).** 요구: provider1 성공→binding **보류** → provider2 성공→보류 → **전부 성공해야** `authenticated:true`, 하나라도 실패하면 로그인 전체 실패 + 보류분 정리. 원인은 두 곳이다 — renderer 가 application provider **첫 하나만** 실행하고(`store.ts:65`), `BindingStore.create` 가 같은 target 의 기존 binding 을 **교체**해(`bindings.ts:56-59`) 순차 로그인을 해도 두 번째가 첫 번째를 지운다(축출된 binding 의 vault 는 아무도 안 지운다). 더 작은 해법(패키지가 두 방식을 내부에서 도는 provider 하나로 합치기)은 `AuthBindingDraft.artifact` 가 **단수**라 성립하지 않는다. 설계: registry `loginChainFor`(같은 pluginId manifest 선언 순서) · transaction 에 `chain{members,index,staged}` + 멤버별 타임아웃 재시작 · `bindings.createMany`(root/child 원자 커밋 + 같은 target 전량 축출 + 축출 vault 정리) · broker stage/commit/**rollback**(보류분 역순 `logout` — **새 AbortController** 로, tx signal 은 이미 abort 돼 `runGuarded` 가 provider 를 안 부른다) · 취소·타임아웃 경로도 롤백 · `authenticated` = application binding 전부 valid. **사용자 확정 3건**: 체인 범위=패키지 단위 · 멤버=암묵(`targets` 선언 + manifest 순서) · 로그인 화면 **진행 표시**(`AuthStepInfo.chain` additive-optional, 채널 수 **82 불변**). AC 17건(사람 실기 1). **구현자 = Claude**(환경에 Codex 부재, 사용자 지시 "핸드오프 작성 및 구현까지"). plan `docs/handoff/0172-multi-provider-login-chain/plan.md`. |
| `0167-session-activity-projection` | impl | **IMPL_DONE (r2)** | **Claude** | 작업 트리 | 2 | **[verify r1]** **FAIL — 고치려던 증상을 결정론적으로 재현한다.** projector 자체는 설계대로다(앱 수명 구독·단조 revision·microtask 병합·key 승격·tombstone) 그리고 **legacy publisher 완전 제거(AC17 실측 0건)** 는 요구 이상이다. 문제는 소비 측 한 줄 — 리듀서가 `listening` 에 `busy`(큐 카운트 포함)를 OR 해(`chatReducer.ts:526`), 0154 가 **의도적으로 남기는** `orphaned` 배치 하나로 `sessionBusy` 가 무한 true 가 된다. **실모듈 조립 실행으로 확정**(`transport:idle`·`foreground:idle`·`deliveryPendingCount:1` → `listening:true`) = 보고 ②-a. 또 plan 이 renderer 소유로 명시한 `inflight` 가 **말없이 main 파생(`ev.foreground!=='idle'`)으로 이전**됐다 — 지금은 `queueMicrotask` 배칭 덕에 취소 직후 되살아나지 않지만 **우연히 안전**하고 테스트로 고정돼 있지 않다. 리듀서 테스트 헬퍼가 `busy=(transport==='listening')` 로 고정돼 **위험 분기가 한 번도 실행되지 않는다**. AC 22건 중 ✅11 / ⚠️8 / ❌2. 파생 D3(잔여 고착)·D4(inflight 소유권 — 사람 결정)·D5(헬퍼)·D6(상수 이름 불일치). verify `docs/handoff/0167-session-activity-projection/verify.md`.  **활동 스냅샷 투영 + 대기 UX (설계 r2, 2026-08-04).** **선행 0165·0166 — 순차 병합 강제.** **r2**: 잔여 상태·발행이 0165 에서 **이 문서로 이관**(유일 publisher) + **legacy 직접 producer 제거**(`beginListenPhase`/`endListenPhase`·`reconcileInterrupt` 발행 — revision 없는 지각 이벤트가 이미 지운 경고를 되살린다) · **transport 공식 확정**(lease 존재가 아니라 `postTurnHoldsSession(step)` — lease 로 판정하면 정상 응답 중에도 대기 라벨이 켜진다) · **activity clock 모순 해소**(`lastActivityAt` 을 스냅샷에서 **제거**, 경과·무활동은 renderer 로컬 시계 — dedupe 에서만 빼면 '활동이 오는데 무활동으로 보이는' 모순) · **`promoteProjection`**(clientKey→sessionId 승격 시 revision 원자 이전) · **hydrate 는 store 적용까지 검증** · **AC7 을 main admission 테스트로 이동**(0153 의 `pendingCount>0` 유지가 옳다) · count 를 `queuedCount`/`deliveryPendingCount`(메시지 수)로 확정 · `IDLE_HINT_MS=30_000` 확정 · 문구 중립화. 0143 유지(사용자 결정) — 애니메이션 제거 없이 라벨만, `sessionBusy` 불변. AC 22건. plan `docs/handoff/0167-session-activity-projection/plan.md`. |
| `0166-session-chain-lease` | impl | **IMPL_DONE (r2)** | **Claude** | 작업 트리 | 2 | **[verify r1]** **FAIL — 인수 기준이 구현에 맞춰 다시 쓰였다.** lease 상태머신(`preparing`·`active`·`closing`·CAS·leaseId 해제·`swapChild`·자원 보존 closing)은 깔끔히 들어왔고, 구현자가 잡은 동시 send 순서 경합 3건(같은 ms `admittedAt` 역전·preparing 중복 계상·lease+child 동시 abort)은 설계보다 정확했다. 그러나 **A18·19·21·22·26·27·28·29·32 — 9건이 구현 커밋에서 재작성**됐다(`git show 03ff691 -- plan.md` 로 확정): `submitSteer`+`expectedToken` 요구 소멸(새 A18 은 0165 AC7 과 동일) · "PostToolBatch 직접 push 0" 이 "직접 push 하되 결합" 으로 반전 · `retireChannel` 정착 대상 반환 보증 삭제 · **spawn handshake(`openChannel`) 미구현** · 명명 테스트가 "…suite"·"…fence" 같은 지시 불가 서술로. 포트 3종(`SubmissionAttemptPort`·`SessionControlPort`·`ChannelLifecyclePort`)·`openChannel` 은 **grep 히트 0**. 게다가 **이 핸드오프의 존재 이유인 미보고 결함 4종에 테스트가 하나도 없다** — `restartGateState`(D4 업데이트 게이트) 0회 · `stopSubagent`(D6) 0회 · `discardSession` 0회 · shutdown 순서(D5) 0회 · `chat:send`×2 이중 체인(D7) 0회. AC 32건 중 ✅9 / ⚠️16 / ❌6. 부수 발견 — `closing` lease 입력 거부가 **"이 백엔드는 끼어들기를 지원하지 않습니다"** 라는 엉뚱한 사유를 띄운다(`chat-turn.ts:302-313`). 파생 D1~D5. verify `docs/handoff/0166-session-chain-lease/verify.md`.  **세션 소유권 lease + 제출 트랜잭션 (설계 r2, 2026-08-04).** **선행 0165 — 순차 병합 강제.** 미보고 현행 결함 5종을 한 뿌리에서 닫는다 — **D7** 동시 전송 2건이 이중 체인·이중 런타임(busy 판정 `:394` ↔ 등록 `:552` 사이에 `await resolveTurnProvider`) · **D4** 재시작 게이트가 `all()` 기반이라 **작업 중 업데이트 설치** 허용 · **D5** shutdown 이 idle 풀만 닫아 **active 서브프로세스 잔존** · **D6** 제어 상태가 턴마다 리셋돼 **listen 중 서브에이전트 중단 미도달** · **D3** '세션 전체 중단' 이 active runtime 미도달. **r2 보강**: **spawn handshake**(최초 prompt·프렐류드가 **LiveTurn 이전에** `createSessionInputStream` 으로 적재되므로 `submitSteer` 만으로는 네 경로 중 셋이 Runtime 을 안 지난다 — `openChannel(reqWithoutInput)` 로 교체) · **adapter outcome 계약**(`pushTurn: Promise<void>` 가 `input.push()` 의 boolean 을 버려 '명시적 거절' 이 관측 불가 → `accepted`·`rejectedBeforeAccept`·`stale`) · **재시도 규칙 0165 에서 이관**(거절만 1회 재시도, accepted 후 무이벤트는 재전송 금지) · **`beginMany`**(프렐류드 N개 전부 성공 또는 전부 무효) · **`acceptsQueuedInput`**(preparing·spawn 전에는 `canSteer=false` 라 admission 과 live capability 를 분리 — 안 그러면 '끼어들기 미지원' 에러가 뜬다) · **준비 실패 = draft 복원 고정**(자동 leader 승격 금지) · **`closing` 이 자원 보존 + discard/shutdown 전용**(취소는 정상 종료 — 0067 AC3 정합) · **activation CAS** · **commit fencing** · **open 정본 3상태** · **`ChannelLifecyclePort`**(토큰이 경계를 넘어야 `retireChannel` 이 정착 대상을 반환). AC 32건. plan `docs/handoff/0166-session-chain-lease/plan.md`. |
| `0165-cancel-residue-and-listen-hang` | impl | **IMPL_DONE (r2)** | **Claude** | 작업 트리 | 2 | **[verify r1]** **FAIL — 취소가 미소비 이벤트를 통째로 버린다.** 설계 A·B·C 의 구조는 올바르게 들어왔고(AC1·2·7·10·12 실측 green), 구현자가 **설계 결함 1건을 잡았다** — r7 의 "unframed 를 다음 openFrame 앞에 합류" 는 사용자 프레임에 안전하지 않아(과거 자동 턴 result 가 새 질문에 재귀속) listen 만 backlog 를 인수하고 일반 send 는 채널을 격리하게 고쳤다. 그러나 취소 처방이 과했다: `markAborted` 가 `frame.end()` → **`Frame.discard()`**(큐를 먼저 비운다, `:60-63`)로 바뀌어 **이미 도착한 delta 와 `telemetry` 까지 폐기**한다 — **프로브 실행으로 확정**(A 만 소비, B·C 소실). 결과 ⓐ 중단 직전 부분 답변의 꼬리가 화면·DB 양쪽에서 사라지고(plan UX 표 "내용 보존" 위반) ⓑ 같은 배치의 telemetry 유실로 **그 턴 usage/cost 가 조용히 미집계**된다. 덜 비싼 대안 있음(취소 후 `error` 만 배제 / 프레임 `cancelled` 플래그로 드레인 허용·신규 error push 만 거부). 또 **보고 증상 ① 이 닫혔다는 기계 증거가 없다** — AC14(`chat:send→cancel→send` 에서 error 0건)의 테스트 파일 부재. AC5·11·13·15 도 미검증이고, 특히 AC11 은 테스트의 `orphanUnconfirmed` 호출이 **전부 인자 없음**이라 chainId 스코프가 한 번도 실행되지 않았다. AC 16건 중 ✅5 / ⚠️6 / ❌4. 파생 D10(취소 폐기)·D11(배치 합류)·D12(respawn 이월)·D13(파일명 기준). verify `docs/handoff/0165-cancel-residue-and-listen-hang/verify.md`.  **취소 잔여 — 보고 증상 직결분 (설계 r7, 2026-08-04).** 리뷰 **6라운드**. **r7 에서 내 설계의 구현 불가 3건이 코드로 확정**돼 더 좁혔다 — ⓐ **재시도 AC 는 검증 수단이 성립하지 않는다**(`LiveTurn.pushTurn?: Promise<void>` 가 `input.push()` 의 boolean 을 버린다, `types.ts:24`·`streaming-input.ts:30`) → **0166** ⓑ **최초 prompt·프렐류드는 LiveTurn 이전에 적재**돼 `submitSteer` 만으로 '모든 입력이 Runtime 통과' 가 성립하지 않는다(`claude.ts:327-334`) → **0166** ⓒ **잔여 발행이 0167 과 이중 권위**(지각 legacy 이벤트가 이미 지운 경고를 되살린다) → **0167**. 남은 설계 — **A** 메시지-원자 배치 라우팅(`consumeTurnScoped`·mock 포함) → **보고 ① + 실제 실패 result 일반 누출 해소** · **B** channel incarnation token(`ensureChannel()` 이 기존 토큰 반환, spawn/respawn 때만 발급)으로 세대 검사 + **중단 영수증 정합**(②-b **오탐** 소멸) · **C** `SubmissionAttempt{messageIds[],attemptId,chainId}` + 체인 종료 강등(ABA 차단) + `discardSubmitted` open 확장 · **D** 턴-후 판정 입력 관측. **6차 제안 4건 미채택** — foreground/transport 분리(**사용자 결정 ⑥ 위반**) · `RetiredChainRegistry`(**0067 AC3 '취소 후 같은 채널 재사용' 과 충돌**, `closing` 을 discard/shutdown 전용으로 두면 불필요) · publisher feature flag(같은 빌드라 스큐 없음 → 대신 0167 에 'legacy producer 0건' AC) · 대기 표시 액션 버튼(범위 밖 제품 변경). **정정**: r6 의 "파일이 거의 겹치지 않는다" 는 **틀렸다** → **0165 → 0166 → 0167 순차 병합 강제**, 각 단계 단독 green, 0166 이 바꿀 API 를 표로 예고. AC 16건, **신규 모듈 0 · 신규 포트 0 · 레이어 무변경 · IPC 무변경**. plan `docs/handoff/0165-cancel-residue-and-listen-hang/plan.md`. |
| `0164-buildtime-servers-only` | impl | **IMPL_DONE** | **Claude** | `4c74524` (r1) | 3 | **[impl r3]** **도구 재지정 — 찾기와 읽기를 나눈다.** 사용자 지시: `search` 는 pageId·타이틀·**작성자**만, `getPages` 가 pageId 들로 본문+첨부를 준다. 페이지네이션은 **1턴 50건**이되 "허용치가 낮으면 그 숫자를 따르고" 더 있으면 그 크기로 offset 을 민다. 요구의 핵심은 **오프셋을 요청값이 아니라 응답값으로 민다**는 것 — Confluence 는 사이트 설정에 따라 limit 을 낮추고 그 사실을 응답의 `limit` 으로 알리므로, 모델이 요청 limit(50)을 더하면 서버가 25만 적용한 구간이 통째로 사라진다. `parseSearchResponse` 가 실효값으로 `nextStart` 를 **계산해서** 문장으로 준다(회귀 테스트 고정). 작성자는 `expand=…,history` 의 `history.createdBy`, 빠진 배포는 `version.by` 폴백, 그마저 없으면 필드를 싣지 않는다. `getPages` 도 한 번에 50개까지이고 넘긴 id 는 버리지 않고 `skippedPageIds` 로 돌려준다. **부수 이득**: r2 는 검색이 파일을 써서 **모든 검색이 승인 카드**였는데(그 비용을 r2 에서 보고했다), 둘로 나누니 탐색은 `readOnlyHint:true`(자동 허용) · 내려받기만 `false`(승인)로 정직한 선언과 편한 사용이 동시에 성립한다. 게이트: lint **0 error** · typecheck **3/3** · vitest **1816/1816**. **IPC 86 유지 · 신규 의존성 0.** 다음 = Claude 검증. / **[impl r2]** **D1~D10 전건 대응.** verify r1 의 FAIL 6건 + 같은 날 사용자 보고 4건을 한 라운드에서 처리(사용자 지시 "핸드오프를 할당하지말고 해당 핸드오프에서 진행하라"). **보고 4건은 전부 프로브로 원인을 먼저 확정**했다 — ③ 표가 XML 로 새던 것은 `turndown-plugin-gfm` 이 **머리글 행 없는 표를 `keep()`(원본 HTML)** 하기 때문이고, Confluence 저장 형식의 `<colgroup>` 이 `isFirstTbody()` 를 무너뜨린다(소스 실독 + 케이스별 변환으로 재현). ④ `servers.ts` 가 UI 에 없던 것은 등록이 **패키지 단위 all-or-nothing** 이라 `baseUrl` 끝의 `/` 하나가 provider·connector 를 **전부** 지우는데 흔적이 warn 로그뿐이었기 때문(6가지 입력으로 실행 재현 — 정상 입력은 OK, 등록 경로 자체는 무결). 대응: ① 도구를 **`confluence_search` 하나**로(connector operation 도 1종 — 나머지는 호출자가 사라져 죽은 분기) ② 검색이 hit→페이지→Markdown→참조 첨부까지 한 번에(상한 **기본 5·최대 10**, 페이지 동시성 2) ③ⓐ `JSON.stringify` 폐기하고 순수 렌더러 `search-render.ts` 로 본문을 **감싸지 않고** 싣는다 ③ⓑ `<colgroup>` 제거·첫 행 `<th>` 승격·셀 내 `<p>`→`<br>` 정규화 + **테스트 fixture 를 실제 저장 형식으로 교체**(축약 fixture 였기에 회귀를 못 잡았다) ④ `normalizeServerConfig` 예방 + **`orca:plugin:diagnostics` 신설**(85→86)로 거부 사유를 플러그인 탭 배너에 노출. **D1 은 증상이 아니라 원인을 고쳤다** — provider 선언↔구현 미대조(D4)가 뿌리이고, 그 검사를 넣자 저장소 fixture **3곳에서 같은 불일치가 즉시 드러났다**(P21). 부수: `search` 가 로컬에 파일을 쓰므로 **`readOnlyHint:false`(승인 카드 경유)** 로 정직하게 내렸다 — 사용자 보고 대상. 게이트: lint **0 error** · typecheck **3/3** · vitest **1796/1796**. **신규 의존성 0.** 다음 = Claude 검증(r2). / **[verify r1]** **FAIL — prod 로그인 게이트 회귀(D1).** `AUTH_PLUGIN_PACKAGES` 활성화로 Confluence PAT provider(`targets:['application','connector']`)가 **서버 0개에서도** 등록돼 `required:true` → prod `RootGate` 가 `<LoginFrame/>` 로 앱을 막는다(DEV 는 bypass 로 가려짐). 프로브 실행으로 재현. AC 16건 중 13 ✅ / 3 ⚠️. 파생 D1·D2(`pluginTone` 죽은 코드)·D3(`providerMap` 이중 구현)·D4(registry 가 provider 선언↔구현 미대조)·D5(`hasPlugin` 스킵의 무검증 가정)·D6(AC9 스키마 테스트 부재). 게이트는 전부 green — **게이트가 못 잡는 결함**. verify `docs/handoff/0164-buildtime-servers-only/verify.md`.  **빌드타임 고정 서버 + 행 = 서버 (설계 + 구현, 2026-08-03).** 사용자 방향 전환 — "빌드타임에서 2개의 컨플루언스(다른 base url 빌드타임 고정) 등록" · "UI 에 **2개 항목이 노출**" · "base url 수정은 안된다" · "**auth 인증은 런타임**에 사용자가 직접 입력". 질의로 확정 2건: UI 추가 경로는 **"제거 빌드타임 전용, 단 디버그 패널에 토글 추가해 추가버튼 노출"**(삭제 아닌 게이트) · 실제 주소는 **"자리만 만들어 두라"**. **요구 4건 중 3건은 이미 동작**했다(정적 N서버 등록·주소 수정 채널 부재·런타임 자격증명) — 손볼 것은 "2개 노출" 하나였고 그것이 **0159 명시 결정 ⑦ "행 = 플러그인 패키지"**(사용자 결정)를 건드렸다. 그 결정의 전제(패키지가 provider+connector 를 함께 담는다)가 0161 의 패키지 2분할로 이미 깨져 있었으므로 **행의 단위를 커넥터(=서버)로 뒤집는다** — provider 전용 행이 사라지고, "인증 제공자 0/2" 혼란도 함께 해소된다. **설계가 잡은 함정**: 정적 패키지와 템플릿 공용 패키지가 `manifest.id='confluence'` 를 공유해 배선을 켜는 순간 **중복 등록**이 된다. `restore()` 가 로그만 남기고 계속하므로 **테스트 green 인 채로 매 부팅 오류 로그**가 샐 뻔했다 → `AuthRegistry.hasPlugin()` 신설 + 이미 등록됐으면 공용 패키지 등록을 건너뛴다. **구현자 정정**: 설계가 `PluginRow` 소비처를 3곳으로 셌는데 실제 **5곳**(`pluginGroups`·선택 키 누락) — 선택 키를 안 바꿨으면 행을 눌러도 상세가 안 열리는 조용한 파손이었다. 그 외: `PluginConnectorInfo.connectedProviderId`(선택) 추가로 "**무엇으로 연결됐는지**" 표시(secret 아닌 provider id 만, 부재=미연결) · `Tweaks.pluginAddEnabled`(기본 false) + 디버그 패널 토글 · 열 제목을 `서버`·`주소`·`인증 방식` 으로 · **배포 편집 지점 2곳 → 1곳**(`servers.ts` 만, 배선은 상시 켬 — 빈 배열이면 provider 만 등록). 게이트: lint **0 error**(warning 1 = 0102 베이스라인) · typecheck **3/3** · vitest **1770/1770**(신규 13) · scripts **28/28**. **IPC 85 유지**(DTO 선택 필드 1개) · **신규 의존성 0**. 실제 사내 주소는 **미기입**(사용자 결정). 사람 실기 1건(AC8 디버그 토글). 설계 `docs/handoff/0164-buildtime-servers-only/plan.md`. |
| `0161-connector-instance-templates` | verify | **FAIL** | **Claude** | `9b161e9` | 1 | **[verify r1]** **FAIL — D1 의 진원지.** 0161 이 `instances.restore()` 를 부팅에 넣으며 템플릿 공용 패키지를 **무조건** 등록해, 서버 0개에서도 `application` target provider 가 생기고 prod 로그인 게이트가 켜진다. `modules/AGENTS.md` 의 "provider 0개면 게이트 자동 통과(현행 동작 보존)" 약속을 깼다. **인수 기준 23건 어디에도 걸리지 않았다** — 역방향 탐색으로만 잡혔다. 파생 D1·D4. 교훈: 부팅 시점에 새로 등록하는 설계는 **그 레지스트리를 읽는 기존 소비처를 전수 나열**해야 한다. verify `docs/handoff/0161-connector-instance-templates/verify.md`.  **connector 템플릿 → 사용자 생성 인스턴스 — 설계 + 구현 완료 (2026-08-03).** 구현 주체 이탈: 기능 구현은 원래 Codex 몫이나 환경에 Codex 가 없어 **사용자 지시로 Claude 가 직접 구현**(절차·trailer 는 구현 턴 규약 준수). 사용자 UX 재결정 = "플러그인에서 **추가 버튼** 클릭 시 컨플루언스를 선택, base url·pat 혹은 id/passwd 입력" + "**컨플루언스 플러그인이 템플릿으로 제공**". 0160(IMPL_DONE `c0d1523`) 위에 **인스턴스 수명주기 계층만** 얹는다 — Confluence 로직은 이미 factory 형태라 그대로 재사용(`createConfluenceConnector`·`createConfluenceTools`·`createConfluencePackage`). **0158 결정 2건을 이번엔 완전히 뒤집는다**(정적 connector 전용 · "동적 URL/endpoint 입력을 만들지 않는다") — 단 사용자 결정으로 **정적 경로는 유지**되어 두 모델이 공존한다. 사용자 결정 3건: ① connector ID = **host 파생**(`confluence-wiki-corp`) ② 영속 = **electron-store `connectorInstances` 키** ③ 코드 레벨 `servers.ts` **존치**. host 파생의 두 구멍을 설계에서 닫았다 — 같은 host 의 컨텍스트 경로 충돌은 **경로를 ID 에 포함**해, 주소 수정 시 ID 표류는 **주소를 생성 후 불변**으로(수정 대신 삭제 후 재생성, 모달이 사전 고지). 실측 제약: `AuthRegistry` 는 **`unregister` 0건**(부팅 1회 등록뿐) · 중복 `manifest.id` 거부 + `descriptor.pluginId === manifest.id` 강제 → 인스턴스마다 고유 pluginId 필요 · **중복 provider id 거부** → provider 는 템플릿 공용 패키지에 1회만 등록하고 인스턴스 패키지는 connector+tools 만(교차 패키지 provider 참조는 `manifest.ts:164-165` 가 허용) · 설정 스토어는 새 키를 기본값으로 흡수해 **번호 마이그레이션 불필요**. broker 의 origin·redirect 강제는 `descriptor.baseUrl` 을 읽으므로 **0160 구현이 코드 변경 없이 인스턴스에 적용**된다. **SSRF 표면이 r2 에서 사라졌다가 복귀** — 사내망이 목적이라 private IP 는 차단하지 않고 origin 형태 강제 + redirect 동일 origin 으로 막는다. IPC **82→85**(templateList·instanceCreate·instanceDelete), 신규 의존성 **0개**. 인수 23건(사람 실기 2 = 사내 DC 서버 + 재시작 복원 확인). 설계 `docs/handoff/0161-connector-instance-templates/plan.md`. **구현 (`9b161e9`)** — 신규 8 + 수정 18. ① 계약을 **`contracts/connector-template.ts`** 에 둔다(설계 표는 `features/connectors/templates.ts` 였는데, 구현자(auth-platform/modules)와 레지스트리(features/connectors)가 **다른 feature 슬라이스**라 그대로 두면 교차 import lint error — 설계 이견 3건 중 1건). ② `AuthRegistry.unregister()` 신설(manifest·provider·connector·runtimeTool 4맵 동시 제거). ③ `instance-lifecycle` 의 순서 불변식 — 복원은 **공용 패키지 먼저** 인스턴스 나중, 삭제는 **disconnect → unregister → store remove**, 생성 실패 시 store 롤백. ④ `plugin-host.list()` 에 `source`(`static`|`instance`) — **기본값 `static`** 으로 fail-closed(삭제 버튼은 `instance` 에서만 뜬다). ⑤ **사용자 보고 수정** — `ExtensionsCatalogView` 의 추가 버튼이 `selection.tab !== 'plugins'` 로 **plugins 탭에서만 숨겨져 있었다**. 탭별 디스패치(skills=메뉴·mcp=커스텀 MCP·plugins=서버 추가)로 교체. ⑥ 붙여넣기 보정 — 브라우저 주소창 전체 URL 을 origin+컨텍스트 경로 후보로 갈라 **제안**한다(`/display`·`/pages` 등 뷰 세그먼트는 후보에서 제외, 자동 확정 안 함). 게이트: lint **0 error**(warning 1 = 0102 베이스라인) · typecheck **3/3** · vitest **1725/1725** · scripts **28/28**. `app/chat-turn.continuity.test.ts` 1파일 collection 실패 = electron egress 차단 베이스라인(코드 무관). **사람 실기 대기 2건**(AC21·22 — 사내 Confluence DC 서버 + 재시작 복원 육안). |
| `0146-composer-ime-caret-guard` | impl | IMPL_DONE | Claude | `7e456fa` | 3 | **한글 IME/장문 decoration 후속 r3.** 사용자 Electron 실측에서 caret은 정상 유지. 장문 highlight는 누적 이탈하지만 scroll 변경 즉시 복구되어, static wrap 오차가 아니라 textarea와 deferred decoration의 독립 scroll range/offset clamp가 원인 범위로 좁혀졌다. textarea를 유일 scroll authority로 유지하고 decoration을 동일 gutter의 clip viewport + `translate3d(-scrollLeft,-scrollTop)` projection으로 분리했다. decoration `scrollTop/scrollLeft` write, 사후 layout effect/rAF 재동기화는 없다. deferred tokenization·controlled text·`field-sizing`·revision/atomic clear 유지. r3 Composer 5 files·17/17, lint 0 error(기존 warning 1), typecheck 3종 PASS. **장문 max-height 자동/수동 scroll·zoom 재검증 전 highlight correctness PASS 금지.** H3 재현은 DOM-owned 별도 escalation. |
| `0116-release-0-3-1-auto-publish` | impl | IMPL_DONE | Claude | `882b021` | 1 | v0.3.1 버전 동기화 + 태그 릴리스 자동 Publish로 updater 피드 가시화. 검증 대기. |
| `0031-skills-mcp-wiring` | impl | IMPL_DONE | Claude | `d4f82ee` | 1 | Codex 구현 완료 — customize 목업 제거·connector→mcp 리네임·스킬 sources IPC(author/upload/enabled)·MCP textarea 모달·enabled filter deploy·workspace cwd 싱크·IPC_CONTRACT 동기화. `git diff --check` 및 금지어 grep 통과. npm 게이트는 불완전 `node_modules` 환경으로 실행 불가(verify 재실행 필요). **Claude 후속 버그수정(사용자 요청 4건)**: ① 어댑터 스킬 그룹 라벨 `어댑터 스킬`→`CLAUDE 스킬` ② 워크스페이스 스킬 그룹/항목 페이지 표시 제거(local 채팅영역 처리는 추후) ③ 케밥 팝오버 우측 정렬(`Popover` `align='end'`, 화면 밖 오버플로 해소) ④ "채팅에서 사용해보기" 클립보드→nav state 컴포저 프리필(`/<스킬명> `). 게이트 lint/typecheck/typecheck:test/test **409/409**(Node ABI 재빌드 후 green), 레이어 경계 0. |
| `0051-lifecycle-taxonomy-refinement` | impl | IMPL_DONE | Claude | (push 후) | 1 | 0050 설계의 **용어·축 정제 (문서 전용 = Claude 직접 impl).** ① '세션' 과부하 해소 → **Orca Session(대화 기록 진실) / SessionRuntime(일시적 실행 핸들) / SDK resume context(외부 binding)** 3분리(GLOSSARY §1·§3). ② 결정 ⑭ 교정 — cap/LRU/idle-close 가 세는 유닛=SessionRuntime → **동시성=자원 라이프사이클(§2)**, 오케스트레이션=handoff 만; 출시 `orchestration/` 코드명 유지(문서만 정제·분기 메모, 결정 2). ③ **2축 정제 절(§A) 신설**: 세로(소유) + 가로(턴 파이프라인 stream→reduce→persist∥forward + 권한 재진입) + 누락 **TurnCoordinator** 1급화 + DB=_기록_ 진실 한정(reseed≠recovery) + Conversation Continuity/Knowledge Curation(Future 서비스 층, memory.md 폐기→knowledge artifact) + Staging(P0 출시/P1/Future). ④ provider-runtime TurnCoordinator 포인터. 코드 변경 0(비범위: TurnCoordinator 1급화·코드 리네임·continuity 구현은 후속). 사람 확인 대기: 0050 구현 재적용/PR 결정. |
| `0103-dev-db-userdata-isolation` | impl | IMPL_DONE | Claude | `9640438` | 1 | dev/prod 데이터 격리 (비기능 = Claude 직접 구현, 사용자 결정 2건: 격리=userData 디렉토리 분리·신호=`import.meta.env.DEV`). `index.ts` 가 `app.whenReady()` 이전 dev 에서만 `userData` 를 sibling `orca-dev` 로 리디렉션(`app.setPath('userData', devUserDataDir(app.getPath('appData')))`) → DB·WAL·마이그레이션 백업·secret-store 통째 격리. 순수 헬퍼 `devUserDataDir`(`infra/config/paths.ts`) + 단위 테스트. prod 번들은 `import.meta.env.DEV` dead-code 제거. `db/index.ts`·`migrate.ts` 무수정(디렉토리 분리 이점). app/AGENTS.md DB 정책 갱신. **후속 문서(동 브랜치)**: app/AGENTS.md §빌드/실행 에 better-sqlite3 ABI 제약 환경(egress 403) 게이트 가이드 추가 — 실패 서명 목록 + DO/DON'T(DB 로드 스위트 실패는 baseline 분리 보고·ABI 뒤집기 금지·electron 로딩/build 검증은 CI/사람 몫). 검증 대기. |
| `0104-sqlite-abi-node-revert` | impl | IMPL_DONE | Claude | `a9bf02b` | 1 | sqlite ABI 버그수정 (비기능=Claude 직접, 원류 0103). **문제**: `npm install` 후 `npm run dev` 가 Node-ABI 고착 — `ensure-sqlite-abi.mjs` electron 판정이 마커 메타데이터만 보고(binary-blind) 실제 바이너리 ABI 미확인. npm 의 prebuild-install 이 바이너리를 Node-ABI 로 되돌려도 마커는 `{target:electron}` 이라 fast-path 가 재빌드를 skip. **수정**: electron `needsRebuild` 에 실제 로드 프로브 겸용(`loadBetterSqlite||!markerMatches`) → Node-ABI 감지 시 재빌드. 회귀 테스트 + 기존 electron 테스트 hermetic 주입. **가이드**: app/AGENTS.md 에 "어떤 명령이 ABI 를 뒤집는가"(lint/tsc/format=중립·test/install/dev/build=flip) + `npm test`→build 순서가 마찰의 실체 + ABI-안뒤집는 테스트 실행법. 게이트: ensure-sqlite-abi 7/7·lint 0·typecheck 0. 검증 대기(실 ABI 전환 실기는 CI/사람). |
| `0105-native-binary-executable-resolve` | impl | IMPL_DONE | Claude | (push 후) | 1 | 패키징 빌드 native binary 실행 실패 버그수정 + 공식 설치본 지원 (비기능=Claude 직접, 사용자 결정 2건: 공식/PATH 우선·env override 없음). **Root cause(SDK 0.3.143 소스 확인)**: `query()` 가 native binary(`@anthropic-ai/claude-agent-sdk-<plat>-<arch>/claude[.exe]`)를 `require.resolve`→직접 spawn 하는데, electron-builder 가 `node_modules` 를 `app.asar` 로 묶어 해석 경로가 asar *내부* → `existsSync`=true(asar-aware fs)지만 spawn 불가 → "native binary … exists but failed to launch"(dev 는 asar 없어 미재현). **수정**: ① `asarUnpack` 에 `node_modules/@anthropic-ai/claude-agent-sdk-*/**` 추가(실디스크 언팩) ② 신규 `adapters/claude-executable.ts` — `pathToClaudeCodeExecutable` 실경로를 **PATH → ~/.local/bin(공식 인스톨러) → 번들 asar-언팩본** 순으로 해석(순수 함수+주입, 비패키징=undefined 로 SDK 기본 위임) ③ `claude.ts` runCompletion·sendMessage 양 query options 에 주입. 게이트: typecheck 3종 0·lint 0(경고 1=0102 무관)·어댑터 224/224(신규 12). **패키징 실행 실기는 egress 403·비-Windows 로 이 환경 불가 → build:win 산출물 실기(공식설치본/번들폴백 2케이스)는 CI/사람 대기.** 상세=`0105-native-binary-executable-resolve/plan.md`. |
| `0111-weekly-usage-fixed-budget-external-reconcile` | impl | IMPL_DONE | Claude | (push 후) | 1 | 사용량 계산 3축 (비기능=Claude 직접). **① 주간 버짓**: 동적 런웨이(남은예산÷남은일수, 분모가 계속 이동)→ 고정 일할 `limitUsd×weekDaysInMonth/daysInMonth`(경계 주는 이달 몫만) → weekPct 가 이번 주 실지출에만 반응. **② 주/일 사용량 정합(fresh)**: 외부 권위 월간(=Orca+외부 총액)에 로컬 추정치를 `외부월/로컬월` 배 비례 스케일(week≤month 보존), 로컬월≈0 시 경과일 균등 분배 폴백. **③ stale 폴백·복구**: 외부 fetch throw/null 시 `external-usage-service` 가 캐시 baseline 으로 폴백(reject 안 함)+`lastFetchOk` 로 stale 의미화 → 순수 파생이 월=`max(baseline,로컬월)`·주/일=로컬 원값, 재성공 시 권위값 자동 복구. `limits.ts`/`clock.ts` 순수 + `external-usage-service.ts` 소폭. 게이트 typecheck/lint ✅, 대상 3 스위트 22/22·전체 순수 853 pass(DB 로드 5파일은 ABI egress 베이스라인, 변경 무관). 소비자 UI/훅 무변경. verify PASS 대기. |
| `0112-usage-summary-daily-tokens` | impl | IMPL_DONE | Claude | `2d76b9e` | 1 | 설정 사용량 요약 구현 (기능이지만 사용자 직접 지시로 Claude 가 설계+구현, 브랜치 `claude/settings-usage-summary-5f8dql`). ① **주기적 실행 그룹 제거** — UI 만(사용자 확정: main 스케줄러·tweak 유지), `usageSchedule.{ts,test.ts}`·관련 i18n 키 삭제. ② **사용량 요약**: 기간 탭(최근 7일/30일/전체) + 일별 토큰 차트(tokens per day, **recharts@^3 신규 의존 — 사용자 승인**, 토큰 = input+output+cache 4종 합산 사용자 확정, M 단위 소수 1자리 `formatTokens`) + 모델별 내역(총 토큰 내림차순·Meter). 신규 IPC `orca:cost:usageStats`(cost 4→5, 총 65→66) + `sumUsageByDaySince`/`sumUsageByModelSince`(로컬 일자 버킷, lazy stmt) + 공유 순수 `shared/usage/stats.ts`(rangeSince/fillDailySeries/aggregateWeekly/totalTokens). '전체' 90일 초과 시 주 단위 표시 집계 + 730일 캡. 게이트 lint 0·typecheck 3종 0·vitest 901/901(117파일, +신규 3스위트; `chat-turn.continuity` 1파일 = electron 바이너리 egress 차단 로드 실패, 변경 무관 베이스라인). IPC_CONTRACT·TRD §4 동시 갱신. verify 대기. 사람 확인 대기: 시각 검증(차트·두 테마)·실데이터 실기·PR 머지. |
| `0114-better-sqlite3-asar-unpack` | impl | IMPL_DONE | Claude | (push 후) | 2 | **r2 — 사용자 실기에서 위치 수정 후 ABI mismatch 확인.** 설치본의 unpacked 바인딩은 Node 22 ABI 127, Electron 39는 ABI 140 요구. 릴리스가 `npm test`로 Node ABI 전환 후 패키징하는데 `npmRebuild: false`가 electron-builder의 최종 native rebuild를 차단한 것이 근본 원인. `npmRebuild: true`로 복구해 패키저가 대상 Electron ABI를 최종 보장하고, r1 `asarUnpack`은 유지한다. YAML 설정값·ABI 스크립트 7/7·lint/typecheck 통과, Windows 설치본 실기 대기. 상세=`0114-better-sqlite3-asar-unpack/plan.md` 파생 이슈 r2. |
| `0115-single-instance-lock` | impl | IMPL_DONE | Claude | (push 후) | 1 | 단일 인스턴스 강제 (비기능=Claude 직접 구현, 사용자 결정 2건). 패키징 빌드 한정 `app.requestSingleInstanceLock()`(`app.isPackaged` 게이트 — dev HMR 재시작 경합 제외) + 두 번째 실행 시 `second-instance` 핸들러가 기존 창 복원·포커스(`focusMainWindow`) + `createWindow` 창 참조 보관(`mainWindowRef`, `closed` 항등 가드 해제) + `whenReady` 조기 반환(락 미획득 인스턴스는 Bootstrap/창 미생성). `app/src/main/index.ts` 1파일·신규 의존성 0·IPC 무변경. 게이트 lint ✅(0 error)/typecheck ✅(3/3)/순수 app 테스트 12 passed. 인수 1·2(중복 실행 차단·기존 창 포커스)는 패키징 설치본 2회 실행 사람 실기 대기(egress 차단으로 electron build/실기 불가 — 환경 제약). 상세=`0115-single-instance-lock/plan.md`. |

## 진행 중 작업 메모

> **0165~0167 구현 r2 갱신(2026-08-04)** — 아래 세 행의 긴 비고는 이력을 보존한다. 상태 열은
> 이 주석이 supersede한다: **셋 다 `impl/IMPL_DONE` r2 · 다음 주체 = Claude(재검증).**
> **구현자 = Claude**(환경에 Codex 부재, 사용자 지시). verify r1 의 FAIL 사유를 코드로 닫았다.
>
>
