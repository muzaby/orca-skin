# 디스패치 보드 (Claude Code ↔ Codex)

> **두 에이전트 모두 착수 전 이 표를 가장 먼저 읽고, 작업 후 갱신한다.** "지금 누구 차례인가"의 단일 진실원.
> 상태 머신·절차 정본은 [`AGENTS.md`](AGENTS.md). 완료된 작업은 [`../PHASES.md`](../PHASES.md) 표로 승격된다.

## 단계 / 상태 범례

- **단계**: `plan` → `impl` → `verify`
- **상태**: `DRAFT` · `READY` · `IN_PROGRESS` · `IMPL_DONE` · `PASS` · `FAIL`
- **다음 주체**: `Claude` (설계/검증) · `Codex` (구현) · `—` (종료)

## 활성 / 이력

| slug                            | 단계   | 상태 | 다음 주체 | 대상 커밋 | 라운드 | 비고 |
| ------------------------------- | ------ | ---- | --------- | --------- | ------ | --- |
| `0140-context-budget-diagnostics` | impl | IMPL_DONE | 사용자→Claude | `(pending)` | 1 | **재설계 프로그램 Phase 0/5 (세션 런타임 소유 Context Budget).** 도넛 분모 200k 버그 3연속(0134→0139→현재) → 증분 패치 실패. 사용자 결정: **세션(런타임) 레벨 재설계** + Phase 0~4 전체 + SDK 실측 1순위 + 제목 haiku 격리 포함. 본 Phase 0 = **진단 전용**(동작 변경 0). `[PHASE0-DIAG]` 임시 debug 로그 2곳 — claude.ts events() result 에서 `reqModel(=spawnedModel 후보)·observedMainModel·modelUsage[키].contextWindow` 상관 방출 + chat-turn `resolveTurnProvider` 에서 `oneMillionContext` 방출. 목적: 실 Bedrock `global.anthropic.claude-sonnet-5` 에서 세 문자열(spawnedModel↔message.model↔modelUsage 키)이 실제로 어떻게 달라 승격이 깨지는지(R2 실패지점: 키 형태 불일치/mainModel 오염/미포착) 확정 → Phase 1 리졸버 정규화 규칙 못박기. **사용자 실행 대기**: dev 로그 스위치 ON + 1턴 → 로그 회수 → plan "[검증자 기입]" 표에 실데이터 기록. 게이트 lint 0 error/typecheck 3분할 0/vitest 378/378(adapters+chat lib). 비기능 = Claude 직접. |
| `0139-context-window-multimodel-turn` | verify | PASS* | — | `0b2b84a` | 1 | 검증 PASS* — 인수 **8/8** 기계 충족, 게이트 lint 0 error(1 pre-existing warning = TanStack Virtual 무관)/typecheck 3분할 0/vitest **1104/1104**(신규 claude-map 4 + contextWindow 1; `chat-turn.continuity` 1파일 로드 실패 = electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC/DB 변경 0. 사용자 버그리포트 — Sonnet 5(네이티브 1M)인데 **도구·서브에이전트 턴에서 도넛 컨텍스트 분모가 200k 로 붕괴 + 이후 도구 없는 대화로도 복원 안 됨**. 0134 가 top-level `contextWindow` 승격을 `models.length===1` 게이트에만 걸어 멀티모델 턴(서브에이전트 다른 모델·동시 실행 제목 haiku 로 `result.modelUsage` 다중 키)을 미승격 → renderer `contextWindowFor(undefined)`=200k. `modelUsage` 는 **턴 누적**(claude-map:432)이라 haiku 키 세션 잔류 → **200k 고착**(순수 대화로도 복원 불가). 사용자 실증: available model 에서 haiku 제거 시 소멸. 사용자 결정 **A 한정(메인 모델 승격만)** + 제약 "메인은 세션 중 가변". 수정: `MapContext.mainModel`(턴-스코프 — 마지막 non-child assistant `message.model`) 캡처 + `normalizeResultTelemetry(r, mainModel)` 가 멀티모델 턴에서도 `primary = 단일 ? models[0] : (mainModel∈modelUsage ? mainModel : 미승격)` 으로 이번 턴 메인 모델 window 를 top-level 승격 → 누적/전환 무관하게 분모가 매 턴 메인 추종(sonnet-5→1M, 전환 시 haiku→200k). renderer `contextWindowOf` 무변경. 비범위(사용자 A 선택): B(제목 haiku 오염 벡터 차단 — maybeStart 타이밍/cwd 격리, CLI 집계 추정 의존 → 실기 후 별도)·분자 스냅샷 child 가드·contextWindow DB 영속. 비기능(버그수정) = **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: 도구/서브에이전트 턴 후 도넛 1M·순수 대화 유지·모델 전환 추종·PR 머지. |
| `0137-composer-restore-dedup` | verify | PASS* | — | `4bfc389` | 1 | 검증 PASS* — 인수 **4/4** 기계 충족, 게이트 lint 0 error(1 pre-existing warning 무관)/typecheck 3분할/vitest `chatStore.test.ts` 42/42, 레이어 경계 0, 신규 의존성 0, store/IPC/DB 변경 0. 사용자 버그리포트 — 중단 버튼으로 held 메시지 전량 취소 시 그 텍스트가 composer 로 복원되는데(0067 확정 5), 사용자가 지운 뒤에도 턴 진행(스트리밍/telemetry) 재렌더마다 **취소 텍스트가 textarea 에 재복원**됨. 근본 원인 3중 결합: ① store `draftRestore` 가 소비 후 clear 안 됨(`chatStore.ts:96/126/418`) ② ChatTile 이 매 렌더 새 객체 리터럴 `storeRestore` 로 `restoredDraft` prop churn(`ChatTile.tsx:60-67`) ③ Composer 복원 effect 가 `[restoredDraft]` 객체참조 의존 + **중복적용 가드 부재**(`Composer.tsx:172-181`) → churn 마다 재발화. 수정: 바로 위 `initialDraft` 의 `seededRef`(`Composer.tsx:160-170`)와 대칭인 `restoredRef` id 가드 추가 — `restoredDraft.id`(=store `seq`) 단위로 1회만 적용, 객체 churn 무관하게 재발화 차단. 최소 수정(단일 파일·effect 한 곳) — store 소비-clear 나 ChatTile useMemo 는 범위 확대/미래 churn 미방어라 비채택. 비기능(버그수정) = **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: held 전량 취소 → 1회 복원 → 지운 뒤 턴 진행 시 미재복원 시각 확인·PR 머지. |
| `0136-background-subagent-runtime` | verify | PASS* | — | `046d54e` | 1 | **중기** — 검증 PASS\* — 인수 **10/10** 기계 충족, 게이트 lint 0 error(1 pre-existing warning 무관)/typecheck 3분할/vitest **1099/1099**(신규 22 — claude-map 3·session-runtime listen 6·background-tasks 8·coordinator 5; `chat-turn.continuity` 1파일 로드 실패 = electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC/DB 변경 0. CLI 2.1.198+ "서브에이전트 기본 백그라운드" 의미론의 런타임 수용(0135 와 짝). ① claude-map 이 SDK `tool_use_result` 의 `async_launched` 구조화 영수증(tool_result 블록 단수일 때)을 result 로 매핑 → renderer `isAsyncLaunchedResult` '실행 중' 유지(renderer 무변) ② 턴 종료 후 `unframed` 적체를 **listen 턴**(`TurnRequest.listen` — 입력 push 없는 프레임 소비, `stallTimerFor` 로 stall 미무장)으로 라이브 배달 — settle/persist/relay/usage/title 기존 버스 파이프라인 재사용 ③ busy-send 릴리즈 밸브(`endListenFrame` — draining 없이 unframed 이월) + `BackgroundTaskTracker`(started/settled·콜드 spawn clear·채널 사망 합성 settled=failed 정착). F1(영수증 vs 권위결과 구분) 선조치. 비범위: `BackgroundTasksChanged` 레벨 신호·remote_launched·제품 기본 전환(Open Question). 비기능(버그수정) = **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: 백그라운드 라이브(런치→listen 배달→settled 정착)·F2 inflight 깜빡임 시각·PR 머지. |
| `0135-subagent-foreground-restore` | verify | PASS* | — | `2cd306b` | 1 | **단기** — 검증 PASS\* — 인수 **7/7** 기계 충족, 게이트 lint 0 error/typecheck 3분할/vitest **1099/1099**(canusetool 스위트 포함)+scripts 25/25, 레이어 경계 0, 신규 의존성 0. SDK 0.3.143→0.3.215 점프(CLI 2.1.198 "Subagents now run in the background by default")로 foreground 가정 붕괴 → 도구/서브에이전트 inflight 미표시·완료 오표시·백로그 지연 배달(사용자 버그리포트, 본 세션 검토). `makeCanUseTool` 서브에이전트 분기에서 `ORCA_SUBAGENT_BACKGROUND` off 기본일 때 `run_in_background: false` **명시 주입**(SDK 문서의 공식 opt-out)으로 구 foreground 고정 — 모델 명시값(true/false) 보존, on(=1) 경로 불변, deny 우선 불변. 비기능(버그수정) = **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: 서브에이전트 foreground 실행 라이브·PR 머지. |
| `0134-context-window-from-sdk` | verify | PASS | — | `31f0e3c` | 1 | 검증 PASS — 인수 **9/9** 충족, 게이트 lint 0 error(1 pre-existing warning)/typecheck 3분할/vitest **1077/1077**(`chat-turn.continuity` 1파일 로드 실패 = electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC 채널/DB 무변경(텔레메트리 additive 필드만). 컨텍스트 도넛 분모 버그수정 — `contextWindowFor` 문자열 휴리스틱('1m' 포함=1M, 그 외 200k)이 1M 컨텍스트 현행 모델(Sonnet 5·Sonnet 4.6·Opus 4.6/4.7/4.8)을 전부 200k 로 오인식(도넛 5배 과대·경고 조기 점화). SDK result `modelUsage[].contextWindow`(sdk 0.3.215 `ModelUsage` 실측, 종전 버려짐)를 claude-map 이 추출·단일 모델 top-level 승격 → renderer 분모 단일 진입점 `contextWindowOf`(① top-level ② modelUsage[model] ③ 휴리스틱 폴백)로 도넛/상태 팝오버/UsagePanel 3곳 치환. 폴백 휴리스틱은 1M 패밀리 마커(`sonnet-5`·`sonnet-4-6`·`opus-4-6/7/8`·`fable`·`mythos`·`1m`)로 갱신(재로드 복원 경로 — sonnet-4-5 오탐 없음 테스트 고정), mock 은 `contextWindow: 200k` 명시 패리티. 부수: SDK 스펙 `"latest"`→`"0.3.215"` 정확 핀(lock 동기, `npm ci` 정합 검증 통과 — 0128 타입 드리프트 재발 방지). 검토 파생 — Sonnet 5 비용 상이는 SDK(CLI 2.1.215) 내장 정가($3/$15) 추정 vs 실청구 인트로($2/$10, ~2026-08-31) 차이로 앱 결함 아님 → 비범위(디스클레이머 0122 커버). 비기능(버그수정) = **Claude 직접 plan→impl→verify**. PHASES 승격. **파생 관찰 O1(해소)**: 재생성 lock 신버전 프리티어가 lint `--fix` 마다 무관 파일 union 포맷 리플로우 → 12파일 1회 정리 커밋 `148c3f4`(`Handoff: none` 트리비얼 카브아웃, typecheck 0 error)로 해소. **사람 실기 대기**: 1M 모델 세션 도넛 분모(`xxk/1000k`)·재로드 복원·PR 머지. |
| `0133-object-storage-update-feed` | verify | PASS* | — | `7d8de3e` | 1 | 검증 PASS\* — 인수 **7/7** 기계 충족, 게이트 lint 0 error(1 pre-existing warning 무관)/typecheck 3분할/순수 vitest **20/20**(orca-file+updater-feed; DB/electron 미로드), 레이어 경계 0, 신규 의존성 0, IPC/DB 변경 0. 폐쇄망 자동 업데이트 피드를 오브젝트 스토리지(AWS S3 / MinIO 등 S3-호환)·GitHub Enterprise 로 **코드 수정 없이** 가리키도록 `orca.json` `update` 스키마 확장. electron-updater@6 이 s3 옵션을 런타임 `getS3LikeProviderBaseUrl` 로 generic base URL 로 변환(+`endpoint`=MinIO)·github `host`=GHE 를 네이티브 지원 → **config passthrough**(신규 의존성 0). ① `orca-file.ts`: 신규 `S3UpdateConfigSchema`(bucket 필수+region/endpoint/path) + github `host`/`protocol` optional + Disabled 필드 수용 + union 확장 ② 피드 조립을 electron-free 순수 `app/updater-feed.ts` `resolveUpdateFeed`(5분기·미설정 필드 생략, 0124 분리 선례)로 빼 단위 테스트 ③ `updater.ts` `configureFeed()` 를 위임으로 축약(포트 `setFeedURL?(unknown)` 무변경) ④ 가이드 2건(closed-network-extensions §4·release-operations) s3/MinIO/GHE 예시. 사용자 직접 구현 지시 → **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: MinIO/S3·GHE 라이브 피드 감지·electron 패키징·`npm test` 전체(better-sqlite3 electron ABI egress 차단)·PR 머지. |
| `0132-settings-density` | verify | PASS* | — | `f179ada` | 1 | 검증 PASS\* — 인수 **5/5** 기계 충족, 게이트 lint 0 error/typecheck 3분할/resources 패리티 **3/3**(better-sqlite3 electron ABI egress 차단으로 `npm test` 전체·`dev` 실기는 CI/사람 몫). 디버그 패널 전용(개발 빌드)이던 **밀도(density) 컨트롤을 설정 → 일반 → 환경설정 그룹에 노출**(사용자 요청). 새 상태/스키마/IPC 0 — 기존 `t.density` Tweak(`Tweaks`·`DEFAULTS`·하이드레이션·`SettingsSchema`·`TweakProvider` 루트 font-size 적용 완비) 재사용. `GeneralTab.tsx` 에 `DENSITY_OPTIONS` 상수 + 언어 행 다음 `SettingsRow` select(폰트/언어 패턴 1:1, 조밀/보통/넓게) + i18n `settings.general.density*` 5키(ko/en). DebugPanel density 라디오는 유지(같은 Tweak 이라 자동 동기화). 비기능(기존 배관 UI 노출) = Claude 직접 plan→impl→verify. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: 설정 모달 밀도 select 노출·전역 밀도 전환/영속·디버그 패널 동기화 시각 확인·PR 머지. |
| `0131-simplify-121-130-cleanup` | verify | PASS | — | `d65d1f7` | 1 | 검증 PASS — 인수 **3/3** 충족, 게이트 lint 0 error/typecheck 3분할/vitest **1059/1059**(`chat-turn.continuity` 1파일 로드 실패 = electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC 무변경. `/simplify` 0121~0130 코드 범위(184파일 +5526/−892) 4관점 리뷰 — 병렬 에이전트 4기가 세션 한도로 실패해 **직접 인라인 전수 리뷰로 대체**. 이 묶음이 디자인 통일성·리팩토링·확장점 정비 계열이라 대부분 이미 정리됨(0121 Button/MenuItem/Modal 수렴·0127 continuity 마커 `shared/continuity-lang` 수렴·0122 사장 `formatApproxCost` 삭제 재확인). 적용 dedup **1건(F1)**: 0129 신규 `PinnedSection` 이 `SessionRow` 의 hover-reveal 케밥(⋮) 버튼 마크업(버튼 크롬 + `aria-haspopup`/`aria-expanded` a11y 속성)을 복제 → 공용 `shared/ui/KebabButton`(0121 Button/MenuItem 공용화 결) 로 수렴, group 노출 클래스·aria-label·open·onToggle·ref 만 prop(Tailwind JIT 위해 `group-hover/<name>:grid` 는 호출부 리터럴). 렌더 DOM·클래스·a11y 바이트 불변(동작 보존). 스킵 1건(S1, false positive): `opacity-0 group-hover/*:opacity-100` hover-노출은 서로 다른 요소의 한 줄 유틸이라 공용 버튼화 부적절. 비기능 = **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기**: 사이드바 케밥(최근/고정 프로젝트) hover·메뉴 열림 1회·PR 머지. |
| `0130-closed-network-extension-points` | verify | PASS* | — | `99c2a24` | 1 | 검증 PASS\* — 인수 **10/10** 기계 충족, 게이트 lint 0 error/typecheck 3분할/vitest **1059/1059**(신규 sso 12+레지스트리 2+스키마 4; `chat-turn.continuity` 1파일 로드 실패 = electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0. 폐쇄망(사내) 외부확장 지점 — 커스텀 SSO 로그인 체인 + 정적 사용량 provider. main 은 **불변 확장점 2파일**(기존 `contracts/usage-report.ts` 무변경 + 신설 `contracts/sso.ts`, additive-optional-only·파괴 변경은 v2 파일 신설·런타임 동적 로딩 금지 명문화)과 opt-in 배럴 레지스트리만 제공 — 회사별 구현은 포크 브랜치의 `features/sso/modules/<사>/`·`features/providers/static/modules/<사>/` + 배럴 한 줄(touch-only 4곳, `docs/guides/closed-network-extensions.md`). 사용자 확정 3건: 게이트+토큰 공유(SecretStore `provider:<key>:` 핸드셰이크)·검토+스캐폴딩 구현·명시적 배럴. ① SSO = main 단일 `login(ctx)` 훅(스텝 프레임워크 배제 — rule of three): `SsoContext` 에 fetch/signal·exec(CLI 체인, shell 미경유)·openAuthWindow(격리 `sso` 파티션·preload 없음)·secret(`sso:<key>:`)·providerSecrets(usage 공유)·setProviderEnv(settings.json env 리터럴 병합, `mergeProviderEnv`+`invalidateAll`) ② `SsoService`(timeout race·단일 inflight·throw 격리·stateEvent 브로드캐스트) + IPC 3채널(§2.13-c, 69→72) — 핸들러는 start() 최상단 조기 등록(0109 창 오픈 레이스) + 비-critical `sso-restore` 부트 스텝 ③ RootGate prod 실전화: 모듈 0개 = 현행 자동 통과, 등록 시 게이트 활성(LoginView 가 모듈 선언 `fields` 제네릭 렌더), DEV 게이트+bypass 불변, prod 는 status 실패를 required:false 로 기본화 금지(fail-closed), renderer 스텁 `login/sso.ts` 삭제 ④ secret facade infra 승격(usage re-export 무회귀) ⑤ 외부 에이전트 자족 가이드 — 두 `modules/AGENTS.md`(영어 요약 병기) + 폐쇄망 배포 가이드. 사용자 지시(산출물 범위 확정)로 **Claude 직접 plan→impl→verify**. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: 더미 모듈 prod 빌드 게이트 왕복·모듈 0개 prod 무게이트·DEV bypass 불변·LoginView 필드 시각·PR 머지. |
| `0129-sidebar-pin-title-autosize` | verify | PASS* | — | `e221077` | 1 | 검증 PASS — 인수 **8/8** 충족, 게이트 lint 0 error/typecheck 3분할/vitest **1042/1042**(신규 pin 스키마 5 + DB 왕복 2; `chat-turn.continuity` 1파일 로드 실패 = electron egress 베이스라인), 레이어 경계 0, 신규 의존성 0. 사이드바 "고정됨(pinned)" 섹션 + 대화 제목 인라인 편집 자동 너비(첨부 Claude Desktop UI 재현). 사용자 지시 "핸드오프 문서 작성 및 구현" → **Claude 직접 plan→impl→verify**. 확정 결정 2건(사용자 질의): 고정 영속=**DB 컬럼**(`sessions`/`projects`.`pinned_at`, 마이그 0015)·행 아이콘=**고정됨+최근 항목 모두**(말풍선/폴더 Material). ① 제목 자동 너비 = `RenameInput.autoSize`(CSS `field-sizing:content` + min/max 폭, JS 측정·CSP 무관) → `ChatTitleBar`(flex-1 제거)·`SessionRow` 인라인 편집 가변. ② 고정 데이터 계층 = `pinned_at INTEGER`(NULL/시각=정렬 키) + IPC 2채널(`session/project:setPinned`) + store `setPinned`(refresh) + DTO `pinnedAt`. ③ `PinnedSection`(고정 프로젝트 chevron 접기펼치기 + `useProjectSessions` 지연 조회·고정 대화 행), cross-feature 는 app 셸 props-only 주입(4-layer 경계). ④ 행 아이콘(대화 말풍선·프로젝트 폴더) + 4개 표면 고정 토글(ChatTitleBar 아이콘+kebab·SessionRow kebab·ProjectInfoHero 핀 버튼·ProjectCard hover). 마이그레이션 0015 를 러너+테스트 4곳+`EXPECTED_MIGRATIONS` 5지점 등록. IPC_CONTRACT 69채널 동기. PHASES 승격. **사람 실기 대기(PASS\* 이유)**: 제목 가변 폭·고정됨 접기펼치기·4개 표면 토글·행 아이콘 시각 확인·PR 머지. |
| `0128-same-provider-model-respawn` | verify | PASS* | — | `65d5303` | 1 | 검증 PASS(기계) — 인수 **6/6** 충족, 게이트 lint 0 error·`session-runtime` 테스트 **22/22**(+3 신규)·변경 파일 typecheck 0, 레이어 경계 0, 신규 의존성 0, IPC/DB 변경 0. 사용자 보고("모델 sonnet→haiku 변경 후 답변은 haiku 인데 로그는 sonnet") — **진단으로 최초 가설(로그 오기록) 반증**: 진단 로그가 "haiku 전환" 턴의 `message.model=claude-sonnet-5`(실제 생성=sonnet)·비용이 **sonnet 만 증가**(haiku 불변)임을 확증 → 로그는 진실이었고 진짜 결함은 **같은 provider 모델 변경이 실제 생성에 안 먹음**(라이브 `/model`/setModel 이 이미 스폰된 서브프로세스 생성 모델을 못 바꿈; fresh spawn 만 `options.model` 준수). 사용자 결정으로 **respawn 접근** 채택 — 최초 claude-map 정정/재귀속/진단은 전량 revert. 수정: `SessionRuntime.spawnedModel` 기록(0125 `spawnedProviderSettings` 수명 패턴 복제) + `chat-turn` **메인 턴** respawn 조건에 `resolved.model !== spawnedModel` 추가(0118/0125 동형) → 모델 변경 턴이 콜드 spawn(resume)으로 새 model 생성. **연속 턴(0067 AC7)은 무변경**(원 턴 model 계승, 0119/0126). 비기능(버그수정) = Claude plan→impl→verify 전담. **baseline 분리**: `typecheck:node` `claude.ts:465 TS2322`(SDK `interrupt()` 타입 드리프트, `package-lock.json` 세션 시작 시 `M`) = 본 변경 무관(diff 0). **사람 실기 대기(PASS\* 이유)**: 모델 변경 턴에 `engine.spawn.started` 출현 + 생성/비용/`chat.turn.completed` model=haiku 정합·PR 머지. |
| `0127-continuity-donut-lang` | verify | PASS | — | `8aaa0c8` | 1 | 검증 PASS — 인수 **9/9** 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **1032/1032**(신규 24 — Node ABI 소스 리빌드, `chat-turn.continuity` 1파일 로드 실패는 electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0. 사용자 보고 2건 수정 — ① **핸드오프 분기 도넛/경고 비승계**: 도착 턴 telemetry 가 압축 경계 전의 승계 컨텍스트(원본 전체 이력)를 실어 도넛/경고·turn_usage 원장·재로드 복원이 원본 값을 물려받던 누수를 매퍼 단일 지점(`TurnRequest.handoff`→`MapContext.handoffArrival` — 경계 전 스냅샷 미캡처 + 경계 없는 result 컨텍스트 3종 제거, 연속 턴은 forkFrom 동렬 delete)에서 차단. 경계 통과 후 압축-후 근사(0065)·fork 의 `lastTelemetry` 승계는 유지 ② **continuity 문구 선호 언어 스냅샷**: `/compact` 자동 메시지·제목 마커(`[분기]/[핸드오프]`)를 발생(draft 생성) 시점 `settings.language` 파생 ko/en 2종(사용자 결정 A/B — uiLocale i18n 비대상 유지)으로 조립. 신규 `shared/continuity-lang.ts` 단일 조립점이 renderer draft·main initialTitle 이중 하드코딩을 수렴하고, payload `continuityLang` 스냅샷(부재 시 main settings 파생 폴백)이 draft↔물질화 문자열 일치를 보장. en 템플릿은 ko 동일 구조 + `Write the summary in {선호 언어}.` IPC_CONTRACT §2.1·provider-runtime §8 동기. 비기능(버그수정) = Claude plan→impl→verify 전담. PHASES 승격. 사람 확인 대기: 실기(핸드오프 직후 도넛 미측정→압축 후 근사·en 제목/템플릿·재로드 미복원)·PR 머지. |
| `0126-continuation-settings-recheck` | verify | PASS | — | `9abeadd` | 1 | 검증 PASS — 인수 **6/6** 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **1009/1009**(`chat-turn.continuity` 1파일 로드 실패는 electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC/DB/렌더러 변경 0. steer/queue × provider 경계 검토(사용자 요청) 파생 갭 2건 수정 — ① 자동 연속 턴(0067 AC7)이 `{...request}` 로 stale settings 를 재사용(채널-사망 분기는 낡은 blob 으로 respawn): 반복마다 원 턴 키 고정 재해석 + 0125 판정 재실행 → teardown/신선 blob 주입 ② busy send 경계 가드가 렌더러(0119) 전용: `reserveOnBusySession` 에 `crossesProviderBoundary` 백스톱(거부·held 미적재, null 보수적 허용). 의미론 불변(연속 턴 provider/model = 원 턴 계승 — "선택은 다음 사용자 send 부터"). 비기능(버그수정) = Claude plan→impl→verify 전담. PHASES 승격. 사람 확인 대기: 실기(busy 중 토큰 로테이션+steer → 연속 턴 새 토큰 respawn)·검토 §비고 I-4 의미론 판단·PR 머지. |
| `0125-settings-change-respawn` | verify | PASS | — | `899604b` | 1 | 검증 PASS — 인수 **7/7** 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **1009/1009**(신규 7 — Node ABI 소스 리빌드, `chat-turn.continuity` 1파일 로드 실패는 electron egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC/DB/렌더러 변경 0. 세션 유지 중 **같은 provider 의 settings.json 제자리 수정(토큰 로테이션·base URL 교체)이 살아있는 채널에 미반영**되던 갭 수정(버그리포트 "api base url/auth token 동적 변경 검토" 파생) — `SessionRuntime` 이 spawn 주입본을 불투명 기록(`spawnedProviderSettings`, 0016 중립) + 순수 판정 `providerSettingsChangedSinceSpawn`(null 보수적 false·동일 참조 fast-path·내용 비교) + chat-turn respawn 조건 `crosses ‖ settingsChanged` 확장(0118 teardown·이월 배선 재사용). busy 중 수정·orca.json 전역 env·fs.watch 비범위. 비기능(버그수정) = Claude plan→impl→verify 전담. PHASES 승격. 사람 확인 대기: 실기(세션 중 settings.json 토큰 수정 → 다음 send respawn·새 env 적용, provider 왕복 반영)·PR 머지. |
| `0124-log-wiring` | verify | PASS* | — | `2f56498` | 1 | **검증 PASS\*(r1) — 인수 12/12 기계 충족(AC1 JSONL 샘플·AC12 스위치 실기만 electron egress 제약으로 사람/CI 대기), 게이트 lint 0 error(no-console 포함)·typecheck 3종 0·vitest 1002/1002(+신규 wire-log 5·handle 4·미러 게이트 2; `chat-turn.continuity` 1파일 로드 실패 = electron 바이너리 egress 베이스라인)+scripts 25/25, 레이어 경계 0, 신규 의존성 0, IPC 채널 67 불변.** 로그 배선 — prod info 카탈로그 배선(app/boot/db/session/chat/engine/update/scheduler/extensions/settings/ipc, 발화 근거 `파일:라인` = plan 구현 보고) + `console.*` **35곳/20파일(0123 후 재실측 — 구 62/30 은 stale)** 전면 이관(이관 표 첨부, 예외=`infra/log/index.ts` 2곳) + eslint `no-console: error`(`infra/log/**` override 한정) + 턴 `runWithLogContext` correlationId 배선. **구현 전 수석 엔지니어 비판적 검토(사용자 지시)로 plan 개정(`7b174d2`)**: 사용자 결정 3건 — ① 디버그 패널 wire 스위치 → **"로그" 스위치 통합 게이트**(`DebugMockState.log` 개명, ON=wire 기록+콘솔 미러 / OFF=콘솔 침묵, 0123 무조건 dev 미러 대체·기본 OFF·비영속) ② **델타 2종 전 경로 배제(구 콘솔 wireLog 덤프 포함)** — wireLog 의 console.log 제거·주입 sink 경유 `ipc.wire.event` debug 로 흡수(AC3 eslint 모순 동시 해소) ③ wire 레코드 payload 전체(dev 전용·스위치 게이트·redaction 통과). getLogger 는 electron-free `infra/log/registry.ts` 로 분리(순수 vitest 보존, 0068 동형). Claude 전담 plan→impl→verify. 상세=`0124-log-wiring/{plan,verify}.md`. 사람 확인 대기: dev 실기(로그 스위치 토글→콘솔 미러·`ipc.wire.event`·델타 미기록·JSONL 샘플)·PR 머지. |
| `0123-logging-system` | verify | PASS | — | `cefb1ec` | 1 | 검증 PASS — 인수 **14/14** 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **991/991**(신규 48 — Node ABI 소스 리빌드로 DB 스위트 포함 전체 green)+scripts 25/25, 레이어 경계 0, 신규 의존성 0. **사용자 지시: "클로드가 모두 진행" — Claude 가 plan→impl→verify 전담.** 사람 확인 대기: electron 실기(JSONL 실파일·크래시 flush·prod dead-code 빌드)·PR 머지. 로그 시스템 구현 — 중앙 LogManager(`infra/log/`, db 싱글턴 패턴)·JSONL `<userData>/logs/`(10MB×5 로테이션)·중앙 redaction(key+값 패턴)·`serializeError`·session/correlationId(AsyncLocalStorage)·IPC 브리지(`orca:log:emit` one-way send + zod, main 이 공통 필드 강제 부여)·전역 장애 훅(uncaughtException/render-process-gone 등 + 종료 flush)·dev(debug+콘솔 미러)/prod(info 파일만) 정책·반복 억제(fingerprint 60s). 신규 의존성 0(electron-log 미도입). 비범위: 원격 전송(PRD §11 OQ4)·crashReporter·Support Bundle·audit 분리(Future)·call site 이관(→0124). 문서: IPC_CONTRACT log 도메인 + `arch/backend/observability.md` 신설. |
| `0122-composer-status-popover-refresh` | verify | PASS | — | `986fdaf` | 2 | 검증 PASS(r2) — 인수 r1 9/9 + r2 6/6 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **943/943**+scripts 25/25(electron 1스위트 로드 실패는 egress 403 환경 베이스라인), 레이어 경계 0, 신규 의존성 0. **r2(사용자 피드백)**: 비용 행을 "오늘 비용" 이 아닌 **"이 세션에서 사용한 비용"(세션 한정 총합)** 으로 복구 — 비용 SSOT 인 `turn_usage` 원장의 세션 SUM(`sumSessionCostUsd`)을 `LoadedSession.costUsd` 로 시드(IPC payload 확장, 채널 수 불변·IPC_CONTRACT 동기) + reducer `sessionCostUsd` 가 라이브 `telemetry.costUsd` 누산(fork/handoff 미승계·압축 유지), 하단 "표시된 비용은 예상치" 디스클레이머 복구(`costDisclaimer`). r1: 대화 길이 상태 팝오버 개편(사용자 6건, 렌더러 전용): 대화 길이 행 수치화(`138k/200k 69%` — 도넛과 동일 `contextTokens`/`contextWindowFor` 파생, view model `contextUsage` + i18n `lengthValue`) · 오늘 사용량/비용 행·디스클레이머 제거(카피 키 `usage`/`disclaimer`/`usageTodayLabel`/`costTodayLabel` 폐기 + 유일 소비자를 잃은 `costToday` 프롭 체인(pages 3곳→ChatTile/ChatView→Composer)·`formatApproxCost`·`cost.approx` 동반 제거) · pill 점에 경고등 펄스 `animate-status-beacon`(currentColor 로 warn/danger 톤 자동, 키프레임이 정적 ring halo 를 첫 레이어로 유지, reduced-motion 폴백) · 상태 팝오버 `align="center"` + 공용 Popover 에 open 동안 resize 재계산(전 팝오버 공통 개선). 사용자 직접 지시 라운드 — Claude 직접 plan→impl→verify. PHASES 승격. 사람 확인 대기: 시각 실기(펄스·수치 행·중앙정렬·리사이즈 추종)·PR 머지. |
| `0121-design-consistency-audit` | verify | PASS | — | `f667ff0` | 5 | 검증 PASS — 인수 **11/11** 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **934/934**+scripts 25/25(electron 1스위트 로드 실패는 egress 403 환경 베이스라인), 레이어 경계 0, 신규 의존성 0, IPC/main 무변경. 디자인 통일성 감사(버튼·모달 blur·i18n·폰트, 사용자 결정 5건) — 비기능 = Claude 직접 plan→impl→verify: backdrop 단일 소스 `MODAL_BACKDROP_CLASS`+전 모달 blur 통일(dead `blurBackdrop` 제거), **UpdateDialog 이중 backdrop 버그 해소**+공용 Modal 이관, EngineFormModal 흡수(r6/shadow-xl/serif 18px), Installer/Auth ESC·바깥클릭+busy 가드(`useEscToClose` 공용화), 신규 `shared/ui/MenuItem`+Button `danger` variant 로 그룹 A~E 치환(raw `<button` 115→71·`<Button>` 35→67+MenuItem 14, 잔존=특수 정당화 목록), focus 링 3체계→`ring-focus` 표준, i18n 우회 12곳+영어 aria 5곳 ko/en 이관, dom-architecture §1.2/§1.5 를 2원 마운트 구조로 개정(코드 승격 — 사용자 결정). 후속 제안 문서화: 타이포 전면 토큰화(arbitrary px 305곳)·main `{raw}`→`{key}` i18n(+parts.ts '중단' 커플링). PHASES 승격. **r2(사용자 피드백)**: 설정 모달 × 상단 중앙 노출(r1 회귀 — Button `relative`↔`absolute` className 충돌) 신고 + "일반 모달에 × 불필요" 지시 → 모달 크롬 × 전면 제거(Modal 크롬·SettingsModal·UpdateDialog, 닫기=Esc·백드롭·footer 버튼), dom-architecture §1.5 명문화. **r3(사용자 피드백 4건)**: 스킬 그룹 라벨 i18n(sourceId 키+카탈로그)·돋보기/browse 제거·add engine 버튼 primary 통일·EngineCard 삭제 `danger-ghost`(채움 없음, 신규 variant). **r4**: composer 모델/작업량 메뉴 항목 공용 MenuItem(`align="start"`) 수렴 + 동일 상수 공유하던 권한모드 메뉴 동반 수렴, `composer/menuItem.ts` 예외 삭제. **r5**: `danger-ghost` 에 contained 동일 테두리(border-t5) — 엔진 편집/삭제 짝 통일. 사람 확인 대기: 시각 실기(blur 톤·치환 버튼·닫기 UX·en 라벨·모달 × 부재·스킬/엔진 r3·composer 메뉴 r4)·PR 머지. |
| `0120-simplify-107-119-cleanup` | verify | PASS | — | `184230e` | 1 | 검증 PASS — 인수 16/16 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **934/934**+scripts 25/25(electron 1스위트 로드 실패는 egress 403 환경 베이스라인), 레이어 경계 0, 신규 의존성 0, IPC/타입 표면/동작 무변경. `/simplify` 0107~0119 코드 범위(91파일) 4관점(재사용·단순화·효율·altitude) 리뷰 dedup 15건 적용: 불변식 단일 소유화(writer `finalizeTurn` 수렴·reducer `TURN_END_RESET`·recovery `recoverSessionHistory` 복합 진입점·플러그인 경로 feature 헬퍼 SSOT) + 0109 잔여 직렬 fs 서로소 병렬화(deployer/plugin-package/seed) + 파서 재사용(`parseDayKey` export·`aggregateWeekly`→clock `boundaries`) + 렌더 파생 useMemo(UsageTab/차트) + 죽은 `Tweaks.scheduler` 배관·`lastFetchOk` boolean Map→Set·user-skills 액션 문구 단일화·마지막 청크 뒤 setImmediate 제거. 스킵 3건 기록(themeStore=0108 재렌더 정밀도 의도·tick 라벨 사전계산=이득 미미·engine CRUD 배포 우회=동작 변경이라 **파생 이슈 D1** 후속 핸드오프 권고). 비기능 = Claude 직접 plan→impl→verify. PHASES 승격. 사람 확인 대기: 사용량 탭 시각 1회·PR 머지. |
| `0119-busy-steer-provider-gate` | verify | PASS | — | `6332ba1` | 1 | 검증 PASS — 인수 7/7 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **934/934**(+13 — electron 1스위트 로드 실패는 egress 403 환경 베이스라인), 레이어 경계 0, 신규 의존성 0, IPC/main/DB 변경 0. 0118 후속 UX 가드(렌더러 전용): busy 세션에서 provider 경계를 넘는 모델이 선택된 동안 steer 차단 — `BEGIN_TURN` 이 턴 provider 를 스냅샷(`turnProviderKey`, 종료 4경로 초기화·SET_MODEL 불변·자동 연속 턴도 같은 액션 재사용) + 순수 판정 `steerBlockedByProviderBoundary`(`features/chat/lib/steerGate.ts`, null 보수적 허용) → Composer placeholder 를 신규 `placeholderProviderBoundary`(사용자 선택 문구) 로 교체·`feedbackMode` 차단(중단 버튼 유지)·Enter 가드 + store `send()` 이중 방어(pendingSteer 미적재·IPC 미호출). 본래 provider 재선택/턴 종료 시 자동 복구. 비기능(0118 후속) = Claude 직접 구현, PR #261 합류. PHASES 승격. 사람 확인 대기: busy 중 차단 UI·원복 복구 실기 (verify §책임 분리). |
| `0118-provider-boundary-respawn` | verify | PASS | — | `b929d9e` | 1 | 검증 PASS — 인수 7/7 충족, 게이트 lint 에러 0/typecheck 3분할/vitest **921/921**(+scripts — electron 1스위트 로드 실패는 egress 403 환경 베이스라인), 레이어 경계 0, 신규 의존성 0, IPC/렌더러/DB 변경 0. provider 경계를 넘는 모델 변경 시 살아있는 세션 채널의 env 가 낡아 채팅이 끊기는 버그(0067 persistent 채널이 0010 "매 턴 env 주입" 가정을 무효화) 수정: 순수 판정 `crossesProviderBoundary`(`features/providers/provider-boundary.ts`, 전체 키 비교·null 보수적 false) + chat-turn 이 acquireRuntime 직후 경계+채널생존 시 `teardownChannel()`(public 승격) → 그 턴이 spawn(resume) 콜드 패스로 새 env/providerSettings 주입, `takeForRespawn` 이월은 기존 경로 그대로. busy 세션(held 예약) 중 provider 변경은 비범위 문서화. 비기능(버그수정) = Claude 직접 구현. PHASES 승격. 사람 확인 대기: 실기 provider A→B 전환(응답 provider 변경 + resume 연속성) (verify §책임 분리). |
| `0117-claude-settingsources-project-local` | verify | PASS | — | `34804a6` | 1 | 검증 PASS(구현 범위) — 인수 **8/8 기계 충족 + 2 사람 실기 대기(AC#5 provider settings 실환경·AC#8 wire log `claude:*` 노출)**, 게이트 lint 에러 0/typecheck 3분할/test **915/915**(+scripts 25 — electron 1스위트 로드 실패는 egress 403 환경 베이스라인), 레이어 경계 0, 신규 의존성 0. claude query `settingSources: ['project','local']` 명시(user 배제 → provider settings 결정론 적용, 양쪽 call site) + `~/.claude/skills` 는 deploy 가 렌더하는 `dist/claude/plugins/claude/` **래퍼 플러그인**(매니페스트 + `skills` 정션/심링크, plan r2 — r1 `~/.claude` 직주입 폐기)으로 보전 + 어댑터 스킬 `claude:` 네임스페이스 + `adaptPlugins` 복수 root 화 + backup/rm 링크 대상 보존 테스트 고정. 0023/0028 "settingSources 생략" supersede. 사용자 지시로 Claude 가 plan→impl→verify 전담. PHASES 승격. 사람 확인 대기: provider settings 실기·wire log 스킬 노출·배포 빌드(asar)·Windows 정션 실기 (verify §책임 분리) — 미인식 시 복사 폴백(가이드 §6) 후속. |
| `0116-release-0-3-1-auto-publish` | impl | IMPL_DONE | Claude | `882b021` | 1 | v0.3.1 버전 동기화 + 태그 릴리스 자동 Publish로 updater 피드 가시화. 검증 대기. |
| `0001-handoff-bootstrap`        | verify | PASS | —         | 78f1601   | 1      | 협업 인프라 자체 부트스트랩. Claude 단독 수행(설계+구현+검증).                                                                                                                                                                                                                                                                                                         |
| `0002-cost-token-tracking`      | verify | PASS | —         | 999c99b   | 1      | 비용·토큰 추적. 14/14 충족, 게이트 260/260. 스키마 통일(`usage_events`→`turn_usage`+`turn_model_usage`) + 모델별 영속 + 일/주/월 누적(main 싱글턴 + renderer 미러). PHASES 승격. (대상 커밋 INDEX 기재 `4213cad`→실 `999c99b`, verify 위생 노트 ①)                                                                                                                     |
| `0003-debug-panel-mock-adapter` | verify | PASS | —         | 5ef793c   | 1      | 검증 PASS — 인수 18/18 충족, 게이트 4종(lint/typecheck/test 273/build) 통과, prod dead-code 제거·레이어 경계 0 확인. IPC_CONTRACT(§2.13 debug 도메인 신설·총 37·§3 permission.resolved 발행 주체) + layers(features/debug·FloatingPanel) 갱신 완료. PHASES 승격. (대상 커밋 INDEX 기재 `78f1601`→실 `5ef793c`, verify 위생 노트 ①)                                     |
| `0004-auto-session-title`       | verify | PASS | —         | ba36b8d   | 1      | 검증 PASS — 인수 9/9 충족, 게이트 3종(lint/typecheck/test 283) 통과, 레이어 경계 0, 신규 의존성 0. Haiku 1-shot 제목 요약 + `title_source`(0007) rename 보호 + `SessionAdapter.complete` seam + `sessionTitleEvent` 전-창 push. IPC_CONTRACT 37→38. PHASES 승격. (대상 커밋 INDEX 기재 `9c4f1a1`→실 `ba36b8d`, verify 위생 노트 ①) 사용자 피드백 3건은 `0005` 로 후속. |
| `0005-title-completion-fixes`   | verify | PASS | —         | c13bd44   | 1      | 0004 사용자 피드백 반영(모델 별칭 `'haiku'` · `settingSources` 제거 · `complete` 호출 경로 정리). 인수 6/6, 게이트 3종(283 tests) 통과. **비기능 작업 = Claude 구현** 규칙 첫 적용. 사람 확인 대기: 실환경 haiku 사용·settings env 적용 (verify §책임 분리).                                                                                                           |
| `0006-composer-status-line`     | verify | PASS | —         | 393c8c8   | 1      | 검증 PASS — 인수 10/10 충족, 게이트 3종(lint/typecheck/test 286) 통과, 레이어 경계 0(cost 는 page 주입), 신규 의존성 0, 카피 표 1:1. Tier1 pill + Tier2 팝오버 presentational 셸 + view-model 순수함수 + 임시 상태근사 TODO. PHASES 승격. (대상 커밋 INDEX 기재 `b94cc76`→실 `393c8c8`, verify 위생 노트 ①) 사람 확인 대기: 시각 검증·카피 어감 (verify §책임 분리).   |

| `0007-transcript-render-memo` | verify | PASS | — | a68e465 | 1 | 스트리밍 재렌더 범위 축소(성능) — 비기능 = Claude 직접 구현. `groupTurns` useMemo + 턴/메시지/마크다운 memo + `useSessionHandlers` deps 안정화(Sidebar memo 복원) + Header memo. CDP rAF 측정(35턴 mock): 최대 블로킹 549~614ms → 133~166ms. 게이트 lint/typecheck ✅, test 283/290 — 실패 7건은 better-sqlite3 ABI 환경(변경 무관, verify §게이트). PHASES 승격. 사람 확인 대기: 스트리밍 스크롤 시각 검증 (verify §책임 분리). |
| `0008-chat-anchor-reserve` | verify | PASS | — | e113eb4 | 1 | 검증 PASS — 인수 12/12, 게이트 4종(lint/typecheck/test 315/electron-vite build) 통과, 레이어 경계 0, 신규 의존성 `zustand@^5`(사용자 승인). 스크롤 앵커링 CSS 예약공간 전환(Exchange + `min-h-[50cqh]`, 50% 미드라인 유지·여백은 다음 메시지까지 유지 — 사용자 결정) + Zustand chat store 선행 도입(델타→live, state.md §1.4 개정) + 커밋 경로 카드 격리(`reconcileSegments`) + `StreamingMarkdown` 꼬리 재파스. PHASES 승격. 사람 확인 대기: 시각 검증·재렌더 실측·cqh 실기 (verify §책임 분리). |
| `0009-orca-config` | verify | PASS | — | 1deae14 | 1 | 검증 PASS — 인수 14/14 충족, 게이트 lint/typecheck PASS·범위 테스트 26/26, 레이어 경계 0(SDK 어휘 `CLAUDE_CODE_USE_*` 는 `adapters/claude-env.ts` 격리), 신규 의존성 0, IPC/preload/renderer/`shared` 변경 0. `~/.config/orca/orca.json` 템플릿 생성·3단 관용 파싱·main 싱글톤(`load/get/agentFor`) + claude env 매핑/합성(sendMessage·complete) + `${VAR}` 공용 확장(미해결 키만 드롭) + 문서 4건(TRD §6.8/standardization §5.1/security 예외/GLOSSARY). 전체 test 328/335 — 실패 7건은 better-sqlite3 ABI 환경 제한(`db/queries.test.ts`, 변경 무관, 0007 동일 계열). PHASES 승격. 사람 확인 대기: 실환경 bedrock/vertex·평문 apiKey 주입 (verify §책임 분리). |
| `0010-agent-model-select` | verify | PASS | — | c193166 | 2 | 검증 PASS(r2) — 인수 **15/15** 충족, 게이트 lint/typecheck/test **349/349 (48파일)** 통과(Node ABI 재빌드로 `db/queries.test.ts` 포함 전체 green), 레이어 경계 0, 신규 의존성 0. r1 FAIL 사유(공유 헬퍼 `insertSession` 오염→`0006_turn_usage` 마이그레이션 테스트 회귀)를 r2 가 0005 스키마용 인라인 INSERT 로 해소(`queries.test.ts` 1파일·프로덕션 무변경). `${adapter}-${provider}` 합성 키 + `orca:agent:list`(도메인 `agent`, 38→39) + Composer 모델 UI(턴 단위·라이브 채널 없음) + 세션 adapter 잠금·provider 턴 전환·`provider_key`(마지막 사용) 영속·secret store 토큰(DB 비밀 0) + `authToken` 리네임 + `/agents` 동적화 + mock 빗금 규약 + 문서 6건. PHASES 승격. (대상 커밋 INDEX 기재 r1 `c2a90d8`→실 `0a3c043`, r2 `ecf9752`→실 `c193166`, 위생 노트 ①) 사람 확인 대기: 시각 검증·실환경 bedrock/secret store 토큰 (verify §책임 분리). |

| `0011-main-decompose` | verify | PASS | — | 5e835f7 | 1 | 단계적 아키텍처 리팩토링 스테이지 1/3 (백엔드). router.ts 1,070→139줄 분해(handlers/ + chat/) + TurnRegistry sessionId 키잉(멀티세션 토대 — 동시 세션 턴 허용) + getSessionById(N+1 제거) + closeDb 와이어 + config/crypto 이동 + 레거시 1회성 이전 제거(사용자 결정). 게이트 4종 ✅ test 351/351. 비기능 = Claude 직접 구현. 사람 확인 대기: GUI 회귀(채팅·승인·취소). |

| `0012-ipc-cleanup` | verify | PASS | — | 03cc1f5 | 1 | 단계적 아키텍처 리팩토링 스테이지 2/3 (IPC). runtime 고아 3채널 제거(사용자 결정 — 채널 39→36, RuntimeStatus 타입 main 이동) + `ipc/registry.ts` handle 헬퍼로 전 invoke 채널 safeParse 단일 경로(실패 정책 'reject'/{fallback} 등록부 명시 — 동작 변경 0) + IPC_CONTRACT 전면 동기화(agent §2.2-b 편입·§4 ErrorCategory 정정). 게이트 4종 ✅ 351/351. 비기능 = Claude 직접 구현. |

| `0013-renderer-multisession-store` | verify | PASS | — | bce274f | 1 | 단계적 아키텍처 리팩토링 스테이지 3/3 (프론트엔드). chatStore 멀티세션 외피(sessions Record + activeKey + NEW_CHAT_KEY 승격 — main TurnRegistry 대칭, 사용자 결정) + ev.sessionId 키 라우팅(비활성 세션 백그라운드 누적·활성 UI 격리) + sessionCache/LOAD_SESSION_FROM_CACHE 폐기(Record 가 캐시 흡수) + Backend/Sessions/Projects/Cost Context 4종 → Zustand store(bootstrap-only Provider). reducer 무변경(키 라우팅은 store — state.md §1.4 개정). 게이트 4종 ✅ 354/354. 비기능 = Claude 직접 구현. 사람 확인 대기: GUI 시각 회귀 + 멀티세션 실기. |

| `0014-provider-settings-dist` | verify | PASS | — | `9585cb7` | 1 | provider settings dist 재구조화 (비기능 = Claude 직접 구현, 사용자 결정 4건 반영). `sources/settings/<adapter>/<provider>/settings.json`(어댑터-네이티브 SSOT) + 어댑터당 meta.json → deployer 가 `dist/<engine>/plugin/`(공유 플러그인) + `<provider>/.claude/settings.json` 렌더 → 런타임은 SDK `resolveSettings`(@alpha, flat 폴백) + `filterEscalatingDefaultMode` + `${VAR}`/secret 주입 → `query({settings, settingSources: []})` **격리모드**(0005 결정 폐기). orca.json `agents` 제거(`{version, env?}` — 클린 브레이크) + `toClaudeEnv` 삭제(TRD 레시피 표 대체) + agent:list 원천 교체(페이로드 shape 0010 유지, renderer 변경 0) + 로더 주입 seam(opencode). 게이트 4종 ✅ 372/372 (+28). 사람 확인 대기: **OAuth 격리모드 실기(1순위)** · bedrock 실환경 · ModelMenu 회귀 · 기존 ~/.claude env 의존 사용자 이전 안내. |

| `0015-settings-flag-string` | verify | PASS | — | `210f0ac` | 1 | 0014 후속 버그수정 (비기능 = Claude 직접 구현). SDK `Options.settings` 는 d.ts 와 달리 런타임 transport 가 **문자열만** 지원 (0.3.143~0.3.175 동일 — argv 에 무직렬화 push, 객체는 `"[object Object]"` 로 강제 변환되어 0014 의 settings 가 적용된 적 없음=조용한 실패). 수정: 로더 반환을 `{settings, env}` 로 분리 → `adaptSettings` 는 **인라인 JSON 문자열** 주입(env 제외 — argv 평문 비밀 방지) + 신규 `adaptEnv` 가 provider env 를 subprocess env 로 병합. 읽기 경로·dist 레이아웃 0014 유지, renderer/IPC 변경 0. 게이트 4종 ✅ 375/375 (+3). 사람 확인 대기: **실기 턴 1회 settings/env 적용 확인(1순위)** · OAuth 격리모드 · bedrock 실환경. |

| `0016-provider-neutral-core` | verify | PASS | — | `99469df` | 1 | 검증 PASS — 인수 8/8(+4-bis) 충족, 게이트 4종(lint/typecheck/test 375/build) 통과, 레이어 경계 0, 신규 의존성 0. `NormalizedEvent` 11 variant 전부 `provider` 제거(write-only·session.backend 중복=이중 진실원 A2). `send.ts`(classify=adapter.id·세션-이전 에러 provider 부재)/`persist.ts`(insertSession.backend=titleAdapter.id)/`chatStore.ts`(lastBackend+OQ7 제거) 리터럴 제거 + `ClassifyContext.provider`·`ClassifiedError.provider` optional + `agentPermissionRequest` 시그니처. IPC_CONTRACT §3·§4 동기화. **기준 6 carve-out(사용자 결정 "코어/오케스트레이터만")**: 렌더러 기본-백엔드 UI(backendStore·Composer·InstallerDialog·chatReducer.backend·Avatar)는 '기본 백엔드' Open Question 소관 후속. 구현 주체=Claude(비기능). PHASES 승격. 사람 확인 대기: 채팅 GUI 회귀(에러/권한 카드·backend 표기). |

| `0017-main-layer-boundaries` | verify | PASS | — | `c26419d` | 1 | 검증 PASS — 인수 6/6 충족, 게이트 4종(lint/typecheck/test 375/build) 통과. `main` 레이어 DAG(L0 shared→L1 domain→L2 adapters→L3 ipc) `app/src/main/AGENTS.md` 신설 + `eslint-plugin-boundaries` 를 `src/main/**`·`src/shared/**` 로 확장(하향만·상위참조 error) + `import/no-cycle`(import/parsers 로 .ts 추적, 임의 순환 주입 검증 후 원복) — 기존 순환 0. D2 분해: `provider-settings.ts` → `provider-registry`·`model-resolve`·`env-merge` + 배럴 re-export(12 importer 무회귀). `installer`=L2 분류로 유일 상위참조 해소(코드 이동 0). 신규 devDep `eslint-plugin-import ^2.32.0`(사용자 승인). 비기능=Claude 직접 구현. PHASES 승격. |

| `0018-settings-branded-types` | verify | PASS | — | `37e95e1` | 1 | 검증 PASS — 인수 7/7 충족, 게이트 4종(lint/typecheck/test 377/build) 통과. 0015 "비밀↛argv" 불변식을 `delete settings.env` 관례 → **컴파일타임 branded 타입**으로 격상: `ArgvSafeSettings`(env 제거)·`SubprocessEnv` phantom unique symbol + 분리·브랜딩은 단일 생성자 `splitProviderSettings` 1곳(as 격리). `adaptSettings`=ArgvSafe만·`adaptEnv`=SubprocessEnv만 수용 → **env 머금은 객체→argv 컴파일 에러**. `@ts-expect-error` 음성 타입 테스트 2건을 신규 `typecheck:test`(main 테스트 타입체크, 빌드 emit 무영향) 게이트로 고정. `delete settings.env` ad-hoc 제거. 런타임 동작 0015 와 동일(런타임 속성 0). 문서 3건(security/standardization/TRD). 비기능=Claude 직접 구현. **구조 견고화 3-part 시리즈(0016·0017·0018) 완료.** PHASES 승격. 사람 확인 대기: 실환경 턴 1회 settings/env 적용. |

| `0020-composer-cc-layout` | verify | PASS | — | 08c95fe | 1 | 검증 PASS — 인수 9/9 충족, 게이트 4종(lint/typecheck/test 381/build) 통과(better-sqlite3 Node ABI 재빌드 후 전체 green — red 9건은 0019 dual-ABI 클래스, 본 변경 무관), 레이어 경계 0, 신규 의존성 0. Composer 를 CC 레이아웃으로 재배치 — 전송버튼 입력행 inline(`flex items-end`·원형 `rounded-full`) · footer 우측 `모델·작업량·도넛` · 좌측 `권한모드+"+"`(첨부/현재프레임/Skill 은 `AttachMenu` 로). **작업량(effort) 풀스택 배선**: SDK `Options.effort`(low~max) 를 `model` 흐름 미러로 store→`SendChatMessageSchema`→`TurnRequest.effort`→`claude.ts` query options 전달(per-turn, 기본 high·`EffortLevel`=L0 shared). 신규 `EffortMenu`·`AttachMenu`·`effort.ts` + 테스트 3종. autogrow `max-h-40`→`max-h-56`. IPC_CONTRACT chat send payload 갱신(채널 36 유지). PHASES 승격. (대상 커밋 INDEX 기재 Codex env `97f4c52`→본 브랜치 실 `08c95fe`, verify 위생 노트 ①) 사람 확인 대기: 시각 회귀·autogrow 실기·실환경 effort 적용. |

| `0019-test-abi-green` | verify | PASS | — | `555fbb5` | 1 | 검증 PASS — 게이트 위생(0010~0018 구조감사 후속). **plan(2026-06-14, 377·Node24·`db/queries.test.ts`)은 stale** — 구현 커밋 `555fbb5`(2026-07-10, HEAD)은 0086 까지 진행된 현재 코드(784 규모·`infra/db/`·Node22 ABI127) 위. 현재 환경 기준 대조. 인수 **5/7 완전 충족(1·3·4·5·7)** + **2·6 환경 제약**(Electron 바이너리 egress 403 → dev/build 실기·순서스모크 미실행, 로직은 단위+`--check`로 대리검증). 핵심: 신선 트리 `npm test` 가 `pretest`→`[sqlite-abi] node: rebuilt`(로드실패 감지 후 자동 `npm rebuild`)→`infra/db`·`settings-migration` **27/27 정직 green**(수동 rebuild 0) = 0019 표적 "영구 9-red" 해소. 멱등 fast-path 0.098s·`--check` 불일치 exit1(회귀가드)·fingerprint marker(ver/lockfile hash)·node:test 6/6·lint(+scripts)/typecheck green. electron transitive 3스위트(11 tests)는 바이너리 403 로 로드불가(0019 무관·`src` 무변경). `postinstall` 통일(`ensure-sqlite-abi.mjs electron`). 비기능=Claude 직접 구현. PHASES 승격. 사람 확인 대기: 네트워크 완전환경 build 스모크·dev/build electron ABI 실기. |

| `0021-engine-model-crud` | verify | PASS | — | `91be400` | 1 | 검증 PASS — 인수 9/9 충족, 게이트 4종(lint/typecheck/test **390/390**/build) 통과(better-sqlite3 Node ABI 재빌드 후 green), 레이어 경계 0(agentStore=shared), 신규 의존성 0. 엔진&모델 페이지 CRUD: claude provider 추가/편집/삭제(블러 3단 마법사 모달) + write IPC(`engine:add/update/delete/read`)가 `sources/settings/claude/<provider>/settings.json` 원자적 생성 + 순수 `extractModels`(env 키 → meta.json, 1m 변형·추출 0 시 'SDK 기본 모델' 폴백, 단위 테스트) + 뷰 분해(EngineCard/EngineModelList) + agents 공유 store(refresh)로 카드·Composer/ModelMenu 싱크 + 카드 실모델명 부각. IPC_CONTRACT 36→40(engine 도메인 4채널). 구현 주체 = Codex. PHASES 승격. (대상 커밋 INDEX 기재 `50b8dce`→실 `91be400`, 위생 노트 ①) 사람 확인 대기: UI 시각 검증·실환경 provider 추가/싱크/bedrock (verify §책임 분리). |
| `0022-tool-approval-overlay` | verify | PASS | — | `3f22e86` | 2 | 검증 PASS(r2) — 인수 7/7 충족, 게이트 4종(lint/typecheck/test **381/381**/build) 통과(better-sqlite3 Node ABI 재빌드 후 전체 green), 레이어 경계 0, 신규 의존성 0, IPC 무변경. **r1**(Codex `afc7270`): 도구 승인을 입력 위 additive 카드 스택(`AskUserQuestionCard` 패턴)으로 + 입력 비활성. **r2**(Claude 직접 구현, 사용자 피드백 5건): ① composer 를 패널 스택(`flex flex-col gap-2`)으로 ② 컨트롤 라인을 입력 패널에서 **분리한 별도 패널**(그 패널만 bg 투명·borderless) ③ 패널 간격(스크린샷) ④ 승인 카드 버튼 `거부`(좌)/`세션 동안 허용`+`허용`(우)·`Ctrl+Enter` 라벨 제거(핸들러 유지)·title→설명→요약 간격·`description` 본문 노출(`toolDescription`) ⑤ **입력 비활성화 되돌림**(텍스트 입력 가능·중단 버튼 우선, `HighlightedTextarea` `disabled` prop 원복). plan_review 는 입력 대체 유지(분기 분리). 사람 확인 대기: 패널 간격·승인 카드 시각 톤·입력가능 실기(verify §책임 분리). |

| `0023-skill-mcp-isolation-docs` | verify | PASS | — | `12c7e65` | 1 | Skill/MCP 표준 정렬 + settingSources 격리 해제 **문서 선행**(비기능 = Claude 문서, 사후 정식화). 호환 자산(skill·mcp·AGENTS.md)=Orca SSOT·SDK 표준 경로 거울(skill→`.claude/skills`·mcp→루트 `.mcp.json`), 비호환(agents·commands·hooks·plugin)=engine-specific 연기. `settingSources` 옵션 생략(SDK 기본 user/project/local)→`~/.claude` 전역 상속 + `disallowedTools` 차단. **handoff 0014/0015 격리모드 폐기(supersede)** — 0014/0015 historical 보존(미수정). 문서 5건(standardization §2/§5.1/§5.2·TRD §6.8·adapters §1.3/§1.7/§3.1/§3.2·security §1.4·PHASES). 인수 8/8, 코드 게이트 N/A(문서)·정합성 grep 모순 0. PHASES 승격. 코드 정렬은 0024. |
| `0025-debug-wire-log-toggle` | verify | PASS | — | `878ef2b` | 1 | 검증 PASS — 인수 6/6 충족, 게이트(typecheck/lint/`mock.test.ts` 4) 통과, 레이어 경계 0, 신규 의존성 0, **신규 IPC 채널 0**(debug 2 유지). 디버그 패널 "Wire 메시지" 토글(기본 off) → main `sendChatEvent` 단일 chokepoint 가 `NormalizedEvent` 를 `[wire] <type> …` 로 터미널 출력. 기존 `DebugMockState`/`debugSetMock` 재사용 + `context.ts` 모듈 스코프 플래그(`setWireLog`, DEV 가드라 프로덕션 무출력). 콘솔만·채팅 transcript/DB 무변경(사용자 확정: 메인 터미널/콘솔만). 비기능 = Claude 직접 구현. PR #95. PHASES 승격. 사람 확인 대기: `npm run dev` 토글 실기(터미널 `[wire]` 출력) (verify §책임 분리). |
| `0024-skill-mcp-isolation-impl` | verify | PASS | — | `5d622cb` | 1 | 검증 PASS(구현 범위) — 인수 **9 충족 + 1 보류(D1)**, 게이트 4종(lint/typecheck/test **390/390**/build) 통과, 레이어 경계 0(deployer/paths=L1·claude-adapt/settings=L2), 비밀 불변식(.mcp.json placeholder·env↛argv branded) 보존, 신규 의존성 0. 0023 문서 정본을 코드에 정렬: deployer 신 레이아웃(skill→`.claude/skills`·`.mcp.json` placeholder 배포·manifest/agents/commands/hooks/settings dist 복사 제거) + paths 헬퍼(distSkillsDir/distMcpJsonPath, distPluginDir 제거) + adaptSkills `{skills:'all'}` + adaptSettings settingSources 제거(inline JSON settings 유지) + settings 읽기 sources 정합 + conformance `['.claude/skills']` + 테스트(deployer 9/claude-adapt 23/conformance/settings) + 0023 마커 해소. **D1 사람 결정 대기**: `disallowedTools` 차단 목록 + 사용자 allow 규칙의 canUseTool(RISKY_TOOLS) 우회 대응 — 계획상 보류(`#4` 일부), 확정 시 후속 라운드. 비범위: cwd 설치(복사)·`~/.claude/plugins` scan·opencode. 구현 주체 = Claude. PHASES 승격. (대상 커밋 INDEX 기재 `2705a3b`→실 `5d622cb`, 위생 노트 ①) 사람 확인 대기: 실환경 `~/.claude` 상속·turn settings 적용 (verify §책임 분리). |

| `0026-settings-json-model-parser` | verify | PASS | — | (push 후) | 1 | meta.json 제거 + settings.json 모델 파서 도입 (비기능 = Claude 직접 구현, 사용자 결정 3건). 파생 캐시 `meta.json` 폐기 → 열거 시 각 provider 의 settings.json 을 신규 순수 파서 `claude-model-parser`(`[1m]` 분리·env 모델 키·명시 모델 우선·노출 목록 내 default 1개)로 파싱. **필터링**: 커스텀(`ANTHROPIC_DEFAULT_*_MODEL`) 있으면 그것만, 전무 시 sonnet/opus/haiku 3개(`model:null`). 출력 형태 `AgentModelView={alias,model,isCustom,oneMillionContext,isDefault}` 렌더러까지 전면 도입 + `model:null`→bare alias SDK 해석. `extractModels`·`updateMeta`·`OrcaModelSchema`·`EngineWriteResult.models`·`ProviderEntry.label`·빈-폴백 제거(legacy 정리). handoff 0021(추출)·0014(meta 도입) supersede. 게이트 lint/typecheck ✅·영향 391 passed(db 9건 ABI 환경). 문서 3건(TRD §6.8/IPC_CONTRACT/standardization). PHASES 승격. 사람 확인 대기: ModelMenu/EngineCard 시각 검증·실환경 alias 전송. |

| `0027-claude-adapter-rename` | verify | PASS | — | `cde7b94` | 1 | 검증 PASS — 인수 8/8 충족, 게이트 lint/typecheck/test **400/400**(better-sqlite3 Node ABI 재빌드 후 전체 green — 1차 9-red 는 0019 dual-ABI 환경, 리네임 무관), 레이어 경계 0, 신규 의존성 0. 내부 어댑터/엔진 id `claude-code`→`claude`, provider key `claude-*`, settings/dist/hooks 경로 `claude/` 통일(60파일). D1-a 클린 브레이크+graceful fallback(마이그레이션 없음·`send.ts` byKey 폴백), D2-a 파일/클래스 리네임(`ClaudeAdapter`·`adapters/claude.ts`). 외부/SDK 어휘(`@anthropic-ai/claude-code`·`.claude/` 경로·`CLAUDE_CODE_USE_*`·문서 참조) 보존(`rg "claude-code" app/src` 버킷 C 2건만). 문서 6건 동기화. PHASES 승격. (대상 커밋 INDEX 기재 `fb652e9`→실 `cde7b94`, verify 위생 노트 ①) **위생 노트 ②**: impl 커밋 본문 리터럴 `\n` 로 trailer 미파싱 + 제목이 리네임 범위 과소기술 — history 기록용, 후속 커밋 규약 준수로 해소. 사람 확인 대기: ModelMenu/EngineCard 시각 회귀·실환경 provider 싱크·레거시 provider_key 폴백 실기. |

| `0028-settings-env-override` | verify | PASS | — | `3bf78bc` | 1 | provider settings.json 을 `~/.claude/settings.json` 과 동일 취급 (비기능 = Claude 직접 구현, 사용자 결정 2건). `Query()` 격리 해제로 상속된 `~/.claude/settings.json` env 를 Orca provider env 가 못 덮어쓰던 버그 — env 를 `options.settings`(settings 레이어)에 verbatim 실어 덮어쓰고, `options.env` 엔 시스템(턴) env 만. **handoff 0015/0018 의 "env↛argv" 불변식 폐기(supersede)**: split·branded 타입(`splitProviderSettings`·`ArgvSafeSettings`/`SubprocessEnv`)·음성 타입 테스트 + Orca 고유 `${VAR}` 확장·secret-store 토큰 주입(`provider:<key>`→ANTHROPIC_API_KEY, 토큰 저장 UI 미구현=dead code) 제거. argv 평문 노출은 앱-환경구성 메커니즘의 수용된 트레이드오프(same-user). 디스크 평문 0 유지. 문서 3건(security §1.4·TRD §6.8·standardization §5)·scaffold 주석 갱신. 0015/0018 historical 보존(미수정). |
| `0029-main-simplify-cleanup` | verify | PASS | — | `3c381b6` | 1 | `/simplify app/src/main` 정리 + perf (비기능 = Claude 직접 구현, PR #100). **1차(`4b50832`)**: 중복 상수/헬퍼 통합 — `num`(claude-map)·`PROVIDER_NAME_RE`(3곳→`config/provider-key`)·`VAR_RE`(→`mcp/expand`, store 정규식 재컴파일 제거)·`DEFAULT_ORCA_CONFIG`(→`orca-file`) + `combineSignals`→`AbortSignal.any`. **2차(`3c381b6`)**: 1차 건너뛴 항목 재평가 — cost `recompute` 3회 스캔→단일 조건부 집계 `sumUsageByBoundaries`(`sumUsageSince` 제거), provider `list` 어댑터별 캐시(`invalidateAll` 합류). **재평가 무조치**: `requestApproval` 클로저=턴 수명 캡처라 누수 아님; opencode 일반화·`process.env` 복사는 범위 제외(사용자 지시). 인수 9/9, 게이트 lint/typecheck/typecheck:test/test **397/397**(Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. PHASES 승격. 사람 확인 대기: 외부 수동 편집 staleness 정책 수용·cost/모델 UI 회귀. |

| `0030-system-prompt-policy-structure` | verify | PASS | — | `f2fc12c` | 1 | "시스템 프롬프트 관리 가이드 ver2"(Opus 4.8) Orca 적합성 검토·마이그레이션 (비기능 = Claude 직접 구현, 사용자 결정 3건: 문서+코드 / 5장 전면 구축 / 버킷2 재검토). 가이드 주장을 코드와 1:1 대조해 4버킷 분류 → `PY_AGENT_RULES`(코드 상수)를 가이드 5장 구조 `app/src/main/prompts/`(policies/\*.md · registry · loader · `buildAppend` 단일 문자열 · platformHints 스캐폴드)로 무회귀 이주(loader.test 가 바이트 동일 잠금) + 교정 가이드 `docs/arch/backend/system-prompt.md` 신설 + 버킷2 충돌(settingSources local 제외/settings.env 단일주입/옵션 캐싱)을 §5 Open Questions(OQ-A/B/C, 현행 유지 권고·변경은 사용자 결정 후 별도 핸드오프)로 등재. 참조 5문서 정정. 인수 9/9, 게이트 lint/typecheck/typecheck:test/test **406/406**(Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. PHASES 승격. 사람 확인 대기: 버킷2 OQ 재검토 결정·실환경 정책 append 적용. |

| `0031-skills-mcp-wiring` | impl | IMPL_DONE | Claude | `d4f82ee` | 1 | Codex 구현 완료 — customize 목업 제거·connector→mcp 리네임·스킬 sources IPC(author/upload/enabled)·MCP textarea 모달·enabled filter deploy·workspace cwd 싱크·IPC_CONTRACT 동기화. `git diff --check` 및 금지어 grep 통과. npm 게이트는 불완전 `node_modules` 환경으로 실행 불가(verify 재실행 필요). **Claude 후속 버그수정(사용자 요청 4건)**: ① 어댑터 스킬 그룹 라벨 `어댑터 스킬`→`CLAUDE 스킬` ② 워크스페이스 스킬 그룹/항목 페이지 표시 제거(local 채팅영역 처리는 추후) ③ 케밥 팝오버 우측 정렬(`Popover` `align='end'`, 화면 밖 오버플로 해소) ④ "채팅에서 사용해보기" 클립보드→nav state 컴포저 프리필(`/<스킬명> `). 게이트 lint/typecheck/typecheck:test/test **409/409**(Node ABI 재빌드 후 green), 레이어 경계 0. |

| `0032-study-agent-analyses` | verify | PASS | — | `263f55e` | 1 | opencode·hermes-agent 아키텍처 분석 보고서를 `docs/etc/study/` 레퍼런스로 추가 (비기능 = Claude 직접 구현, 회고적 정식화). 자체 구현(프레임워크 없는) 코딩 에이전트 2종의 콜스택·핵심모듈·컨텍스트 압축·비용/토큰·권한 게이트를 Orca 설계 비교 참고용으로 적재(opencode 13파일·hermes 13파일=26). 코드 변경 0(`app/**` 무수정)·`.gstack/` 미포함·trailer 규약 준수. 인수 6/6, 앱 게이트 N/A(문서). PHASES 승격(레퍼런스 자료 행, `lightweight-llm-strategy.md` 동결). PR #105. 사람 확인 대기: 분석 내용 사실 정확성·PR 머지. |

| `0033-runtime-resilience` | verify | PASS | — | `57a89af`(+verify 보강) | 1 | 검증 PASS — 인수 **15/15** 충족, 게이트 4종(lint/typecheck(node+web+test)/test **418/418**/build) 통과(의존성 설치 + better-sqlite3 Node ABI 재빌드 + electron 바이너리 설치로 impl 보고의 환경 제한 해소), 레이어 경계 0(변경=L3 ipc·L1 db·L0 shared·렌더러 features/chat 하향 의존), 신규 의존성 0. idle timeout(120s, 매 이벤트 reset)·부분턴 `messages.complete`(생성 0→telemetry 1, 마이그 0009 backfill·인라인 INSERT 회귀테스트)/미완료 칩·`turn.aborted`(user_cancelled, `turn.owner` 타깃·세션키 라우팅으로 비활성 세션 격리) 취소 ack·pre-stream retry(eventsReceived===0 한정·최대 2회·1s→2s·AbortSignal 존중). IPC_CONTRACT §3 turn.aborted variant 동기화. **verify 동반 수정 4건**(impl 미실행 게이트에서 드러난 lint 반환타입·테스트 픽스처 2건·`ProjectInsert` dead 필드 — 프로덕션 동작 무변경). PHASES 승격. (대상 커밋 INDEX 기재 `c31ac25`→실 `57a89af`, 위생 노트 ①) 사람 확인 대기: GUI 수동검증(타임아웃/부분턴 칩/멀티세션 취소/자동재시도)·IDLE/MAX_RETRIES 기본값 (verify §책임 분리). |

| `0034-multi-right-panels` | verify | PASS | — | `686fee1`(R1~R7, `2591cbd` 기반) | 1 | 검증 PASS — 인수 **19/19** 충족(2건 사용자 피드백 대체형), 게이트 lint/typecheck/test **427/427**(origin/main 리베이스 후 green), 레이어 경계 0, 신규 의존성 0, IPC 무변경. **기반(Codex `2591cbd`)**: 단일 계획 타일을 `rightPanelTiles` + `tileRegistry`/`rightPanelLayout` col-major 2행 레이아웃으로 일반화(계획/서브에이전트/예약1·2·케밥·리사이즈·slide). **후속 R1~R7(Claude 직접 구현, 사용자 피드백 수렴)**: ① 타일 헤더 케밥→닫기(x)+계획 복사·타이틀 간격 축소 ② 외곽/열간 리사이즈 우측도킹 방향 버그수정 ③ 타일 제거 위치 안정화(평탄→열구조) ④ 열 제거 FLIP 슬라이드·타이틀바 설정아이콘 제거·케밥 선택 시 닫기 ⑤ 열 슬라이드 내용키 추적 ⑥ 행 제거 grow 방향(justify-end)·열 제거 잔여타일 재생성 제거 ⑦ `/simplify` 리팩토링: 열 안정 id(`{id,tiles}[]`)로 `entering` 우회책 제거·애니메이션 3→2 메커니즘. PHASES 승격. 사람 확인 대기: UI 시각 검증(등장/제거 애니메이션·리사이즈 방향·헤더 닫기/복사·케밥)·PR 머지. |

| `0035-project-right-panel-cards` | verify | PASS | — | `cf5c6fb` | 1 | 프로젝트 상세 우측 패널(`ProjectInstructionsSidebar`) 지침/파일 섹션을 첨부 이미지(Claude 데스크톱)대로 **테두리+라운드 카드**로 재현 (비기능 = 시각 스타일링, Claude 직접 구현). 평면 `border-b` 구분선 → 독립 카드 2개(`rounded-r6 border bg-panel`·`gap-4 p-5`), `편집` 텍스트버튼·`준비 중` 배지 제거 → 헤더 `+` 아이콘 버튼 통일, 파일 드롭존에 겹친 문서 일러스트(인라인 SVG·테마 추종). 우측 패널을 카드 컴포넌트 3개로 분해(InstructionsCard·FilesCard·FileDropIllustration), 셸은 조립만. IPC/스토어/모달 로직 무변경. 게이트 lint/typecheck/typecheck:test/test **422/422** 통과(Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. 사람 확인 대기: UI 시각 검증(카드 톤·일러스트·테마 3종·xl 미만 스택). **라운드 2(랜딩 리파인, 사용자 리뷰 5건)**: 구조 구분선 제거(헤더 border-b·aside border-l/t, 카드 테두리는 유지) + 제목 라인 우측 핀+케밥 메뉴(세부사항 수정/삭제[빨강], 동작 미배선) + 지침 카피 "Claude" 제거 + 파일 카피 "폴더" 추가 + 중앙:우측 6:4 중앙정렬 단일 블록(`max-w-[1200px]`·고정 `gap-x-10`, 초과 폭은 좌우 바깥 여백 증가) + `pin` 아이콘 신설. **+ simplify(`cf5c6fb`)**: 카드 chrome/헤더를 `SidebarCard` 셸로 추출(중복 제거)·hero 메뉴 클래스 정리. **검증 PASS(r1)** — 인수 13/13, 게이트 lint/typecheck/test 422/422, 레이어 경계 0, 신규 의존성 0. PHASES 승격. 사람 확인 대기: UI 시각 검증 + PR #111 머지. |

| `0036-ui-polish-batch` | verify | PASS | — | `cd246c2`+`5fcb839` | 1 | 검증 PASS — 인수 **9/9**(7건 + 추가 8·9) 충족, 게이트 lint/typecheck(node+web+test)/test **427/427**(better-sqlite3 Node ABI 재빌드 후 green — 1차 11-red 는 `db/queries.test.ts` dual-ABI 환경=0019 계열, 변경 무관), 레이어 경계 0, 신규 의존성 0, IPC 무변경(채널 40 유지). UI 버그/미구현 일괄 보정 (비기능 = Claude 직접 구현). ① Popover 기본 `min-w-[240px]` 제거로 케밥 등 메뉴 호버=콘텐츠 너비 일치(의존 ChatTitleBar/SkillDetail 은 `min-w-[200px]` 명시) ② Popover 측정 기반 placement flip(`top` 공간 부족 시 아래로, visibility:hidden 1패스 측정) ③ nav '자동화' 비활성+빗금(`NAV_DISABLED_HATCH`, 확장·접힘) ④ Skills&MCP 랜딩 '플러그인 탐색' 카드 제거(+onBrowsePlugins prop 정리) ⑤ transcript 툴카드 그룹/단일 펼침 본문 `border border-t5` ⑥ ChatTitleBar adapter/session id 부제 제거 ⑦ telemetry 도넛 패널 `align='end'` 우측 정렬 ⑧ 컨트롤 우측 zone(모델·작업량·도넛) `align='end'`·좌측 zone 좌측정렬 ⑨ `+` 메뉴 '현재 프레임' 제거 + Skill→입력란 `/` 삽입으로 `SkillAutocomplete` 단일 진입(`SkillsMenu.tsx` 삭제). PHASES 승격. 사람 확인 대기: UI 시각 검증 9건·PR 머지. |

| `0037-skill-detail-markdown-shared` | verify | PASS | — | `b17070f` | 1 | 검증 PASS — 인수 7/7 충족, 게이트 4종(lint/typecheck/test **427/427**/build) 통과(better-sqlite3 Node ABI 재빌드 후 green — 1차 11-red 는 `db/queries.test.ts` dual-ABI 환경=0019 계열, 렌더러 전용 변경 무관), 레이어 경계 0, 신규 의존성 0, IPC 무변경. depth3 스킬 상세(`SkillDetail`)의 본문 마크다운이 bare `ReactMarkdown` 으로 헤딩·리스트·링크·표가 밋밋하게 떨어지던 버그 수정 (비기능 = Claude 직접 구현). 채팅 마크다운 렌더러(`Markdown`+`CodeBlock`)를 `shared/ui/markdown/` 으로 `git mv` 승격(boundaries 상 `features/skills`↛`features/chat` 차단 회피) → chat 7 importer 경로 갱신·skills 재사용. `Markdown` 에 선택적 `className` prop(미지정 시 채팅 톤 `DEFAULT_WRAPPER`)·`memo`/0007 주석 보존, skills 는 패널 톤(`text-ink2 leading-[1.7]`) 주입. `CodeBlock`(shiki) 그대로 재사용으로 코드블록 구문강조 일치. plain-text 토글·`McpDetail` 무변경. PHASES 승격. 사람 확인 대기: depth3 본문/채팅 회귀 시각 검증·PR 머지. |

| `0038-turn-state-error-ux` | verify | PASS | — | `a886f67` | 1 | 검증 PASS — 인수 **8/8** 충족, 게이트 4종(lint/typecheck(node+web+test)/test **429/429**/build) 통과(better-sqlite3 Node ABI 재빌드 후 green — 1차 11-red 는 `db/queries.test.ts` dual-ABI 환경=0019 계열, 변경 무관), 레이어 경계 0, 신규 의존성 0, IPC 채널 40 유지(NormalizedEvent variant 추가). 턴 상태/에러 UX 버그수정 5건 (비기능 = Claude 직접 구현). #1 도구 거부/취소 후 in-progress 멈춤 + #2 API 400 무한대기 → `send.ts` 종료 이벤트 보장(`sawTerminal`+폴백 telemetry) + `turn.retrying` 이벤트로 재시도 진행 렌더("재시도 N/M", reducer `retry`) + `claude-map` result-error→error 표면화. #3 쿨 테마 에러색 파랑 → 에러 컴포넌트 `rust`→`bad` 토큰(`bg-bad/10`·`text-bad`). #4 mock 시나리오 프래그먼트화 + 도구/사용자요청 in-progress 종료 + `full` 전체 항목화(도구호출→에러점프). #5 세션 복원 시 모델 라벨 `<provider>` 단독 → Composer 가 modelFamily 누락 시 provider 기본 모델 자동 셋업. 거부/취소 의미론 유지. IPC_CONTRACT §3 `turn.retrying` 추가. 사용자 결정 3건 반영. PHASES 승격. **위생 노트 ①**(대상 커밋 기재 `6c13da0`→실 `a886f67`) **②**(impl trailer `Agent: codex`·제목 범위 과소기술 — history 기록용). 사람 확인 대기: 거부/취소·재시도 배너·에러색·세션 복원 모델·mock 실기 시각 검증·PR 머지 (verify §책임 분리). |

| `0039-attachment-thumbnails` | verify | PASS | — | `f55c440` | 1 | **전면 개정(2026-06-23)** — 컴포저 첨부 일원화 + 동시성 경고 + workspace 경로 추상화. **폐기**: 프로젝트 페이지 첨부·RAG·`project_files`·manifest·런타임 AGENTS.md 생성(코드 미존재 → 문서 제거). **Part 1 컴포저 첨부**: 다이얼로그·**DnD**(`webUtils.getPathForFile`)·**클립보드 paste**(이미지 인라인 바이트) 3경로(txt/md/image)→입력 위 썸네일 칩(렌더링 기준 claude.ai)→전송 시 txt/md 추출(BOM·truncate)은 영문 wrapper(id·sentinel·metadata) text 블록, 이미지는 user 메시지 `image` 블록(`source:{type:'base64',media_type,data}`); `createTurnInputStream` 블록배열 일반화. 추출기 `files/attachments.ts`(`AttachmentExtractor`+`TextExtractor`, PDF 는 이음매만 — **신규 의존성 0**). **Part 2 동시성 경고**: 같은 projectId 실행 중 query 면 컴포저 `<알림>` 레이어 경고(차단·락 없음), 메모리 `ConcurrencyRegistry`(finally decrement·자기제외), `concurrency:event` 브로드캐스트. **Part 3**: `getWorkspacePath(projectId,sessionId?)`(userData base) 로 단일 cwd 대체. 이미지 한도=capability 파생(하드코딩 금지). IPC 46→49. 사용자 확정(컴포저 일원화·DnD/paste 포함·이미지=image 블록·PDF/의존성 없음·렌더링 claude.ai). 구현 주체=Codex(+Claude 후속 8커밋). **검증 PASS(r1)** — 인수 9/9(AC7 의도 편차 동반), 게이트 lint/typecheck(node+web+test)/test **463/463 (68 files)**(better-sqlite3 Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0, IPC 46→49(files 1→3 + concurrency 1). 3경로 첨부(다이얼로그/DnD `webUtils.getPathForFile`/paste 인라인)→썸네일 칩·영문 wrapper(id/sentinel/metadata/truncate)·image `source:base64` 블록·`createTurnInputStream` 블록배열 일반화·`AttachmentExtractor`/`TextExtractor`(BOM·truncate, PDF 미등록)·`ConcurrencyRegistry`(증감·finally·렌더러 자기차감)·`getWorkspacePath`. **편차 ①** AC2 wrapper=string 병합(SDK streaming-input 실패 회피, `c215a32`) **②** AC7 base=`~/.config/orca/projects`(userData 아님 — 확장 싱크 인접성, `b3426d1`). **범위 확장**: transcript 첨부 영속 썸네일(plan 비범위 → 사용자 피드백 가산 `2509210`). PHASES 승격. **위생 노트 ①**(INDEX 기재 `92020c4`→실 f6238d8 `f55c440`). 사람 확인 대기: AC2/AC7 편차+영속 범위 설계 수용·첨부 UX/시각 검증·실환경(이미지 전송·동시 query 경고·cwd 싱크)·PR 머지. |

| `0040-new-chat-race-early-registration` | verify | PASS | — | `aef1d82` | 1 | 새-채팅 전송 동시성 버그(전송→새대화→전송 시 2번째 세션 소멸·버블 소실) 해소 = **직렬 디스패치 게이트 + 낙관적 멀티 엔트리**(렌더러 `chatStore` 로컬 draft 키 + 세마포어(1) `pendingNewChatKey` + FIFO `newChatQueue` + 원자 re-key·FIFO 승격). 화면은 즉시 멀티턴, main 진입은 단일 진입로(직렬) — id 발급 후 다음 진입. **IPC·main pending 모델 무변경**(draftId IPC 폐기) — main 변경은 제목 트리거·`promote` 신원가드(resume 동시 시 새-채팅 오승격 차단, Codex 검토 Issue 1) 2건 한정. + R2: `session.updated`(init) 에 최근대화 등록(렌더러 `recentsEpoch`→셸 refresh)·제목 생성(`titles.maybeStart` init 호출, turn-end 안전망 유지). 구현 주체=Codex. |
| `0041-ask-ui-refine` | verify | PASS | — | `96c8c5d` | 1 | 검증 PASS — 인수 **8/8** 코드 충족(증거 `파일:라인`, `verify.md`), 게이트 lint ✅/typecheck ✅(node+web+test)/test **실행분 476 green**(better-sqlite3 Node ABI 재빌드 후; `persist`·`send.runtime-resilience` 2파일은 main 프로세스·electron 바이너리 미설치로 import 차단 — 렌더러 전용 0041 무관), 레이어 경계 0(`features/chat` 내부), 신규 토큰/의존성 0, IPC/reducer/파싱 무변경(순수 표현 계층). AskUserQuestion 시각 정제(라운드1~3) — ① 선택 톤 중립화(주황→`bg-t3`+중립 경계, accent=라디오/체크 인디케이터만) ② 헤더 chip 무채도·sentence-case ③ 번호배지 수직중앙·verbose 힌트줄 제거 ④ 헤더 `1/N` 카운터 + multiQuestion 이전/다음 화살표 ⑤ `AskExchange` 단일 좌측 결합 버블(`질문 답변`, `bg-bubble-user`) ⑥ 카드 `mb-2` 제거(컴포저 `gap-2` 이중간격 해소) ⑦ 키보드/자동진행/기타 상호배타/a11y 무회귀 ⑧ themed 토큰만. 구현 주체 = Claude(비기능). **검증 환경 f6238d8 는 별개 작업 `0228c47` 가산 레이어 포함(`isAskResolved` 미해소 버블 차단 — 0041 AC 무회귀).** PHASES 승격. 사람 확인 대기: UI 시각 검증(3 테마·cool=파랑 선택 톤·픽셀 일치)·PR 머지. |
| `0042-subagent-transcript` | verify | PASS | — | `a674588` | 1 | 검증 PASS — 인수 **10/10** 충족, 게이트 lint/typecheck(node+web+test)/test **502/502 실행분**(better-sqlite3 Node ABI 재빌드 후 green — 실패 2파일 `persist`·`send.runtime-resilience` 는 electron 바이너리 미설치 import 차단=환경 제한, 변경 무관), 레이어 경계 0, 신규 의존성 0, IPC 채널 50(parentToolRunId 필드 추가). 서브에이전트(Task) 도구호출 표면(이미지 6~8) — **백엔드 데이터모델 + 렌더러**. **Part A 백엔드**: `claude-map` 가 `parent_tool_use_id` 캡처(`readParentToolRunId`) → `NormalizedEvent`/`AppMessagePart` 에 `parentToolRunId` 전파 → `subagentTasksFromMessages`/`childMessageForParentToolRunId` 가 부모 Task 키잉 child 트랜스크립트로 분기(메인 세그먼트 제외) + IPC_CONTRACT §3·§6 동기화 + 단위테스트. **Part B 렌더러**: Task 전용 행/카드(`agent_task` kind·호출/중단) + 우측 `subagent` 타일 child 트랜스크립트(메인 `AssistantMessage`/`ToolGroup`/`ToolCard` 재사용) + 뒤로가기 리스트↔상세. 1단 중첩만. 구현 주체 = **Codex**(+Claude 보정 `383f3c1`·`0ea6dff`·`bf8b6a5`). PHASES 승격. (대상 커밋 INDEX 기재 Codex env `47d9206`→본 브랜치 실 `a674588`, 위생 노트 ①) 사람 확인 대기: 이미지 6~8 시각 검증·실환경 캡처. |

| `0043-subagent-ui-feedback` | verify | PASS | — | `a674588` | 1 | 검증 PASS — 인수 **9/9** 충족, 게이트 lint/typecheck/test **502/502 실행분**(electron 미설치 2파일 환경 제한 제외·Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. 0042 후속 — 사용자 스크린샷 피드백 보정 (구현 주체 = Claude). **작업 중단 버튼은 사용자 요청 철회로 미배치.** ① 메인 transcript 서브에이전트 양식: 그룹 헤드 `실행 중 에이전트 N개`(진행 중)·그룹 항목 `에이전트 실행 중 <model> <title> <elapsed>`·단일 `에이전트 실행 중 <model> · <현재도구> · <도구수>` — 신규 `AgentTaskRow`(store 구독 child 메타 + 로컬 시작시각 앵커 + `useElapsed`/`formatElapsed` 공유훅, StatusLine 재사용) + ToolGroup 헤더 분기. ② **백엔드 child 텍스트 캡처**: `claude-map` `message.completed` 에 `parentToolRunId` 전파 → text `AppMessagePart` 보존(persist/복원, 메인 preview/세그먼트 제외) → `childMessageForParentToolRunId` 가 child text/reasoning 수집 → 우측 상세에 완료 답변 출력(+result summary 폴백). ③ 서브에이전트 상세 간격 `--chat-turn-gap`(메인 transcript 양식). mock: 전 subagent 시나리오 `agentLabel`+child `message.completed` 추가·`subagent_task_multi` 동시실행화·신규 `subagent_task_running`(MockScenarioId 12→13). IPC_CONTRACT 동기화. 게이트 typecheck/lint ✅·test 471 passed(+1)·잔여 12 fail 3파일은 better-sqlite3 ABI/electron 미설치 환경 제한(base 동일·변경 무관). 사람 확인 대기: UI 시각 검증·실환경 child 텍스트 캡처. |

| `0044-subagent-meta-stop` | verify | PASS | — | `a674588` | 1 | 검증 PASS — 인수 **9/9** 충족, 게이트 lint/typecheck/test **502/502 실행분**(electron 미설치 2파일 환경 제한 제외·Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0, IPC 채널 49→50(`stopSubagent`). 0043 후속 — 실환경 피드백(구현 주체 = Claude, SDK 통합). **근본 원인**: claude-map 이 SDK `task_*` 시스템 이벤트를 버려(“Phase 3 미사용”) 시간·도구수 미추적 + 모델 소스 부재로 'Explore'(subagent*type) 표시 + `forwardSubagentText` 미설정으로 서브에이전트 답변 미forward. **구현**: ① `forwardSubagentText:true`(claude.ts) → child 답변 캡처(0043 경로 재사용). ② claude-map 이 `task_started/progress/notification` → 신규 `subagent.task` NormalizedEvent(taskId·subagentType·duration_ms·tool_uses·last_tool_name) + child assistant `message.model` 캡처 + ctx 누산 → 부모 Task `tool.call.completed.subagentMeta`(model/duration/toolUses) 영속. ③ store transient `subagentMeta`(toolUseId 키, reducer 미경유) + `useSubagentMeta` → AgentTaskRow 가 모델(`modelDisplayLabel` 매핑 'Haiku 4.5')·라이브 경과시간(단일·그룹 모두)·현재도구·도구수 표시. ④ 우측 리스트 running 카드 '대화록 보기' 우측 네모·라운드·채움없음 중단 버튼(`stop` 아이콘, 카드 `<button>`→`<div role>` 재구성). ⑤ `orca:chat:stopSubagent`(49→50채널) → `LiveTurn.stopTask`(`query.stopTask(taskId)`, foreground 거부 시 `backgroundTasks` fallback) — turn 전체 아닌 Task 단위. mock subagent.task 라이프사이클+running 진행 시연. IPC_CONTRACT 동기화. 게이트 typecheck/lint ✅·test 478 passed(+신규)·잔여 12 fail 3파일 환경 제한(base 동일). **사람 실환경 검증 대기**: forwardSubagentText 답변·task*\* 메타·stopTask 실제 중단(foreground/​background fallback). |

| `0045-handoff-design-rules` | verify | PASS | — | `9b82b19`(구현) + 회고 `49fbed9`·`0b896af`·`0d81844` | 1 | 검증 PASS — 인수 **8/8**, 게이트 N/A(문서)·grep 3자 정합 7항목 ✅, 위생 0. 핸드오프 규약 정비의 회고적 정식화 + 자기검토 보정 (비기능 = Claude 직접 구현). 세션 3커밋(설계 강화·구현/검증 지침·진입 트리거)을 핸드오프로 편입 + A1 트리비얼/메타 카브아웃·A3 라이브 출처·A4 파생이슈 주체(검증자 대리)·B1 분류 휴리스틱·B2 선조치 tie-breaker·B3 체크박스 가드·번호 max+1. 새 plan 템플릿 첫 도그푸드. **PHASES 미승격**(인프라 메타 — 0001 선례). 사람 확인 대기: 카브아웃 남용 경계·회고적 정식화 수용·PR. |

| `0046-approval-idle-pause` | verify | PASS | — | (push 후) | 4 | **라운드 4(PR #137 GUI) — 동시 서브에이전트 승인 idle pause/reset 경쟁 수정**: 단건=정상/3개동시=무한inflight 의 진짜 원인 — `send.ts` 이벤트 루프가 매 이벤트 `idle.reset()` 하는데 다른 서브에이전트 child 이벤트가 승인 pause(clear)를 재무장(무력화). 수정: `createIdleTimer` `paused` 플래그(보류 중 reset no-op) + `options.signal` 존중(SDK 취소→broker deny·카드 정리) + `[approval]` 진단 로그(wireLog 게이트). test 509/509 실행분. **사람 GUI 확인 완료(2026-06-25): 3개 동시 승인 정상 진행·inflight 해제.** 라운드 1~4 종료(머지 승인은 사용자). <br>— 라운드 2: **라운드 2(PR #137 GUI 피드백) — 동시 도구 승인 큐화**: 서브에이전트·병렬 tool_use 가 `permission.requested(tool_approval)` 를 동시 방출하는데 렌더러 `pendingToolApproval`(단일 슬롯)이 덮어써 직전 카드 소실 → 사라진 approvalId 가 broker 영구 보류(서브에이전트 inflight 고착) + `pendingApprovals`>0 로 idle 영구 pause. 수정: `pendingToolApprovals` 큐(append/approvalId별 제거) + Composer 스택 렌더 + ToolApprovalBody prop화(렌더러 전용, 백엔드 무변경). test 509/509 실행분. 라운드 1 = 아래 원문. <br>— 라운드 1 검증 PASS — 인수 7/7 충족, 게이트 typecheck(node+web+test) ✅·lint ✅(boundaries 0)·test **508/508 실행분**(better-sqlite3 Node ABI 재빌드 후 green; 2 suites `persist`·`send.runtime-resilience` 는 electron 바이너리 미설치=프록시 다운로드 중단 환경 제한, 0033/0041~0044 동일 계열), 신규 의존성 0, IPC/DB 무변경. 승인 카드(plan/ask/위험도구) 대기 중 idle 타이머(120s) abort 버그수정 (비기능 = Claude 직접 구현). 0033 idle 타이머 미흡점: canUseTool await 동안 이벤트 정지 → idle 미reset → 120s 후 턴 abort+카드 auto-deny+늦은 클릭 무시. ① idle pause/resume + 승인 보류 중 멈춤(스트림 stall guard 보존) ② owner-gone(webContents destroyed/render-process-gone) 이벤트 가드로 자가치유 대체 ③ [Q1] `IpcRouter.shutdown()` will-quit 진행턴 settle+abort ④ [Q2] 로드 시 incomplete 메시지 orphan tool_call→합성 aborted tool_result(크래시 백스톱). 재시작 승인 UI 재현 안 함(불변식: 중단됨만, 사용자 결정). PHASES 승격. 사람 확인 대기: GUI(2분 대기·stall·창닫기·Cmd+Q/kill -9 재시작) 실기. |

| `0047-plan-panel-comments` | verify | PASS | — | `ff5f3a3` | 1 | 계획 패널 인라인 코멘트(첨부 2장 기능 동일 구현, 구현 주체=Claude). 본문 드래그→작성 팝오버 + 밑줄 하이라이트(오버레이) + composer 코멘트 칩(편집/삭제) + 휘발성 reducer 상태(`planComments`/`activePlanCommentId`, RESOLVE_PLAN/NEW_CHAT 클리어). 전송=계획 revise 흐름에 묶고 `ApprovalResolution.deny.planFeedback`(IPC 확장, 채널 수 불변)로 구조화 데이터 전달 → main `prompts/plan-feedback.ts` 가 `attachment.ts` 컨벤션의 sentinel 태그(`ORCA_PLAN_FEEDBACK`)로 직렬화(영문 instruction·escaping·neutralize) → 어댑터 ExitPlanMode deny message. 게이트 typecheck/lint ✅, test **528/528 passed**(2 import-차단 파일 `persist`·`send.runtime-resilience` 은 electron 바이너리 미설치 환경 제한=0033/0046 계열, 변경 무관). 신규 테스트 39(plan-feedback 7·planComments 9·protocol.permission +2·reducer +2 등). 후속 버그 대응: 선택 확정 document mouseup 보강 + 텍스트 노드별 rect 측정으로 팝오버 미표시/공백 하이라이트 문제 수정. 2차 후속: 팝오버 앵커를 텍스트 rect 기반으로 변경 + 공용 Popover 수평 clamp + 수정 펼침 시 수락 버튼을 기존 수정 위치로 이동. 3차 후속: 작성 팝오버를 mouseup release 위치 위로 배치 + 코멘트 추가 시 composer 수정 영역/textarea 자동 활성화 및 좌측 거부/수락 숨김. 4차 후속: Popover center align 으로 point anchor 중앙 배치 + approval card textarea mirror auto-grow/resize 제거/focus border 제거/흰 배경 적용. 5차 후속: 계획 코멘트 팝오버를 contentRef 하위 absolute DOM 으로 재구현(bottom 기본/top flip) + 팝오버 textarea auto-grow/resize 제거 + Claude Code 웹 스타일 적용. 게이트 typecheck/lint ✅, test ✅ 538/538(`npm rebuild better-sqlite3` 로 Node ABI 복구 후). 6차 후속: ① **팝오버 내부 클릭 시 소멸 버그** 수정(선택 시작/종료에서 `[data-context="floating"]` 제외 — 팝오버 자손 클릭이 selection collapse 로 draft 비우던 문제) ② `테스트` 플레이스홀더→`코멘트 추가…` ③ quote 헤더 제거(aria-label 로만 유지)·CC 웹 캡처(edit 모드) 톤 정리 ④ approval card 수정 textarea 테두리 복원(`border border-t5`+focus-within). 게이트 typecheck/lint ✅, test ✅ 529 passed(electron 미설치 2 suite 환경 제한). 7차 후속: ① composer 승인 카드 `수정` 펼침 시 좌측 `거부`/`수락` 버튼 제거(좌측 그룹 게이트 `!commentFeedbackActive`→`!reviseExpanded`, dead 분기 정리). ② **`거부` deny 미전달 버그** 수정 — `rejectPlan` 의 `{deny, interrupt:true}`+`CANCEL_CHAT` 이 main `approvals.respond` 의 동기 `controller.abort()` 와 레이스(broker.resolve=마이크로태스크)로 deny 가 SDK 전파 전 쿼리를 죽여 `PLAN_REJECT_MESSAGE` 유실 → clean deny(`{deny}`)+`RESOLVE_PLAN` 만으로 변경(사용자 결정=deny 전달 후 자연 종료). `rejectPlan` 회귀 테스트 2건 추가. 게이트 typecheck/lint ✅, test ✅ 531 passed(electron 미설치 2 suite 환경 제한). **검증 PASS(r1)** — 인수 7/7 충족(증거 `verify.md` 매트릭스), 게이트 typecheck/lint(boundaries 0)/test 531/531, 신규 의존성 0, 사용자 "정상동작 확인"(거부 deny 전달·수정 버튼·팝오버 시각). PHASES 승격. 사람 잔여(차단 아님): 실환경 거부 후 모델 응답·턴 자연 종료 체감·팝오버 픽셀 최종 확인. |

| `0048-theme-palette-white` | verify | PASS | — | `8d79db8` | 1 | **검증 PASS(r1) — 인수 6/6 충족, 게이트 typecheck(node+web+test)/lint(boundaries 0)/test 531/531 실행분, 신규 의존성 0, IPC/DB 무변경.** 테마 팔레트에서 classic(아이보리)·cool(블루) 제거 → Claude Code 웹 참고 **뉴트럴 니어화이트** `white` 1종으로 대체(구현 주체=Claude, 비기능 UI 스타일링). 결과 팔레트 = white(루트 `@theme` 기본) + dark 2종. ThemeId/ThemePref/zod enum 4곳 `'white'\|'dark'` 통일 + `SettingsSchema.theme` `.catch('white')` 로 옛 값(classic/cool) 무손실 강등(density/sidebarWidth 보존) + tokens.css 루트 팔레트 뉴트럴화(bg #ffffff·사이드바/카드 중립 그레이·t3 #e8e8e6·코랄 강조 #c96442 유지)·`[data-theme='cool']` 블록 삭제·dark 유지 + DebugPanel 라디오 화이트/다크 + CodeBlock cool→one-light 매핑·THEMES 항목 제거 + 주석/AGENTS.md(세→두 테마) 정합. 게이트 typecheck(node+web+test) ✅·lint ✅(boundaries 0)·test **531/531 실행분**(better-sqlite3 Node ABI 빌드 후 green; 2 suites `persist`·`send.runtime-resilience` electron 바이너리 미설치 환경 제한, 0033/0046 계열). 신규 의존성 0, IPC 채널/DB 스키마 변경 0. 사람 확인 대기: GUI 시각 검증(화이트 톤·코드블럭·테마 토글)·tokens.css hex 미세조정·PR 머지.
| `0050-lifecycle-orchestration-redesign` | verify | PASS | — | 구현 `d8ec70d`(C1~C4) + 갭보강 `0535be9`(인수 2·6c, Claude) | 2 | **검증 PASS(r2 final)** — 인수 **11/11** 충족, 게이트 lint/typecheck(node+web+test) 0 error·test **542 passed**(electron 의존 `send.runtime-resilience`·`persist` 2파일은 바이너리 미설치 환경 제약, 6d 는 `timers.test.ts` 가 대체 커버), 레이어 경계 0, 신규 의존성 0. 사전 비판적 검토(라운드1 verify 전)에서 인수 3(상태머신 이중 SSOT)·11(uv 미폐기)·커밋 위생(타입·trailer) 갭 지적 → 구현 `d8ec70d` 가 인수 3(turn 플래그 제거·런타임 파생 읽기)·11(uv+정책 cascade 폐기) 해소 및 trailer 규약 회복. 잔여 인수 2·6c(FakeSessionRuntime 모드-불변 테스트) 는 비기능이라 사용자 결정으로 Claude 가 직접 보강(`0535be9`). 보드 위생 교정: 유령 해시 `3c737fb`·placeholder `f6238d8` → 실 도달 커밋. PHASES 승격. 사람 확인 대기: 실환경 GUI 회귀(cold→live·취소·StallTimer·크래시 복구)·uv 제거 후 Python MCP 폴백·electron 게이트 정상환경 재실행·PR 머지. 상세 = `verify.md`. **(원 설계 기록)** **라이프사이클·오케스트레이션 구조 재설계 (P0).** (구 0049 — main의 `0049-design-token-ui-fixes` 와 번호 충돌로 0050 재번호. 재번호 작업은 0051.) 설계 정본 = `@docs/etc/orca_lifecycle_orchestration_design_draft_ko.md`(엔지니어링 리뷰 8건 반영) + 기준자 2편(`lifecycle_management`·`orchestration_report`) + real-world 4편(`study/opencode`·`study/hermes-agent`). **앱-레벨 2축 모듈 신설**(2026-06-28 사용자 결정): 최상위 **`src/main/lifecycle/`**(앱·프로세스·세션 생명주기 — SessionRuntime·상태머신·레지스트리·timers·recovery) + **`src/main/orchestration/`**(애플리케이션 레벨 턴/세션 조율 — concurrency·admission seam). SessionRuntime 은 lifecycle 의 구성원. 핸들 수명(턴→세션 OneShot) + coarse 상태머신(단일 소유·비영속) + StallTimer/IdleCloseTimer 분리 + resume/부팅 dangling 마감 + 축출 훅 예약. **uv Python runtime 폐기 포함**(배선·정책·build·`runtime:status` IPC 제거). **P0 = OneShot + 견고성**(리뷰 1), Persistent/steer-queue/cap 은 P1, cold-fallback 은 Future. 재배치: `ipc/chat/turn-registry.ts`→`lifecycle/session-registry.ts`, `send.ts:createIdleTimer`→`lifecycle/timers.ts`, `concurrency-registry.ts`→`orchestration/concurrency.ts`, close 정책 `claude.ts:335`→SessionRuntime. 인수 11개·P0 테스트 4종. **구현 전 Codex 설계리뷰 6건 반영(설계 보강 §1~6)**: registry↔turn-context _분할_(개명 아님)·dangling recovery DB 쿼리 명세(message_id 필수)·`LiveTurn.close()` 계약 신설·mode-invariance 는 FakeSessionRuntime fixture(Persistent 미구현)·uv 잔재 체크리스트(**IPC 무변경** — runtime 채널 2026-06-11 기제거)·**C1~C4 단계 커밋 경계**(C1 추출→C2 SessionRuntime→C3 recovery→C4 uv폐기). **+Codex 2차 자문 6건(라운드 2)**: provenance 라운드화·`LiveTurn.close()` 전 어댑터(mock 포함) 적용·C1 send.ts re-export 무회귀·**uv 제거가 유일 정적 정책(python-runtime) 제거를 동반→stableAppend 빔·prompt 테스트 갱신→C4 별도 PR 권장**·dangling recovery 가 incomplete assistant(`complete=0`)도 markComplete·pendingByOwner+promote(0040 가드) 보존. **PR 경계(2026-06-29 결정): PR-A=C1~C3(구조) + PR-B=C4(uv폐기+정책 cascade), 한 핸드오프 2 PR.** **+Codex 3차 자문 4건(라운드 3)**: SessionRuntime 에 stopTask/backgroundTask 포함(서브에이전트 제어 회귀 방지)·close 트리거=정규화 terminal{telemetry,error,turn.aborted}(raw result 비참조)·uv 제거는 0050 포함(PR-B 출시, 연기 아님)·dangling recovery 는 assistant-message 단위+죽은 프로세스 전제. **+Codex 4차 자문 5건(라운드 4)**: PR-A/PR-B=한 핸드오프 순차 impl 라운드(Criteria-Pending 인수11·INDEX 2커밋 라벨, F1)·상태머신에 abort cause(user_cancelled/stall/retry)·recovery 용 message-scoped upsert(전역 tool_run_id 오갱신 방지)·recovery DB-only 경로(부팅엔 owner/InflightTurn 없음)·**reason 'aborted' 통일**('interrupted' 폐기). **Step0 보정**: renderer 가 이미 `settleOrphanToolParts`(LOAD)로 표시-복구 → ⑪의 효용은 DB 자기일관성(F2: 풀 구현 유지). 구현 주체=Codex(대규모, 사용자 재지정 가능). |

| `0052-turn-coordinator-firstclassing` | verify | PASS | — | (push 후) | 1 | **0051 §A 가로축 구동체 TurnCoordinator 1급화 (P1 키스톤, 비기능 = Claude 직접 plan→impl→verify).** 0051(문서전용)은 이미 PASS — 본 핸드오프가 §A 가 축복한 가로축(stream→reduce→persist∥forward + 권한 재진입)을 `ipc/chat/send.ts:handleChatSend`(~390줄)/25-필드 `InflightTurn` 에서 **L1 `lifecycle/turn-coordinator.ts`** 로 **동작 보존** 추출. ① 코디네이터(L1)=retry+consume·reduce·persist∥forward fan-out·terminal 합성·error/retry·stall 소유 + `beginApprovalPause()`. ② **의존 역전** — L3(persist·forward·title) 구체 클래스 비import, sink 인터페이스(`lifecycle/turn-sinks.ts`: TurnPersistSink·TurnEventSink·TurnTitleHook)만 주입(기존 L3 클래스 무변경 구조적 만족), 컴포지션 루트 send.ts 배선. ③ settle 3종(`lifecycle/settle.ts`, 주입 persist+forward)·순수 subagent 빌더(`lifecycle/subagent-settlement.ts`) L1 이동 + **무회귀 배럴 re-export**(router.shutdown 3-arg·resilience 테스트·subagent-settlement.test 무변경). ④ send.ts 셸화(셋업+`coordinator.run`+finally)·cancel/stopSubagent→settle.ts. ⑤ 가로축 첫 단위테스트(turn-coordinator 5·settle 6)+resilience 7=14 green. 게이트 typecheck/lint(boundaries L1→L3 0)/build ✅·test 541 passed(12 fail=`db/queries.test.ts` better-sqlite3 Node ABI=클린 트리 동일·0019 계열·변경 무관). 비범위(후속): InflightTurn god-object 풀분리·Persistent+IdleCloseTimer·Supervisor cap/LRU·steer/queue admission·`orchestration/` 리네임. 사람 확인 대기: 실환경 GUI 회귀(일반 턴·승인·취소·subagent stop·재시도)·PR 머지. |

| `0053-runtime-supervisor-spine` | verify | PASS | — | (push 후) | 1 | **0051 §A staging P1 — Runtime Supervisor(세로축 unit #3) 척추 추출 + 단일 멱등 close/abort (비기능 = Claude 직접 plan→impl→verify).** 0050(P0)·0052(가로축)에 이어 §A.5 라이프사이클 자원 supervision 묶음의 **척추만 먼저** — 정책(cap/LRU/IdleClose)은 Persistent 전까지 죽은 코드라 0054 로 분리. ① **`lifecycle/supervisor.ts` 신설** — `RuntimeSupervisor` 가 `SessionRuntimeRegistry` 를 소유(조회/등록/승격 위임) + `release(turn)`(WeakSet 멱등 teardown) + 0054 seam(cap admission·idle/LRU·IdleClose·concurrency 이관) 주석. ② **`abortTurn(turn,cause)` standalone** — `live.markAborted+controller.abort` 를 한 곳에(chatCancel·stall 공유; onOwnerGone 은 pre-run runtime 직접 mark = 의도적 예외). ③ 컴포지션 루트(`router.ts`) supervisor 생성·주입(chat·approvals·shutdown), `ChatDeps.turns`→`supervisor`. ④ 소유 이관으로 dead 가 된 `TurnRegistry` alias·배럴 value re-export 제거(타입 배럴만). **동작 보존**(이벤트 시퀀스·DB parts·UX 무변경, 정책 0). 게이트 typecheck(node+web+test) ✅·lint ✅(boundaries L1↔L3 0·no-cycle 0)·신규 `supervisor.test.ts` 7 green + lifecycle 전부 green. 환경 제한: `db/queries.test.ts`(better-sqlite3 Node ABI)·electron 미설치 2 suite(`persist`·`send.runtime-resilience`)는 import 차단=프록시 다운로드 환경(0033/0046 계열·변경 무관). 비범위(0054): Persistent runtime·cap admission·LRU/idle eviction·IdleCloseTimer·concurrency 소유 이관·steer/queue·`orchestration/` 리네임. 사람 확인 대기: 실환경 GUI 회귀(일반 턴·취소·창 닫힘·stall)·PR 머지. |

| `0054-persistent-runtime-idle-close` | verify | PASS | — | (push 후) | 1 | **0051 §A staging P1 키스톤 — Persistent SessionRuntime(close-policy) + IdleCloseTimer 실구현 + Supervisor 거버넌스 (비기능 = Claude 직접 plan→impl→verify).** 0053 척추 위에 정책의 *전제*를 세운다: idle/LRU/cap 이 死코드인 이유가 OneShot(매 턴 fresh·즉시 폐기)이므로, **소유/배선**으로 long-lived 핸들을 만든다. ① **close 정책 파라미터화(decision ⑳)** — `SessionRuntime(adapter, closePolicy)` 단일 클래스 + `reusable` getter, `OneShotSessionRuntime` 무회귀 alias, 스트리밍 메커니즘·상태머신 불변(동작 보존). ② **IdleCloseTimer 실구현(decision ②)** — `timers.ts:createIdleCloseTimer(onIdle, IDLE_CLOSE_TIMEOUT_MS=300s)`, StallTimer 와 별 상수·정반대 트리거. ③ **`lifecycle/runtime-pool.ts` 신설** — 세션 키 idle 보존 + IdleCloseTimer 소유(타이머를 런타임 아닌 소유자에 둠), 정책-자유(reusable 만 진입). ④ **Supervisor 거버넌스** — `acquireRuntime`(풀 재사용 or factory)·`releaseRuntime`(정상종료 reusable 만 보존, 그 외 close)·`closeIdleRuntimes`; **turn teardown(release)≠runtime close** 분리, 기존 멱등 `release` 불변. ⑤ **게이트(capability+config)** — `ORCA_PERSISTENT_RUNTIME=1` 만 persistent, **기본 OneShot → 게이트 OFF 동작·이벤트·DB·UX 무변경**. ⑥ **순환 차단** — `abortTurn`→`lifecycle/abort.ts` 분리(supervisor→pool→timers→supervisor cycle 회피), supervisor 무회귀 re-export. **정책은 비움**(cap/LRU·concurrency 이관은 0055 seam). 게이트 typecheck/lint(boundaries·no-cycle 0)/test **578 passed**·lifecycle 9파일 55 green(신규 runtime-pool 7·timers·supervisor·session-runtime 확장). 환경 제한: electron 미설치 2 suite(`persist`·`send.runtime-resilience`)는 import 차단=프록시 바이너리 다운로드 환경 — **electron stub 재실행 시 9 green**(7=resilience, 변경 무관, 0033/0046/0052/0053 계열). 비범위(0055): cap admission·LRU/idle eviction(`evictIdle`)·ConcurrencyRegistry 소유 이관·steer/queue + true streaming-input·`orchestration/` 리네임. 사람 확인 대기: 게이트 ON 실환경 GUI 회귀(cross-turn 재사용·idle reap·shutdown)·정상환경 게이트 재실행·PR 머지. |

| `0051-lifecycle-taxonomy-refinement` | impl | IMPL_DONE | Claude | (push 후) | 1 | 0050 설계의 **용어·축 정제 (문서 전용 = Claude 직접 impl).** ① '세션' 과부하 해소 → **Orca Session(대화 기록 진실) / SessionRuntime(일시적 실행 핸들) / SDK resume context(외부 binding)** 3분리(GLOSSARY §1·§3). ② 결정 ⑭ 교정 — cap/LRU/idle-close 가 세는 유닛=SessionRuntime → **동시성=자원 라이프사이클(§2)**, 오케스트레이션=handoff 만; 출시 `orchestration/` 코드명 유지(문서만 정제·분기 메모, 결정 2). ③ **2축 정제 절(§A) 신설**: 세로(소유) + 가로(턴 파이프라인 stream→reduce→persist∥forward + 권한 재진입) + 누락 **TurnCoordinator** 1급화 + DB=_기록_ 진실 한정(reseed≠recovery) + Conversation Continuity/Knowledge Curation(Future 서비스 층, memory.md 폐기→knowledge artifact) + Staging(P0 출시/P1/Future). ④ provider-runtime TurnCoordinator 포인터. 코드 변경 0(비범위: TurnCoordinator 1급화·코드 리네임·continuity 구현은 후속). 사람 확인 대기: 0050 구현 재적용/PR 결정. |

| `0049-design-token-ui-fixes` | verify | PASS | — | (push 후) | 3 | **라운드 3 — Primary 버튼 잉크화 + 전송 투명 + 도넛 색(사용자 후속)**: `Button` primary 변형을 warm rust→중립 다크(`bg-ink text-bg`)로 전환(AskUserQuestion 제출·도구카드 허용/수락/수정 일괄) + `ModalActions`·raw `bg-rust text-white` CTA(프로젝트/엔진/스킬MCP/백엔드 다이얼로그)·라디오 checked 모두 잉크화 · 전송 버튼 투명(`uncontained`) · MCP transport 세그먼트 선택 `bg-t3 text-t8` · ApprovalCard '플랜 열기'/textarea·스킬 모달 에러 토큰 정합(`bad`) · 도넛 progress 초록(`good`<0.6)→노랑(`warn`<0.85)→빨강(`bad`). r2 의 "rust=주요 CTA 유지"를 사용자가 뒤집음(모노크롬). rust 잔존=danger(삭제)·CameraView(Future). 게이트 540/540. 변경 17파일. <br>— 라운드 2 — 강조 상태 전반 중립화 + Composer 아이콘(사용자 후속 5건): 케밥 패널 활성 시 강조 제거(`open` 만 `bg-t3`) · Composer 전송=신규 `enter`(↵)/중단=`stop`(투명 라운드 네모) · 엔진 'default' 배지·추가 다이얼로그 선택/스텝퍼/선택됨/focus 중립화(`bg-t3`/`text-t8`/`bg-ink text-bg`/`border-border-strong`)·오류 `bad` 토큰 · 스킬 1depth(`CustomizeRail`) 활성·3depth 본문 토글·공용 `Toggle` on(`bg-ink`) 중립화 · 프로젝트 CTA raw `#fff` 제거. rust 는 주요 CTA·danger 만 유지(Q2). 게이트 lint/typecheck/test **540/540**. 변경 9파일(`ChatTitleBar`·`Composer`·`Icon`+enter·`Toggle`·engine 3·skills 2·projects 1). <br>— 라운드 1: 진입 UI 정리 + 타이틀바 3버튼 + 엔진/스킬 디자인 토큰 정합 (비기능 = Claude 직접 구현). ① 부팅 InstallerDialog 자동 오픈 보류(`bootstrapBackend` 분기 제거, `setInstallerOpen` 액션 보존 — 기능 완성 후 재활성) ② nav 사이드바 footer BackendStatus 미렌더(`footerSlot=null` + Sidebar 빈 풋터 가드) ③ 채팅 타이틀바 3버튼을 raw 버튼으로 통일: 케밥 press 색을 warm `fill-selected`→press 표면 토큰 `t3`(우측 패널 활성/메뉴 열림 시 `aria-pressed`), 복사 버튼=전체 대화 마크다운 클립보드 복사(`partsText`+check 피드백), 검색(돋보기)=`disabled`+빗금(border 토큰 사선) ④ 엔진&모델 페이지·엔진추가 다이얼로그·스킬&MCP 하위 페이지의 비테마 `cream-50`(다크 override 없음)→themed `bg2`(engine 3 + skills 5 파일). **검증 PASS(r1)** — 인수 7/7, 게이트 lint/typecheck(node+web+test)/test **540/540**(better-sqlite3 Node ABI 재빌드 후 green; 1차 12-red `db/queries.test.ts`=0019 ABI 환경, 렌더러 변경 무관), 레이어 경계 0, 신규 의존성 0, IPC/DB 무변경. 사람 확인 대기: 3 테마 시각 검증(케밥 press 톤·검색 빗금·엔진/스킬 카드 표면)·복사 실기·PR 머지.

| `0055-runtime-resource-governance` | verify | PASS | — | `dfe447a` | 1 | **0051 §A staging P1 — 자원 거버넌스(cap/LRU/idle eviction + ConcurrencyRegistry 소유 이관).** 0054(Persistent 전제) 위에 정책 계층을 **mechanism↔policy 분리**로 채운다: `EvictionPolicy`(순수 LRU·Map 비의존·victim 키 반환)·`RuntimeCapPolicy`(기본 무제한·union=`accept\|evict-idle`) 주입, ConcurrencyRegistry **단일 진실원**(소유 Supervisor 이관+getter·`RouterContext.concurrency` 제거·**파일 미이동=결정 2 준수**), idempotent close 단일 helper(기존 4경로 합류·self-idle vs LRU 이중close 0). count=active+idle·**eviction victim=idle only**·`getRuntimePopulation()`. **기본값에서 게이트 OFF/ON 동작·이벤트·DB·UX 0 변경**. Codex QA 7건 반영. **검증 PASS(r1)** — 인수 **8/8** 충족, 게이트 lint/typecheck(node+web+test)/test **588/588 로드분** green(신규 0055 테스트 eviction-policy 3·runtime-pool 3·supervisor 4 포함; better-sqlite3 Node ABI 재빌드 후 green), 레이어 경계 0(boundaries·no-cycle), 신규 의존성 0, IPC 무변경. 환경 제약: electron 미설치 2 suite(`persist`·`send.runtime-resilience`)는 프록시 바이너리 차단으로 import 불가 — 정상환경 9 green=597(impl 보고 정합·0050/0053/0054 동일 계열). keepIdle prev-교체 회귀 조사 → take 선행으로 무영향 확정. **위생 노트 ①**: INDEX/impl 기재 `0baaea4`(Codex env)→본 브랜치 실 `dfe447a`. PHASES 승격 보류(lifecycle P1 시리즈 0052~0055 일괄·0056 후). Open Question(사용자): cap 수치·평가세션 cap 회계(over-cap reject/queue 는 0056 이관). 비범위: steer/queue·reject(0056)·`orchestration/` 리네임·true streaming-input. 사람 확인 대기: 게이트 ON 실환경 GUI 회귀·정상환경 electron 게이트 재실행·PR 머지. 구현 주체=Codex(기능). |

| `0056-turn-admission-steer-queue` | verify | PASS | — | `84354a3` | 1 | **0051 §A staging P1 — 턴 admission 추상화(framework only).** busy 같은-세션 입력의 admission 을 `AdmissionController`(L1 순수 결정)+`AdmissionPolicy`(주입)로 1급화 — **이번 범위 = 추상화 seam + 현행 default 무회귀 보존만**. 검토 교정: 현행 default 는 `send.ts:163-179` 의 **hard reject**(≠ ConcurrencyRegistry 경고게이트=0039 별개 축) → 기본 `RejectDuplicatePolicy` 가 그 가드 1:1 재현, 인라인 가드 제거→controller 위임(이중게이트 0). `AdmissionDecision=accept\|reject\|queue\|steer` 중 **queue/steer 는 예약 seam(enactment 0)**. enactment 레이어 분리(L1 결정·L3 reject enact). **OQ 결정(2026-06-30 사용자)**: 의도 default=**steer**(단 streaming-input 포트 `injectMessage` 부재로 _현재 불가_ → 후속 종속)·빌드범위=**framework only**·cap 축=**accept inert**(0055 미러). 감독 스코프=**Orca Session 안**(세션별 가로축; cap 인구 거버넌스는 app-wide 별개 축). 비범위(후속): steer/queue 메커니즘·cap reject/queue·`orchestration/` 리네임. 무회귀(이벤트·DB·UX 0)·IPC 무변경·신규 의존성 0. 선행 0055. 구현 주체=Codex(기능). **검증 PASS(r1)** — 인수 **8/8** 충족, 게이트 lint/typecheck(node+web+test)/test **592/592** green(better-sqlite3 Node ABI 재빌드 후; 신규 `admission-controller.test.ts` 4/4 — accept-미질의·existing/new-slot reject·queue/steer 미반환), 레이어 경계 0(boundaries·no-cycle), 신규 의존성 0, IPC 무변경. 구현자 보강 4건(discriminated `AdmissionDecision`·policy↛error shape·`AdmissionTarget` 이중케이스·queue/steer fallback)=선조치 가능 범주·인수 의도 보존. 환경제약: electron 미설치 2 suite(`persist`·`send.runtime-resilience`) import 불가(0050~0055 동일 계열, 정상환경 601 정합). **위생 노트 ①**: INDEX/impl 기재 `d76d153`(Codex env)→본 브랜치 실 `84354a3`. **위생 노트 ②**: impl 커밋 본문 리터럴 `\n` 로 trailer 미파싱(0027 동형, history-only). PHASES 승격(0052~0056 lifecycle P1 시리즈 일괄, 0055 지연분 해소). 사람 확인 대기: 후속 steer/queue UX 시각검증·실환경 중복-send reject 실기 (verify §책임 분리). |

| `0057-composer-cwd-panel` | verify | PASS | — | impl `0058095` + verify선조치 `0c1921a` | 1 | **cwd 진입점 — 랜딩=Composer 패널, 세션=타이틀 `📂 basename / 제목`(요구 변경: 세션 Composer 패널 노출 취소→타이틀 이동).** baseline default, 선택 cwd 는 새 세션 출생 시 고정·영속(`sessions.cwd` 마이그레이션/insert/load/resume 해석), 세션 후 클릭=`shell.openPath`. `files:pickDirectory`·`files:openPath`, `SendChatMessage.cwd`, basename 순수함수/tests, IPC_CONTRACT(50→52). 구현 주체=Codex(기능). **검증 PASS(r1)** — verify 전 수석엔지니어 리뷰 4건: F2(resume `getSessionById` 이중호출 정리)·F3(`📁`→`📂`)·F4(`openPath` 경로 화이트리스트=실재 디렉토리+projects루트하위∨실재 세션cwd, 임의 실행파일 오픈 차단) **선조치**(Claude 비기능 `0c1921a`), **F1(프로젝트 랜딩 미선택 라벨=`default`↔실 출생 cwd=프로젝트 파생폴더 불일치)=사용자 결정 현행 유지+한계 기록**(세션 시작 후 init 자동 보정·cwd 의미론은 후속 분리). 인수 **9/9**(요구 변경 #1·#7 재해석), 게이트 lint/typecheck(node+web+test)/test **602 passed**(신규 isWithinDir 3·session-cwd 2·path-basename·protocol cwd/OpenPath green), 레이어 경계 0, 신규 의존성 0. 환경제약: electron 미설치 2 suite import 불가(0050~0056 동일 계열, 정상환경 606). **위생 노트**: INDEX/impl 기재 `f6238d8`(Codex env)→본 브랜치 실 impl `d970fa0`→`082621f`→`0058095`. 사람 확인 대기: 타이틀/랜딩 패널·이모지 톤(테마3종)·실환경 openPath 시각검증·PR 머지. **파생이슈 D1(해결 `443552e`)**: 작업 폴더 변경 후 `@` 멘션 팝오버가 이전 cwd 항목을 보이던 캐시 회귀 — `useFileAutocomplete` cwd 변경 시 캐시 폐기(게이트 602 passed·시각검증 대기). |

| `0058-orca-plugin-restructure` | verify | PASS | — | impl `98a74c0` + verify선조치 `07b0c6b` | 1 | **claude dist 확장 배포 계층을 Claude Code 플러그인(`plugins/orca/`)으로 재구성 + 런타임 `options.plugins` 로드 + cwd 파일 싱크 제거.** ① `dist/claude/plugins/orca/`(`.claude-plugin/plugin.json`·`skills/`·`agents/`·`hooks/`·`.mcp.json`) 재구성(deployer·paths·`claude-plugin-package.ts`·`ExtensionDeploymentService`) ② SDK `options.plugins` 주입(`adaptPlugins`) + Orca skill `orca:<name>` 필터(`adaptSkills`) ③ `workspace-sync.ts`·`syncedCwds` 삭제(cwd 싱크 폐기) ④ deploy 를 query 전 선행 보장(boot 1회+CRUD+턴 진입 멱등). MCP=plugin `.mcp.json`·`options.mcpServers` 레거시 유지. 구현 주체=Codex(실체=impl `98a74c0`). **검증 PASS(r1)** — 인수 **11/11**, 게이트 lint/typecheck(node+web+test)/test **606/606** green(better-sqlite3 Node ABI 재빌드 후; 신규 `adaptPlugins` 3·`adaptSkills` 네임스페이스·`deployer` plugin 레이아웃 11 포함), 레이어 경계 0, 신규 의존성 0, IPC 무변. **verify 전 수석엔지니어 리뷰 선조치 3건(Claude 비기능)**: F3(`adaptPlugins` 매니페스트 존재 가드 복원 — AC#5/엣지케이스#2, deploy 실패 시 미존재 경로 주입 방지)·F4(dead `markDirty` 제거)·F-lint(impl 이 남긴 `ClaudeAdapter` 빈 생성자 lint 에러 제거). 환경제약: electron 미설치 2 suite(`persist`·`send.runtime-resilience`) import 불가(0050~0057 동일 계열, 정상환경 재실행 green). **위생 노트 ①**: plan/INDEX 기재 impl `0d3d8fb`(Codex env)→실 `98a74c0`. **위생 노트 ②**: 구현이 `docs(handoff)` 커밋에 코드 23파일 번들 + trailer `11/11` 이 게이트 미실행 상태 주장(F2) — 본 verify 재실행+선조치로 정정. PHASES 승격. 사람 확인 대기: plugin `.mcp.json` SDK 실로드·`orca:` skill init 노출·실환경 턴·정상환경 electron 게이트·PR 머지. 상세=`verify.md`. |

| `0059-steer-queue-feedback` | verify | PASS | — | impl `a72e2bc` + D1 `2feab72` | 1 | **검증 PASS(r1)** — 인수 **12/14 충족 + 2 설계전환 보류**(AC5 `SteerQueuePolicy` admission swap → explicit `chat:steer` action path 로 전환·AC8 채널 카운트=verify 선조치로 해소), 게이트 lint/typecheck(node+web+test)/test **618 passed**(신규 steer-queue/turn-coordinator/streaming-input 3파일 19 green; electron 미설치 2 suite `persist`·`send.runtime-resilience` import 불가=0050~0058 동일 계열, 변경 무관), 레이어 경계 0(신규 L1 `steer-queue`), 신규 의존성 0. explicit chat:steer IPC + `SteerQueue`(multi→single 병합) + renderer 3상태 feedback UX. **파생 D1(resolved, Claude `2feab72`)**: `flushedSteer` 오버레이 제거→`steer.flushed`=즉시 일반 커밋 user 메시지 + main flush 경계 어시스턴트 메시지 마감으로 DB↔라이브 정렬 일치(`rg flushedSteer` 0건 확인), pending=항상 `italic text-ink3`. **verify 선조치(Claude 문서)**: `IPC_CONTRACT` 채널 수 `총 52→54`·`chat 4→6`(`CHANNELS` 실측 정합). **위생 노트 ①**: INDEX/plan 기재 `7cefcbe`(Codex env)→실 impl `a72e2bc`. PHASES 승격. 사람 확인 대기: explicit chat:steer 설계전환 승인·renderer UX 시각검증(pending 폰트·flush 위치·재시작 후 동일 렌더·hover 취소 재주입)·PR 머지. 상세=`verify.md`. |

| `0060-steer-flush-boundary` | verify | PASS | — | `7d481ce` + 파생 D1·D2 `90e49f5` + 파생 D3·D4 `8e5d3fd` + 파생 D5 `f449c67` | 4 | **파생 이슈 r2(2026-07-02, Claude 직접 구현) — 주입 명세 v2 대조 후속**: 사용자 제공 "Pending 메시지 주입 시점 명세 v2"(CLI v2.1.198 추출 검증)가 r1 의 미검증 가정(user echo)을 [V] 확증 → **D1(echo 기반 커밋)**: steer stdin 메시지에 `uuid`+`priority:'next'` 명시(SDK 0.3.143 타입 지원, 범프 0), claude-map 이 텍스트 user echo 를 `input.echo`(main 내부 variant, renderer 미전달)로 승격, TurnCoordinator 커밋 신호를 echo 매칭(uuid 1차/text 폴백, 미매칭 무시=허위 커밋 구조적 불가)으로 전환 — `isSteerFlushBoundary` 경계 근사 제거. **D2(턴 종료 이월)**: 턴 종료 무조건 flush 제거(소비분만 커밋, 미소비 pending 큐 잔존) + 다음 `chat:send` 에서 steer row 선영속·프롬프트 병합 이월(renderer 는 send 시점 로컬 커밋, `steer.flushed` 미발신). **D5=resolved(2026-07-02, `f449c67`, 검증 PASS r4)**: 사용자 버그리포트(steer 반영됐는데 버블 미승격) 원인 = **echo 자체가 CLI 직렬화 게이트에 막혀 미발화** — `replayUserMessages` 기본 false + SDK 기본 argv 에 `--replay-user-messages` 부재(v0.3.143 바이너리 내장 JS 추출 실측). 수정: `claude.ts` `extraArgs:{'replay-user-messages':null}` 상시 전달. 활성 시 echo=원문 content+`source_uuid`(=batch uuid) 확인 — 매칭 설계 그대로 성립, §9 (a)/(e) 실측 항목 동시 해소. 게이트 642 passed. 실기 E2E(steer→echo→승격) 재확인 1순위. **D3·D4=resolved(2026-07-02, `8e5d3fd`, 검증 PASS r3 — 인수 9/9·게이트 641 passed)**: 구현 전 수석엔지니어 검토로 SDK 0.3.143 dts 전제 3건(PostToolBatch·`agent_id`·uuid/priority) 직접 실증 + 보완 5건 반영(SteerQueue held/flushed 컬렉션 분리·게이트 훅 fail-open·`takeSteerFlush` 동적 sessionId·`injectMessage` dead 배선 전면 제거·canSteer 의미 갱신). `chat:steer`=로컬 홀드만, `claude-adapt.makeSteerGateHook`+`mergeHooks`, `TurnRequest.takeSteerFlush` 의존 역전. TurnCoordinator·renderer·IPC 무변경. 게이트 641 passed. 설계 요지: steer 를 stdin 즉시 주입 대신 **로컬 홀드 + PostToolBatch callback 훅 게이트에서 병합 단일 user 메시지(batch uuid)로 주입** — 취소 창=게이트 도래까지 100%(시간 유예 아닌 이벤트 마감), flush→echo sub-second 창은 취소 거부(echo 커밋이 버블 복원=정직 화해), D4 1버블·모델 컨텍스트 1:1 구조 보장. orca 편차: Stop 게이트 비채택(턴-스코프 수명 충돌 — C1 은 D2 carryover)·SubagentStop 불요. 실측 확증: SDK 0.3.143 PostToolBatch 지원·`adaptHooks` callback 파이프라인·`agent_id` 메인 루프 판별자·stdin FIFO 강화. 상세=`plan.md` "D3·D4 설계 확정 요지". 게이트 lint/typecheck/test **630 passed**(신규 echo/carryover 테스트 포함, electron 2 suite 환경 제약 동일). IPC_CONTRACT §2/§3 갱신(채널 무변, `input.echo` variant=내부 전용 명시). 사람 실측 대기: echo uuid 보존·C1/C2/C5 실기·carryover 통합. 상세=`plan.md` 파생 이슈 챕터·`verify.md` r2. **이하 r1 →** **검증 PASS(r1)** — 인수 **7/7 충족**(경계 판정 5케이스 단위테스트 대조: 최상위 tool.call.completed settle flush·서브에이전트 내부 비경계·병렬 settle·telemetry·pending 없음 persist 0), 게이트 lint/typecheck(node+web+test)/test **618 passed**(electron 미설치 2 suite 제외, 0050~0059 동일 계열), 레이어 경계·순환 0, 신규 의존성 0, IPC 무변경. 0059 후속 버그수정(비기능 = Claude 직접 구현) — steer pending 이 **입력 즉시 flush** 되던 문제: 0059 가 flush 신호를 producer-pull(`onConsume`/`nextInjectedInput`)로 골랐으나 SDK 는 입력 AsyncIterable 을 eager drain 하므로 pull ≠ turn 경계였다. 수정: flush 를 입력 push 에서 분리(streaming-input `push`=리터럴 전달만)하고 TurnCoordinator 가 관찰하는 **turn 경계**(`isSteerFlushBoundary`: 최상위 tool.call.completed 배치 settle / telemetry)에서 `consumeSteerForInput` 구동. dead 배선(`onConsume`/`nextInjectedInput`/`consumeInjectedInput`/`onInputConsumed`) 제거(`rg` 0건 확인). renderer/persist 무변경(호출 시점만 이동). PHASES 승격. 사람 실측 대기: 서브에이전트 위임 시 취합 시점 커밋 재현·user echo 신호 실측(디버그 `[wire]`)·PR 머지. 상세=`verify.md`. |

| `0061-concurrency-lifecycle-fold` | verify | PASS | — | fold `188526d` + verify 선조치 리네임 | 1 | **검증 PASS(r1) — 인수 5/5 + F2(네이밍) verify 선조치 해소.** verify 진입 전 수석엔지니어 비판적 검토(모듈화/추상화) 수행 → 동작보존 이동 품질 높음, 지적 5건. **F2**: fold 후에도 클래스/파일명이 `Concurrency`(실체=프로젝트별 active turn 카운터, 객체보관 안 함→`Registry` 부정확)라 `runtime-cap-policy` 와 이름충돌·주석봉합 → **사용자 결정으로 verify 선조치 리네임**: `ConcurrencyRegistry`→**`ActiveTurnTracker`**·`concurrency.ts`→`active-turn-tracker.ts`·`ConcurrencyGate`→`ActiveTurnGate`·supervisor getter `concurrency`→`activeTurns`(내부 식별자만). **IPC/UX 경계 어휘 보존**(`CHANNELS.concurrencyEvent`·`concurrencyApi`·`concurrencyByProjectId`=동시실행 경고=경계에선 옳은 말) → IPC 무변경. `rg "ConcurrencyRegistry\|ConcurrencyGate" app/src`=0. 게이트 lint/typecheck/test **618 passed**(2 electron-import suite=환경제약·변경무관, better-sqlite3 Node ABI 재빌드 후), 경계·순환 0, 신규 의존성 0. 설계서 §A.2·⑭·§A.3 fold+리네임 정합. **위생: F1** fold 커밋 `188526d` `Agent: codex` trailer=오기(비기능=Claude 소관), 본 검증 커밋 본문서 정정. **F4** AGENTS.md L1 목록 `prompts` 추가(누락 정확도 수정, AC밖·무해). PHASES 승격. 사람 확인 대기: 동시 query 경고 배지 시각검증·PR 머지. 상세=`verify.md`. **이하 원 구현 요지 →** **구현 완료 — 0051 §A.2 결정 2 해소, 동시성=라이프사이클 코드 정합.** 0051 P1 은 0052~0060 에서 전부 착지(all PASS) → 잔여는 seam 활성화(Open Question)·Future(Continuity)·결정 2 리네임뿐. 사용자 결정(2026-07-01)=결정 2 해소를 0061 범위로 채택 + 목표구조=**concurrency→lifecycle 폴딩 + `orchestration/` 이름 예약**. 판별식(§A.2): cap/LRU/registry 가 세는 유닛=SessionRuntime=자원 → 동시성=라이프사이클; 진짜 오케스트레이션=handoff/fork/continuity(Future §A.4, 아직 코드 0). 현 `orchestration/` 안엔 라이프사이클 자원 `concurrency.ts`(순수 leaf, 소유자 Supervisor 는 이미 lifecycle) 하나뿐 → `lifecycle/` 로 이동(+test)·빈 `orchestration/` 제거·import 3(supervisor/router/supervisor.test)+주석 2(turn-coordinator/supervisor)+`app/src/main/AGENTS.md` L1 목록 갱신·설계서 §A.2 "fold+이름예약"으로 정합. **eslint 무변경**(L1=와일드카드 `src/main/*`). 동작·이벤트·DB·IPC·UX 0 변경(순수 이동)·신규 의존성 0. 인수 5/5, 게이트 lint/typecheck/test 627 통과(Node ABI 재빌드 후). 비범위: 진짜 오케스트레이션 코드 신설·`orchestration/` 즉시 재생성(YAGNI)·seam 활성화·Continuity 구현. |

| `0062-main-feature-slices` | verify | PASS | — | 버스/재배치 선행 `3507ffb`·lifecycle C13 `6014124`·ipc C14 `10b6d80`·prompts+엄격화 C15 `6577a6a`·네이밍+AGENTS C16 `d3a3b7c` | 1 | **검증 PASS — 인수 16/16, 게이트 lint/typecheck/test 640/build green.** main 프로세스를 아키텍처 스펙(feature 수직 슬라이스 + adapters 한정 ports&adapters + 얇은 infra + app composition root)으로 재구성 완료. `src/main` 최상위 = 정확히 `{app,contracts,adapters,features,infra}` + `index.ts`/`env.d.ts`. **핵심**: ① infra/bus 단일 turn.event 파이프라인(구독 순서 usage→history→title→relay SSOT=bootstrap) ② lifecycle→contracts+features/{sessions,chat} ③ ipc→app/{bootstrap,chat-turn,handlers,context}+features/{history,approvals,chat}+infra/ipc ④ feature 교차 import 0(boundaries v6 강제·위반 샘플 error 확인) ⑤ prompts 데드코드+Installer 제거·네이밍(Bootstrap/HistoryWriter/UsageTracker/ApprovalBroker/TurnContext) ⑥ AGENTS.md 재작성. shared/preload/renderer diff 0. 이연(비-AC): contracts/ports Runtime\* 구조적 중복 통합(의미적 타입 리스크로 후속). 상세=`0062-main-feature-slices/{plan,verify}.md`. 사람 확인 대기: 실환경 채팅/승인/취소/설치/멀티세션 GUI 회귀(순수 이동의 런타임 동일성). **비기능 리팩토링(Claude 직접 plan→impl→verify) — main 프로세스 아키텍처 스펙 정렬.** 첨부 `Main Process Architecture Spec v0.1` 규격(feature 수직 슬라이스 + adapters 한정 ports&adapters + 얇은 infra + app composition root)으로 `app/src/main`(~96 prod 파일, 10.1k LoC) 재배치. 현 구조=0017 L0→L1→L2→L3(위생 양호하나 도메인 17개가 L1 평면 + `ipc/chat/send.ts` 612LoC·`ipc/router.ts` God-object 허브). 목표: `src/main/{app,contracts,adapters,features,infra}`. 사용자 확정 3건: ① **infra/bus 도입 포함**(history/usage/title/relay 를 단일 파이프라인 `turn.event` 구독자로 전환, TypedBus 자체구현·신규의존 0) ② **이름 유지 3종**(`SessionAdapter`·`NormalizedEvent`·`adapters/`), 그 외 모호명 과감히 변경(`IpcRouter`→`bootstrap`·`TurnPersistence`→`HistoryWriter`·`CostTracker`→`UsageTracker`·`InteractionBroker`→`ApprovalBroker`·`InflightTurn`→`TurnContext` 등) ③ **미사용 전부 제거**(빈 `POLICY_REGISTRY`+연쇄·`platformHints`·`OneShotSessionRuntime`·`toOpencodeConfig`/`OpencodeMcp`·`CapabilityProbe`/`RevertManager`·`installer`·무회귀 배럴), mock 어댑터/시나리오=dev 도구로 존치. 결합 해소: adapters 소비 포트 타입(TurnRequest·ResolvedProviderSettings·OrcaMcpConfig·SteerFlushBatch·Extracted\*) 상향 이동으로 extensions↔lifecycle 상호참조·adapters→domain 8방향 해소, `TurnContext`=`contracts/`(타입 전용). eslint boundaries 신 elements(main-root/app/adapter-impl(capture engine)/adapters/features(capture feature)/contracts/infra/shared)로 features↔features 차단. IPC 무변경(CHANNELS/zod/`shared`·`preload`·`renderer` diff 0). 인수 16개·설계 상세=`0062-main-feature-slices/plan.md`. 비범위: DbQueries repo 분해(후속)·electron-trpc(TRD RPC 금지)·코드 없는 스펙 feature 신설. |
| `0063-ports-adapter-alias` | verify | PASS | — | `b57e7ae` | 1 | **검증 PASS — 인수 5/5, 게이트 lint/typecheck/test 640/build green.** 0062 유일 이연 항목 해소(비기능=Claude 직접). `contracts/ports.ts` 의 `Runtime*` 가 adapters ports&adapters 정본(`LiveTurn`·`SessionAdapter`·`CompleteRequest`)을 재선언하던 구조적 중복 제거: `RuntimeLiveTurn extends LiveTurn`+거버넌스 훅(optional)·`RuntimeCompleteRequest=CompleteRequest`·`Runtime{Title,Session}Adapter=Pick<SessionAdapter,…>`(부분집합 — 설치 수명주기 제외로 Interface Segregation 보존). `SessionRuntime.live`=raw `LiveTurn`. ports.ts 51→34 LoC. **구현 중 발견**: `RuntimeSessionAdapter=SessionAdapter`(전체)로 잡으니 `session-runtime.test` mock(4-메서드)이 typecheck 실패 → narrower port 확증 → `Pick<…4종>`. 타입 전용(런타임/IPC/renderer diff 0). |
| `0064-conversation-continuity-handoff-fork` | verify | PASS | — | 포팅 `276d4d3` (원 impl r5 = 브랜치 `claude/handoff-62-feedback-tp8t9j`) | 1 | **0051 §A.4 Continuity 착수 — handoff/fork 우선(기능=Codex). r2 설계 개정(2026-07-02, 사용자 정정)**: handoff 를 요약 스테이징 방식에서 **hermes rebind 방식**으로 전면 교체 — 클릭=**즉시 물질화**(fork 로 새 세션 생성) + 자동생성 사용자 발화 **`/compact [핸드오프] …`**(SDK 슬래시 명령, 확정 문안=plan 수록, `{title}` 보간) 첫 프롬프트 전송 → 요약=**도착 세션이 SDK 네이티브 compaction 으로 생성**(Orca 자체 요약 파이프라인·EvaluationSession·summarizeForHandoff 채널 폐기, 신규 invoke 채널 0). 예약된 `orchestration/` L1 재생성: `fork`(**SDK `forkSession`** 위임=전체맥락 lossless·새 id init 발급)·`handoff`(fork 재사용 + 자동 메시지 템플릿·display 무복사). DB `session_lineage`(전용 테이블·CASCADE)+copyMessages. **UX**: handoff=StatusPopover(컨텍스트 도넛) 상시 액션+`nearCompaction` 경고 강조+가드 3종(mid-turn·최소 대화량·중복) / fork=에이전트 답변 호버 분기 아이콘. **불변식(r2 분리)**: fork=클릭 시 DOM draft 뷰만·첫 보내기에 lazy 물질화·취소=no-op / handoff=클릭 즉시 물질화·실패 시 출발 세션 무변경. **신규**: `system/compact_boundary` NormalizedEvent 정규화(현재 claude-map 드롭) — 구현 선행=wire log(0025) 실측. IPC: send `forkFrom?`/`handoffFrom?`+`TurnRequest.fork`→`claude.ts` forkSession. 비범위: reseed cold-fallback·knowledge artifact/KB·lineage UI·fork 점-분기(SDK 미지원)·EvaluationSession(후속 재도입 여지). OQ: 점분기·출발 세션 lineage 표시. **구현 완료(Claude — 사용자 지시로 주체 전환, 2026-07-02)**: 인수 9/9 자체 체크, 게이트 lint/typecheck(3종)/test **87파일·645 passed** green. wire 실측은 원격 환경 제한(중첩 claude 서브프로세스 SIGKILL)으로 불가 → 사용자 승인 하 **방어 구현**(SDK sdk.d.ts 타이핑으로 compact_boundary shape·forkSession 옵션 확정). 구현자 조정 3건(plan [구현자 기입]): `TurnRequest.forkFrom?: string`(boolean 대체) · `message.user` 에코 variant(자동 메시지 표시, 재량 지점) · "상시 노출"=도넛 Tier2(TelemetryPanel 팝오버) 하단 액션+StatusPopover 병행. ⚠️ 2건 보고(handoff 도착 세션 title 초기값 UX · compact 요약 텍스트 스트림 가시성 — 실기 확인 필요). **r2(실기 피드백 4건, 사용자 확정 반영)**: ① StatusPopover 단일 권장 액션(warn=`/compact` 전송 실구현·danger=핸드오프, 새 대화 버튼 제거) ② 멈춤/이동불가 수정 — 라우트 가드 소스-URL 한정 + entry 없는 sessionId 이벤트의 pending draft 폴백 라우팅 + 빈 draft PendingAssistant + startHandoff 큐 거부 + 통합테스트(send.continuity) ③ fork 아이콘 마지막 어시스턴트 턴 한정 ④ LineageBanner(원본 링크, sessionLoad lineage 복원). 게이트 647 passed. **r3(실기 피드백 4건)**: 도넛 팝오버 핸드오프 제거(기준 8 "상시 노출" 사용자 피드백으로 폐기) · **PostCompact hook**(사용자 지정) 으로 compact_summary 를 assistant 메시지로 승격(`withPostCompactHook`, manual 만) · `[분기]/[핸드오프] <원본 제목>` 초기 제목(initialTitle + 자동 제목 억제 + 렌더러 draft 즉시 반영). 게이트 650 passed. **r4(실기 피드백 4건, 2026-07-03)**: CLI 번들 실측(v0.3.143)으로 compact 내부 확정 — ① `/compact <지시문>` 은 **작동하나 加算**(기본 압축 프롬프트의 9섹션+`<analysis>/<summary>` 구조가 지배 — 결과 유사는 정상, 템플릿 축약은 OQ) ② 에코를 send 수리 직후(sessionId 없는 `message.user`→pendingNewChatKey 라우팅)로 이동해 [user→inflight→요약] 순서를 SDK init 지연과 무관하게 보장(coordinator 에코·`echoUserText` 폐기, 요약 드레인 sessionId 확정 전 보류) ③ `extractCompactSummary` 로 `<summary>` 내용만 표시(compact_summary 는 원문 전체 — 실측) ④ continuity draft 를 nav '최근 대화' 에 즉시 노출(`useContinuityDraftRows`+`DraftSessionRow` 주입, 이탈 생존·행 삭제·같은 부모 재생성 교체, rename 불가). 게이트 659 passed(88파일). **r5(실기 피드백 3건, 2026-07-03)**: ① compact 완료 후 도넛/경고 고착 수정 — 압축 턴 telemetry 의 컨텍스트 3종이 압축 _전_ 요약 요청 입력이라 `claude-map` 이 경계에서 스냅샷 무효화 + 압축 후 실측 부재 시 요약 크기(출력 토큰)로 근사(재로드 turn_usage 까지 정합, 비용 원장 무손실) ② '원본 열기' 즉시 동작 — draft 위 URL=/chat/<부모> 라 navigate no-op + 라우트 가드 차단 → LineageBanner 가 부모 행 클릭과 동형으로 `loadSession` 직접 전환 ③ fork 세션 '분기된 지점' 구분선 — `fork_boundary` 파트 신설(compact 동형): 물질화 시 복사 이력 뒤 영속 + draft 프리필 합성 + `ForkBoundaryMarker` 렌더(handoff 현행 유지). 게이트 662 passed(88파일). 검증(verify) 대기. **포팅(2026-07-04, Claude)**: 원 구현은 구(pre-0062) 구조 브랜치 `claude/handoff-62-feedback-tp8t9j` 에서 번호 0062 로 진행됐으나 main 의 0062(main-feature-slices)·0063 선점으로 **0064 로 재번호** 후 신구조에 이식 — db→infra/db·persist→features/history/writer(도착 물질화는 구조적 포트 주입)·send→app/chat-turn·orchestration→features/orchestration·turn-context→contracts/turn. **검증 PASS(r1, 2026-07-04) — 인수 9/9(기준 8 은 r3 supersede 기준) + 포팅 정합 6/6, 게이트 lint/typecheck(3종)/test 675 passed(90파일, electron 2 suite 포함) green, 신규 의존성 0·신규 IPC 채널 0.** 구현자 ⚠️ 2건(#6 title 초기값·#7 요약 가시성)은 r3/r4 에서 코드로 해소 확인 — 실기 확인만 잔여. PHASES 승격. 사람 확인 대기: r5 재테스트 체크 3종(도넛 하락·원본 열기·분기 구분선)+실기 E2E(wire log)·핸드오프 템플릿 축약 OQ·PR 머지. 상세=`verify.md`. |

| `0065-nav-draft-unify-simplify` | verify | PASS | — | `2e5b142` | 1 | **0064 후속 3건(사용자 지시 2026-07-04) — 비기능+소규모 UX(Claude 직접).** ① **r5 도넛 고착 결함 실측·수정**: 로컬 실기(CDP+wireLog)로 재현 — compact 턴 `result.usage` 전부 0(요약 사용량은 modelUsage 에만 계상), r5 근사 전제 붕괴 → 원장·renderer 게이트 이중 스킵 → 고착. 수정: claude-map 근사 폴백=modelUsage 출력 합 + chatReducer `session.compacted` 시 stale lastTelemetry 클리어. 실측 확증 도넛 8%→1%·재로드 정합. 원본 열기·분기 구분선은 실기 정상. ② 단순화: fork/handoff draft 시드 dedup(`continuityDraftSession`)·훅 일반화(`useDraftSessionRows`/`useActiveDraftKey`)·SessionRow 메뉴 0 이면 kebab 미렌더. ③ **'새 대화' nav 즉시 노출 통일**: 활성 새-대화 슬롯을 draft 행 경로로 최상단 노출(kebab 無·이탈 시 소멸·물질화 시 DB 행 교체) — 이탈 생존 라이프사이클은 continuity 전용 유지(빈 새 대화=보존 상태 없음). 게이트 677 passed(90파일·신규 2). 신규 의존성·IPC·DB 0. 사람 확인 대기: 핸드오프 도착 세션 도넛 실기·StatusPopover warn /compact 실기. 상세=`{plan,verify}.md`. |

| `0066-session-pending-message-queue` | verify | PASS | — | `699a6c1` | 1 | **검증 PASS(r1) — 인수 8/8, 게이트 lint/typecheck(3종)/test 678 passed(90파일) green, 신규 의존성·IPC·DB 0.** 브랜치 `claude/handoff-59-steer-queue-kavl7q` 마이그레이션 판정 + 세션별 pending message queue 개념 승격(비기능=Claude 직접, 사용자 지시 2026-07-04). **마이그레이션 판정 = 이식할 것 없음**: 대상 브랜치 4 커밋(aaa868d·90e49f5·bd2dbcc·917b613)은 전부 main 에 patch-equivalent 재착지(`git cherry` 전건 `-`, main 대응 16c2c62·7d7cfe3·646f26b·e0f9305)이고 후속 D3·D4(`55b5127`)·D5(`a115bc1`)까지 main 이 앞섬 — 0062 재구조화로 `features/chat/` 에 배치 완료 상태였음. 실작업 = ① `SteerQueue`→**`PendingMessageQueue`** 개념 일반화(모듈 헤더 명문화 — 사용자 턴=일반 메시지 즉시 커밋(drainAll 이월 포함)·어시스턴트 턴=예약 held→PostToolBatch flush→echo 커밋·renderer 간접 관찰(steer._)·취소=held 한정) ② `drainForFlush`→`drainAll`·데드 `hasConsumed` 제거(`rg` 0건) ③ admission `queue`/`steer` 예약 seam 폐기(`AdmissionDecision`=accept\|reject — 0056 framework-only seam 의 실체는 explicit chat:steer+본 큐가 이미 대체) ④ 경계 무변경(변경 11파일 전부 `src/main/**`·`takeSteerFlush`/`SteerFlushBatch`/`steer._` 경계 어휘 보존=0061 F2 선례). PHASES 승격. 사람 확인 대기: "통로" 해석(물리 enqueue 강제 아닌 즉시-커밋 경로+개념 명문화 = simple is best) 승인·steer 실기 회귀. 상세=`{plan,verify}.md`. |

| `0067-long-lived-session-queue` | verify | PASS | — | W1 `d124c05`·W2 `f079945`·W3 `90d501b`·W4 `8afe764` | 1 | **Long-lived 세션 채널 전환 + Pending Message Queue 완전 일원화 (구조 전환 = Claude 직접, 사용자 확정 2026-07-05).** 확정: ① long-lived 직행(세션 종료 = 프로그램 종료/LRU 축출만 — 매 턴 resume 폐기) ② 동시 런타임 cap **5**(LRU evict-idle, IdleCloseTimer 폐기) ③ 모든 프롬프트 큐 경유(구조 페이로드 — content blocks+첨부, `chat:steer` 채널을 `chat:send` 로 흡수, 채널 54→53) ④ renderer pending-first(전 사용자 메시지 연회색+기울임 → echo 커밋 승격, `message.queued/committed/cancelled` 일반화) ⑤ 중단 버튼 = interrupt(채널 생존)+held 전량 취소+composer draft 복원 ⑥ clientKey(draft UUID)로 세션 키 발급 전 라우팅. 훅 2계층 확정: 주입 제어(PostToolBatch flush 게이트 + UserPromptSubmit 프레임 오픈 신호) / 커밋 관측(echo 단일 — uuid 보존·판정-후 방출). 핵심 구조물: SessionRuntime 프레임 demux(1 스트림 다중 턴, "1 프레임=1 턴" 코디네이터 모델 보존)·자동 연속 턴(턴 종료 시 held 잔여 push→CLI 픽업)·admission 삭제(0056 supersede). **검증 PASS(r1, 코드) — 인수 12/12, 게이트 lint 0·typecheck 3종 0·test 685/685(88파일)·build green, 신규 의존성 0.** 구현 선조치 5건(취소 신호 채널 분리·draining 중 send=respawn·releaseRuntime interrupting 보존·takeForRespawn 소비분 폐기·중단 draft 이중 복원 방지 — plan [구현자 기입]). ⚠️ CLI 자동 픽업(C7/C8 잔존) 자동 프레임 오픈은 비의존 설계로 미배선(버퍼 무유실 합류) — wire 실측 후 필요 시 후속. PHASES 승격. **사람 확인 대기(1순위)**: 실기 wire 실측 4건(턴별 result 프레이밍·자동 연속 픽업·interrupt 생존·5 프로세스 메모리) + UX 6종(pending→committed·도구 턴 steer·자동 연속·취소 후 재사용·LRU 축출·재로드 정렬)·PR 머지. 상세=`{plan,verify}.md`. |

| `0068-turn-open-optimistic-commit` | verify | PASS | — | impl `a881d77` | 1 | **0067 실기 버그픽스(비기능=Claude 직접) — 사용자 리포트 2건**: ① 랜딩→transcript 전환 지연 ② 턴-시작 메시지가 답변 중에도 pending 표시 + echo 커밋 시 위치 점프. 근본 원인 = pending-first 를 턴-시작 메시지까지 적용 + `messages` 커밋을 echo 까지 지연(0067 실측 리스크 ② "echo sub-second" 전제 실기 반증). 교정: **턴-시작 메시지 = renderer 낙관 커밋(clientId 합류·reducer 멱등·invoke 거부 롤백), steer 예약·핸드오프 자동 메시지만 pending 유지** — main echo 커밋 경로(DB 정본)·IPC 스키마 불변, 랜딩 게이트 `!inflight` 이중 방어. + **wire 계측 2점**(echo↔훅 논쟁 판정용): `input.echo` 관측 로그·UserPromptSubmit/PostToolBatch 훅 tap(`[wire]` 토글 연동, `infra/ipc/wire-log.ts` electron 비의존 분리). **검증 PASS(r1 코드)** — 인수 8/8, 게이트 lint 0·typecheck 3종 0·test **687/687(88파일)**·build green, 신규 의존성·IPC 0. 사람 확인 대기(1순위): 버그 재현 소멸 실기 + **wire 로그 판독**(push 프롬프트 UserPromptSubmit 발화 여부·echo 순서) → main 커밋 신호 echo→훅 교체 Open Question 판정. 상세=`{plan,verify}.md`. |

| `0069-turn-open-consume-on-response` | verify | PASS | — | impl `fc15866` | 1 | **0068 wire 실측 후속(비기능=Claude 직접, 사용자 확정 "a로 진행")** — 실측 확정: UserPromptSubmit 은 push 프롬프트에 발화하나 **uuid 부재 + init 이전 발화**(새 세션 rekey 전 = 훅-커밋 불가), echo 는 uuid 보존하나 **모델 출력 시작 이후 도착**(도구-first 턴 재로드 정렬의 좁은 창). 교정: **턴-시작 배치(프롬프트+프렐류드) = 첫 모델 출력(MODEL_OUTPUT_EVENTS 앵커)이 소비 증거 — echo 불요**, user row 가 항상 첫 영속 어시스턴트 파트보다 먼저 커밋. **steer(mid-turn 게이트 flush) = echo uuid 매칭 유지**(응답 진행은 소비 증거가 못 됨, D2). 늦은 echo 는 consumed 매칭 실패로 무해. 부수 이득: echo 없는 mock 턴도 user row 영속 성립. **0068 OQ(커밋 신호 echo→훅 교체) 종결** — 훅은 주입 제어 계층 확정. 검증 PASS(r1 코드) 인수 5/5, 게이트 lint 0·typecheck 3종 0·test **693/693(88파일)**·build green. 사람 확인 대기: 실기 회귀(도구-first 재로드 정렬·steer 승격·block 케이스). 위생: dev 앱 실행 중 better-sqlite3 잠금 → rename 우회, **dev 재시작 시 `npm run postinstall` 필요**(0019 미구현 실비용). 상세=`{plan,verify}.md`. |
| `0070-drop-hook-wire-tap` | verify | PASS | — | impl(본 정리 커밋) | 1 | **0069 후속 정리(`/simplify`, 비기능=Claude 직접)** — 0068 이 심은 wire 계측 중 **목적 종결분 제거**. `makeHookWireTap()`(UserPromptSubmit/PostToolBatch 관측 훅)은 두 판정 질문((a) push 프롬프트 UserPromptSubmit 발화 여부 (b) echo↔스트림 순서)만을 위한 일회성 실측 장치였고, **0069 가 그 OQ 를 종결**했다 — 그럼에도 매 턴 세션 hook 설정에 콜백을 등록 중이었다. 제거: `makeHookWireTap`(claude-adapt) + 어댑터 배선(claude.ts) + 고아 `wireLog` import. **남김**(진단 가치·열린 검증): `input.echo` 로그(steer echo 승격 진단의 유일 창 — renderer 미전달이라 일반 `[wire]` 미포착)·`wire-log.ts`(0025 디버그 패널)·`HookCallback` import(잔존 사용). 동작 불변(tap 은 `{}` 반환 관측 전용). 검증 PASS(r1) 인수 5/5, lint 0·typecheck 3종 0·영향 스위트 59/59 green(DB 3스위트 실패는 사전존재 better-sqlite3 ABI, 무관). 상세=`{plan,verify}.md`. |

| `0071-backend-arch-runtime-doc-sync` | verify | PASS | — | (문서 — push 후) | 1 | **백엔드 아키텍처 문서를 0062 main 재편 + 0066~0070 런타임 재작업에 정합(비기능=문서=Claude 직접, 사용자 요청 2건: ①백엔드 arch 문서 ②참조 문서 갱신).** `runtime-ipc.md` §1(단일 inflight 폐기→멀티세션·세션별 pending message queue held/flushed/consumed·장수명 세션 채널 프레임·RuntimeSupervisor cap/LRU·TurnCoordinator 2 sink)·§2(채널 31→SSOT 인용 53·`infra/ipc/handle` 헬퍼·`turn.event` 버스 파이프라인) 재작성. `overview.md` §3 5-슬라이스 트리(`app/·features/·adapters/·contracts/·infra/`)·§3.1 Bootstrap 부트·§4 상태표(멀티세션 ✅·`ClaudeAdapter`·`ExtensionBuilder`·경로 정정·Python런타임 행 삭제). `adapters.md`/`terms.md` CapabilityBuilder→ExtensionBuilder(구명 병기). 참조: `docs/AGENTS.md`(40→53 채널)·`ARCHITECTURE.md`·`ux-domains.md`(채널 SSOT+링크)·`TRD.md`(멀티세션 anchor). 검증 PASS(r1) 인수 **7/7**, 코드 게이트 N/A(문서)·정합 grep 0·링크 유효. 비범위 후속(파생 D1·D2): 빈 레거시 디렉토리(`ipc/`·`orchestration/`) 코드 정리·`claude-code`/`ChatEvent` 전역 개명. 상세=`{plan,verify}.md`. |

| `0072-login-sso-gate` | verify | PASS | — | (push 후) | 1 | **로그인 랜딩 + SSO 부팅 게이트(신규 UI, 라이브 Codex 부재로 Claude 직접 구현, 사용자 확정 4건 + 추가 UX 3건).** 앱 부팅 시 `RootGate`(라우터 앞) 가 미인증·bypass off 면 사이드바 없는 풀-프레임 `LoginFrame`(슬림 타이틀바 재사용 + 중앙 오르카 이미지=제목 대체 + 카드 검정 `SSO로 로그인` 1버튼) 렌더. SSO=`features/login` Zustand store — `runSso` **항상 실패**(~800ms 후 throw) → 버튼 위 `text-bad` 메시지, inflight 시 "로그인 중"+`animate-spin`, 성공 경로(`authenticated`+`/new`)는 향후 실로직 교체용 완비. 디버그 패널 "SSO 로그인" 그룹(신규 `PanelButton`) — **bypass 토글**(`Settings.ssoBypass` **영속** — debugMock 비영속이라 이전, store 공유로 게이트 즉시 반응 + 재시작 스킵) + **SSO 개발 버튼**(console.log). 경계 해소: `DebugPanel` `ssoSection?` 슬롯 prop 을 app 레이어(OverlayLayer·LoginFrame)가 주입(features 교차 import 회피). 로그인 화면에도 DebugPanel 렌더(항상-실패 갇힘 방지). **신규 IPC 채널 0**(Settings 필드만). 검증 PASS(r1) 인수 **8/8**, 게이트 lint 0·typecheck 3종·test **682**·build green(파일-레벨 3 실패=electron 바이너리 차단 환경 제한, 무관). 오르카 = 첨부 근사 픽셀아트 PNG(원본 교체는 사용자). 사람 확인 대기: 실 `npm run dev` 시각/드래그 · 오르카 원본 교체. 상세=`{plan,verify}.md`. |
| `0074-workspace-isolation-guide` | verify | PASS | — | `a6bc047` (가이드) | 1 | **Agent SDK 코드레벨 workspace 격리 권한 구성 가이드(범용 독립 문서, 비기능=Claude 직접 plan→impl→verify, PR #192).** 첨부 초안(`dontAsk`+PreToolUse 훅)을 SDK 문서 대조·검증 후 재설계: **격리를 `dontAsk` 가 아니라 PreToolUse 훅(모드-독립)에 둔다** — `dontAsk` 는 `canUseTool` 을 건너뛰어 `AskUserQuestion`·`ExitPlanMode`·승인 카드를 자동 거부(사용자 정정: 대화·자동진행 흐름 유지). 훅은 밖=`deny`, 안·read예외=**pass-through(`{}`)** 로 하위 승인·모드 로직 보존(초안의 `allow` 는 mode·canUseTool 우회라 폐기). read 예외(`~/.claude`·`~/.config/orca`·런타임, 최소권한 test-first) + `additionalDirectories` `[]` 기본·옵션/훅 동일 배열 공유 + 모드별 격리 유지 표 + Bash 정적격리 한계 명시. 출처 사이트 전량 표기(agent-sdk/permissions·user-input·settings·hooks + 초안 + 내부 근거). 코드 게이트 N/A(문서), 정합성 W1 원문 대조 모순 0. 범용 독립이라 PHASES 미승격·문서 인벤토리 미등록. 사람 확인 대기: 배치 위치(`docs/guides/`)·대상 앱 §7 체크리스트 실측·PR 머지. 상세=`{plan,verify}.md`. |
| `0073-system-prompt-structured-header` | verify | PASS | — | (push 후) | 2 | **구조화 시스템 프롬프트 헤더 매 턴 주입(비기능+소기능 = Claude 직접, 사용자 결정 2건).** **r2**: 프로젝트 name·지침을 `# Project` 섹션 안에 함께 포맷화(`Active project:` + `Project instructions:\n<본문>`), projectName 부재면 둘 다 생략. `buildSystemHeader(projectInstructions?)` + `append=헤더 단일 문자열`(별도 join 제거). extensions 41/41(+queries 55). **r1**: `claude_code` preset append 앞단에 `# Orca`(정체성 framing="Windows desktop app"+`app.getVersion()`)·`# User`(`settings.language` 신규·`settings.accountInstructions`)·`# Project`(프로젝트 name) 헤더를 순수 `buildSystemHeader`(신규 `features/extensions/system-header.ts`)로 조립 → `[header, instructions].join('\n\n')`. 주입 **메커니즘 무변경**(preset+append), append **내용**만 확장. `accountInstructions` 미배선("추후") 해소 + `language` 설정 신규(default `한국어`, 스키마 2곳). `getProjectInstructionsForSession`→`getProjectContextForSession`(name 포함). 실행환경(cwd/platform/date)은 preset 동적섹션 존치라 재주입 안 함. **신규 IPC 채널 0**(Settings `language` 필드만). 무캐시 유지(계정/프로젝트 지침 편집 다음 메시지 즉시 반영). 검증 PASS 인수 **7/7**, 게이트 typecheck 3종·lint(경계 0)·test **690**(신규 8: system-header 5+builder 3, 실 sqlite 조립). 3 test 파일 실패=electron 바이너리 차단(무관). 문서: system-prompt.md §2A 신설·§2 HISTORICAL·§3 갱신·IPC_CONTRACT. 사람 확인 대기: 실 `npm run dev` 헤더 육안·계정지침 세션중 편집 반영. 상세=`{plan,verify}.md`. |

| `0075-workspace-isolation-perms` | verify | PASS | — | `389cbe1`(+r2·r3) | 1+r2·r3 | **74 가이드를 Orca 어댑터에 실제 배선(비기능 보안 하드닝 = Claude 직접 plan→impl→verify). r2: `~/.claude` 를 read-only→write 예외로(plan 산출물·skill 설치). r3: 실효 없는 `screenBashCommand` 제거 + opencode 참고 `# Tools` 시스템 프롬프트 정책 추가(전용툴 라우팅 + Bash workspace 스코프 유도) — 격리는 구조 파일툴=가드 강제/Bash=프롬프트 유도. 사용자 요청.** sandbox 없이 작업 폴더 밖 r/w 를 막는 **PreToolUse 워크스페이스 가드 훅**을 `claude.ts sendMessage` 옵션에 배선(`makeSteerGateHook` 동형 조각 → `mergeHooks`). 격리=모드 독립(훅 평가 1순위), 안·read예외=**pass-through(`{}`)** 로 기존 `makeCanUseTool` 승인 카드·plan/acceptEdits 보존(`allow` 미사용). read 예외(`~/.claude`·`~/.config/orca`·런타임)=read 허용/write 차단, **단 세션 cwd(`projects/<...>`)는 writeRoots 라 write 허용**(사용자 정정). `additionalDirectories` `[]` 기본·옵션/훅 동일 배열. 사용자 결정: 경로 격리만(disallowedTools 미도입)·read 예외 처음부터 포함. `isWithinDir`/`orcaConfigDir` 재사용, `runCompletion`(도구 0) 제외. 신규 `workspace-guard.ts`+테스트. |

| `0076-menu-material-icons` | verify | PASS | — | (push 후) | 1 | **메뉴/앱 전역 아이콘을 Google Material 로 교체(비기능 시각 자산 = Claude 직접 plan→impl→verify, 사용자 결정 2건).** 범위=브랜드 로고(`OrcaLogo`)·로그인 PNG 제외 전 아이콘, 방식=인라인 SVG path 교체(새 의존성 0). `shared/ui/Icon.tsx` 를 stroke 기반(16×16)→**Material Symbols Outlined**(fill 기반·viewBox `0 -960 960 960`)로 전환: 렌더를 `fill={color}`(기본 currentColor)로 바꾸고 `IconProps` 에서 데드 `stroke`/`fill` prop 제거, `IconName` 50종 유지로 58 사용처 무회귀(리네임 0). `ICONS` 50종을 gstatic Material Symbols Outlined 원본에서 스크립트 추출(50/50 검증)해 교체+glyph 주석. 유일 `stroke` 사용처(`CustomizeLanding` briefcase) 1줄 정리. name→glyph: plus→add·x→close·cpu→memory·cam→photo_camera·board→dashboard·capture→center_focus_strong·alert→warning·kebab→more_vert·fork→fork_right·sun/moon→light_mode/dark_mode 등. **검증 PASS(r1) — 인수 7/7, 게이트 lint 0·typecheck 3종 0·test 700/723**(23 red=better-sqlite3 ABI 환경 4파일, 순수 renderer 변경 무관·0019 계열), 레이어 경계 0(shared/ui+skills 내부), 신규 의존성·IPC·DB 0. **후속(2026-07-06)**: 언어 항목 `chat`→신규 `globe`(Material language)·설정 사용량 탭 `bolt`→신규 `chart`(bar_chart), glyph 50→52, typecheck/lint ✅. 사람 확인 대기: `npm run dev` 아이콘 시각 톤/무게(Outlined vs 기존 stroke)·capture/fork/board 매핑 어감 승인. 상세=`{plan,verify}.md`. |

| `0077-boot-orchestration` | verify | PASS | — | `dee1e5b` | 2 | **검증 PASS — 인수 7/7(선택 6 포함) 충족.** 게이트 lint 0·typecheck 3종 0·test **723/723**(runnable; 로드 실패 3 스위트는 electron 바이너리 프록시 차단 환경 제한·0077 무관, 구현자 env 734), 레이어 경계 0(오케스트레이터=`app/` 셸→feature `init*` 하향), 신규 런타임 의존성 0. 로그인 게이트 통과 후 랜딩 전 `BootScreen`(spinner-only·`role=status`·reduced-motion) 게이트(`RootGate` 3단) + 단계별 `[boot] <id> running/ok(Nms)/warn` 콘솔 + 유일 필수 `landing-target` 실패→`LoginFrame` 에러배너/재시도 + 비필수 degrade 계속 + landingTarget SSOT(이중 IPC 제거·신규 라우트 0) + store `init*`/`subscribe*` 분리(이중 fetch 회피) + main `BootReport` 완료 스냅샷 IPC(`orca:boot:report`, IPC_CONTRACT 54→55 §2.1-b). 구현자 편차(plan 카탈로그 `sessions` mandatory→구현 non-mandatory)는 최소필수 원칙으로 **타당**. **비차단 파생 이슈 3건**(O1 비필수 단계가 AppLayout 게이팅=랜딩 지연 가능·제품 UX 사용자 결정 / O2 dead code `bootstrap*` 4 / O3 docs/AGENTS.md 채널수 사전 드리프트). PHASES 승격. 사람 확인 대기: `npm run dev` BootScreen 시각·에러/재시도 실기·O1 게이팅 granularity·시작 SLA 수치(OQ6). 상세=`{plan,verify}.md`. |

| `0078-skills-deployment-pipeline` | verify | PASS | — | `9dc21d3` | 1 | **검증 PASS — 인수 11/11 충족.** 게이트 lint 0·typecheck 3종 0·targeted 10/10·full **712 runnable green**(21 red=better-sqlite3/electron 바이너리 403 환경 제한·0078 무관·0019 계열), 레이어 경계 0, 신규 의존성·IPC 0. 구현자 선조치 4건(marker traversal 방어·manifest/marker 런타임 검증·실복사 builtin만 marker 기록·`resolveBuiltinSkillsDir` testable seam) 모두 plan 강화. 위생 노트①: 구현 커밋 본문 리터럴 `\n\n` 로 trailer 파싱 불가(코드 무관·소급 미수정). PHASES 승격. 사람 확인 대기: packaged 산출물 실번들·후속 실 skill 배송 시 seed→deploy→scan 실기. Skills 배포 파이프라인 완성 (기능 = Codex 구현). 앱에 번들된 기본 skill 을 최초 부팅/버전 업 시 `~/.config/orca/sources/skills` 로 seed → 기존 `extension-deploy` 가 같은 부팅에서 `dist/<engine>/plugins/orca/skills` 로 배포. 사용자 결정 2건: ① **빈 파이프라인**(builtin skill 0개, `manifest.json` `skills:[]` — 배포 코드/빌드만 완성) ② **버전 올라가면 덮어쓰기**(manifest `version`≠marker 면 builtin 덮어쓰기+prune, non-builtin 사용자 skill 은 절대 미영향). 신규: `resources/builtin/skills/manifest.json`·`features/extensions/skills/seed.ts`(+test, root 주입 순수·`scaffold.ts` 동형)·`bootstrap.ts` `builtin-skill-seed` 스텝(config-dir 후·extension-deploy 전, `critical:false`)+`builtinSkillsDir()`(`app.isPackaged` 분기)·`electron-builder.yml` `extraResources`(resources/builtin→builtin)+asar 중복 제외. 신규 의존성·IPC 0. 구현 완료: marker traversal 방어·manifest/marker 런타임 검증·실복사 builtin만 marker 기록·dev/packaged 경로 helper 테스트 포함. 게이트 lint/typecheck/test 744 ✅, build:unpack 은 Electron 다운로드 403 환경 제한으로 packaging 산출 확인 보류. 상세=`plan.md`. |

| `0079-usage-limits-donut-panel` | verify | PASS | — | (push 후) | 1 | **검증 PASS — 인수 9/9, 게이트 lint 0·typecheck 3종 0·test 745 passed.** 사용량 한도 UI(신규 기능 = Claude 직접, 사용자 지시 + Q&A 3건). ① 설정→사용량 "월간 사용 한도"(초기 $90·null=무제한) 1-depth 조정 뷰(`useTweaks` 반응형) ② Composer 도넛 팝오버 `TelemetryPanel`→`UsagePanel`(컨텍스트 프로그레스바+구분선+주간/월간 한도 바+`>` 설정 이동, 캐시 분해 행 제거) ③ 실사용량 SSOT 단일화 — main `UsageTracker`→`useCostSummary` 를 shared 순수 `computeUsageLimits`(유일 계산 지점)로 파생, 설정·도넛이 공용 `useUsageLimits` 참조. 주간 예산=남은한도÷이달남은일×주남은일(사용자 제안, 분모=이번주 envelope 로 완성·워크드 예시 31%/33% 테스트). 공용 시간 유틸 `shared/time/{clock,resetLabels}`(main boundaries 재노출·OS 로컬). 외부 보정=seam(no-op)만·후속. 설정 열림 전역 스토어. 신규 IPC/의존성 0, 레이어 경계 0. test 3 suite fail=electron 바이너리 403 환경 제한(0050 계열·무관). 사람 확인 대기: `npm run dev` 도넛/설정 시각·한도 편집 동시 갱신·무제한 표기 실기·PR 머지. 상세=`0079-usage-limits-donut-panel/{plan,verify}.md`. |

| `0080-usage-feedback-colors-sync-providers` | verify | PASS | — | (push 후) | 1 | **검증 PASS — 인수 10/10, 게이트 lint 0·typecheck 3종 0·test 753/753 runnable green.** 0079 사용량 UI 후속 피드백 4건(비기능+소기능 = Claude 직접, 사용자 Q&A 2건 확정). ① 프로그레스바 색 **파랑→노랑→빨강**(`Meter` low tone `good`→`info`=`bg-indigo`·`UsageCircle` low→`--color-indigo`, 임계 0.6/0.85 유지) ② 설정 사용량 한도 하단 **동기화 버튼**(`SyncRow` "마지막 업데이트: <상대시각>"+새로고침 `animate-spin`·`useCostRefresh` 30s 틱·`costStore.refreshCost` refreshing 가드·`cost:summary` 핸들러 recompute 전환·순수 `relativeTimeLabel`+test) ③ 도넛 `UsagePanel` 사용량 한도 **`>` 아이콘 버튼만 클릭**(라벨 비클릭 분리) ④ 설정 사용량 하위 **provider 서브항목**(agentStore 파생 nav) — 각자 실사용 한도 바+월 한도 설정. **실 provider별 집계**(`turn_usage ⨝ sessions.provider_key`, NULL 세션 비귀속) + **provider별 독립 한도**(신규 migration `0012_provider_limits` · `sumUsageByBoundariesForProvider`/`getProviderLimit`/`setProviderLimit` upsert +test 3건 · `UsageTracker.providerSummary` 미캐시) + 전역 '월간 사용 한도' 유지. IPC cost 2→4(`providerSummaries`·`setProviderLimit`, `ProviderUsageEntry`·zod·preload·costApi·IPC_CONTRACT 57채널). 교차-feature 0(settings 구조적 `ProviderUsageController` 로컬 선언, app `SidebarUserButton` 이 `useCostRefresh`·`useProviderUsage` 주입) · 신규 의존성 0 · `Icon` className 추가. test 3 suite fail=electron 바이너리 403 환경 제한(0050 계열·무관). 사람 확인 대기: `npm run dev` 색상/동기화 spin/provider 서브탭 시각·다중 provider 실집계·PR 머지. 상세=`0080-usage-feedback-colors-sync-providers/{plan,verify}.md`. |

| `0081-usage-cost-placeholder-provider-nav` | verify | PASS | — | (push 후) | 1 | **검증 PASS — 인수 6/6, 게이트 lint 0·typecheck 3종 0·test 753/753 runnable green.** 0080 사용량 UI 후속 피드백 3건(비기능+소기능 = Claude 직접). ① 진행바 파랑 **#4a5b8c→#2a78d6**(`--color-indigo` 값만 교체·사용처 `Meter`/`UsageCircle` 2곳 국소·임계/warn 무변) ② 도넛 '사용량 한도' `>` 클릭 시 **현재 composer provider 서브탭으로 이동**(`onOpenUsageSettings(providerKey?)` 시그니처 확장·`Composer` 세션 `providerKey` 전달·3 페이지 `openSettings(key?providerTabId(key):'usage')`·`providerTabId` 배럴 노출·null/미구성 폴백=전역 탭) ③ 전역 '사용량' 탭을 **Claude Code /cost 유사 플레이스홀더**로 교체 — 기존 한도 바·동기화·"한도 설정" 전부 제거(provider 하위 탭으로 이관됨), 안내문구+"추후 구현 예정" 표시. `UsageTab` props(`usageLimits`/`costRefresh`) 제거·`SettingsModal`·`SidebarUserButton` 주입 정리(unused 0), `SyncRow`/`CostRefreshView` 는 `ProviderUsageTab` 재사용 위해 유지. 신규 IPC/DB/의존성 0, 레이어 경계 0. test 3 suite fail=electron 바이너리 403 환경 제한(0050/0080 계열·무관). 사람 확인 대기: `npm run dev` 새 파랑 색·도넛→provider 탭 이동·/cost 플레이스홀더 시각·PR 머지. 상세=`0081-usage-cost-placeholder-provider-nav/{plan,verify}.md`. |

| `0082-donut-provider-aware-usage` | verify | PASS | — | (push 후) | 1 | **검증 PASS — 인수 5/5, 게이트 lint 0·typecheck 3종 0·test 753/753 runnable green.** 0081 후속 피드백 1건: "도넛이 모델 선택에 따라 주간/월간 사용율이 갱신 안 됨"(비기능 버그수정+소기능 = Claude 직접). **원인**: 도넛 `usageLimits` 가 `useUsageLimits()`(**전역** 계정 summary+전역 월 한도)로 파생돼 세션 선택 provider(`providerKey`)와 무관. **수정**: 신규 `useProviderUsageLimits(providerKey)`(`features/cost/hooks/`) — provider 있으면 `costApi.providerSummaries([key])` 로 그 provider `ProviderUsageEntry` 조회→`computeUsageLimits(summary, limitUsd)`, 없으면(또는 조회 대기/불일치) 전역 폴백, `costStore.lastUpdatedAt`(턴 종료) 시 재조회. 3 페이지(`ChatPage`+`useChatSession` import·`NewChatLandingPage`·`ProjectLandingPage`)가 세션 `providerKey` 로 파생 주입(`useUsageLimits`→`useProviderUsageLimits`). 배럴 노출·레이어 경계 0(파생은 page). 선조치: 초기 effect 내 동기 `setEntry(null)` 이 `react-hooks/set-state-in-effect` 위반→제거하고 memo 가드(`entry.providerKey===providerKey`)로 폴백 흡수(동작 동일). 신규 IPC/DB/의존성 0. test 3 suite fail=electron 바이너리 403 환경 제한(무관). 사람 확인 대기: `npm run dev` 모델/provider 전환 시 도넛 갱신 실기·PR #206 머지. 상세=`0082-donut-provider-aware-usage/{plan,verify}.md`. |

| `0083-ui-polish-header-nav-transcript` | verify | PASS | — | `aa70413` | 1 | **검증 PASS(r1) — 인수 13/13, 게이트 lint 0·typecheck 3종 0·test 753/753 runnable green.** 앱 헤더·nav·transcript·composer·프로젝트 UI polish(비기능 = Claude 직접). ① 헤더 햄버거 `버전`→중앙 모달(`__APP_VERSION__` Vite define·신규 IPC 0·blur, r2) ② 접힘 버튼/접힌 nav active 빨강(`bg-fill-selected`/`text-rust`)→nav 토큰(`bg-fill-uncontained-active text-t9`, `Button.squishClass`+`Sidebar`) ③ `자동화` nav 미노출+frontend docs sync ④ transcript 제목 `<프로젝트 버튼> / <제목>`+cwd 폴더 아이콘·검색 버튼 숨김·케밥 예약(`reserved1/2`) 필터 ⑤ 긴 토큰 오버플로 공통 `UserBubbleText`(`overflow-wrap:anywhere`, 사용자 결정=줄바꿈)×4 버블 ⑥ `YellowDot` 전면 제거(AssistantTurn·ApprovalCard·파일, rg 0) ⑦ composer 모드 설명 분리·idle placeholder `스킬을 보려면 /를 입력하세요.`(사용자 확정) ⑧ 프로젝트 파일 카드 전체 빗금 bg. **r2**: titlebar rename/delete 복구(**원 AC7 사용자 재결정** — page 계층 chat+sessions store 동시 갱신)·인라인 `RenameInput`·삭제 `ConfirmDialogHost`(shared 추출로 SessionRow 중복 제거)·collapsed brand. 신규 의존성/IPC/DB 0, 레이어 경계 0(features/chat↛projects/sessions=page 주입). 실패 3파일=electron 바이너리 403 환경 제한(0050 계열·렌더러+docs 변경과 무관). 초회 test better-sqlite3 ABI 24red 는 rebuild 후 해소(0019 계열). PHASES 승격. 사람 확인 대기: `npm run dev` 시각 검증 7건(버전 모달·nav active 톤·제목/폴더 아이콘·긴 URL 줄바꿈·인라인 rename·삭제 다이얼로그·파일 빗금)·모드 설명/placeholder 어감·PR 머지. 상세=`verify.md`. |

| `0084-app-distribution-auto-update` | verify | PASS | — | `71928c9` | 1 | **검증 PASS(r1) — 인수 6/6 충족, 게이트 typecheck 3종·lint 0·test 768 passed(신규 15 포함), 레이어 경계 0, 신규 의존성/IPC/`package.json`/`electron-builder.yml` 0.** 앱 설치 파일 배포 + 자동 업데이트 하드닝(기능 = Codex 구현). ① DB 다운그레이드 가드 `DbSchemaTooNewError`(code `DB_SCHEMA_TOO_NEW`, applied↔known 이름 **집합** diff·순서 무관·user_version 미도입)를 `applyMigrations` 진입부에 — `initDb`→bootstrap `stepSync('db-init',{critical:true})` 로 throw 전파해 부팅 차단 ② WAL-안전 백업 `createMigrationBackup`(`VACUUM INTO`·디스크 여유 선검사·**pending>0 && apply 전** 1회·`orca.db.backup.before-<ver>.<ts>`)·실패 시 마이그레이션 중단(테스트가 integrity_check+WAL row+적용전 `_migrations` 로 복구성 검증) ③ 점프-안전 회귀(first-6→전12 + `MIGRATION_NAMES` 이름/순서 고정) ④ settings 버전 마이그레이션 `migrateRawSettings`(per-key `recoverKnownSettings` 보존→무음 리셋 방지·`settingsVersion`/`lastAppVersion` 내부 raw 메타·공개 Settings/patch 미노출) ⑤ 안전 재시작 순수술어 `canRestartForUpdate`(생성/tool/DB write/인덱싱 중 false)+`Bootstrap.restartGateState()` seam(UI/IPC 표면 0, 0085 소비). **위생 노트 ①**: INDEX 기재 `fbee41a` 는 Codex 환경 phantom hash — 실 구현은 `71928c9`("docs" 제목이나 513 insertions 로 코드 전부 포함). test 3 suite fail=electron 바이너리 403 환경 제한(0050 계열·0084 무관, 정상환경 779). **파생 D1(0085 이월)**: `activeDbWriteCount` seam 은 현재 backup section 만 관측(광역 write 계측·인덱싱 subsystem 후속). PHASES 승격. 사람 확인 대기: 다중 점프/롤백 실기·재시작 술어 0085 소비·PR 머지. **비범위→후속 0085(OQ3)**: electron-updater 배선·서명·호스팅(generic vs GitHub Releases)·CI·채널·staged rollout·UI·텔레메트리(OQ4)·라이센스(OQ5)·SemVer·TRD §4 uv 드리프트. 상세=`0084-app-distribution-auto-update/{plan,verify}.md`. |

| `0085-auto-update-ux` | verify | PASS | — | `ef41e95` | 1 | **검증 PASS(r1) — 기능 인수 AC1–AC8 = 8/8 충족, 게이트 lint 0·typecheck 3종 0·test 773 runnable pass(3 suite=electron 바이너리 403 환경 제한·0050/0083 계열·코드 무관), 레이어 경계 0, 신규 의존성=승인된 `electron-updater@^6.8.9` 1건, 비밀 미저장.** 인앱 자동 업데이트 UX(기능 = Codex). ① electron-updater 배선(`autoDownload=false`·`autoInstallOnAppQuit=false`·6 이벤트 broadcast·확인 실패 비치명+noop 폴백) ② 시작 확인 1회(`index.ts` createWindow 뒤 `checkForUpdatesOnStartup`) ③ update IPC 6채널(invoke check/download/quitAndInstall+snapshot state·push state/progress, `handlers/update.ts`·`send.ts` broadcast, IPC_CONTRACT 57→**63**·도메인 18·§2.3-b 신설) ④ 렌더러 `features/update` store/provider(costStore 미러·mount 시 snapshot init 로 event 유실 복구)+preload `orca.update`+`updateApi`+`App.tsx` 합성 ⑤ 헤더 조건부 버튼(available/downloading/ready/installing) ⑥ OverlayLayer blocking 다이얼로그(ESC/backdrop close 없음·busy 중 닫기 미노출·Orca 토큰) ⑦ **idle-gated 설치**: `canRestartForUpdate(restartGateState())`(0084 seam) before/after **이중 게이트 재확인**+`installPending` 락(chat admission 거부)+턴 수 변화 시 `refreshGate` broadcast+비-idle 거부 사유 ⑧ 클린 종료=기존 `will-quit`→`shutdown()`+`closeDb()` 재사용. 배포호스트=GitHub Releases `muzaby/orca-skin`+`orca.json.update` override(사내/폐쇄망, 비밀 미저장), autoInstallOnAppQuit=false 사용자 확정. **AC9 부분충족(게이트 green + 테스트 열거 1/3)**: IPC 스키마·orca update 파싱 ✅, **store 순수변환·설치 게이트 분기 테스트 ✗ → 파생 D1(open)**(안전 크리티컬 무테스트, 후속 라운드/Claude 비기능 직접 권장). PHASES 승격. **비범위→OQ3 후속 release-ops**: 서명 인증서·`verifyUpdateCodeSignature`·CI·채널·staged rollout·텔레메트리(OQ4)·라이센스(OQ5)·SemVer. 사람 확인 대기: 패키지 실기(시작 확인→헤더 버튼→다이얼로그→다운로드→idle 설치)·시각 톤·실 GitHub Releases 피드·PR 머지. 상세=`0085-auto-update-ux/{plan,verify}.md`. 0084 하드닝 후속(기능 = Codex). 사용자 UX 4건: ① 앱 시작 시 확인 ② login→home(`/new`) 진입 시 헤더에 업데이트 버튼 노출(**새 버전 등록 시에만**) ③ 버튼 클릭 시 안내창+진행 ④ 설치는 **모든 SessionRuntime idle(+DB)** 일 때만. **핵심 seam 재사용**: `canRestartForUpdate(restartGateState())`(0084 `update-restart.ts`+`bootstrap.ts:259`) 가 곧 "모든 런타임 idle(=`supervisor.all().length===0`)+tool0+DB write0+인덱싱X" 게이트 — 신규 판정 불필요. **D1 해소 근거**: better-sqlite3 동기 모델상 `quitAndInstall`(메인 스레드) 시점 in-flight write 없음 + 턴 중 write 는 `isGenerating` 으로 차단 → 광역 write 계측 불필요. **인수(9)**: electron-updater 배선(autoDownload=false·확인 실패 비치명) · 시작 확인 · 업데이트 IPC(invoke check/download/quitAndInstall + push state/progress, `IPC_CONTRACT.md` 동시 갱신) · 렌더러 `features/update` store(costStore 미러)+preload+api+Provider · 헤더 조건부 버튼 · `OverlayLayer` 안내 다이얼로그 · **idle-gated 설치**(비-idle=거부+사유·레이스 가드=admission 락+직전 재확인) · 클린 종료(will-quit→shutdown+closeDb 재사용) · 게이트. **신규 의존성 electron-updater**(전달 모델 사용자 승인 확정). **비범위→OQ3 배포-타임(후속 release-ops)**: 실 피드 URL(generic vs GitHub Releases)·서명 인증서·`verifyUpdateCodeSignature`·CI·채널·staged rollout·`autoInstallOnAppQuit` 채택·텔레메트리(OQ4)·라이센스(OQ5)·SemVer·TRD §4 uv 드리프트. 상세=`0085-auto-update-ux/plan.md`. |
| `0086-debug-dummy-update` | verify | PASS | — | c8520c7 | 1 | **디버그 패널 더미 업데이트 테스트 하네스(0084/0085 dev 검증용, 비기능 = Claude 직접 구현).** 게이트 typecheck 3종 0·lint(boundaries) 0·`electron-vite build` ✅(test 30fail=better-sqlite3 네이티브 바인딩 미빌드 환경 제한·DB 테스트 한정·본 변경 무관), 레이어 경계 0, 신규 의존성/IPC/DB/메인 0. dev 전용 렌더러 변경으로 실 서버 없이 0085 UX 재현: ① 디버그 패널 "업데이트" 그룹 + "더미 업데이트" `PanelToggle`(update feature 가 `UpdateDebugSection` export → OverlayLayer 주입, SSO 슬롯 패턴 미러 — debug→update 교차 import 회피) ② 토글 ON → 헤더 arrowR 우측 Material `download` 버튼 + 우측 상단 파란 동그라미(inline SVG·`--color-indigo` 재사용·신규 토큰 0) ③ 클릭 시 mock 흐름(다운로드 0→100%→ready→installing→**앱 종료 없이** 리셋, 실 `updateApi` 미호출). **실경로 보존**: 헤더 버튼·뱃지는 `updateState.status` 만 파생(실 릴리스·더미 동일 노출), `download()`/`quitAndInstall()` 은 `dummyMode===false` 시 기존 `updateApi` 실경로 무변경, `initUpdate`/`subscribeUpdate.onState` 는 `dummyMode` 가드로 실 idle 이 mock 을 덮지 않게 skip. 변경 파일=`features/update/store/updateStore.ts`·`features/update/components/UpdateDebugSection.tsx`(신규)·`features/update/index.ts`·`features/debug/components/DebugPanel.tsx`·`app/OverlayLayer.tsx`·`app/Header.tsx`. 사람 확인 대기: `npm run dev` 육안 검증(토글→헤더 버튼/파란 뱃지→다운로드 진행→설치 mock)·PR 머지. 상세=`0086-debug-dummy-update/{plan,verify}.md`. |

| `0087-cicd-release-pipeline` | verify | PASS | — | `fb72b1e` | 1 | **검증 PASS(r1) — 인수 11/11 충족, 게이트 lint 0·typecheck 3종 0·vitest 773/773 + node --test 24/24(3 suite=electron 바이너리 403 환경 제한·0019/0085 계열·무관), 레이어 경계 0(`app/src` 무변경), 신규 npm 의존성 0.** CI/CD 구성 — Windows unsigned NSIS + GitHub Releases 릴리스 파이프라인 (OQ3 release-ops 1차분, 비기능 인프라 = Claude 직접 구현, CI 그린필드). 사용자 확정: 게시=GitHub Releases 유지(public·draft=수동 배포 게이트)·트리거=`v*` 태그+dispatch(항상 dry-run)·러너=`windows-latest`·테스트=CI 자동 검증+수동 시나리오 문서·버전 0.1.0(릴리스 0건이라 1.0.0→0.1.0 하향 무해). ① 버전 0.1.0+lock 동기화 ② `ci.yml`(push main/PR paths `app/**`·Node 22·npm ci→마이그레이션 가드→lint→typecheck→test) ③ `release.yml`(fail-fast 버전 검증→마이그레이션 가드→게이트→NSIS 빌드→draft 게시 installer/latest.yml/blockmap→sha512 산출물 검증→artifact 상시 업로드, `build:win -- --publish always` 로 ABI 훅 보존) ④ 스크립트 3종+테스트 24종(`validate-release-version`·`check-migrations-appendonly` 동기화+append-only 기계 강제·`validate-dist` sha512 재계산) ⑤ `docs/guides/release-operations.md`(절차·수동 체크리스트·롤백 `DB_SCHEMA_TOO_NEW`→백업 복원·unsigned/SmartScreen). `npm test` 가 `scripts/*.test.mjs` 자동 포함(Node 22.22 디렉토리 인자 버그로 glob 채택 — plan 구현자 기입 #1). PHASES 승격. 사람 확인 대기: 머지 후 ci.yml green·release.yml dispatch dry-run·`v0.1.0` 태그 실릴리스·수동 체크리스트·draft Publish. 상세=`0087-cicd-release-pipeline/{plan,verify}.md`. |

| `0088-ci-trigger-versioning` | verify | PASS | — | `412c2bc` | 1 | **검증 PASS(r1) — 인수 4/4 충족, 게이트 lint 0·typecheck 3종 0·vitest 773/773 + node --test 24/24(3 suite=electron 403 환경 제약·무관), `app/src` 무변경(레이어 영향 0).** 0087 후속 조정(비기능 = Claude 직접 구현). ① `ci.yml` `on:` 에서 `pull_request` 제거 → `push(main)` 자동 + `workflow_dispatch` 수동만(js-yaml 파스로 부재 확인, 브랜치 CI 는 로컬 게이트로 대체) ② `app/package.json` 에 `release:patch|minor|major`(=`npm version <bump> -m "chore(release): v%s"`) 도입 — package.json+lock bump·커밋·`v*` 태그 원샷, release.yml 트리거·`validate-release-version` 정합(버전 0.1.0 시작 유지, 실 bump·태그=사용자 몫) ③ `release-operations.md` 절차를 `npm run release:*` + `git push --follow-tags` 로 갱신, 게이트 행 트리거 설명 갱신, SemVer(pre-1.0) 정책 소절 추가(단조 증가·마이그레이션 시 최소 patch). PHASES 승격. 사람 확인 대기: main push 시 ci.yml 자동 green·첫 `npm run release:minor`→`v0.2.0`→release.yml. 상세=`0088-ci-trigger-versioning/{plan,verify}.md`. |

| `0089-release-0-1-0` | verify | PASS | — | `3495770` | 1 | **검증 PASS(r1, 코드 범위) — 인수 6/6 + AC7(릴리즈 실행)=PR 머지 후 후속, 게이트 lint 0·typecheck 3종 0·vitest 773/773·node --test 24/24·electron-vite build ✅(3 suite=electron 바이너리 403 환경 제한·무관), 레이어 경계 0, 신규 의존성/IPC 0.** v0.1.0 첫 Windows 릴리즈 (비기능 = Claude 직접 plan→impl→verify). 요구 2건: ① 배포 빌드 디버그 패널 제거 — 기존 `import.meta.env.DEV` 가드로 이미 충족(코드 변경 0) ② SSO 로그인 disable — SSO 항상-실패 스텁 + bypass 토글이 디버그 패널 전용이라 prod 데드락 → **배포 빌드에서 로그인 게이트 스킵**(사용자 확정, dev 는 게이트·디버그 패널·bypass 토글 무변경). 가드는 사용처 인라인 `import.meta.env.DEV`(RootGate 판정 2곳 + LoginFrame LoginView) — 설계 수정 r1: 모듈 경계 상수는 Rollup 미폴드로 LoginView 가 prod 번들 잔존(빌드 grep 실측) → 인라인 전환 후 `SSO로 로그인`·`로그인 우회` 0건. 추가: `package.json` 템플릿 메타데이터(description/author/homepage) 정리(사용자 확정, app/AGENTS.md 원칙 6). 릴리즈 실행: 버전 0.1.0 유지(bump 없음) → PR 머지 후 머지 커밋 `v0.1.0` 태그 push → release.yml → draft 자산 3종 확인, **Publish=사용자**. 상세=`0089-release-0-1-0/plan.md`. |

| `0090-engine-modal-single-step` | verify | PASS | — | `119132f` | 1 | **검증 PASS(r1) — 인수 9/9, 게이트 lint 0·typecheck 3종 0·vitest 786 passed + node --test 24/24(3 suite=electron 바이너리 403 환경 제한·무관), 레이어 경계 0, 신규 의존성 0, IPC 63→64.** 엔진 추가 모달 단일 화면 재설계(기능이나 사용자 명시 지시로 Claude 직접 plan→impl→verify) — 1·2 depth(엔진/공급자 선택)·Stepper·이전/닫기 제거, adapter=claude 칩 고정, 공급자는 드롭다운(4종·간결 설명·기본 anthropic·선택 시 템플릿/이름 갱신), settings.json 은 Material `file_open` 버튼으로 `~/.claude/settings.json` 자동완성(신규 `orca:engine:importUserSettings`, `handlePlain`·adapters `readUserClaudeSettings`), Esc/백드롭 닫기. 추가 요구: 설치 후 첫 부팅 스캐폴드가 `~/.claude/settings.json` env 판별(`classifyClaudeEnv` — bedrock/vertex/custom/anthropic)로 provider 를 전문 verbatim 시딩(부재/파싱 실패=기존 anthropic 빈 템플릿 폴백, 빈 상태 1회 트리거 불변=업데이트 미개입). 사람 확인 대기: 시각/실기 검증(단일 화면·드롭다운·불러오기·첫 부팅 시딩)·PR 머지. 상세=`0090-engine-modal-single-step/{plan,verify}.md`. |

| `0091-scheduler-periodic-execution` | verify | PASS | — | `9805bc4` | 1 | **검증 PASS(r1) — 인수 8/8 충족, 게이트 lint 0·typecheck 3종 0·vitest 791 passed(3 suite=electron 바이너리 403 환경 제한·무관, 격리 재실행 scheduler/migrate/queries 29/29 green), 레이어 경계 0, 신규 의존성 croner 승인 반영.** 앱 내 주기적 실행/스케줄링 모듈 (기능 = Codex 구현). main-프로세스 스케줄러 엔진(croner, 신규 의존성 사용자 승인) + 첫 소비처 "주기적 사용량 recompute" job. 신규 슬라이스 `features/scheduler/`(register/schedule/`protect` 겹침방지/`nextRun`/`stopAll` dispose, job action 은 컴포지션 루트 주입=교차 feature 회피) + 마이그레이션 `0013_schedules.sql`(`schedule_runs` 실행이력) + `SettingsSchema.scheduler.usageRecompute{enabled,cron}`(기존 `settingsSet` 경로, 신규 IPC 0·`IPC_CONTRACT` 무변경) + `Bootstrap` 배선(initDb/UsageTracker 후 생성·주입·start, `shutdown()` 이 closeDb 앞 `stopAll` + `disposed` 가드) + Tweaks 토글. **비범위(후속)**: 사용자정의 스케줄 CRUD·Routines UI·`/routines` 라우트 / **무인 대화 실행 job**(턴 파이프라인 `chat-turn.ts:504-514` owner-coupling → headless owner 추상화 선결, 본 설계는 action 포트로 확장점만) / 놓친 실행 catch-up. 사용자 결정 3건(범위=코어+새로고침 / 엔진=croner / 첫 대상=사용량 recompute). PHASES 승격. (대상 커밋 INDEX 기재 Codex-env `cf76574`→본 브랜치 실 `9805bc4`, 위생 노트 ①) 사람 확인 대기: Tweaks 시각 검증·실환경 주기 발화 실기. 상세=`0091-scheduler-periodic-execution/{plan,verify}.md`. |

| `0092-main-simplify-cleanup` | verify | PASS | — | `2681732` | 1 | **검증 PASS(r1) — 인수 16/16, 게이트 lint 0·typecheck 3종 0·vitest 801 passed + node --test 24/24(2 suite=electron 바이너리 403 환경 제한·베이스라인 동일 실패 확인·무관 — 기존 3 suite 가 resilience 테스트 electron 비의존화로 2 로 감소), 레이어 경계 0, 신규 의존성 0, IPC 무변경.** `/simplify @app/src/main` 결과 정리(비기능 = Claude 직접 plan→impl→verify). 4관점 리뷰 18건 중 15건 적용: 재사용 3(`errorMessage`→infra/errors·`isRecord`→shared/obj·`readJsonFile`→infra/config, 사본 3~5중 통합) + 데드코드 6(`onUnframedEvent`·`adaptMcp`·`abortCause` getter·`RuntimePool.onReap`·chat-turn 무회귀 re-export 5종·dist 경로 헬퍼 3종) + 단순화 1(`freshTurnLocalState` 팩토리) + 효율 5(**`SettingsStore` write-on-read 제거**(read 마다 zod 마이그레이션+동기 디스크 쓰기 → lazy 1회+patch 시)·`McpStore` getAll 호이스트·mcp.json 파스 캐시·스킬 스캔 병렬화·adapters 메모이즈, 신규 캐시 테스트 5건). 3건 스킵(AskUserQuestion 리터럴·provider-registry Claude 파서·slugify — 설계 변경 수반, plan §비범위·verify §결론, ①②는 후속 핸드오프 후보). 사람 확인 대기: 실행 중 settings/mcp.json 외부 편집 미반영 2건(verify §사람 확인 대기)·PR 머지. |

| `0093-frontend-simplify-cleanup` | verify | PASS | — | `e85c7d4` | 1 | **검증 PASS(r1) — 인수 14/14, 게이트 lint 0·typecheck 3종 0·vitest 804 passed(+신규 3, 2 suite=electron 바이너리 403 환경 제한·0092 동일 계열·무관), 레이어 경계 0, 신규 의존성 0, IPC/preload/main 무변경.** `/simplify @app/src/shared @app/src/preload @app/src/renderer` 결과 정리(비기능 = Claude 직접 plan→impl→verify, 0092 의 프론트엔드 대응편, 순 -80줄). 4관점 리뷰 dedup 후 13건 적용: 데드코드/파생 3(`MOCK_HATCH_BG` 삭제·`MODE_LABELS` 파생화·`MENU_ITEM` 단일화) + 재사용 5(**모달 4종 `Modal` 셸 이관**(Create/EditInstructions/AddMcp=풀 이관+`ModalActions.cancelDisabled` 신설, Settings=크롬리스 `panelClassName` — Modal 주석의 미완 일반화 완료)·`relativeTimeLabel` 재사용·`AnchoredDropdown` 셸 추출·`useTokenAutocompleteState` 훅 추출·`basenameForDisplay` 재사용) + 효율 3(**라이브 reasoning `StreamingMarkdown` 전환**(델타 프레임당 전문 재파스 제거 — 텍스트 경로 0008 패리티)·`subagentTasksFromMessages` 메모·`tokenize` 메모) + 단일 진실원/배치 2(`FILE_EDIT_TOOLS`/`FILE_TOOLS` 파생 export + `isAgentTaskName` 일원화·`StatusLine` shared/ui→features/chat 이동). 5건 스킵(부트 병렬화=UX 트레이드오프·EngineFormModal=조건부 Esc·UpdateDialog=비닫힘 정책·rename fan-out=2줄+의도 주석·descriptor 전면화=대형 — plan §비범위, ①②는 후속 후보). 사람 확인 대기: 모달 4종 시각 회귀(크롬 통일 수용)·extended thinking 스트리밍 실기·PR 머지. |

| `0094-arch-docs-sync` | verify | PASS | — | `04aa676` | 1 | **검증 PASS(r1) — 인수 13/13, 코드 게이트 N/A(문서 전용·`app/src` 무변경), 인용 경로 55건 실존 0 누락, 수치 실측 대조(마이그레이션 13·main 슬라이스 9·renderer 13 도메인·settings 17 키), 위생 스캔 0.** 아키텍처 문서 16개 + `docs/AGENTS.md` frontend 행을 0077 시점(54658f6)→현재 코드로 동기화 (비기능 문서 = Claude 직접 plan→impl→verify). 반영: scheduler 9번째 슬라이스(0091)·electron-updater 자동 업데이트(0084~0086)·skills 부트 시딩(0078)·사용량 한도 UI UsagePanel(0079~0082)·UI polish(0083)·CI/CD 릴리스 v0.1.0(0087~0089)·엔진 모달+`~/.claude/settings.json`(0090)·마이그레이션 13종·IPC 64채널·게이트/scripts/의존성 표(zustand·electron-updater 추가, playwright 미설치 표기). pre-drift 동시 정정(테마 2종·TweaksPanel→DebugPanel·`/agent`·0062 개명 경로·provider_key 0008·pendingDelta→live.text·markdown 위치). **파생 이슈 D1(사람 결정 대기)**: IPC_CONTRACT §2.4 Settings 타입 블록 stale(theme enum·spendingLimitUsd/scheduler 누락) — SSOT 변경 절차 대상이라 비범위, 후속 여부 사용자 결정. 상세=`0094-arch-docs-sync/{plan,verify}.md`. |

| `0095-docs-core-sync` | verify | PASS | — | `1139437` | 1 | **검증 PASS(r1) — 인수 11/11, 코드 게이트 N/A(문서 전용·`app/**`/`docs/spec/**`/`docs/etc/**` 무변경), 수치 3중 정합(IPC 64 채널·20 도메인 분포 검산 = 헤더 총계 / settings 17 키 실측 / 잔존 stale grep 0).** docs/ 코어 문서 9종 동기화 (spec·etc 제외 — 0094 후속, 비기능 문서 = Claude 직접 plan→impl→verify). ① IPC_CONTRACT §2.4 17 키·white/dark·SettingsPatch shape(**0094 파생 D1 해소**) + §1/§2 notify·boot 도메인 정합(20개·합계 64) + §2.13 wireLog ② GLOSSARY — 제거된 Python Runtime/uv/buildPyEnv 정리(0050 PR-B)·Project 구현 완료·NormalizedEvent `provider` 오류 삭제·경로 현행화·**Scheduler/사용량 한도 신설** ③ TRD — §6.7 6→17 키·§4 스택(Playwright 미도입·OQ3 확정·Python 런타임 제거)·§5 11/36→64 채널·§2 **F11~F15** 신설·§9 CI/CD·§10 해소 anchor 6건 ④ PRD — ~~OQ1~~/~~OQ3~~/~~OQ8~~ 해소 표기(관례 준수)·OQ9 부분 진전·§7/§8/§9 완료분(의도 변경 0) ⑤ PHASES — 0090/0092/0093/0094 행 승격(0086 은 dev 하네스 생략 각주)·현재 상태 문단 ⑥ git-template `Claude-Session:` trailer 정정 ⑦ claude-code-spec `--model`/`--effort` ✅ 채택(0010/0020) ⑧ guides/workspace `~/.claude` write **Orca 편차 노트**(0075 r2)+§3.4 SUPERSEDED 정합 ⑨ docs/AGENTS.md 인벤토리 행 정합. release-operations 는 검산 결과 최신 — 무수정. **파생 D1(사람 결정 대기)**: 루트 AGENTS.md "`Criteria-*`=Codex 만" 문구 ↔ 현행 관례 불일치(범위 밖). 상세=`0095-docs-core-sync/{plan,verify}.md`. |

| `0096-i18n-ui-locale` | verify | PASS | — | `bdecef8` | 1 | **검증 PASS(r1) — 인수 10/10, 게이트 lint 0·typecheck 3종 0·vitest 818/818(+신규 23)·scripts 24/24(2 suite=electron 403 환경 제한·0092~0095 동일 베이스라인), `ko-KR` 잔존 grep 0, 레이어 경계 0, 신규 의존성=승인된 i18next/react-i18next(devDeps).** i18n(한국어/영어) + OS 타임존 명시 날짜 포맷 (기능이나 사용자 명시 지시로 Claude 직접 plan→impl→verify). 사용자 확정 4건: 범위=인프라+핵심 화면 / 라이브러리=i18next 도입(신규 의존성 승인) / 타임존=OS 자동 감지만(선택 UI 없음, "저장 UTC·표시 로컬 PC 보정") / 주체=Claude 직접. ① settings `uiLocale`(ko/en, default ko — 기존 `language`=LLM 응답 언어와 별개) ② `renderer/shared/i18n/` 신설(i18next 동기 init·ko 카탈로그 SSOT·`en: typeof ko` 키 패리티·typed t()·`useI18n`) ③ `datetime.ts` — 모든 Intl 포맷터에 명시 timeZone(기본=OS 감지·테스트 주입)·같은-날 판정 타임존 기준(DST 안전)·`ko-KR` 하드코딩 5 사이트 전부 치환 ④ `shared/time`·`computeUsageLimits` locale 파라미터(기본 ko 하위 호환) ⑤ 핵심 화면 마이그레이션(설정 모달 5종·Sidebar/Header/SidebarUserButton·UsagePanel·날짜 표면 5종) ⑥ 언어 스위처(플라이아웃 활성화 + GeneralTab select). 게이트: lint 0·typecheck 3종·vitest 818/818(+신규 23) + scripts 24/24 (2 suite=electron 403 환경 제한·0092~0095 동일 베이스라인). 비범위: 잔여 ~140 파일 문자열(후속)·타임존 선택 UI·clock.ts 경계 타임존화·LLM 응답 언어 UI. PHASES 승격. 사람 확인 대기: en 카피 어감(`resources/en.ts`)·`npm run dev` 라이브 전환/영속 실기·PR 머지. 상세=`0096-i18n-ui-locale/{plan,verify}.md`. |

| `0097-i18n-remaining-screens` | verify | PASS | — | `899bb45`~`d61867c` | 1 | **검증 PASS(r1) — 인수 10/10(4·10 은 구조 검증, 실기=사람), 게이트 lint 0·typecheck 3종 0·vitest 821/821(+3)·scripts 24/24(2 suite=electron 403 환경 제한), 잔존 한글 grep = D8 화이트리스트만, 신규 의존성 0.** 0096 후속 — renderer 잔여 하드코딩 문자열 전체 마이그레이션(사용자 확정: 범위=잔여 전체 일괄 / 주체=Claude 직접). C1~C7 7 커밋: common 원자+nav/search → errors/notify/store 폴백(`UiMessage` 키 저장) → chat.toolMeta(키맵·`ToolGroupSegment` 구조화·plural `_one/_other`)+transcript → composer/status/rightpanel/approval → skills/projects 모달군 → engine(errorKey 검증)/backend/update/debug/camera/login → 랜딩/부트+sweep. 문장 중간 강조는 `Trans`+태그(`<hl>`/`<mono>`/`<c>`). 비범위 유지: `[분기]/[핸드오프]` 마커(영속 계약)·`shared/time`/datetime locale 파라미터 계열·main 프로세스. PHASES 승격. 사람 확인 대기: 라이브 en 전환 실기·en 카피 어감·OS 알림 실기·PR 머지. |

| `0098-static-provider-usage-correction` | verify | PASS | — | `84454c2` | 2 | **검증 PASS(r2) — 미충족 0, 게이트 lint 0·typecheck 3종 0·Vitest 833/833(112파일, +7)·scripts 24/24(2 suite=electron 403 환경 제한·0092~0097 동일 베이스라인), 레이어 경계 0, 신규 의존성 0.** 정적 provider = 플러그인 모듈(`StaticUsageProviderModule[]`) 규약 + authoritative `ExternalUsageReport`(로컬 `summary` 유지·`effectiveLimit`만 외부 quota 보정) + 0014 `provider_usage_report_cache` 영속(오프라인 stale) + `http-usage-report` config sugar + 5분 스케줄러 백그라운드 refresh + cost IPC 확장. **r1(FAIL)→r2**: r1 지적 3부류를 (a) §11 테스트 5범주 이행(http-usage-report 매핑/null·실 DB upsert/read 왕복·refresh IPC zod·hook ctx 전달·null→캐시 stale 폴백·provider-agnostic registry 순회) (b) §3/§D 예시훅→**계약 경계 문서화+ctx 테스트로 재조정**(2026-07-13 사용자 결정: hook 내부 인증/매핑은 provider 소유, 포맷화 제외) (c) §9 stale 배지·§6 30s 틱→**후속 핸드오프 연기**(사용자 결정, 5분 스케줄러 유지) 로 종결. 코어 무편집 회귀 테스트로 "배럴 추가만으로 인식" 고정. PHASES 승격. 상세=`0098-static-provider-usage-correction/{plan,verify}.md`. 사람 확인 대기: effective-limit 바·상대시각 시각 검증·실환경 provider report fetch·PR 머지. |

| `0099-static-provider-optout-settings-polish` | verify | PASS | — | `7df8a78` | 1 | **검증 PASS — 인수 13/13 충족, 게이트 lint 0·typecheck 3종 0·Vitest 802/834(32 red=better-sqlite3 네이티브 ABI egress 403 환경 제한·0098/0019 동일 베이스라인·코드 무관, 실패 6파일 전부 DB 로드)·scripts 24/24, 레이어 경계 0, 신규 의존성 0, IPC 무변경, 하드코딩 hex/rgb 0.** 신규 0099 스위트(static 레지스트리 2 + usage schedule 3)는 네이티브 무의존 5/5 green. ① 정적 provider 기본 주입 제거 — `STATIC_USAGE_PROVIDERS=[]`(배럴 `modules/index.ts` 위임)·bedrock/vertex/custom `settings.json` 미생성(기존 편집분 보존)·core 무편집 시연(bootstrap·service·scheduler·IPC·enumeration·tracker·materializer diff 부재, opt-in=`modules/<name>/`+배럴 1줄)·비활성 `_example` 템플릿(typecheck 되되 미수집). ② `주기적 실행` GeneralTab→UsageTab 이동 + cron `<input>` custom 선택 시에만 활성(`usageRecomputeSelectValue`/`isUsageCronInputEnabled` 재사용, `customSelected` 전이 선조치)·i18n `settings.general.*`→`settings.usage.*`(ko/en 패리티 test green). ③ 블루 토큰 신설(`--color-toggle-on`·`--color-selected`·`--color-selected-soft` 양 테마, `--color-indigo` 메터 전용 유지)→토글 on·활성 탭·테마 세그먼트·provider 서브탭·FloatingPanel(`#34c759` 소멸) 통일. 구현 주체=Codex(사용자 요청). PHASES 승격. 상세=`0099-static-provider-optout-settings-polish/{plan,verify}.md`. (대상 커밋 INDEX 기재 Codex env `b308c59`→본 브랜치 실 `7df8a78`, 위생 노트 ①) 사람 확인 대기: 블루 색값·다크 대비(WCAG)·시각 톤·레이아웃 이동·실환경 provider 모듈 opt-in·PR 머지. |
| `0100-simplify-96-99-cleanup` | verify | PASS | — | `ac12ebd` | 1 | **검증 PASS(r1) — 인수 8/8, 게이트 lint 0·typecheck 3종 0·Vitest 804/836(32 red=better-sqlite3 네이티브 ABI egress 403 환경 제한·0098/0099 동일 6파일 집합 격리 재실행 확인·코드 무관)·scripts 24/24, 본 변경 스위트(time·usage·i18n) 32/32 green, 레이어 경계 0, 신규 의존성 0, IPC 무변경.** `/simplify 범위: 핸드오프 96~99` 결과 정리(비기능 = Claude 직접 plan→impl→verify, 0092/0093 계열). 4관점 병렬 리뷰: 효율 0(clean), dedup 후 적용 4: ① F1 `summaryForLimit` 투영 3중복(렌더러 사본 2 + main 데드 `summaryForEffectiveLimit`) → `shared/usage/limits.ts` `computeProviderUsageLimits(entry)` 단일화 + 도달 불가 `?? entry.limitUsd` 폴백 제거(IPC 필드는 유지) ② F2 **A1(사용자 확답으로 0096 "locale 파라미터" 결정 supersede)**: `shared/time` 인라인 ko/en 사전 소멸 — `relativeTime` 데이터 반환·`reset.ts`(`nextWeekReset`/`nextMonthReset`)·`UsageLimitBar.resetLabel→resetAt+period`, 문장화는 renderer `formatRelativeTime`/`formatResetLabel` + 카탈로그 `time.*`(ko/en 패리티 green), **표시 문자열 바이트 동일**(구 테스트 기대값 datetime.test 로 승계 고정, Intl weekday/month short 실측 일치) ③ F3 SyncRow 3중 삼항 → `fetchedAt ?? updatedAt` ④ F4 `formatMonthDay` barrel export 제거. 스킵 2 기록: `stale` 배관(0098 r2 사용자 결정 pre-wire) · `limitUsd` 필드 제거(IPC 계약 변경 비범위). PHASES 승격. 사람 확인 대기: 라이브 언어 전환 실기(재설정 라벨·상대시각)·PR 머지. 상세=`0100-simplify-96-99-cleanup/{plan,verify}.md`. |

| `0101-markdown-pretty` | verify | PASS | — | `58b0960` (r1) | 2 | **r1 PASS — 인수 5/5.** react-markdown `COMPONENTS` 맵 무의존 폴리시(태스크리스트·표 정렬·h5/h6·취소선). **r2 PASS — 인수 6~11, 게이트 lint 0·typecheck 3종 0·renderer 248/248(+5 isCodeBlock·DB 스위트 ABI-403 미로드), 레이어 경계 0, 신규 의존성 0.** 사용자 리포트 버그수정: **언어 헤더 없는 ` ``` ` 펜스(트리 구조)가 인라인 코드("단일 백틱")로 오분류**되던 문제 — react-markdown v9 `inline` prop 제거로 `!className` 휴리스틱이 언어 없는 펜스·들여쓰기 블록을 인라인 처리. 순수 헬퍼 `isCodeBlock(className,text)=Boolean(className)||text.includes('\n')` 로 교체(단위 테스트 5) + 인라인 코드 테두리·배경을 사용자 버블 배경 `bg-bubble-user` 로 통일 + **언어 없는 코드블록 헤더 라벨 `text`**(사용자 추가 요구 2건). 비기능=Claude 직접. 상세=`0101-markdown-pretty/{plan,verify}.md`. 사람 확인 대기: 시각 검증(트리 펜스 블록 렌더·인라인 배경 톤·`text` 라벨·테마 2종)·PR 머지. |
| `0102-transcript-virtualization` | verify | PASS* | — | `621b5f2` | 1 | **게이트/구조 PASS — 인수 6/6 중 #6(게이트·경계) ✅ 기계검증, #1~5(언마운트·앵커·스크롤·성능)는 electron 403 환경 제약으로 사람 실기 대기(코드 결함 아님, 0019 선례).** lint 0(경고 1=TanStack↔React Compiler 수용)·typecheck 3종 0·renderer 243/243, 레이어 경계 0, 신규 의존성 `@tanstack/react-virtual`(승인). "virtualized head + unvirtualized tail" — 과거 확정 교환만 가상화, 마지막 스트리밍 교환은 비가상 tail 로 0008 예약공간 앵커 보존. 신규 `useTranscriptVirtualizer` + `TranscriptView` head/tail 분할 + TRD §2·AGENTS 채택 기재. 비기능=Claude 직접. 상세=`0102-transcript-virtualization/{plan,verify}.md`. 사람 확인 대기: **런타임 실기(전송/fill 무점프·과거행 정합·DOM 노드수 감소)**·PR 머지. |

| `0103-dev-db-userdata-isolation` | impl | IMPL_DONE | Claude | `9640438` | 1 | dev/prod 데이터 격리 (비기능 = Claude 직접 구현, 사용자 결정 2건: 격리=userData 디렉토리 분리·신호=`import.meta.env.DEV`). `index.ts` 가 `app.whenReady()` 이전 dev 에서만 `userData` 를 sibling `orca-dev` 로 리디렉션(`app.setPath('userData', devUserDataDir(app.getPath('appData')))`) → DB·WAL·마이그레이션 백업·secret-store 통째 격리. 순수 헬퍼 `devUserDataDir`(`infra/config/paths.ts`) + 단위 테스트. prod 번들은 `import.meta.env.DEV` dead-code 제거. `db/index.ts`·`migrate.ts` 무수정(디렉토리 분리 이점). app/AGENTS.md DB 정책 갱신. **후속 문서(동 브랜치)**: app/AGENTS.md §빌드/실행 에 better-sqlite3 ABI 제약 환경(egress 403) 게이트 가이드 추가 — 실패 서명 목록 + DO/DON'T(DB 로드 스위트 실패는 baseline 분리 보고·ABI 뒤집기 금지·electron 로딩/build 검증은 CI/사람 몫). 검증 대기. |

| `0104-sqlite-abi-node-revert` | impl | IMPL_DONE | Claude | `a9bf02b` | 1 | sqlite ABI 버그수정 (비기능=Claude 직접, 원류 0103). **문제**: `npm install` 후 `npm run dev` 가 Node-ABI 고착 — `ensure-sqlite-abi.mjs` electron 판정이 마커 메타데이터만 보고(binary-blind) 실제 바이너리 ABI 미확인. npm 의 prebuild-install 이 바이너리를 Node-ABI 로 되돌려도 마커는 `{target:electron}` 이라 fast-path 가 재빌드를 skip. **수정**: electron `needsRebuild` 에 실제 로드 프로브 겸용(`loadBetterSqlite||!markerMatches`) → Node-ABI 감지 시 재빌드. 회귀 테스트 + 기존 electron 테스트 hermetic 주입. **가이드**: app/AGENTS.md 에 "어떤 명령이 ABI 를 뒤집는가"(lint/tsc/format=중립·test/install/dev/build=flip) + `npm test`→build 순서가 마찰의 실체 + ABI-안뒤집는 테스트 실행법. 게이트: ensure-sqlite-abi 7/7·lint 0·typecheck 0. 검증 대기(실 ABI 전환 실기는 CI/사람). |

| `0105-native-binary-executable-resolve` | impl | IMPL_DONE | Claude | (push 후) | 1 | 패키징 빌드 native binary 실행 실패 버그수정 + 공식 설치본 지원 (비기능=Claude 직접, 사용자 결정 2건: 공식/PATH 우선·env override 없음). **Root cause(SDK 0.3.143 소스 확인)**: `query()` 가 native binary(`@anthropic-ai/claude-agent-sdk-<plat>-<arch>/claude[.exe]`)를 `require.resolve`→직접 spawn 하는데, electron-builder 가 `node_modules` 를 `app.asar` 로 묶어 해석 경로가 asar *내부* → `existsSync`=true(asar-aware fs)지만 spawn 불가 → "native binary … exists but failed to launch"(dev 는 asar 없어 미재현). **수정**: ① `asarUnpack` 에 `node_modules/@anthropic-ai/claude-agent-sdk-*/**` 추가(실디스크 언팩) ② 신규 `adapters/claude-executable.ts` — `pathToClaudeCodeExecutable` 실경로를 **PATH → ~/.local/bin(공식 인스톨러) → 번들 asar-언팩본** 순으로 해석(순수 함수+주입, 비패키징=undefined 로 SDK 기본 위임) ③ `claude.ts` runCompletion·sendMessage 양 query options 에 주입. 게이트: typecheck 3종 0·lint 0(경고 1=0102 무관)·어댑터 224/224(신규 12). **패키징 실행 실기는 egress 403·비-Windows 로 이 환경 불가 → build:win 산출물 실기(공식설치본/번들폴백 2케이스)는 CI/사람 대기.** 상세=`0105-native-binary-executable-resolve/plan.md`. |

| `0106-simplify-101-105-cleanup` | verify | PASS | — | `0ec71f7` | 1 | **검증 PASS(r1) — 인수 3/3, lint 0 error(경고 1=0102 TanStack↔React Compiler 무관)·typecheck 3종 0·adapters vitest 224/224(21파일)·ensure-sqlite-abi 7/7, 레이어 경계 0, 신규 의존성 0, IPC/타입 시그니처 무변경.** `/simplify 범위: 핸드오프 101~105` 결과 정리(비기능 = Claude 직접 plan→impl→verify, 0100 계열). 4관점 병렬 리뷰: diff 전반 클린(순수 헬퍼 주입형 분해+테스트, 0102 는 `3f1c13d` 로 개별 /simplify 기적용, `ensure-sqlite-abi` 는 `needsRebuild` 재사용으로 이미 개선), dedup 후 적용 2(둘 다 0105 claude executable): ① F1 `...(claudeExecutable ? {pathToClaudeCodeExecutable} : {})` 삼항 스프레드 2곳(runCompletion·sendMessage) 바이트 동일 중복 → 모듈 상수 `claudeExecutableOption` 1회화 ② F2 `'claude.exe' : 'claude'` 실행파일명 판정 3곳(`findOnPath`·`officialInstallPath`·`bundledCandidates`) → 순수 헬퍼 `claudeBinName(platform)` 통일(`ext` 제거). 스킵 기록: `toUnpackedPath`·`isCodeBlock`·`devUserDataDir`(재사용 대상 없는 순수 신규)·TranscriptView head/tail(`3f1c13d` 기적용)·`findOnPath` 수동 구분자(platform 파라미터화=크로스플랫폼 테스트용 의도)·효율 발견 0. 순수 dedup 이라 기존 스위트 무수정 green 이 회귀 가드(신규 테스트 불요). DB 로드 스위트는 better-sqlite3 ABI egress-403 베이스라인(adapters/scripts 만 편집, DB 무접촉). PHASES 승격. 상세=`0106-simplify-101-105-cleanup/{plan,verify}.md`. 사람 확인 대기: PR 머지. |

| `0107-streaming-persist-hotpath` | verify | PASS | — | `223e353` | 1 | 성능 시리즈 1/4 (비기능=Claude 직접). 스트리밍 영속 핫패스 — `synchronous=NORMAL`(WAL 권장, per-commit fsync 제거) + `messages.content`(FTS 캐시) 기록을 블록마다→**마감 시 1회**로(초선형 FTS 재색인 해소, 경계=telemetry·commitUserMessage·chatCancel `finalizeTurn`) + finalize 이전 종료 복구 `rebuildIncompleteMessageContent`(부팅+세션 send, recover **이전**). **검증 PASS — 인수 8/8, 게이트 lint 0·typecheck 3종 0·vitest 878/878(Node ABI 재빌드로 DB 스위트 포함 전체 green)·scripts 0 fail.** PHASES 승격. 사람 확인 대기: 스트리밍 체감 실기·durability 트레이드오프·PR 머지. |

| `0108-streaming-highlight-incremental` | verify | PASS | — | `2dd1e35` | 1 | 성능 시리즈 2/4 (비기능=Claude 직접). 스트리밍 코드블록 O(n²) 하이라이트 해소 — tail 을 `MarkdownStreamingContext` 로 감싸 CodeBlock 이 스트리밍 중 shiki skip(plain 고정, 플리커 0, 확정/커밋 시 1회) + `advanceStableBlocks` 증분 분할(전문 동치 property 테스트) + SessionRow `memo` + themeStore 싱글톤 observer + DiffBody `useMemo`. **검증 PASS — 인수 8/8, markdownBlocks 11/11(전문 동치 property) 포함 게이트 전체 green.** PHASES 승격. 사람 확인 대기: 시각 검증(스트리밍 plain→확정 1회 하이라이트)·CDP rAF 실측·PR 머지. |

| `0109-boot-window-first-async-deploy` | verify | PASS | — | `4951a2f` | 1 | 성능 시리즈 3/4 (비기능=Claude 직접). 콜드스타트 흰 화면 + CRUD 프리즈 해소 — 창을 `start()` **이전** 생성 + 신규 `orca:boot:whenReady` 게이트(IPC 64→65, renderer 부트 첫 mandatory 스텝 `main-ready`) + deployer/plugin-package/seed `fs/promises` 전환 + `ExtensionDeploymentService` in-flight 직렬화(코얼레스) + 스킬/MCP/engine CRUD·턴 게이트 await. 파생 이슈 D1(engine 직접 deploy 경로 직렬화 밖, open). **검증 PASS — 인수 8/8, 배포 직렬화·main-ready 스텝 테스트 포함 게이트 전체 green(878/878).** PHASES 승격. 사람/CI 확인 대기: 콜드스타트·CRUD 무정지·prod app:// 실기. |

| `0110-attachment-encode-chunking` | verify | PASS | — | `1ace298` | 1 | 성능 시리즈 4/4 (비기능=Claude 직접). 이미지 첨부(최대 32MB) base64 동기 인코딩의 send/미리보기 프리즈 해소 — `bufferToBase64Chunked`(3의 배수 정렬 3MiB 청크 + `setImmediate` 양보, 단일 인코딩 바이트 동치 테스트) + provider settings mtime 체크 `statSync`→`fs/promises.stat`. **검증 PASS — 인수 5/5, 동치 테스트(크기 11×청크 6) 포함 게이트 전체 green.** PHASES 승격. 사람 확인 대기: 대형 첨부 실기·PR 머지. |

| `0111-weekly-usage-fixed-budget-external-reconcile` | impl | IMPL_DONE | Claude | (push 후) | 1 | 사용량 계산 3축 (비기능=Claude 직접). **① 주간 버짓**: 동적 런웨이(남은예산÷남은일수, 분모가 계속 이동)→ 고정 일할 `limitUsd×weekDaysInMonth/daysInMonth`(경계 주는 이달 몫만) → weekPct 가 이번 주 실지출에만 반응. **② 주/일 사용량 정합(fresh)**: 외부 권위 월간(=Orca+외부 총액)에 로컬 추정치를 `외부월/로컬월` 배 비례 스케일(week≤month 보존), 로컬월≈0 시 경과일 균등 분배 폴백. **③ stale 폴백·복구**: 외부 fetch throw/null 시 `external-usage-service` 가 캐시 baseline 으로 폴백(reject 안 함)+`lastFetchOk` 로 stale 의미화 → 순수 파생이 월=`max(baseline,로컬월)`·주/일=로컬 원값, 재성공 시 권위값 자동 복구. `limits.ts`/`clock.ts` 순수 + `external-usage-service.ts` 소폭. 게이트 typecheck/lint ✅, 대상 3 스위트 22/22·전체 순수 853 pass(DB 로드 5파일은 ABI egress 베이스라인, 변경 무관). 소비자 UI/훅 무변경. verify PASS 대기. |

| `0112-usage-summary-daily-tokens` | impl | IMPL_DONE | Claude | `2d76b9e` | 1 | 설정 사용량 요약 구현 (기능이지만 사용자 직접 지시로 Claude 가 설계+구현, 브랜치 `claude/settings-usage-summary-5f8dql`). ① **주기적 실행 그룹 제거** — UI 만(사용자 확정: main 스케줄러·tweak 유지), `usageSchedule.{ts,test.ts}`·관련 i18n 키 삭제. ② **사용량 요약**: 기간 탭(최근 7일/30일/전체) + 일별 토큰 차트(tokens per day, **recharts@^3 신규 의존 — 사용자 승인**, 토큰 = input+output+cache 4종 합산 사용자 확정, M 단위 소수 1자리 `formatTokens`) + 모델별 내역(총 토큰 내림차순·Meter). 신규 IPC `orca:cost:usageStats`(cost 4→5, 총 65→66) + `sumUsageByDaySince`/`sumUsageByModelSince`(로컬 일자 버킷, lazy stmt) + 공유 순수 `shared/usage/stats.ts`(rangeSince/fillDailySeries/aggregateWeekly/totalTokens). '전체' 90일 초과 시 주 단위 표시 집계 + 730일 캡. 게이트 lint 0·typecheck 3종 0·vitest 901/901(117파일, +신규 3스위트; `chat-turn.continuity` 1파일 = electron 바이너리 egress 차단 로드 실패, 변경 무관 베이스라인). IPC_CONTRACT·TRD §4 동시 갱신. verify 대기. 사람 확인 대기: 시각 검증(차트·두 테마)·실데이터 실기·PR 머지. |

| `0113-ci-pr-win-path-test-fix` | verify | PASS | — | (push 후) | 1 | CI 수정 (비기능=Claude 직접). **① claude-executable.test.ts Windows 실패**: POSIX 문자열을 기대값에 하드코딩한 테스트 4건이 host-aware `join`(win 러너=백슬래시)과 어긋나 `undefined` 반환→실패(로컬 green/CI red). win32 테스트 선례대로 기대값을 `join(...)`·`bin=win?claude.exe:claude` 로 host-독립 계산. 소스(`claude-executable.ts`) 무변경. **② 모든 PR CI**: `ci.yml` 에 `pull_request` 트리거 추가(paths 필터 유지 — 사용자 확정), **0088 "PR CI 없음" supersede**(ci.yml 주석·app/AGENTS.md CI 표 갱신). 게이트: 대상 vitest 17/17·lint 0 error·typecheck 3/3·`path.win32` 시뮬 4/4 PASS. windows-latest 전체 `npm test`·실기는 egress 열린 PR CI 최종 판정 대기(환경 제약, 코드 무관). |

| `0114-better-sqlite3-asar-unpack` | impl | IMPL_DONE | Claude | (push 후) | 2 | **r2 — 사용자 실기에서 위치 수정 후 ABI mismatch 확인.** 설치본의 unpacked 바인딩은 Node 22 ABI 127, Electron 39는 ABI 140 요구. 릴리스가 `npm test`로 Node ABI 전환 후 패키징하는데 `npmRebuild: false`가 electron-builder의 최종 native rebuild를 차단한 것이 근본 원인. `npmRebuild: true`로 복구해 패키저가 대상 Electron ABI를 최종 보장하고, r1 `asarUnpack`은 유지한다. YAML 설정값·ABI 스크립트 7/7·lint/typecheck 통과, Windows 설치본 실기 대기. 상세=`0114-better-sqlite3-asar-unpack/plan.md` 파생 이슈 r2. |

| `0115-single-instance-lock` | impl | IMPL_DONE | Claude | (push 후) | 1 | 단일 인스턴스 강제 (비기능=Claude 직접 구현, 사용자 결정 2건). 패키징 빌드 한정 `app.requestSingleInstanceLock()`(`app.isPackaged` 게이트 — dev HMR 재시작 경합 제외) + 두 번째 실행 시 `second-instance` 핸들러가 기존 창 복원·포커스(`focusMainWindow`) + `createWindow` 창 참조 보관(`mainWindowRef`, `closed` 항등 가드 해제) + `whenReady` 조기 반환(락 미획득 인스턴스는 Bootstrap/창 미생성). `app/src/main/index.ts` 1파일·신규 의존성 0·IPC 무변경. 게이트 lint ✅(0 error)/typecheck ✅(3/3)/순수 app 테스트 12 passed. 인수 1·2(중복 실행 차단·기존 창 포커스)는 패키징 설치본 2회 실행 사람 실기 대기(egress 차단으로 electron build/실기 불가 — 환경 제약). 상세=`0115-single-instance-lock/plan.md`. |

| `0138-listen-turn-optin-foreground` | impl | IMPL_DONE | Claude | (push 후) | 1 | inflight 고착 회귀 원복 (비기능=Claude 직접, 사용자 방향 결정 "135 엄격"). 0136 이 도입한 listen 턴이 정반대 회귀를 냄 — 백그라운드 서브에이전트 대기를 1급 턴으로 오모델링해 세션을 busy 포획(activeTurns++·hasSession)하고 백로그 배달로 inflight 를 점등한 채 stall 미무장으로 영구 개방 → "도구 완료됐는데 main inflight 고착 + steer 미커밋 + 중단 후 재전송 벌크". 정적 확정: `subagent.task started` 는 `task_started`(백그라운드 전용)에서만 방출 → **listen 턴 개시 ⟺ 서브에이전트 실제 백그라운드 실행**. 수정: chat-turn 턴-후 루프에서 `backgroundSubagents` off(기본)면 listen 턴을 열지 않고 종료(트래커 clear + 진단 로그 `chat.subagent.background-unexpected`, 강제 정착=truncate 회피). opt-in(`=1`) 경로·0136 인프라 불변. 게이트 lint 0·typecheck 3/3·순수 vitest 132/132(0136 프리미티브 회귀 0). 비범위: listen 런타임 하드닝(사용자가 "136 대체" 미채택)·SDK 핀 롤백(진단 로그 근거 확보 후 결정)·O1 draining 레이스(중단 직후 재전송 retryable 에러, 독립 이슈). 라이브 실기·PR 머지 사람 대기. 상세=`0138-listen-turn-optin-foreground/plan.md`. |

> 새 작업: 기존 행 중 `max(번호)+1` 로 행을 추가하고 `<NNNN-slug>/plan.md` 를 생성한다.
