# Orca 구현 페이즈 이력

> 독자: AI agent. **완료 이력의 정본은 `git log`** 이고, 본 문서는 사람이 읽는 요약이다.
> `app/AGENTS.md` 에서 분리(2026-06-05) — 영속 지침 파일의 컨텍스트 로드 부담을 줄이기 위해 changelog 성격의 표를 여기로 옮겼다.
> 현재 작업 규칙·구현 지침은 [`../app/AGENTS.md`](../app/AGENTS.md) 에 있다.

## 현재 상태

**Phase 3++** — 로컬 SQLite SSOT + 세션 히스토리 + DOM Architecture + 4-layer Feature 아키텍처 + MCP/Skill 통합 + uv 기반 Python 런타임 + provider 표준화 리팩토링(배포 A · 런타임 B · 프론트 C) + **정규화 parts persistence(AppMessagePart) + reasoning(확장사고) 영속·라이브 스트리밍 + ToolRendererRegistry 표준 taxonomy + 메시지 내 콘텐츠 순서 보존 렌더(Option B) + TelemetryPanel(비용·모델·지연)**.

## 현재 작업 중 (In Progress)

진행 중인 Claude Code ↔ Codex 협업 작업의 라이브 상태는 디스패치 보드에 있다. **여기서는 링크만 둔다** (변동성 정보의 정본은 보드, 완료 시 아래 "페이즈 표" 로 승격).

- 디스패치 보드: [`handoff/INDEX.md`](handoff/INDEX.md) — 작업별 단계(plan/impl/verify)·상태·다음 행동 주체.
- 협업 규약·템플릿: [`handoff/AGENTS.md`](handoff/AGENTS.md).

## 페이즈 표

| Phase   | 범위                                                                                                                                                   | 상태                        |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------- |
| Phase 1 | mockup 시각 재현 + Tailwind CSS v4 마이그레이션. chat / projects / engine / skills 4개 화면 + Tweaks. 캡처는 placeholder                               | **완료**                    |
| Phase 2 | IPC 채널 + zod 검증. **Claude Code 단일** 어댑터. 세션 재개. UI 데이터를 mockup 하드코딩 → IPC props 로 교체                                           | **완료 (claude-code 단독)** |
| Phase 2+ | `electron-store` 영속화 — Tweaks (theme/density/sidebarCollapsed), `lastSessionId`, `lastBackend`, window bounds                                       | **완료**                    |
| Phase 2++ | Composer 스킬 UX — `SKILL.md` 스캔 · `orca:skills:list` IPC · 3-chip 행 (첨부·현재 프레임·Skill) · Skill picker popover · 활성 스킬 `/skillname` chip 강조 · 인라인 자동완성 dropdown | **완료**                    |
| **Phase 3** | **로컬 SQLite SSOT** — `better-sqlite3@^12` + `<userData>/orca.db` (sessions / messages / tool_calls). 마이그레이션 러너 + WAL · foreign_keys. 이벤트 → DB persist (router turn-local 상태; 당시 `ChatEvent`, 이후 PR #47 에서 `NormalizedEvent` 로 전환). 사이드바 "최근 대화" 실세션 연동 · **비동기 lazy load** (메타 부팅 1회 + messages 진입 시점 1회) · **메모리 캐시** (재진입 IPC 생략) · **즉시 제목 표시** (state.title). 사이드바 행 **kebab 메뉴** (이름 변경 / 삭제) + inline rename. 부팅 시 lastSessionId 자동 복원 (메시지까지 비동기 fetch). | **완료 (PR #20)**           |
| **Phase 3+** | **DOM Architecture 일괄 적용** — `app-frame-*` 마커 클래스 + `data-behavior` / `data-state` / `data-axis` / `data-context` / `data-platform` 체계. 4종 신규 기능: ① Custom titlebar (`frame: false` + macOS `titleBarStyle: 'hidden'` + 윈도우 컨트롤 IPC 3개 + `orca.platform` sync 노출), ② Grid z-stack (`#app-frame-overlay` modal backdrop 전용 z 부호 반전 + `#app-frame-modal` focus-trap + `#app-frame-debug` floating UI 호스트 3슬롯), ③ Sidebar resize-handle (180–480px clamp, `Settings.sidebarWidth` 영속화), ④ Tile structure (`pane-host > pane-row > tile` 마크업). SSOT 는 [arch/frontend/dom-architecture.md](arch/frontend/dom-architecture.md). | **완료 (브랜치 `claude/charming-galileo-7lAqY`, 커밋 `45e129f` + 정정 `acf1295`)** |
| **Phase 3++ (PR #25)** | **`frame/` + `screens/` 슬롯 분리** — 셸 슬롯(Frame/Header/Sidebar/ChatTile/Composer/Modal/Debug) 은 `frame/` 으로, 도메인 화면 (Chat/Camera/Projects/Engine/SkillsMcp/Captures) 은 `screens/` 로 1:1 정렬. 컴포넌트 rename: `Titlebar` → `Header`, `ChatPane` → `ChatTile`, `*Pane` / `*Placeholder` / 화면 이름 → `*Screen` 접미사. 마커 보강: `app-frame-composer-repo` (3-chip 행 wrapper) · `app-frame-session-row` (sidebar 세션 행) · `app-frame-floating` (body portal Popover / Autocomplete). `docs/GLOSSARY.md` 에 *Frame/Tile/Screen/Header/Slot* 정의 + 사용 금지 어휘에 *Pane* / *Titlebar (셸 헤더 의미)* 추가. | **완료 (PR #25, 커밋 `1d68e05`)** |
| **Phase 3++ (PR #26)** | **ChatTile 분해** — 620줄 단일 파일에 모인 transcript 컴포넌트 5개 + composer 부속 2개 + 시간/JSON 포맷 유틸을 슬롯 디렉토리로 추출. 신규: `screens/chat/{format.ts, ToolCard, MessageMeta, AssistantMessage, UserMessage, PendingAssistant}.tsx` + `frame/composer/{ComposerChip, SkillsMenu}.tsx` 총 8개. ChatTile.tsx 잔류 = 셸 컴포지션 + state hook 연결 + 입력 핸들러만 (369줄). 에이전트 원칙 9 (단일 파일 분해 가이드: 한 파일 5+ 컴포넌트 또는 400줄 초과 시 분해 검토, 새 파일은 슬롯 디렉토리에 배치) 신설. | **완료 (PR #26, 커밋 `3d202ff`)** |
| **Feature-based 구조 감사 + [ARCHITECTURE.md](ARCHITECTURE.md) 엄격 갱신 (PR #28)** | **완료 (PR #28)** — `frame/` 완전 해체 결정 (→ `app/` + `features/` + `shared/`), `pages/` = 조립만, `router` = which, App Shell §3.A 조립 규칙 신설, §3-2 목표 트리 + §10 구현 대기 행 추가. 코드 변경 없음 — 문서만. | **완료 (PR #28)** |
| **Feature-based 아키텍처 구현 + ESLint boundaries 강제 (PR #29)** | **4-layer 렌더러 아키텍처 전면 구현.** `app/providers/` 6개 → `features/<X>/providers/` 4개 + `shared/navigation/` + `shared/theme/` 로 이전. `frame/` + `screens/` + `state/` 완전 해체 → `app/` (셸) · `pages/` (조립) · `features/` (도메인 6개) · `shared/` (범용) 4-layer 완성. `useSkills` → `shared/hooks/`. `useProjectSessions` → `features/sessions/hooks/`. `ProjectDetail` 3-파일 (`ProjectDetailPage` + `ProjectDetailView` + `ProjectDetailScreen`) → `pages/ProjectLandingPage.tsx` 단일 파일 통합. 신규: `useProjectChatLanding` · `ProjectSessionsPanel` · `ProjectLandingHeader` · `ProjectInstructionsSidebar`. `eslint-plugin-boundaries` v6 (`boundaries/dependencies`) 로 레이어 역방향·cross-feature import 회귀 차단. | **완료 (PR #29)** |
| **URL/path 라우팅 전환 (`app://` 커스텀 스킴 + BrowserRouter)** | **Context-기반 `ScreenId` enum 라우팅 → URL/path 라우팅 전면 전환.** `react-router-dom@^7` 도입. main: `protocol.registerSchemesAsPrivileged` 로 `app://` 표준 스킴 등록 + `protocol.handle` 의 SPA fallback (자산 파일은 그대로, 그 외 path 는 `index.html`); production 로딩이 `loadFile` → `loadURL('app://renderer/')`. renderer: `NavigationProvider` 삭제 → `BrowserRouter`. `app/router.tsx` switch → `<Routes>` (`/`=BootRedirector · `/new`=ChatPage · `/chat`=Navigate→/new · `/chat/:sessionId`=ChatPage · `/projects` · `/projects/:projectId` · `/engine` · `/skills` · `/captures` · `*`→/new). 신규 `BootRedirector` 가 `settings.lastSessionId` 조회 후 `/chat/<id>` 또는 `/new` 로 replace — `useChat` 의 부팅 자동 복원 effect 제거. 신규 `useChatRouteSync` 가 URL ↔ ChatState 양방향 동기화 (방향 1: URL→loadSession/newChat, 방향 2: 첫 턴 완료 시 `/new` → `/chat/<id>` `replace`). `Sidebar`/`useSessionHandlers`/`NewChatButton`/`ProjectsView`/`ProjectLandingPage` 의 `navigate(screen)` → `navigate('/path')`. `electron.vite.config.ts` 에 `enforce: 'post'` 플러그인으로 `base: '/'` 강제 (electron-vite preset 의 production `./` 덮어쓰기 우회). 삭제: `shared/navigation/NavigationProvider.tsx`, `shared/navigation/screens.ts`, `shared/types/screen.ts`. 신규 `shared/navigation/routes.ts` (path 패턴 + 라벨 + breadcrumb). | **완료** |
| **Phase 3++ (Sidebar brand + nav 재구성 + Header 5-버튼 툴바)** | **셸 정렬.** Sidebar `app-frame-sidebar-brand` = newChatSlot → 🐋 + "Orca" 로고 (브랜드 정체성만). Sidebar nav = 3-항목 (새 대화 `/new` · 프로젝트 `/projects` · 자동화 `/routines` placeholder). collapsed 상태 아이콘 컬럼도 동일 NAV + 활성 강조 동적화. Header `app-frame-header-left` 의 brand/breadcrumb/프로젝트명 제거 → 5-버튼 액션 툴바 (햄버거 popover + 종료 menuitem · 사이드바 접기 토글 · 검색 · 뒤로 · 앞으로). `NewChatButton.tsx` 삭제 + `features/chat/index.ts` barrel export 정리. shared/ui/Icon.tsx 에 4종 신규 아이콘 추가 (`clock`/`menu`/`arrowL`/`arrowR`). `shared/ui/Popover.tsx` 에 `placement?: 'top' \| 'bottom'` 확장 (default 'top' — 기존 호출처 무영향). | **완료** |
| **Phase 3++ (FTS5 대화 검색 모달 + IPC)** | **대화 이력 전체 검색.** `0003_messages_fts.sql` 마이그레이션 (FTS5 가상 테이블 `messages_fts` + INSERT/UPDATE/DELETE 트리거 + 기존 메시지 백필). `DbQueries.searchMessages` prepared statement (FTS5 quote escape + 모든 토큰에 prefix wildcard `*` 부착 — 어느 토큰이든 미완성일 수 있다는 가정). 신규 IPC 채널 `orca:search:messages` + zod 스키마 + preload `orca.search.messages` + renderer `searchApi.messages`. `app/SearchModal.tsx` 신설 — `#app-frame-modal` 슬롯 안에 conditional mount (React 19 useEffect 내 setState 금지 회피), 150ms debounce + request id supersede + `<mark>` split-parse XSS 방어 + ↑↓/Enter/Esc 키보드. `AppLayout` 이 `searchOpen` useState lift, Header `onOpenSearch` / OverlayLayer `searchOpen+onCloseSearch` props 로 전파. Worker thread 도입 보류 (FTS5 latency 단위 ms — 향후 perf 회귀 시 utilityProcess 위임 검토). | **완료** |
| **Phase 3++ (Sidebar 활성 효과 URL 동기화)** | **활성 표시의 진실은 URL.** `useSessionHandlers` 의 `currentSessionId` 를 `matchPath('/chat/:sessionId', pathname)` 로 도출 — 기존 `ChatContext.state.sessionId` 의존 제거 (캐시/IPC 호출 용도로만 잔존). 다른 라우트로 이동 시 모든 세션 행 활성 자동 해제. Sidebar NAV[0] '새 대화' isActive 도 `p === '/new'` 로 좁힘 (`/chat/<id>` 진입 시 활성 해제). `handleDeleteSession` 의 `wasActive` 검사도 새 `currentSessionId` 사용. | **완료** |
| **Phase 3++ (MCP 서버 지원)** | **전역 MCP 서버 등록·관리 + query 주입.** 사이드바 nav 4번째 항목 'Skills & MCP'(`/skills`) 신설. `SkillsMcpView` 의 MCP 섹션을 하드코딩 mockup → 실데이터(`useMcpServers`)로 교체 (토글·삭제·추가/편집 모달 `AddMcpServerModal`). 신규 IPC 도메인 `orca:mcp:*` 4채널 + `McpStore`(`src/main/mcp/store.ts`, electron-store `orca-mcp`). **인증값은 Electron `safeStorage` 로 암호화 저장** (authEnc base64, renderer 엔 `hasAuth` boolean 만 노출 — 보안 베이스라인 준수). transport 2종: stdio(command·args·인증 env) / streamable-http(url·Bearer 토큰). `handleChatSend` 가 `buildQueryOptions()` 로 활성 서버를 `query().options.mcpServers` + `allowedTools`(`mcp__<name>__*`) 에 주입. `SessionAdapter.sendMessage` 에 `mcp?: McpQueryOptions` 인자 추가. Skills(좌측)·권한 섹션은 mockup 유지. | **완료** |
| **Phase 3++ (uv 기반 Python 런타임 내장)** | **agent 의 Python 실행을 위한 격리 uv 환경.** `<userData>/runtime` 에 uv venv + 인터프리터(3.12) 격리 — 시스템 비오염. `src/main/runtime/` 3모듈 (`paths`·`env`·`PythonRuntime`): `buildPyEnv` 가 `UV_*`/`PATH` 단일 소스 생성(인덱스/미러는 operator env pass-through — 미설정 시 공개 PyPI/github 기본), `PythonRuntime` 가 멱등 초기화 상태기계(`.ready` 마커 + 자가복구 + EventEmitter status). `IpcRouter.start()` 가 `ensure()` 비동기 킥, `handleChatSend` 가 `getEnv()` 를 SDK `query().options.env` 에 주입 + `PY_AGENT_RULES`(uv run / uv pip 규약) 를 systemPrompt 에 합류. 신규 IPC 도메인 `orca:runtime:*` 3채널(status/prepare/statusEvent, 모든 창 브로드캐스트). 렌더러 `features/runtime/` (RuntimeProvider · useRuntime · RuntimeStatus 사이드바 풋터 위젯 · RuntimeModal — InstallerDialog 스트리밍 패턴, 실패 시 재시도). 패키징: uv 바이너리 `scripts/fetch-uv.mjs` → `resources/bin/` + electron-builder `extraResources` 동봉. `SessionAdapter.sendMessage` 에 `env?` 인자 추가. | **완료** |
| **MCP & Skill 통합 레이어** | **정규 소스 = `~/.config/orca/mcp.json`**(순정 Claude `mcpServers` 스키마 + `${VAR}`). 비밀은 평문 금지 — `secret-store`(safeStorage, env-var 이름 키잉)에 저장하고 소스엔 `${VAR}` 플레이스홀더만. `${VAR}` 확장 resolver 순서 = safeStorage → process.env (미해결 시 서버 드롭+사유). **양 백엔드 대칭 변환기** `toClaudeConfig`/`toOpencodeConfig` (둘 다 `Record<string, <Backend>Mcp>` 동형 반환, opencode 는 순수 함수+테스트만 — 어댑터 미구현). `enabled`/`description` 은 settings(`mcpEnabled`/`mcpMeta`) 가 보유(mcp.json 순정 유지). 레거시 `orca-mcp` → 파일 모델 1회 마이그레이션. **확장 정규 레이어**: `~/.config/orca` 디렉토리 *자체*가 Claude 로컬 플러그인(`.claude-plugin/plugin.json` + 정규 소스 `skills/`·`agents/`·`commands/`)이 되어 query() 에 `plugins:[{local, path: ~/.config/orca}]`+`skills:'all'` 로 명시 로드. Skill 은 양 백엔드 공통(opencode 가 `.claude/skills` 네이티브 글로빙), agents/commands 변환기·opencode 머티리얼라이저·Hook/full-plugin 은 백엔드 종속이라 future anchor. **Vitest 도입** (expand/convert 14 케이스). | **완료** |
| **Phase 3++ (plan 우측 분할 타일)** | **계획을 우측 분할 타일로.** `ExitPlanMode` 가 제출한 `plan` 마크다운을 채팅 tile 우측의 `PlanTile`(`app-frame-tile-separator` 분리선 + `useDragResize({invert})` 280–640px)에 표시하고 **자동 오픈**(plan_review → `planTileOpen=true`). 수동 토글은 채팅 titlebar 우측 끝 `panelR` 버튼. 검토 종료 후에도 `planContent` 유지(읽기전용). `ChatState` 에 `planTileOpen`/`planTileWidth`/`planContent` 추가, `useChat` 에 `togglePlanTile`/`openPlanTile`/`closePlanTile`/`setPlanTileWidth`. `Icon` 에 `panelR` 추가. | **완료** |
| **Phase 3++ (승인 카드 + AskUserQuestion 네비게이션)** | **plan 승인 UX 를 Claude Code 웹 패턴으로.** 계획 검토 중 Composer **입력창을 `PlanApprovalCard` 로 교체**(amber dot + "Claude가 계획을 제안했습니다" + 플랜 열기 / 거부·수정…·수락, `Ctrl/Cmd+Enter`=수락). `PlanReviewCard`(인라인) 삭제, `PlanTile` 은 **내용 전용**(헤더 복사 `CopyIconButton` + 닫기)으로 축소. **AskUserQuestion 카드**: 질문 2+개를 한 화면에 쌓던 것 → **질문 1개씩 + 좌우 화살표 + "N/총" 카운터**(←→ 키 네비, `current` 상태). 모두 렌더러 한정(reducer/IPC 불변). | **완료** |
| **범용 Provider Runtime 재설계 (설계 + 인터페이스)** | **OpenCode/Claude 공통 정규화 계층을 기존 아키텍처 문서에 흡수.** 별도 문서 없이 [arch/backend/provider-runtime.md](arch/backend/provider-runtime.md) (정본 — NormalizedEvent · PermissionBridge · ApprovalResolution 2분기 · PendingApprovalStateMachine · AppCommandPolicy 3분기 · PermissionModeController · SessionCapability · RevertManager · ErrorClassifier · AppMessagePart persistence · Telemetry · AuthStore · AuditLog · ModelProviderConfig + 현행→목표 매핑표 + SDK 타입 확정 절차) + [arch/frontend/rendering.md](arch/frontend/rendering.md) §1.6~6.9·§7.6 (ToolRendererRegistry · ApprovalCard 일반화 · StructuredOutput · Streaming lifecycle · TelemetryPanel) 에 인터페이스 코드블럭으로 정의. **Claude 기준 + OpenCode 확장점만**, 각 항목 ①설명②예시③현재 코드 갭④인터페이스 4단. **코드 변경 0** (실구현 rename 범위 밖 — 매핑표로만). `[검증]`/`[미확인]` 태그 보존(두 SDK 미설치). | **설계 확정 / 구현 대기** |
| **표준화 리팩토링 스테이지 A (배포 계층)** | **sources/dist 분리 + ExtensionDeployer + StandardConformance.** `~/.config/orca/` 를 `sources/`(사람 편집 SSOT: instructions/AGENTS.md · skills · agents · commands · mcp/mcp.json · hooks/<engine>) 와 `dist/<engine>/`(생성물, 편집 금지) 로 분리. `config/migrate-sources.ts` 가 구 평면 레이아웃을 1회 이전(멱등). `deploy/deployer.ts` 가 기존 순수함수(`toClaudeConfig` 등)를 *조립*해 `dist/claude-code/` 플러그인 루트 렌더(render→validate→backup-then-write, dryRun 지원). `plugin-bundle.ts` 삭제(deployer 흡수), `adaptSkills` → `distDir('claude-code')`, `mcpJsonPath` → `sources/mcp/mcp.json`. 부트: `ensureConfigDir → migrateConfigToSources → migrateMcpToFile → deploy('claude-code') → scanSkills`. Vitest 11 케이스(migrate-sources·deployer·conformance). **claude-only — OpenCode 는 deployer 의 engine 파라미터·`sources/hooks/<engine>` 자리로 seam.** | **완료** |
| **표준화 리팩토링 스테이지 B (런타임 P0 — NormalizedEvent + PermissionBridge)** | **provider 중립 이벤트/권한 추상화.** ① B1: `runtime-events/normalized.ts` 의 `chatEventToNormalized` 매퍼(ChatEvent→NormalizedEvent). ② B1′: 와이어(`orca:chat:event`)·preload·reducer·router persist·어댑터를 `NormalizedEvent`(`session.updated`·`message.delta/completed`·`tool.call.started/completed`·`telemetry`·`error`)로 전면 전환. `ChatEvent` 는 어댑터 내부 중간표현으로 잔존. reducer 상태(Message/ToolCall) 형태 유지 → transcript 컴포넌트 무변경. ③ B2: canonical `permission.requested`/`permission.resolved` 1급 이벤트 + `PermissionAction`(ask_question/plan_review/tool_approval 3종)·`ApprovalResolution`(2분기, claude `PermissionResult` 동형)·`PermissionOrigin`. `runtime-events/permission-bridge.ts`(`agentPermissionRequest` 합성 + `AppCommandPolicy` 3분기 seam). router 가 ask/plan 을 permission.requested 로 emit(브로커·respond 채널 보존, approvalId=requestId), reducer 가 action.kind 로 분기. Vitest: normalized 8 + permission-bridge 3 + reducer 갱신. **claude-only — `ProviderId` 에 opencode seam.** 검증 한계: 헤드리스라 GUI 시각 회귀 미실행. | **완료** |
| **표준화 리팩토링 스테이지 C (프론트 렌더링)** | **렌더링 추상화(rendering.md §1.6·§7.6).** ① C1: `features/chat/components/transcript/registry.ts` — `ToolRendererRegistry`(`RenderableKind` command/file_edit/file_read/ask/generic + `ToolRenderer{kind,match,Body}` + `resolve`). `ToolCard` 의 도구이름 switch → `registry.resolve(name).Body` 디스패치. `FileBody` 추출. match 는 provider 중립 도구이름(OpenCode seam). Vitest 4. ② C2: `PlanApprovalCard` → **`ApprovalCard`** 일반화(입력-대체 승인 게이트, `permission.requested` action.kind 분기). plan 마크업 그대로 보존. ③ **C 마무리: `tool_approval` 게이트 활성 + 권한 응답 채널 단일화** — `makeCanUseTool` 이 `RISKY_TOOLS` 화이트리스트(Bash/Write/Edit/MultiEdit/NotebookEdit)만 surface(안전 도구 자동 통과). `ApprovalCard` 에 `ToolApprovalBody`(거부/세션 동안 허용/허용 3지) 추가. `askRespond`/`planRespond` 2채널 → 단일 `permissionRespond` + `InteractionBroker<ApprovalResolution>`. "세션 동안 허용" = `allow.updatedPermissions{scope:'session'}` → router `sessionAllowedTools` Map. Vitest: isRiskyTool 3 + canUseTool tool_approval 6 + reducer 5 + protocol 8. **불필요(미채택)**: pendingApprovals 단일 큐·ask 카드 병합(도메인별 분리 저장 `pendingAsks`/`pendingPlanReview`/`pendingToolApproval` 으로 충족). 검증 한계: 헤드리스라 GUI 시각 회귀는 사용자 수동 검증. | **완료** |
| **표준화 리팩토링 정리 (레거시 ChatEvent·cli.\* 제거)** | `ChatEvent` 타입/스키마 완전 삭제(어댑터가 `claude-map.ts` 의 `claudeToNormalized` 로 SDK→`NormalizedEvent` 직접 정규화), `runtime-events/normalized.ts` 제거(매핑 책임 adapters 로 이전), `cli.*` 에러코드 → `sdk.*`, `claude-code.ts` 에서 매핑/헬퍼 분리(`claude-map.ts`·`claude-adapt.ts`), `paths.ts` 미사용 게터 정리. 문서: `GLOSSARY`·`IPC_CONTRACT`·`TRD` 동기화. | **완료 (PR #47, 커밋 `0a27a1c`)** |
| **P0 마무리 — Extension 개명 + Capability(능력 탐지) + RevertManager** | **① "Extension" 개명 (선결 순수 rename):** per-turn 보조기능 묶음(구 `OrcaCapabilities`/`CapabilityBuilder`)을 Extension 어휘로 일괄 개명 — `src/main/capabilities/` → `src/main/extensions/`, `TurnExtensions`/`ExtensionBuilder`, `OrcaHook*`/`OrcaSkillRef` → `Normalized*`, `TurnRequest.extensions`. "capability" 어휘를 능력-탐지 전용으로 비움(동작 변경 0). **② Capability(능력 탐지, provider-runtime.md §4/§15):** 비워진 `src/main/capabilities/` 를 능력-탐지가 차지 — `SessionCapabilities`/`RevertCapabilities`/`CancellationCapability`/`ProviderDescriptor`(순수 DTO, `shared/ipc.ts` SSOT) + `CapabilityProbe`(main). claude 는 두 SDK 미설치(§13)라 **정적 서술자** `CLAUDE_DESCRIPTOR`(`claude-probe.ts`, 필드별 `[검증]`/`[미확인]` 주석) 반환. `SessionAdapter.describe()` + `registry.describeAll/describeActive`, `backend:list` 가 **computed-on-the-fly** 부착(DB 영속 없음). 렌더러 `useBackend.capabilities` → `BackendStatus` 지원-기능 지표 + Composer cancel `cancellation.sessionAbort` 게이팅(page→props, 4-layer 경계 준수). **③ RevertManager seam (§5):** conversation↔file 되돌리기 의미 분리(절대 병합 금지), cap-가드 4메서드, claude 전 cap false 라 throw-only. Vitest 14(claude-probe 6 + revert-manager 4 + describe 2 + registry 2). 문서: `GLOSSARY` · provider-runtime.md §4/§5/§15/§20. | **완료** |
| **PermissionModeController 구현 (권한 모드 라이브 전환, 3단계)** | **세션 진행 중 권한 모드를 라이브로 전환(provider-runtime.md §3).** **① 문서 전제 정정:** §13 "두 SDK 미설치" 거짓 전제 교정 — Claude Agent SDK 는 lockfile `0.3.143` 핀 고정·spec 문서로 타입 확정. 출처 태그 택소노미 신설(`[검증-타입]`/`[검증-런타임]`/`[미확인-런타임]`/`[N/A-claude]`/`[미확인-opencode]`), `CLAUDE_DESCRIPTOR.fork` `false→true`(`Options.forkSession` 실존), `children`/`summarize`/`share`→`[N/A-claude]`. 라이브 전환 선결조건 교정 = **스트리밍 입력 모드**(장수명 `ClaudeSDKClient` 클래스 아님). 설치된 `sdk.d.ts` 교차검증. **② 정규화 + Controller seam:** `shared/permission-mode.ts`(`NormalizedPermissionMode` 6종 + `toClaudePermissionMode`/`fromUiPermissionMode`, 순수·zod/SDK 무의존) + `runtime-events/permission-mode-controller.ts`(세션-키 상태, `RevertManager` 선례의 tested seam). `SessionCapabilities.liveModeSwitch?` 선언. **③ 턴-스코프 스트리밍 + 라이브 전환 + 6종 UI(옵션 A):** 풀 크로스턴 멀티세션은 resume-from-DB SSOT 와 충돌·구동 UI 부재로 후속 분리. 턴마다 `query()` 를 *메시지 1건 후 `result` 까지 열려있는 `AsyncIterable`* 로 시작 → 턴 진행 중 Query 핸들 생존(`setPermissionMode`/`interrupt`/`setModel`). `adapters/streaming-input.ts`(generator 미종료 불변식 격리+테스트), `SessionAdapter.sendMessage → LiveTurn`. 신규 채널 `orca:permission:setMode`(controller SSOT + 진행 중 턴이면 즉시 라이브 전환). 6종 UI(`modes.ts`/`ModeMenu`) + **위험 모드(`bypass`/`dont_ask`) 2-스텝 확인 가드**. `TurnRequest`/`SendChatMessage`/`ChatState.permissionMode` → `NormalizedPermissionMode`. `CLAUDE_DESCRIPTOR.liveModeSwitch=true`. Vitest: streaming-input 4 + permission-mode 5 + controller 6 + protocol(setMode·send 6종) + reducer. 전체 194 통과. `IPC_CONTRACT`(33 채널). 검증 한계: 헤드리스라 라이브 전환 GUI 회귀는 수동 검증. | **완료 (PR #52, ①`f46c752`·②`c8a22f1`·③`1af0092`)** |

| **Track A 잔여 P0 마감 — broker `timed_out` + 문서 동기화** | **`PendingApprovalStateMachine` 의 `timed_out` 분기 구현(provider-runtime.md §3).** `InteractionBroker.register` 에 선택 `opts{timeoutMs, timeoutValue, onSettle}` 추가(하위호환 — 3-인자 호출 무변경). 종료 경로(resolved/timed_out/aborted)를 단일 `settle()` 로 통합(타이머·abort 리스너 정리 일원화), `PendingApprovalState` export. router 는 timeout 미와이어링(벽시계 auto-deny UX 회피 — mechanism 만 준비, OpenCode 이벤트형/서버 TTL 도입 시 소비). 함께 코드보다 뒤처진 문서 drift 동기화: provider-runtime.md §3·§4·§20 의 라이브 `setPermissionMode` 위임(이미 `router.ts`+`claude-code.ts` 구현됨) "PR③ 대기" → 완료 표기. broker fake-timer 7 케이스. 200 통과. | **완료 (브랜치 `claude/sleepy-knuth-SjJTy`, 커밋 `64e3741`)** |
| **AppMessagePart 정규화 Persistence (provider-runtime.md §7)** | **메시지 영속화를 평면 `content`+`tool_calls` → 순서 보존 parts 로 전환.** 정식 배포 전이라 하위호환 없이 단일 전진 구현. `0004_message_parts.sql`(`message_parts(message_id FK, idx, type, tool_run_id, payload_json)` + 기존 데이터 backfill + `DROP tool_calls`; `messages.content` 는 FTS5 text-cache 로 유지). `DbQueries.appendPart`/`upsertToolResultPart`/`loadParts`. `claude-map` 이 `assistant` content 의 `thinking` block → `message.reasoning`(signature opaque). `NormalizedEvent.message.reasoning` + `AppMessagePart` union(8종) + `LoadedMessage.parts`(content/toolCalls 제거). `router.persist` 가 한 턴의 모든 파트를 같은 assistant 메시지에 순서대로 누적(기존 "텍스트→도구→텍스트" 메시지 분절 버그 해소). renderer `Message.parts` 모델 + `lib/parts.ts` 셀렉터, `AssistantMessage` 가 reasoning 접이식 블록 신규 렌더. 기존 `ToolGroup`/`ToolCard`/`AskExchange` 는 셀렉터 합성 `ToolCall` 재사용. **확장사고는 스트리밍과 양립**(설치 SDK 0.3.143 `BetaThinkingBlock` 검증 — 과거 미러 스냅샷의 "thinking↔streaming 트레이드오프" 서술은 stale). 210 통과. DB 단위 테스트는 better-sqlite3 네이티브 ABI 가 node-vitest 미로드라 GUI 수동 검증으로 갈음. | **완료 (브랜치 `claude/sleepy-knuth-SjJTy`, 커밋 `da4ff32`)** |
| **Track C — ToolRendererRegistry 표준화 (rendering.md §1.6)** | **렌더 카드 taxonomy 를 정본 11종으로 정렬.** `RenderableKind` ad-hoc 5종 → 표준 11종(`terminal·file_preview·diff·search·approval·agent_task·session_graph·context_injection·structured_output·error·telemetry`) + 실용 `generic`/`ask`. `match`/`resolve` 가 도구 이름 대신 **`ToolCall`**(tool_call+tool_result 파트 페어 view)을 받아 result shape 검사 가능. claude 소스 없는 카드(search/agent_task/session_graph/context_injection/telemetry)는 seam. 부수 갭 해소: 영속 **error 파트가 로드 세션에서 안 보이던 문제** — `AssistantMessage` 가 error→`ErrorCard`, structured_output→`StructuredOutputCard`(최소 seam) 렌더. `lib/errorLabels.ts` 추출(ChatTile 배너와 라벨 공유). rendering.md §1.2 문서-코드 충돌 정정(스트리밍 중 plain text → 라이브 Markdown, 코드가 진실). 211 통과. | **완료 (브랜치 `claude/sleepy-knuth-SjJTy`, 커밋 `fd6ab42`)** |
| **Track C — reasoning 라이브 스트리밍** | **확장사고를 텍스트와 동형으로 턴 중 라이브 표시.** `NormalizedEvent.message.reasoning.delta`(transient, `message.delta` 동형). `claude-map` 의 `stream_event` 에 `thinking_delta`(`BetaThinkingDelta`) 분기 — `signature_delta` 는 스트림에서 무시(완성 블록이 signature 보관). reducer `pendingReasoning` 누적 → 완성(`message.reasoning`)·`telemetry`·`SEND` 시 비움. `ReasoningBlock.defaultOpen` + `PendingAssistant` 가 라이브 사고를 펼친 블록으로 표시 → 완성 시 영속 접이식 블록 대체. **순수 추가** — 런타임이 `thinking_delta` 미수신 시 현행과 100% 동일 degrade. 215 통과. | **완료 (브랜치 `claude/sleepy-knuth-SjJTy`, 커밋 `62ad32a`)** |
| **Track C — 메시지 내 콘텐츠 순서 보존 렌더 (Option B)** | **한 어시스턴트 턴의 "텍스트 → 도구 → 텍스트" 흐름을 화면에 그대로.** ① **백엔드**: `claude-map` 이 assistant content 의 text 블록을 `assembled` 로 말미에 합치던 것을 폐기하고 **만나는 위치에서 `message.completed` 를 블록 단위 emit**(빈 text 스킵) → 같은 메시지 안의 text 가 더 이상 tool 뒤로 밀리지 않음. ② **렌더러**: `lib/parts.ts` 에 `messageSegments(parts)` 신설 — parts 를 타입별로 뭉치지 않고 **연속 동종**을 묶은 `MessageSegment[]`(`reasoning`/`tools`/`ask`/`text`/`structured`/`error`)로 순서 보존 투영(tool_result 는 toolRunId 맵 페어링, AskUserQuestion 은 개별 `ask`). `AssistantMessage` 가 고정 카테고리 순서(reasoning→도구그룹→텍스트) 대신 세그먼트를 순서대로 기존 컴포넌트에 1:1 매핑. 단일 도구는 헤더 없는 ToolCard, 연속 병렬 도구만 그룹. **sub-agent(Task/Agent)는 별도 처리 없이 `tools` 세그먼트 일반 카드로 순서 안에 자동 렌더(자동 충족).** DB(`message_parts.idx`)·reducer 는 이미 순서 보존이라 무변경. 정식 배포 전이라 과거 dev 행 backfill 불필요(신규 턴부터 적용). claude-map 순서/인터리브/빈텍스트 + messageSegments 5 케이스. 222 통과. 문서 `rendering.md §1.6`·`provider-runtime.md §2` 동기화. | **완료 (브랜치 `claude/sleepy-knuth-SjJTy`, 커밋 `4bc1511`)** |
| **Track C — 스트리밍 throttle(§1.2) + auto-scroll pin(§1.8 ⑤)** | **스트리밍 렌더 비용 절감 + 로그 뷰어식 스크롤.** ① **16ms throttle**: `features/chat/lib/eventCoalescer.ts`(순수, 주입형 scheduler) 가 델타(message.delta·message.reasoning.delta)를 `requestAnimationFrame` 한 틱마다 버퍼링→일괄 dispatch, React 19 자동 배칭이 그 틱의 N개 dispatch 를 1렌더로 묶어 **프레임당 1렌더**(reducer 무변경 — 기존 델타 누적 테스트 그대로 통과). 비-델타(message.completed·tool.call.*·telemetry)는 버퍼 먼저 flush 후 emit 해 순서 보존(Option B). `useChat` 구독부가 코얼레서 생성·주입, 세션 전환(newChat/loadSession)·언마운트 시 `dispose()`. ② **auto-scroll pin**: `ChatTile` 이 맨 아래(`<24px`) pin 일 때만 따라 내려가고, 위로 스크롤하면 pin 해제(과거 고정). 새 user 메시지는 항상 재-pin. ③ **"맨 아래로" 버튼**: pin 해제 시 `Composer` 상단·가로 중앙(`absolute bottom-full left-1/2`, `chevD` 아이콘)에 노출 → 클릭 시 재-pin. props optional 이라 랜딩(NewChat/Project) 미표시. coalescer 4 케이스. 233 통과. 문서 `rendering.md §1.2·§1.8` 동기화. | **완료 (브랜치 `claude/next-task-request-O59s9`)** |
| **Track C — UX 수정 3종 (빈 사고 카드·상단 앵커·컨텍스트 영속)** | **① 빈 reasoning 카드 스킵**: `claude-map`(빈 `thinking` emit 안 함)·`messageSegments`(빈 reasoning 스킵)·`ReasoningBlock`(공백이면 null) 3층 가드. **② 새 user 메시지 상단 앵커(ChatGPT식)**: 전송 시 사용자 버블을 뷰포트 상단으로 smooth 앵커. transcript 끝 예약 spacer(높이 매 렌더 재계산·서브픽셀 가드, 답변 길수록 0 수렴)가 공간 보장, `data-app-user-turn` 마커로 최신 user 턴 식별, 앵커 직후 pin 해제. **③ 컨텍스트 도넛/패널 세션 영속**: 도넛을 `lastTelemetry` 단일 소스로 구동(`pendingInputTokens` 폐기), 비율=`contextTokens(=input+cacheRead+cacheCreation+output)/200_000`(`lib/telemetry.ts`). 마지막 턴 telemetry(JSON)+세션 누적 비용을 `sessions` 행에 영속(`0005_session_telemetry.sql`·`updateSessionTelemetry`), `LoadedSession`+`CachedSession` 으로 복원(`LOAD_SESSION`/`_FROM_CACHE`), `SEND` 가 비우지 않아 턴 중에도 유지 → 재진입/재시작/세션전환 후에도 표시(새 대화만 0). coalescer/parts/claude-map/telemetry/reducer 테스트. 239 통과. 문서 `rendering.md §1.8·§1.9`·`IPC_CONTRACT.md` 동기화. | **완료 (브랜치 `claude/next-task-request-O59s9`)** |
| **Track C — 컨텍스트 도넛/패널 재정의 + usage 원장 + 앵커 교정** | **직전 UX PR 의 피드백 반영.** ① **예약공간을 inflight 턴에만**: 전송 순간 확보→턴 완료·세션 로드·idle 에선 0 으로 해제(하단 빈 공간 제거). ② **컨텍스트 도넛/패널 재정의**: 사용량 토큰=`input+cacheRead+cacheCreation`(**출력 제외**), 비율=`/contextWindowFor(model)`. 윈도우=기본 200k, 모델명에 `'1m'` 포함 시 1M(`lib/contextWindow.ts`, 맵/env 불필요). `TelemetryPanel` 을 컨텍스트 **4행**(입력 누적·캐시·윈도우·사용%)으로 교체, 비용/모델/지연 행 제거. ③ **비용 → usage 원장**: `0005` 를 `sessions` 컬럼 2개 → **per-turn `usage_events`**(`session_id` SET NULL·`model`·`created_at`·토큰·`cost_usd`) 테이블로 재설계(파일·마이그명 `0005_usage_events`). 턴마다 `insertUsageEvent`, 세션 로드 시 `getLatestUsage`+`usageRowToTelemetry` 로 `lastTelemetry` 복원. 시간/모델별 집계(1일/주/월)는 스키마 준비만(UI 후속). `sessionCostUsd`/`lastTurnLatencyMs` state·`LoadedSession.sessionCostUsd` 폐기. contextWindow/telemetry/usageMap/reducer 테스트. 243 통과. 문서 `rendering.md §1.8·§1.9`·`IPC_CONTRACT.md` 동기화. | **완료 (브랜치 `claude/next-task-request-O59s9`)** |
| **Track C — TelemetryPanel (rendering.md §1.9)** | **턴 종료 통계(비용·모델·지연·토큰)를 표면화.** `claude-map` 의 `result→telemetry` 매핑을 `usage{inputTokens,outputTokens}` 단일에서 **`ProviderReportedTelemetry`**(`shared/ipc.ts` — model·input/output·캐시 토큰(`cache_read`/`cache_creation`)·`total_cost_usd`·`duration_ms`·`num_turns`·`modelUsage` camelCase 정규화, cost-tracking.md spec 로 필드 확정 → §8 `[미확인-런타임]` 해소)로 확장. reducer telemetry case 가 `lastTelemetry` + 세션 누적 `sessionCostUsd`(SDK 세션 합계 미제공이라 턴별 누산) + app-measured `lastTurnLatencyMs`(전송→telemetry 벽시계) 산출, 세션 전환/새 대화 시 자동 리셋(initialChatState 스프레드). 신규 `features/chat/components/TelemetryPanel.tsx` — Composer 풋터의 usage 도넛(`UsageCircle`)을 클릭 트리거로 `Popover` 안에 표시(값 없는 행 생략, `lastTelemetry` 없으면 도넛만 — graceful). 와이어 zod 불요(telemetry 는 main→renderer send). claude-map 4 + reducer 3 케이스. 229 통과. 문서 `rendering.md §1.6·§1.9`·`provider-runtime.md §8·§12·§13`·`IPC_CONTRACT.md` 동기화. | **완료 (브랜치 `claude/next-task-request-O59s9`)** |
| **비용·토큰 추적 (handoff `0002-cost-token-tracking`)** | **턴별·모델별 토큰/비용 영속 + 일/주/월 누적.** ① **스키마 통일**: `0006_turn_usage.sql` 가 merge 된 `usage_events`(0005, 불변)를 원 제시안 **`turn_usage`(턴 원장) + `turn_model_usage`(모델별 자식)** 로 id 보존 이관 후 drop. ② **모델별 영속**: `router.ts` telemetry case 가 가드(`dbSessionId && usage && hasContextTokens`) 하에 `insertTurnUsage`(id 반환)→모델별 `insertTurnModelUsage`(`modelUsage` 없으면 `u.model` 단일 폴백) 적재. 세션 로드는 `getLatestTurnUsage`(부모+자식 join, primary=max input_tokens 재구성)→`usageRowToTelemetry` 로 컨텍스트 도넛/패널 복원(`contextWindowFor` 윈도우 판정 보존). ③ **일/주/월 누적**: `cost/boundaries.ts`(로컬타임 경계 순수함수) + `cost/tracker.ts`(`CostTracker` 싱글턴 — `sumUsageSince` SQL SUM ×3, 부팅 1회 `recompute` + 턴 종료마다 `recordAndBroadcast` 로 `cost:summaryEvent` 전 창 push), `cost:summary` invoke 채널. **새 라이브러리 0**(DB SUM 파생). ④ **frontend 미러**: `features/cost/`(`CostProvider`+`useCost`, fetch+구독 읽기전용 — **표시 UI 없음**, seam 까지). `IPC_CONTRACT` +2 채널(총 35). boundaries 2 + queries(0006 이관+turn usage 3) + usageMap 갱신. 260 통과. | **완료 (브랜치 `claude/handoff-protocol-next-stage-jjmgso`, 커밋 `999c99b`)** |
| **디버그 패널 + MockAdapter 하네스 (handoff `0003-debug-panel-mock-adapter`)** | **LLM API 없이 renderer(스트리밍·사고·도구 카드·권한 승인·에러·컨텍스트 도넛)를 라이브 디버깅하는 dev 전용 하네스.** ① **main MockAdapter**(`adapters/mock.ts`, `id='claude-code'` 위장 — ProviderId/Backend/DB/reducer 리플 회피, 실트래픽과 동형): `sendMessage` 가 `getState()` 매 턴 read → `runScenario` LiveTurn, `req.requestApproval` 만 호출해 라우터의 권한 합성·broker 왕복·`flushAskAnswers`·`sessionAllowedTools`·deny+interrupt abort 를 기존 경로로 통과, sessionId 발급/보존·interrupt 종료·control no-op. ② **순수 시나리오 DSL**(`adapters/mock-scenarios.ts`, electron 비의존): `MockStep`(emit/delay/approval/telemetry) + `runScenario` 인터프리터(provider/sessionId 주입·signal.aborted 즉시 종료·주입형 sleep·approval allow/deny 분기·requestApproval 부재 시 자동 allow), 시나리오 8종(`full` 이 `NormalizedEvent` 11종 전수). telemetry 는 `costUsd:0`·토큰 합=`round(ratio×200k)` 로 도넛/`nearCompaction` 구동. ③ **라우터 와이어링**(`import.meta.env.DEV` 게이트): 비영속 `debugMock` 상태 + 핸들러 2개 + 어댑터 선택 1줄 + `requestApproval` 클로저가 broker 해소 직후 **`permission.resolved` 발행**(mock/실 공통 — 11종 전수의 유일 공백 해소). ④ **renderer**: `TweaksPanel`→**`FloatingPanel`** 일반화(+`PanelSelect`/`PanelSlider` atom), `features/debug`(DebugPanel + useDebugMock — Mock 섹션 + 기존 tweaks 이식), `OverlayLayer` `#app-frame-debug` 슬롯 `{DEV && <DebugPanel/>}`. preload `orca.debug` + `debugApi`. **prod dead-code 제거 확인**(out/main 핸들러·out/renderer 패널 부재). `IPC_CONTRACT` +2 채널(총 37, dev 전용 `debug` 도메인). mock-scenarios 9 + mock 4 케이스, 273 통과. | **완료 (커밋 `5ef793c`)** |
| **세션 자동 제목 생성 (handoff `0004-auto-session-title`)** | **새 세션 첫 턴 종료 후 Haiku 1-shot 으로 제목 요약 — 60자 절단 제목을 in-place 교체.** ① **어댑터 seam**: `SessionAdapter.complete(CompleteRequest)` — resume·canUseTool·MCP·skills·hooks 미주입 + `maxTurns:1`·`tools:[]`·`allowedTools:[]`·`settingSources:[]`·`persistSession:false` 로 비대화형 단발 completion(컨텍스트 오염 0). 저가 모델은 어댑터 내부 default(`claude-haiku-4-5`, router 는 model 미강제 — provider 중립), 모델 선택성 오류 시 model 키 생략 재시도 폴백. mock 은 결정적 `'Mock 자동 제목'`. ② **트리거**: `router.persist` telemetry case 종료부에서 `shouldGenerateTitle`(새 세션 × 1회 가드 × `title_source` × 입력 존재, 순수함수) 판정 후 fire-and-forget — resume 경로 제외, 30s 타임아웃, 실패는 `console.warn` 만(graceful degrade — 절단 제목 유지). ③ **rename 보호**: `0007_title_source.sql`(`title_source TEXT DEFAULT 'auto'`) + `updateSessionTitleAuto`(`WHERE title_source != 'user'`) + `renameSession` 이 `'user'` set — 자동 요약이 사용자 제목을 절대 덮어쓰지 않음. ④ **라이브 반영**: `orca:session:titleEvent` 전-창 push(`cost:summaryEvent` 동형) → `useSessions` 행 patch + `useChat` 활성 세션 헤더/캐시 동기화. `normalizeTitle`(따옴표·공백·60자) 순수함수. `IPC_CONTRACT` +1 채널(총 38). title 7 + queries 3 케이스, 283 통과. | **완료 (커밋 `ba36b8d`)** |
| **Composer 상태 알림 줄 (handoff `0006-composer-status-line`)** | **컨텍스트 포화를 비기너가 숫자 없이 인지하는 앰비언트 알림 — Tier1 pill + Tier2 팝오버 presentational UI 셸.** ① **view-model 순수함수**: `composer/statusViewModel.ts` `conversationStatusModel(state, costToday?)` — `safe→null`(DOM 부재), `warn→recommend='compact'`(요약 primary + 새대화 보조), `danger→recommend='newchat'`(요약 숨김), 카피는 `statusCopy.ts`(plan §카피 표 SSOT 1:1, 기술용어 0, `as const` 로 danger 에 compact 키 자체 부재). ② **UI 셸**: `ConversationStatusLine`(pill `<button>` + aria haspopup/expanded/controls + 색맹 대응 상이 카피, `text-warn`/`text-bad` 시맨틱 토큰만) + `StatusPopover`(제목/설명/정보 3줄/버튼/disclaimer 상시) — 기존 `shared/ui/Popover`(`placement="top"`) 재사용, 신규 훅 0. ③ **배선**: 새대화=`chat.newChat()` 실배선, 오늘비용=3 page 가 `useCost().summary?.day.totalCostUsd`→`formatApproxCost`(`features/cost/lib`) 문자열로 props 주입(cross-feature 결합 0, null 시 비용 줄만 생략), 요약=스텁(`console.warn`+TODO). ④ **임시 상태근사**(후속 교체 TODO): `contextTokens/contextWindowFor` ratio — warn ≥0.6, danger `nearCompaction()`∥≥0.85, telemetry 부재 시 safe. 기존 도넛/TelemetryPanel 과 공존. view-model 3케이스 테스트, 286 통과. IPC 무변경. | **완료 (커밋 `393c8c8`)** |

| **스트리밍 transcript 재렌더 범위 축소 (handoff `0007-transcript-render-memo`)** | **델타 프레임마다 transcript 전체 + 앱 셸이 재렌더돼 메인 스레드가 수백 ms 블로킹(35턴 기준 549~614ms 관측)되고 smooth 앵커 스크롤이 점프로 보이던 성능 버그 수정.** ① `ChatTile` 이 `groupTurns(state.messages)` 를 `useMemo` 로 고정 — `message.delta` 는 `pendingDelta` 만 바꾸므로(messages 참조 불변) 델타 프레임에서 `Turn` identity 유지. ② `lib/turns.ts` `turnEquals`(순수 — role·startIndex·메시지 identity) + `UserTurn`/`AssistantTurn` 를 `React.memo` 비교자로 — messages 가 실제로 바뀐 프레임(툴콜 추가)에서도 내용 같은 과거 턴은 skip. ③ `AssistantMessage` memo(reducer `appendAssistantPart` 가 마지막 메시지만 새 객체로 교체하는 불변식 활용) + `Markdown` memo(string source 값 비교) — react-markdown(unified) 재파싱 제거. ④ (프로파일로 발견) `useSessionHandlers` 가 매 렌더 새로 생기는 `chat`/`sessionsCtx` 객체 전체를 useCallback deps 로 써서 `useSidebarSlots` slot 안정화(→ `Sidebar` memo)를 무력화 — 안정([] deps) 함수만 뽑아 의존하도록 교정 + `Header` memo. CDP rAF 측정(동일 35턴 mock, dev): 최대 블로킹 **549~614ms → 133~166ms**, CPU 프로파일에서 markdown/shiki·`beginWork` 핫스팟 소멸(잔여 = dev 전용 오버헤드 + 레이아웃/페인트). turnEquals 4 케이스. 잔여 구조 개선(컨텍스트 셀렉터)은 Phase 4 Zustand 전환에서 흡수. | **완료 (커밋 `a68e465`)** |
| **스크롤 앵커링 CSS 예약공간 + Zustand 스트리밍 파이프라인 (handoff `0008-chat-anchor-reserve`)** | **직전 spacer 구현(JS 측정 DOM 직접 제어)과 reducer 델타 경로를 레거시로 보고 재설계.** ① **Zustand chat store 선행 도입**(state.md §1.4 시점 개정 — 사용자 결정 2026-06-11): `session`(커밋, 순수 `chatReducer` 래핑) + `live`(스트리밍 transient) 슬라이스. 델타는 reducer 우회 → 델타 프레임에 transcript·Composer·셸 재렌더 0, `UseChat`/`useChatContext` 폐기(selector 훅 + `chatActions`). ② **라이브 리프 분해**: `LiveReasoning`/`LiveText`/`LiveStatus` 개별 구독(사고 델타가 본문 Markdown 을 깨우지 않음) + `StreamingMarkdown`(`splitStableBlocks`) 로 프레임당 재파스 O(전문)→O(꼬리). ③ **커밋 경로 카드 격리**: `reconcileSegments` identity 보존 + 리프 memo — `tool.call.completed` 1건 = 변경 카드 1개 재렌더. ④ **CSS 예약공간 앵커**: Exchange 단위 렌더 + `[container-type:size]` + 마지막 라이브 교환 `min-h-[50cqh]`(50% 미드라인 유지) — fill 단계 scrollHeight 불변(레이아웃 JS 0), 초과 시 RO follow, 여백은 다음 메시지까지 유지(무점프 해제). 신규 의존성 `zustand@^5`(승인). 테스트 315. | **완료 (커밋 `e113eb4`)** |
| **orca.json 전역 설정 (handoff `0009-orca-config`)** | **`~/.config/orca/orca.json` 앱 전역 agent/provider 설정 — 부팅 1회 로드해 main 어디서나 동기 접근.** ① **파일 I/O·관용 파싱**: `config/orca-file.ts`(zod 스키마 + `parseOrcaFile` 순수 + `ensureOrcaFile` atomic tmp+rename 템플릿 생성 + `readOrcaFile`) — 3단 관용(전체 손상→기본값+warn·원본 보존 / 항목별 invalid→그 항목만 드롭+사유 / 미지 키→strip). `adapter`/`provider` 는 enum 아닌 string(미래 값이 파일 전체를 죽이지 않게 형태만 검증, 의미 검증은 소비 시점). ② **모듈 싱글톤**: `config/orca-config.ts` `loadOrcaConfig`(부팅 1회, `router.start()` try/warn) / `getOrcaConfig`(lazy `cached ?? load`) / `agentFor(adapter)`(일치 첫 항목). ③ **claude-code env 매핑·격리**: `adapters/claude-env.ts` `toClaudeEnv`(provider→`CLAUDE_CODE_USE_BEDROCK|VERTEX`, apiKey→`ANTHROPIC_API_KEY`, baseUrl→`ANTHROPIC_BASE_URL`, env 레코드 우선·미지 provider 경고+무시) + `mergeAgentEnv`(PATH 소실 방지 단일 관문 — 빈 agentEnv 면 base 비트 동일 보존). SDK 어휘는 adapter 경계 안에만. ④ **`${VAR}` 공용화**: `mcp/expand.ts` `expandVars` 추출·export(`VAR_RE` 단일 출처, `expandEnv` 동작 불변) — secret-store→process.env 어댑트 시점 해석, 미해결은 해당 env 키만 드롭(mcp 의 서버 전체 드롭과 의도적 차이). ⑤ **배선**: `TurnRequest.agent`/`CompleteRequest.agent` 미확장 전달(`extensions/types.ts`·`adapters/types.ts`) → `claude-code.ts` env 합성 2지점(sendMessage + runCompletion) + router 가 `agentFor(adapter.id)` 주입(sendMessage·complete). 평문 apiKey 허용은 보안 베이스라인 명시 예외(사용자 결정, 권장은 `${VAR}`). IPC/preload/renderer/`shared` 무변경, 신규 의존성 0. 신규 테스트 26(orca-file 9 + claude-env 11 + expand 6 보강), 문서 4건(TRD §6.8/standardization §5.1/security/GLOSSARY). | **완료 (커밋 `1deae14`)** |
| **agent×provider 모델 선택 (handoff `0010-agent-model-select`)** | **0009 잔여 범위(TRD §6.8) — orca.json 복수 agent 간 모델 선택을 완성.** ① **provider key 계층**: `config/provider-key.ts`(순수) — `providerKeyOf`(provider 부재→adapter 단독·존재→`${adapter}-${provider(trim·lc)}`) / `agentForProviderKey`(생성방향 매칭) / `dedupeAgents`(중복 key 드롭+warn — 조합 유일성) / `authTokenFor`(secret store `provider:${key}` 우선 → orca.json `authToken` `${VAR}` 폴백, 복호화는 턴 env 합성 시점만) / `modelNameForFamily`·`defaultModelFamily`(`family ?? name`) / `toAgentEnvironments`(화이트리스트 — 비밀·env·baseUrl 미노출). ② **IPC 38→39**: `orca:agent:list`(도메인 `agent` 신설) + `orca:chat:send` optional `providerKey`/`modelFamily`(매 턴 유효) + `orca:session:load` `providerKey` 응답. ③ **세션 잠금 = adapter, provider = 턴 단위**: `sessions.provider_key`(마이그레이션 `0008`, nullable — *마지막 사용 provider* 기록·매 턴 갱신, 토큰은 DB 평문·해시 어느 형태로도 미저장) + `router.resolveTurnAgent`(payload→adapter 일치 시 적용, 불일치/무효→`provider_key`→`agentFor` 폴백+warn) + 제목 생성 경로도 턴 해석 agent 사용. ④ **턴 단위 모델 적용**: `TurnRequest.model`→`claude-code.ts` query options(`...(model?{model}:{})`), 라이브 전환 채널 없음(`LiveTurn.setModel` 호출자 0 유지). ⑤ **renderer**: `useAgents` 훅 + `ModelMenu`(supported agent×family·세션 후 같은 adapter 만) + chat `setModel` 액션(턴 단위) + `/agents` 동적화(하드코딩 제거·`adapter`/`provider`/`models`). ⑥ **`authToken` 리네임**(구 `apiKey` deprecated 별칭+경고) + **mock UI 전 앱 규약 신설**(`MOCK_HATCH_BG`·`data-state="mock"`·disabled). 신규 의존성 0, 레이어 경계 0. 게이트 349/349. 문서 6건(IPC_CONTRACT/TRD §6.8/persistence/security/dom-architecture/ux-domains). 검증 r1 FAIL(공유 테스트 헬퍼 오염 회귀)→r2 PASS. | **완료 (커밋 `c193166`)** |

| **백엔드 분해 + 멀티세션 토대 (handoff `0011-main-decompose`, 리팩토링 스테이지 1/3)** | **router.ts 1,070→139줄 컴포지션 루트화.** 도메인 핸들러 `ipc/handlers/{session,project,mcp,misc}` + chat 파이프라인 `ipc/chat/{send,turn-registry,persist,title-generation,approvals}` 분리(검증·실패 동작 현행 보존 — 통일은 스테이지 2). **InflightTurn 을 WebContents 키(창당 1턴) → `TurnRegistry`(sessionId 키 + 새-채팅 pending 슬롯, `session.updated` 시 승격)** 로 전환 — 서로 다른 세션의 동시 턴 허용(멀티세션 토대), 같은 세션 중복 send 거부. permissionRespond 부수효과는 approvalId→턴 매핑(와이어 무변경). `DbQueries.getSessionById` 신설로 `listSessions(1000).find()` N+1 2곳 제거. `closeDb()` will-quit 와이어. `mcp/crypto.ts`→`config/crypto.ts`(config→mcp 역방향 import 제거). 1회성 레거시 이전(`mcp/migrate`·`config/migrate-sources`) 삭제 — 사용자 결정(2026-06-11, 정식 배포 전). turn-registry 6 케이스, 351 통과. `arch/backend/overview.md` §3 동기화. | **완료 (커밋 `5e835f7`)** |

| **IPC 정리 (handoff `0012-ipc-cleanup`, 리팩토링 스테이지 2/3)** | **runtime 고아 3채널 제거 + invoke 검증 단일 경로화.** `orca:runtime:{status,prepare,statusEvent}` 는 renderer 소비처(구 `features/runtime` UI) 제거 후 preload 에만 남은 고아 채널이라 삭제(사용자 결정 — 채널 39→36, `RuntimeStage`/`RuntimeStatus` 타입은 `main/runtime/` 으로 이동, PythonRuntime 자체는 main 내부 유지·dev 터미널 로깅). 신규 `ipc/registry.ts` 의 `handle(channel, schema, invalid, fn)`/`handlePlain` 으로 전 invoke 채널이 safeParse 단일 경로 — 채널별 실패 정책(`'reject'` vs `{fallback}`)을 등록부에 명시해 현행 동작 보존. 문서: IPC_CONTRACT 전면 동기화(총 36·agent §2.2-b 정식 편입·§2.11 제거 이력·§4 폐기된 `sdk.*` → `ErrorCategory` 8종 정정·§3 0008 어휘 정정·검증 정책 §1 명문화) + overview/TRD/docs-AGENTS 인벤토리. 351 통과. | **완료 (커밋 `03cc1f5`)** |

| **렌더러 멀티세션 store + Context 흡수 (handoff `0013-renderer-multisession-store`, 리팩토링 스테이지 3/3)** | **state.md §1.4 확정 결정(멀티세션 외피) 완성 — 사용자 결정으로 Phase 4 에서 당김.** chatStore 를 `{ sessions: Record<key, { session, live }>, activeKey }` 외피로 전환 — 키 = sessionId, 새 채팅 = `NEW_CHAT_KEY` 슬롯(→ `session.updated` 시 승격, main `TurnRegistry`(0011)와 대칭). 이벤트는 `ev.sessionId` 키 라우팅: **비활성 세션 턴이 백그라운드 누적**되고 활성 UI 를 깨우지 않음(델타 포함), 미지 키 폐기·무ID 활성 폴백(main error 이벤트에 sessionId 부착 보강). `chatReducer` 는 세션-단위 순수 함수로 무변경 — store 가 라우팅 담당("액션 sessionId 인자" 안 대체, 문서 개정). 구 `sessionCache`/`LOAD_SESSION_FROM_CACHE`/`CachedSession` 폐기 — Record 가 캐시 흡수(재진입 IPC 생략 보존), 코얼레서 세션 전환 dispose 제거. **Backend/Sessions/Projects/Cost Context 4종 → feature 별 Zustand store**(bootstrap-only Provider, 0008 선례) — installerOpen 토글·목록 갱신의 서브트리 전파 제거, 소비처 12파일 selector 전환. selector/액션 시그니처 보존(transcript·Composer 무변경). 동시 스트리밍 UX(배지·탭)는 후속 기능. 354 통과. | **완료 (커밋 `bce274f`)** |

| **provider settings dist 재구조화 (handoff `0014-provider-settings-dist`)** | **provider 설정을 orca.json agents[] → `sources/settings/<adapter>/<provider>/settings.json`(어댑터-네이티브 SSOT) + 어댑터당 `meta.json`(라벨·models) 으로 이전하고, SDK 세션을 격리모드로 전환.** ① **dist 재구조화**: deployer 가 플러그인 산출물을 `dist/<engine>/plugin/`(공유 1개) 로, provider settings 를 `dist/<engine>/<provider>/.claude/settings.json` 로 렌더(플러그인 스펙엔 settings.json 불가 — provider 디렉토리 분리). 최초 부팅 `deploy/scaffold.ts` 가 anthropic 템플릿+meta 스캐폴드(멱등). ② **격리모드 주입**: `adapters/claude-settings.ts` 가 SDK `resolveSettings({cwd: distProviderDir, settingSources:['project']})`(@alpha, CLI 동일 머지 엔진) + `filterEscalatingDefaultMode` + env `${VAR}` 확장(키 단위 드롭) + secret-store `provider:${key}` → `env.ANTHROPIC_API_KEY` 주입으로 effective 해석 → `adaptSettings` 가 `query({settings, settingSources: []})` 주입(**0005 "settingSources 미지정" 결정 폐기**, completion 경로 대칭). 함수 부재/dist 미배포 시 flat JSON 폴백. ③ **일반화 seam**: 어댑터-중립 `settings/provider-settings.ts`(`ProviderSettingsService` — 디렉토리 열거 SSOT·meta 관용 파싱·mtime 캐시·deploy 후 invalidate) + 어댑터별 `ProviderSettingsLoader` 주입(opencode 는 자기 포맷 로더만 추가하면 됨). ④ **orca.json 축소**: `{version, env?}`(앱 전역 env — subprocess env 베이스 병합), `agents` 클린 브레이크(경고+무시, TRD §6.8 수동 이전 표), `toClaudeEnv`(`CLAUDE_CODE_USE_*` 매핑) 삭제 — 네이티브 env 레시피 표 대체. ⑤ **경계 교체**: `TurnRequest.agent` → `providerSettings`(불투명 blob), `orca:agent:list` 원천 교체(페이로드 shape 0010 유지 — renderer 변경 0), 턴 폴백(payload→세션 provider_key→기본 provider) 보존. 신규 의존성 0. 372 통과(+28). 문서 6건(TRD §6.8·standardization §5.1/5.2·adapters §1.3·security·INDEX). 사람 확인 대기: OAuth 격리모드 실기·bedrock·ModelMenu 회귀. | **완료 (브랜치 `claude/amazing-hamilton-7ovkzq`)** |

| **provider settings 주입 결함 수정 (handoff `0015-settings-flag-string`)** | **0014 격리모드 주입의 런타임 결함 수정.** SDK `Options.settings` 는 d.ts 상 `string \| Settings` 지만 런타임 transport 가 값을 직렬화 없이 CLI argv 에 그대로 push 한다(0.3.143~0.3.175 확인) — 0014 가 객체를 넘긴 탓에 spawn 이 `"[object Object]"` 로 강제 변환해 **provider settings 가 적용된 적이 없었다**(조용한 실패). 수정: ① `ProviderSettingsLoader` 반환을 `{settings, env}` 로 분리(`ResolvedProviderSettings.env` 추가, 서비스 캐시 동반), ② claude 로더가 effective 에서 env 를 떼어 분리 반환(미확장 `${VAR}` settings 유출 0), ③ `adaptSettings` 가 settings 를 **인라인 JSON 문자열**(`JSON.stringify`)로 flag 레이어 주입(CLI `--settings` 는 인라인 JSON 허용) + env 제외로 **argv 평문 비밀 차단**, ④ 신규 `adaptEnv` 가 provider env 를 턴 env(uv+orca.json) 위에 오버레이해 subprocess env 로 주입(`query({settings: '<json>', settingSources: [], env})`, sendMessage/runCompletion 대칭). 읽기 경로(resolveSettings/flat 폴백)·dist 레이아웃은 0014 유지, renderer/IPC 변경 0(blob 패스스루). 신규 의존성 0. 375 통과(+3). 문서 3건(TRD §6.8·security.md argv 평문 불변식·standardization §5.1). 사람 확인 대기: **실기 턴 1회 settings/env 적용(1순위)**·OAuth 격리모드·bedrock. | **완료 (커밋 `210f0ac`)** |
| **구조 견고화 (provider 중립) (handoff `0016-provider-neutral-core`)** | **디자인 리뷰 스탭3 구조 견고화 1/3 — 코어 백엔드 중립화.** `NormalizedEvent` 11개 variant 전부에서 `provider` 필드 제거 — 어떤 소비자도 읽지 않는 write-only 메타였고 `session.backend`(0010 세션-어댑터 잠금)와 중복된 이중 진실원(스탭1 A2). "어느 백엔드인지" 는 `sessionId` → `session.backend` 로 파생. ① 매퍼/오케스트레이터 리터럴 제거: `claude-map.ts`(MapContext 포함) 미설정, `send.ts` 6 이벤트 + classify 컨텍스트는 `adapter.id` 파생·세션-이전 에러(스키마 검증·활성 백엔드 없음)는 provider 부재(4-bis), `persist.ts` `insertSession.backend = turn.titleAdapter.id`. ② `ClassifyContext.provider`·`ClassifiedError.provider` optional, `agentPermissionRequest` 시그니처에서 provider 제거. ③ 렌더러 `chatStore` `lastBackend` 리터럴 + OQ7 주석 제거(backend store 소관·read 0 무회귀). 기준 6 carve-out(사용자 결정 "코어/오케스트레이터만"): 렌더러 기본-백엔드 UI(backendStore·Composer·InstallerDialog·chatReducer.backend·Avatar)는 '기본 백엔드' Open Question 소관 후속. 비기능=Claude 직접 구현. 게이트 4종 ✅ 375 통과(무회귀). IPC_CONTRACT §3·§4 동기화. 사람 확인 대기: 채팅 GUI 회귀(에러/권한 카드·backend 표기). | **완료 (커밋 `99469df`)** |
| **구조 견고화 (main 경계) (handoff `0017-main-layer-boundaries`)** | **디자인 리뷰 스탭3 구조 견고화 2/3 — main 프로세스 레이어 경계 강제.** renderer 처럼 main 도 하향 의존만 허용(L0 shared→L1 domain→L2 adapters→L3 ipc), 상위 참조 금지. ① `eslint.config.mjs` 에 `src/main/**`+`src/shared/**` 대상 `eslint-plugin-boundaries` 블록 신설(elements main-root/ipc/adapters/domain/shared, 하향만·상위참조 error). `installer` 는 `AdapterRegistry` 사용으로 L2(adapters) 분류 — 유일 상위참조였던 것을 코드 이동 0 으로 해소. ② `import/no-cycle`(maxDepth Infinity) + `import/parsers`(@typescript-eslint/parser 로 .ts 의존 역-에지 추적) — 0011 config↔mcp 같은-레이어 순환 클래스 재발을 빌드 에러로 차단(임의 순환 주입 검증 후 원복, 기존 순환 0). ③ D2 분해: junk-drawer `settings/provider-settings.ts`(322줄·4책임) → `provider-registry`(열거)·`model-resolve`(모델)·`env-merge`(env) + 해석 서비스, 배럴 re-export 로 12개 importer 무회귀. ④ `src/main/AGENTS.md`(+CLAUDE.md stub) 레이어 가이드 신설, 루트 `AGENTS.md` 표 + `app/AGENTS.md` 링크. 신규 devDep `eslint-plugin-import ^2.32.0`(사용자 승인). 비기능=Claude 직접 구현. 게이트 4종 ✅ 375 통과(무회귀). | **완료 (커밋 `c26419d`)** |
| **구조 견고화 (비밀 경계 타입화) (handoff `0018-settings-branded-types`)** | **디자인 리뷰 스탭3 구조 견고화 3/3 — 비밀↛argv 불변식을 컴파일타임 타입으로 격상.** 0015 의 "env 는 argv 아닌 subprocess env" 불변식이 `delete settings.env` 한 줄 + 주석의 런타임 관례였던 것을 branded(nominal) 타입으로 격상. ① `settings/provider-settings.ts` 에 phantom `unique symbol` 기반 `ArgvSafeSettings`(env 제거)·`SubprocessEnv` + **단일 smart constructor `splitProviderSettings`**(분리·브랜딩이 일어나는 유일한 신뢰 경계, `as` 단언 격리). 계약(`ResolvedProviderSettings`·`ProviderSettingsLoader`)·캐시 타입 브랜딩. ② `claude-settings.ts` 가 `delete settings.env` 대신 생성자 호출(ad-hoc 분리 제거). ③ `adaptSettings`=`ArgvSafeSettings`만·`adaptEnv`=`SubprocessEnv`만 수용 → **env 머금은 객체를 argv 로 보내면 컴파일 에러**(Parse, don't validate). ④ `@ts-expect-error` 음성 타입 테스트 2건 + 신규 `tsconfig.test.json`/`typecheck:test`(main 테스트를 타입체크 게이트에 편입 — 브랜드 제거 시 directive 미사용으로 빌드 실패, 빌드 emit 무영향). phantom 브랜드라 런타임 속성 0 — 동작은 0015 와 동일. 문서 3건(security.md 컴파일타임 격상·standardization·TRD §6.8). 비기능=Claude 직접 구현. 게이트 4종 ✅ 377 통과(+2 음성). **→ 구조 견고화 3-part 시리즈(0016·0017·0018) 완료.** | **완료 (커밋 `37e95e1`)** |
| **Composer Claude Code 레이아웃 + 작업량(effort) 배선 (handoff `0020-composer-cc-layout`)** | **Composer 를 Claude Code 데스크톱 톤으로 재배치 + 작업량(thinking effort) 풀스택 구현(사용자 결정).** ① **레이아웃**: 전송/중단 버튼을 footer 우측 → textarea 와 **같은 첫 행** 우측 inline 으로 이동(`flex items-end gap-2`·`self-end`·원형 `rounded-full`). footer 우측은 `모델 칩 · 작업량 칩 · 도넛 usage`, 좌측은 `권한모드 칩 + "+"` 만. 기존 첨부/현재프레임/Skill 3칩은 `+`(`AttachMenu`) 메뉴로 흡수 — `첨부`/`현재 프레임`(비활성) + `Skill`(→ 기존 `SkillsMenu` 오픈). 백엔드 라벨은 패널 `title` 로 흡수(footer 미표시). ② **작업량(effort) 배선**: `model` 흐름을 그대로 미러 — `EffortLevel=low|medium|high|xhigh|max`(L0 `shared/ipc.ts`, main·renderer 공용) → store `effort`/`SET_EFFORT`/`setEffort`(기본 high) → `send` payload → `SendChatMessageSchema.effort` → `TurnRequest.effort` → `send.ts` → `claude-code.ts` query `...(effort?{effort}:{})`(SDK `Options.effort`, per-turn·라이브 전환 없음). 신규 `EffortMenu`(5단 라디오·한국어 라벨)·`AttachMenu`·`effort.ts`. ③ **autogrow**: `HighlightedTextarea` mirror+textarea `max-h-40`(160px)→`max-h-56`(224px). 비범위: 첨부/현재프레임 실기능·effort 라이브 전환·effort DB 영속(세션 상태만). 신규 의존성 0, 레이어 경계 0. 테스트 3종(어댑터 effort·schema effort·reducer SET_EFFORT) + 게이트 4종 ✅ 381 통과(ABI 재빌드 후). IPC_CONTRACT `orca:chat:send` payload 갱신(채널 36 유지). 기능 구현 = Codex. | **완료 (커밋 `08c95fe`)** |
| **Skill/MCP 표준 정렬 + settingSources 격리 해제 (문서 선행, handoff `0023-skill-mcp-isolation-docs`)** | **자산 호환성 기준 SSOT 분리 + SDK 표준 경로 정렬 + 격리 해제 설계를 문서에 먼저 반영(코드는 후속 라운드).** 호환 자산(skill·mcp·AGENTS.md)=Orca SSOT, 비호환(agents·commands·hooks·plugin)=engine-specific 으로 Orca 비관여(추후 claude plugin 지원 — `~/.claude/plugins` 스캔 + query 주입으로 연기). dist=설치 스테이징(SDK 표준 거울: skill→`.claude/skills`, mcp→루트 `.mcp.json`; settings.json 은 거울 예외 — query flag 주입). `query()` 에서 `settingSources` 옵션 **생략** → SDK 기본(user/project/local) 활성으로 사용자 전역 `~/.claude` skill·설정 상속, 끌려오는 allow 규칙은 `disallowedTools` 로 차단. **handoff 0014/0015 의 `settingSources: []` 격리모드 결정 폐기(supersede)** — 0014/0015 문서는 historical 보존, supersession 만 arch/TRD/PHASES 기재. 문서 5건(standardization §2/§5.1/§5.2·TRD §6.8·adapters §1.3/§3.1/§3.2·security §1.4·PHASES). 코드(deployer/claude-adapt/paths)+테스트(`deployer.test`/`conformance`) 정렬은 후속 handoff `0024-skill-mcp-isolation-impl`, "cwd 설치(복사)" 기능은 별건. | **완료 (문서, 커밋 `12c7e65`)** |
| **엔진&모델 페이지 CRUD (handoff `0021-engine-model-crud`)** | **엔진&모델 페이지의 mock 버튼 활성화 — claude-code provider 추가/편집/삭제.** 블러 3단 마법사 모달(engine 드롭다운 claude-code only·provider trim 검증·settings.json textarea + JSON 인라인 검증) + 신규 write IPC 도메인 `engine`(`add/update/delete/read`)가 `sources/settings/claude-code/<provider>/settings.json` 을 `writeJsonAtomic` 원자적 생성/교체/삭제 + 중복·빈값 거부. 순수 `extractModels`(env `ANTHROPIC_DEFAULT_{SONNET,HAIKU,OPUS}_MODEL`→family·`ANTHROPIC_MODEL`/top-level `model`→default·이름 `1m`→`family·1m` 표식)로 `meta.json` 모델 목록 갱신, 인식 0개 시 'SDK 기본 모델' 폴백(단위 테스트 동반). 뷰 분해(`AgentEnvironmentView`+`EngineCard`+`EngineModelList`) + agents 를 `shared/stores/agentStore.ts` 공유 store 로 승격(`refreshAgents()` → 카드·Composer/ModelMenu 동시 싱크, 앱 재시작 불필요). 카드 모델 row 는 실제 모델명 primary(`font-mono`)·family 태그 secondary·default rust 액센트. IPC_CONTRACT 36→40(engine 4채널). 기능 구현 = Codex. 사람 확인 대기: UI 시각·실환경 provider 추가/싱크/bedrock. | **완료 (커밋 `91be400`)** |
| **Skill/MCP 표준 정렬 코드 라운드 (handoff `0024-skill-mcp-isolation-impl`)** | **0023 문서 정본을 코드에 정렬(구현 범위 완료, D1 보류).** deployer 신 레이아웃 — skill→`dist/<engine>/.claude/skills`·MCP→`dist/<engine>/.mcp.json`(`${VAR}` placeholder 보존 복사) + manifest/agents/commands/hooks/settings dist 복사 **제거**. paths 헬퍼 신 레이아웃(`distSkillsDir`/`distMcpJsonPath`, `distPluginDir` 제거). `adaptSkills`=`{skills:'all'}`(plugins 제거), `adaptSettings`=settingSources 옵션 생략(SDK 기본 user/project/local 상속) + inline JSON `settings` flag 유지, `adaptMcp`=mcpServers+allowedTools 유지(외부 `~/.claude` MCP 상속 가능). settings 읽기 출처를 `sources/settings/<adapter>/<provider>/settings.json` 으로 정합. conformance `compatibilityPaths:['.claude/skills']`. 테스트(deployer/claude-adapt/conformance/settings) + 0023 "구현 대기" 마커 해소. 비밀 불변식(.mcp.json placeholder·env↛argv 0018 branded) 보존, 신규 의존성 0. **D1 사람 결정 대기**(보류): `disallowedTools` 차단 목록 + 사용자 allow 규칙의 canUseTool(RISKY_TOOLS) 우회 대응 정책. 비기능 = Claude 직접 구현. 사람 확인 대기: 실환경 `~/.claude` 상속·turn settings 적용. | **완료 (구현 범위, 커밋 `5d622cb`) / D1 disallowedTools 보류** |
| **meta.json 제거 + settings.json 모델 파서 (handoff `0026-settings-json-model-parser`)** | **파생 캐시 `meta.json` 폐기 + settings.json 직접 파싱으로 모델 추출 재구현(비기능 = Claude 직접 구현, 사용자 결정 3건).** 신규 L1 순수 모듈 `settings/claude-model-parser.ts`(`parseClaudeModels`) — `[1m]` 브래킷 분리(`oneMillionContext`)·env 모델 키(`ANTHROPIC_DEFAULT_{SONNET,OPUS,HAIKU}_MODEL`)·명시 모델(`env.ANTHROPIC_MODEL`>`model`, env 우선) 추출 후 **노출 목록 내 default 정확히 1개**(명시 일치→sonnet→haiku→opus 폴백). **필터링(스펙 "항상 3개" 재정의)**: 커스텀이 하나라도 있으면 그 커스텀만 노출, 전무할 때만 sonnet/opus/haiku 3개(`model:null`); 명시-only(DEFAULT 키 전무)도 "설정 없음"→3개. `listProviders` 가 각 provider 의 settings.json 을 관용 파싱(부재/손상→3 alias 기본 열거). 출력 형태 `AgentModelView={alias,model,isCustom,oneMillionContext,isDefault}` 를 렌더러까지 전면 도입(ModelMenu/EngineModelList/modelSelection 정렬, `model:null`→`modelNameForFamily` 가 bare alias 반환→SDK 해석). legacy 제거: `extractModels`·`updateMeta`·`readMeta`·`metaPath`·`OrcaModelSchema`·`OrcaModelConfig`·`EngineWriteResult.models`·`ProviderEntry.label`·`META_TEMPLATE`·"SDK 기본 모델" 빈-폴백. **handoff 0021(추출)·0014(meta 도입) supersede**(historical 보존). 신규 파서 테스트 12 + 영향 테스트 4종 재작성. 게이트 lint/typecheck ✅·영향 391 통과(db 9건 better-sqlite3 ABI 환경). 문서 3건(TRD §6.8/§6.8.1·IPC_CONTRACT §2.2-b/c·standardization §5.1). 사람 확인 대기: ModelMenu/EngineCard 시각·실환경 `model:null` alias 전송. | **완료 (커밋 push 후 기재)** |
| **디버그 패널 Wire 로그 토글 (handoff `0025-debug-wire-log-toggle`)** | **wire message(`NormalizedEvent`) 콘솔 덤프 dev 토글.** 디버그 패널에 "Wire 메시지" `PanelToggle`(기본 off) 추가 — ON 시 main 의 단일 outbound chokepoint `sendChatEvent`(`ipc/context.ts`)가 모든 `NormalizedEvent` 를 `[wire] <type> …` 로 `npm run dev` 터미널에 `console.log`. 토글 상태는 기존 DEV 전용 `DebugMockState`(`wireLog` 필드 1개 확장) + `debugGetMock`/`debugSetMock` 재사용 — **신규 IPC 채널 0**(debug 2 유지). `sendChatEvent` 가 자유 함수라 `ctx` 무접근 → `context.ts` 모듈 스코프 플래그 + `setWireLog()` setter 를 `debugSetMock` 핸들러(`import.meta.env.DEV` 가드)가 동기화, 기본 false 라 **프로덕션 항상 무출력**. 사용자 확정(콘솔만·메인 터미널)이라 채팅 transcript/DB 무변경. 게이트(typecheck/lint/`mock.test.ts` 4) ✅, 레이어 경계 0, 신규 의존성 0. 비기능 = Claude 직접 구현. 사람 확인 대기: `npm run dev` 토글 실기(터미널 `[wire]` 출력). | **완료 (PR #95, 커밋 `878ef2b`)** |
| **claude 어댑터 식별자 리네임 (handoff `0027-claude-adapter-rename`)** | **Orca 내부 어댑터/엔진 식별자 `claude-code`→`claude` 통일(60파일).** 버킷 분류로 변경 대상 격리 — 버킷 A(식별자 리터럴: `Backend`/`ProviderId`/`engine`/`adapter.id`/`SUPPORTED_ENGINE`/zod `z.enum(['claude'])`·`z.literal('claude')`), 버킷 B(파생 디렉토리 `sources/settings/claude/`·`dist/claude/`·`sources/hooks/claude/` — `paths.ts` 의 `Backend` 인자 파생), 버킷 C(외부/SDK 어휘 **무변경**: `@anthropic-ai/claude-code` npm 패키지·`.claude/skills` SDK 경로·`CLAUDE_CODE_USE_BEDROCK\|VERTEX` env·Claude Code CLI/문서 참조). **D1-a**(클린 브레이크 + graceful fallback): 마이그레이션 없음, 레거시 `claude-code-*` provider_key 는 `send.ts` 의 `byKey` 정확매칭 실패 시 세션 provider→기본 provider 폴백(크래시 없음·마지막-사용 provider 만 초기화). **D2-a**(파일/클래스 함께 리네임): `adapters/claude-code.ts`→`claude.ts`(+`*.test.ts`), `ClaudeCodeAdapter`→`ClaudeAdapter`. `provider-key.ts` 최장-접두 분해 로직 불변(주석만 갱신). 문서 6건 동기화(`TRD §6.8`·`IPC_CONTRACT`·`GLOSSARY`·`standardization`·`app/AGENTS.md`·`src/main/AGENTS.md`). `rg "claude-code" app/src` = 버킷 C 2건만 잔존. 게이트 lint/typecheck/test **400/400**(Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. 구현 = Codex. 사람 확인 대기: ModelMenu/EngineCard 시각·실환경 provider 싱크·레거시 provider_key 폴백. | **완료 (커밋 `cde7b94`)** |
| **provider settings env 를 ~/.claude 처럼 options.settings 로 주입 (handoff `0028-settings-env-override`)** | **`Query()` 격리 해제로 상속된 `~/.claude/settings.json` env 를 Orca provider env 가 못 덮어쓰던 버그 수정(비기능 = Claude 직접 구현, 사용자 결정 2건).** provider `settings.json` 을 `~/.claude/settings.json` 과 **동일 취급** — 로더(`adapters/claude-settings.ts`)를 `flatRead + stripEscalatingDefaultMode` verbatim 으로 축소하고 Orca 고유 `${VAR}` 확장·secret-store 토큰 주입(`provider:<key>`→ANTHROPIC_API_KEY, 토큰 저장 UI 미구현=dead code)을 제거. env 를 `options.settings`(settings 레이어)에 그대로 실어 사용자 전역 env 를 덮어쓰고, `options.env` 엔 시스템(턴) env 만(`adaptEnv` 단일 인자). **handoff 0015/0018 의 "env↛argv" 불변식 폐기(supersede)**: `splitProviderSettings`·branded 타입(`ArgvSafeSettings`/`SubprocessEnv`)·음성 타입 테스트 제거, `ProviderSettingsLoader`/`Service` unused `resolve`/`secrets` 파라미터·`router.ts` 배선 정리. argv 평문 노출은 앱-환경구성 메커니즘의 수용된 트레이드오프(same-user). 디스크 평문 0 유지(sources verbatim·dist 미배포). 0015/0018 historical 보존. 게이트 lint/typecheck/typecheck:test ✅·영향 3파일 **36/36**(전체 387/396, db 9건 better-sqlite3 ABI 환경). 문서 3건(security §1.4·TRD §6.8·standardization §5)·scaffold 주석. 사람 확인 대기: 실환경 turn 1회 env 덮어쓰기 실기. | **완료 (커밋 `3bf78bc`)** |
| **main 정리 + 집계/열거 perf (handoff `0029-main-simplify-cleanup`)** | **`/simplify app/src/main` 정리 + 후속 perf(비기능 = Claude 직접 구현, PR #100).** ① **중복 제거(동작 불변)**: 동일 `num` 타입가드 2개→모듈 스코프 1개(`claude-map`), `PROVIDER_NAME_RE`(`/^[A-Za-z0-9_-]+$/`) 3곳 중복→`config/provider-key.ts` 정본(deployer `MCP_KEY_RE` 별칭 포함)·engine-write·provider-registry import, `VAR_RE` 2곳→`mcp/expand.ts` 정본(+ `store.ts` 가 `authVarOf` 호출마다 재생성하던 정규식을 모듈 레벨 비-global 1회 생성으로 교체), `DEFAULT_ORCA_CONFIG` 2곳→`orca-file.ts` 정본, `combineSignals` 수동 `AbortController` 병합(13줄)→`AbortSignal.any`(2줄). ② **perf(1차 건너뛴 항목 재평가)**: cost `recompute` 가 `turn_usage` 를 3회 스캔(`sumUsageSince`×3)하던 것→**단일 조건부 집계** `DbQueries.sumUsageByBoundaries`(`WHERE created_at >= @monthStart` + 구간별 `SUM(CASE WHEN …)`) 1회 스캔(`sumUsageSince` 제거·결과 동등 테스트), 매 `chat:send` 마다 돌던 `ProviderSettingsService.list` 디스크 I/O(readdir+N×readFile+parse)→어댑터별 캐시(기존 `invalidateAll()` 에 합류, `resolve()` mtime 캐시와 동일 수명). ③ **재평가 무조치(오탐)**: "Promise.all" 은 better-sqlite3 동기라 무관(→실제 비효율=스캔 횟수로 재정의), `requestApproval` 클로저는 `RouterContext` 아닌 턴 수명 값+앱 싱글턴만 캡처·`finally` 해제라 누수 아님. **제외(사용자 지시)**: opencode 일반화(`deploy('claude')`·`SUPPORTED_ENGINE`·`claudeErrorClassifier` DI)·`env-merge` `process.env` 복사(SDK `Options.env` 계약). 인수 9/9, 게이트 lint/typecheck/typecheck:test/test **397/397**(Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0, IPC 무변경(채널 40 유지). 사람 확인 대기: 외부 수동 편집 staleness 정책 수용·cost/모델 UI 회귀. | **완료 (커밋 `4b50832`+`3c381b6`)** |
| **시스템 프롬프트 정책 구조화 + 가이드 교정 (handoff `0030-system-prompt-policy-structure`)** | **Opus 4.8 "시스템 프롬프트 관리 가이드 ver2" 의 Orca 적합성 검토·마이그레이션(비기능 = Claude 직접 구현, 사용자 결정 3건: 문서+코드 / 5장 전면 구축 / 버킷2 재검토).** 가이드 주장을 Orca 코드와 1:1 대조해 4버킷 분류 — 버킷1(이미 일치: preset+append·단일문자열·per-turn resume·`excludeDynamicSections` 생략·출력스타일 미사용), 버킷2(충돌→재검토), 버킷3(신규→구축), 버킷4(전제오류→주석). **버킷3 구축**: `PY_AGENT_RULES`(코드 상수 `runtime/env.ts`)를 가이드 5장 구조 `app/src/main/prompts/`(L1 domain)로 무회귀 이주 — `policies/python-runtime.md`(`.md?raw`, 마이그레이션 `.sql?raw` 동형) + `registry.ts`(PolicyBlock·tier·when 메커니즘) + `loader.ts`(`assemblePolicies` 정합 검증) + `buildAppend.ts`(tier/when 필터→`\n\n` join **단일 문자열**, 4블록 버그 회피) + `platformHints.json`/`.ts`(C 룩업 스캐폴드). `loader.test` 가 바이트 동일 잠금. `ExtensionBuilder` 생성자 `pyAgentRules`→`stableAppend`, `router.ts` startup 1회 조립(DB 지침은 매 턴 조회·무캐시 불변). **버킷2(충돌→Open Question)**: settingSources local 제외·settings.env 단일주입·옵션 캐싱을 교정 가이드 `docs/arch/backend/system-prompt.md` §5 OQ-A/B/C 로 등재(현행 0023/0024/0028·무캐시 유지 권고, 변경은 사용자 결정 후 별도 핸드오프). 신설 정본 `system-prompt.md` + 참조 5문서 정정(`docs/AGENTS.md`·`adapters.md §1.4`·`standardization.md §5.4`·`terms.md`·`GLOSSARY.md`). 인수 9/9, 게이트 lint/typecheck/typecheck:test/test **406/406**(신규 prompts 9 포함·Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0, IPC 무변경. 사람 확인 대기: 버킷2 OQ 재검토 결정·실환경 정책 append 적용. | **완료 (커밋 `f2fc12c`)** |
| **에이전트 아키텍처 분석 study 추가 (handoff `0032-study-agent-analyses`)** | **자체 구현(프레임워크 없는) 코딩 에이전트 2종의 아키텍처 분석 보고서를 `docs/etc/study/` 레퍼런스로 추가(비기능 = Claude 직접 구현, PR #105).** opencode(13파일 — `00-index` + 12챕터, 영어: 클라이언트/서버 자체 구현 에이전트, Vercel `ai` SDK 를 폴백으로만·에이전트 루프/프롬프트/툴/권한/컨텍스트 압축/세션 영속화/자체 LLM 프로바이더 프로토콜을 Effect 위에 직접 구현) + hermes-agent(13파일 — `README` + 12챕터, 한국어: Nous Research 자가개선 에이전트, `openai`/`anthropic`/`boto3` 직접 호출 루프 + 턴 종료 후 데몬 스레드 자가개선). 챕터 구성: 개요·전체 아키텍처·콜스택·핵심모듈·특장점·구현 디테일·큰그림(OS 관점)·종합평가·비용/토큰 모듈·컨텍스트 윈도우 추적·비전별 모듈 분류. **성격: 제품 코드 아닌 레퍼런스 자료**(`etc/lightweight-llm-strategy.md` 동결) — Orca 엔진/런타임(SessionAdapter·NormalizedEvent·압축·비용추적·권한 게이트) 설계 비교 참고용. 코드 변경 0(`app/**` 무수정)·`.gstack/` 미포함·trailer 규약 준수. 인수 6/6, 앱 게이트 N/A(문서 전용, handoff 0023 동형). 사람 확인 대기: 분석 내용 사실 정확성·PR 머지. | **완료 (커밋 `263f55e`, 정식화 `b3de19b`)** |
| **런타임 복원력 (handoff `0033-runtime-resilience`)** | **턴 실행에 감독·복구 레이어 추가(비기능 = Claude 직접 구현).** 에러 분류기(`runtime-errors/classifier.ts`)의 `retryable` 신호를 소비하는 곳이 없던 4 격차를 메움. **#1 idle 타임아웃**: 총량이 아닌 무응답(idle) `IDLE_TIMEOUT_MS`(120s) — `send.ts createIdleTimer` setTimeout→abort, 매 이벤트마다 reset(정상 장기 턴 보호), 만료 시 `error(stream_error/retryable)` 1회·`finally idle.clear()` 누수 0. **#2 부분 턴 마커**: 마이그 `0009_message_complete`(`ADD COLUMN complete DEFAULT 1` 레거시 backfill) + assistant insert `complete=0`→telemetry `markMessageComplete(1)`, 크래시 시 `complete=0` 디스크 잔존=시그널, 세션 로드 `incomplete` 노출(`session.ts` 핸들러)→transcript "응답이 완료되지 않았습니다" 칩. **#3 취소 ack**: 신규 `NormalizedEvent` variant `turn.aborted{reason:user_cancelled|timeout}` — cancel 은 에러 아닌 정상 종료라 분리, `turn.owner`(WebContents) 타깃 + 세션키 라우팅으로 비활성(백그라운드) 세션 취소 격리, abort 시 SDK 에러 억제 보존. **#4 pre-stream 재시도**: `eventsReceived===0 && retryable` 한정(스트림 시작 후 재시도는 user 메시지·부분출력 꼬임) 최대 `MAX_RETRIES`(2)·백오프 1s→2s(`abortableDelay` AbortSignal 존중)·user 메시지 재영속 0·소진 시 error 1회. **비범위**(사용자 합의): OS 서브프로세스 감독(#5 — SDK 핸들 미노출)·provider fallback(#6 — claude 단일 무의미). IPC_CONTRACT §3 turn.aborted variant 동기화. 인수 15/15, 게이트 lint/typecheck(node+web+test)/test **418/418**/build(의존성+Node ABI 재빌드+electron 바이너리 설치 후 green), 레이어 경계 0, 신규 의존성 0. 사람 확인 대기: GUI 수동검증(타임아웃/부분턴 칩/멀티세션 취소/자동재시도)·IDLE/MAX_RETRIES 기본값. | **완료 (커밋 `57a89af` + verify 보강)** |
| **우측 멀티 보조 패널 (handoff `0034-multi-right-panels`)** | **단일 "계획" 타일을 제네릭 멀티 타일 레일로 일반화(기반 Codex `2591cbd`, 후속 R1~R7 = Claude 직접 구현·사용자 피드백 7라운드).** 큰 영역 **RightPanel** / 개별 **RightPanelTile**. 상태 모델 `rightPanelTiles`(R7 안정 id `{id,tiles}[]`)+`rightPanelColWidths`/`rightPanelRowSplits`, `tileRegistry`(plan/subagent/reserved1·2 + 헤더 액션)·순수 `rightPanelLayout`(col-major 2행 파생·`ROWS_PER_COL=2`·위치 안정 제거). 리듀서 액션 6종·plan_review 자동활성. **후속 수렴**: ① 타일 헤더 케밥→닫기(x)+계획 복사·타이틀 간격 ② 외곽/열간 리사이즈 우측도킹 방향 버그수정(R3) ③ 제거 위치 안정화(평탄→열구조) ④ 열 제거 FLIP 가로 슬라이드(`useColumnSlideOnReflow`)·타이틀바 설정아이콘 제거·케밥 선택 시 닫기 ⑤ 슬라이드 내용/id 키 추적 ⑥ 행 제거 grow 방향(`justify-end`)·열 제거 잔여타일 재생성 제거 ⑦ `/simplify`: 열 안정 id 로 `entering` 우회책 제거·등장 `animate-tile-in`+FLIP+행 grow 2메커니즘·reduced-motion. IPC/main 변경 0. 검증 PASS(r1) 인수 19/19, 게이트 lint/typecheck/typecheck:test/test **427/427**(origin/main 리베이스 후 green), 레이어 경계 0, 신규 의존성 0. 사람 확인 대기: UI 시각 검증·PR 머지. | **완료 (커밋 `2591cbd` + R1~R7 `686fee1`)** |
| **프로젝트 상세 우측 패널 카드화 + 랜딩 리파인 (handoff `0035-project-right-panel-cards`)** | **프로젝트 랜딩(`ProjectLandingPage`)을 첨부 이미지(Claude 데스크톱)대로 재현(비기능 = 시각 스타일링, Claude 직접 구현, 사용자 리뷰 2라운드).** ① 우측 패널 지침/파일 섹션을 평면 `border-b` → **테두리+라운드 카드 2개**(`SidebarCard` 셸로 chrome·헤더 공유), `편집` 텍스트버튼·`준비 중` 배지 제거→헤더 `+` 아이콘, 파일 드롭존에 겹친 문서 인라인 SVG 일러스트(`FileDropIllustration`, 테마 추종). ② **구조 구분선 제거**(헤더 border-b·aside border-l/t, 카드 테두리 유지) + 제목(h1) 라인 우측 핀+케밥 메뉴(`세부사항 수정`/`삭제`[빨강], 동작 미배선) + 지침 카피 "Claude" 제거 + 파일 카피 "폴더" 추가 + **중앙:우측 6:4 중앙정렬 단일 블록**(`max-w-[1200px] mx-auto`·고정 `gap-x-10`, 초과 폭은 좌우 바깥 여백 증가). ③ 컴포저 `flush` 옵션으로 리딩 거터 제거→hero/세션과 좌우 라인 정렬(ChatTile 은 `ReadingColumn` 유지). `Icon` 에 `pin` 추가. 우측 패널 컴포넌트 분해(InstructionsCard·FilesCard·FileDropIllustration·SidebarCard). IPC/스토어/모달 로직 무변경. 검증 PASS(r1) 인수 13/13, 게이트 lint/typecheck/typecheck:test/test **422/422**(Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. 사람 확인 대기: UI 시각 검증·PR 머지. | **완료 (PR #111, 커밋 `cf5c6fb`)** |
| **UI 보정 일괄 (handoff `0036-ui-polish-batch`)** | **UI 버그/미구현 9건 일괄 보정(비기능 = Claude 직접 구현). 모두 렌더러 표현 계층 — IPC/DB/어댑터 무변경.** **공용 `shared/ui/Popover`(1·2·7·8)**: ① 패널 기본 `min-w-[240px]` 제거→케밥 등 메뉴 호버 영역=콘텐츠 너비 일치(의존하던 `ChatTitleBar`·`SkillDetail` 는 `min-w-[200px]` 명시) ② 측정 기반 placement flip — 패널을 open 동안 항상 마운트(`visibility:hidden` 1패스 측정)하고 `useLayoutEffect` 에서 `offsetHeight` 읽어 요청 placement 가용 공간(`spaceAbove`/`spaceBelow`)과 비교, 부족하면 반대 방향 flip(`GAP=6`·`EDGE_MARGIN=8`)→컴포저 `placement="top"` 메뉴 렌더영역 오버플로 해소 ⑦⑧ `align="end"` 재사용으로 우측 zone(telemetry 도넛·모델·작업량) 우측 라인 정렬·좌측 zone(권한모드·`+`)은 좌측 유지. **개별**: ③ `Sidebar` nav '자동화' `disabled`(no-op·`aria-disabled`·`cursor-not-allowed`)+빗금 배경 상수 `NAV_DISABLED_HATCH`(시맨틱 토큰 `repeating-linear-gradient(--color-border)`, 확장·접힘 공통) ④ `CustomizeLanding` '플러그인 탐색' `ActionCard` 삭제+`onBrowsePlugins` prop 정리(카드 2개) ⑤ `ToolGroup` 컨테이너·`ToolCard` 본문에 `border border-t5`(그룹/단일 펼침 일관) ⑥ `ChatTitleBar` adapter(Dot+backendLabel)·session id 부제 `<div>` 제거(+미사용 prop/`Dot` import/`sessionId` selector 정리, `ChatTile` 호출부 동기화) ⑨ `AttachMenu` '현재 프레임' 항목 삭제 + Skill 진입을 입력란 `/` 삽입→`SkillAutocomplete` 단일 경로로 통합(`menuOpen`/`insertSkillFromMenu`·`SkillsMenu.tsx` 삭제). 신규 CSS 0·신규 의존성 0. 검증 PASS(r1) 인수 9/9, 게이트 lint/typecheck(node+web+test)/test **427/427**(better-sqlite3 Node ABI 재빌드 후 green — 1차 11-red 는 `db/queries.test.ts` dual-ABI 환경=0019 계열, 변경 무관), 레이어 경계 0, IPC 무변경(채널 40 유지). 사람 확인 대기: UI 시각 검증 9건·PR 머지. | **완료 (커밋 `cd246c2`+`5fcb839`)** |
| **스킬 상세 마크다운 공유 렌더러 (handoff `0037-skill-detail-markdown-shared`)** | **depth3 스킬 상세(`SkillDetail`) 본문 마크다운이 프리티 렌더되지 않던 버그 수정(비기능 = Claude 직접 구현).** "스킬 & MCP"(`/skills`) 3-depth 중 depth3 상세 패널의 `MarkdownBody` 가 **bare `ReactMarkdown`** 이라 헤딩 위계·리스트·링크·표가 styled map 없이 기본 HTML 로 떨어졌다(채팅 `Markdown` 의 `COMPONENTS` 맵 부재). ESLint `boundaries` 가 `features/skills`↛`features/chat` 를 차단하므로 채팅 렌더러 직접 재사용 불가 → **공용 `Markdown`+`CodeBlock` 을 `shared/ui/markdown/` 으로 `git mv` 승격**(이력 보존; `CodeBlock` 외부 의존=`shared/ui/CopyIconButton` 뿐·`Markdown` 외부 의존=`CodeBlock` 뿐 → shared 규칙 위반 0)하고 chat 7 importer(`AssistantMessage`·`ReasoningBlock`·`StructuredOutputCard`·`KeyValueBody`·`FileBody`·`PlanTileContent`·`StreamingMarkdown`) 경로 갱신·skills 재사용. `Markdown` 에 선택적 `className` prop(미지정 시 채팅 톤 상수 `DEFAULT_WRAPPER`) 추가 → skills 상세는 패널 톤(`text-ink2`·`leading-1.7`) 주입, `memo`·0007 주석 보존. `CodeBlock`(shiki) 그대로 재사용으로 코드블록 구문강조·복사버튼이 채팅과 일치. plain-text 토글·`McpDetail`(마크다운 미렌더) 무변경, IPC/스토어 무변경, 신규 의존성 0(react-markdown/shiki 기채택). 검증 PASS(r1) 인수 7/7, 게이트 lint/typecheck(node+web+test)/test **427/427**/build(better-sqlite3 Node ABI 재빌드 후 green — 1차 11-red 는 `db/queries.test.ts` dual-ABI 환경=0019 계열, 렌더러 전용 변경 무관), 레이어 경계 0. 사람 확인 대기: depth3 본문/채팅 회귀 시각 검증·PR 머지. | **완료 (커밋 `b17070f`)** |
| **턴 상태/에러 UX 버그수정 (handoff `0038-turn-state-error-ux`)** | **턴 종료 신호·에러 표면화·테마 토큰·세션 복원 모델 5건 버그수정(비기능 = Claude 직접 구현, 사용자 결정 3건).** #1+#2 렌더러가 `inflight` 을 `telemetry`/`error`/`turn.aborted` 에서만 내리는데 거부 후 무result 종료·API 400 무result 종료가 종료신호 없이 끝나 영구 in-progress → `send.ts` `sawTerminal` 추적 + `for await` 정상 종료 후 미터미널·비-abort 면 **폴백 `telemetry`** emit(거부=에이전트 턴 지속·취소=`turn.aborted` 의미론 유지). 재시도는 유지하되 retryable catch 직전 신규 **`turn.retrying`** 이벤트(attempt/maxRetries) emit → reducer `retry` 상태·`PendingAssistant` `재시도 N/M` 배너(`inflight` 유지·다음/터미널 이벤트에서 클리어). `claude-map` 이 SDK `result` 의 `is_error`/비-success subtype 을 telemetry 외 **`error` 이벤트**로 표면화(빈 완료 금지, `makeClassifiedError`/`errorEvent` 재사용). #3 에러 표시(`ErrorCard`·`TurnErrorBanner`·`ToolCard`·`ToolGroup`)의 액센트 `rust`(쿨 테마 파랑 오버라이드) → 시맨틱 `bad` 토큰(`bg-bad/10`·`text-bad`, 전 테마 빨강 일관). #4 mock 시나리오를 프래그먼트 빌더로 추출 — 도구/사용자요청(`tool_calls`·`tool_approval` allow/deny·`ask_question`)은 `closing()` 제거(in-progress 종료→폴백 telemetry 로 정상종료 실경로 재현·거부 경로 #1 가드), `full` 은 text→reasoning→tool_calls→tool_approval→ask_question→plan_review→**도구호출 에러점프**. #5 세션은 `provider_key` 만 영속·modelFamily 미영속 → `Composer` 초기화 effect 가 `providerKey` 있어도 `modelFamily==null` 이면 provider 기본 모델 자동 셋업(영속/마이그레이션 대신 폴백). `turn.retrying` persist skip + chatStore 라우팅. IPC_CONTRACT §2.13/§3 동기화(채널 40 유지). 검증 PASS(r1) 인수 **8/8**, 게이트 lint/typecheck(node+web+test)/test **429/429**/build(better-sqlite3 Node ABI 재빌드 후 green — 1차 11-red 는 `db/queries.test.ts` dual-ABI 환경=0019 계열, 변경 무관), 레이어 경계 0, 신규 의존성 0. 사람 확인 대기: 거부/취소·재시도 배너·에러색·세션 복원 모델·mock 실기 시각 검증·PR 머지. | **완료 (커밋 `a886f67`)** |
| **컴포저 첨부 + 동시성 경고 + workspace 경로 추상화 (handoff `0039-attachment-thumbnails`)** | **파일 입력을 컴포저 첨부로 일원화(일회성 user 턴 주입) + 같은 projectId 동시 query 경고 + cwd 경로 추상화(기반 Codex `92020c4` + Claude 후속 8커밋).** **Part 1 첨부**: 3경로(다이얼로그 `files:pickAttachments`·DnD `webUtils.getPathForFile` 브리지·클립보드 paste 인라인 바이트)→입력 위 썸네일 칩(`AttachmentTray`/`AttachmentChip`/`AttachmentThumb`, 칩 lifecycle 은 `hooks/useAttachments`)→전송 시 txt/md 추출(`files/attachments.ts` `AttachmentExtractor`/`TextExtractor`, UTF-8·BOM·binary 가드·`MAX_FILE_CONTEXT_CHARS=24000` truncate)은 영문 wrapper(`prompts/attachment.ts` `formatAttachmentPromptBlock` — id·sentinel·metadata·자료/지시 분리·escaping)로, 이미지는 user `image` 블록(`source:{type:'base64',media_type,data}`)으로 주입. `buildTurnContent`(이미지 없으면 wrapper 를 본문 string 병합=SDK streaming-input 실패 회피 `c215a32`, 이미지 있으면 블록배열)·`createTurnInputStream(string|ContentBlockParam[])` 일반화. PDF 미등록=신규 의존성 0. **Part 2 동시성 경고**(차단·락 없음): `ipc/chat/concurrency-registry.ts` 순수 `Map<projectId,count>`·`send.ts` increment(`:348`)/finally decrement(`:371`)·`concurrency:event {projectId,count}` 전 webContents 브로드캐스트·렌더러 자기 inflight 차감(`Composer.tsx:122`)→일반화된 `Notice` 패널(`f55c440`, × 닫기·에피소드 dismiss). **Part 3 cwd**: `config/paths.ts getWorkspacePath(project?)`(projectId 파생 디렉토리·mkdir)·router `getCwd`/`send.ts boundProjectId`(resume=`session.project_id`)·`syncExtensionsForTurn`. **이미지 한도**=`capabilities/image.ts imageCapabilityFor`(하드코딩 금지). **범위 확장**(사용자 피드백): transcript 첨부 영속 썸네일(`AttachmentView`·DB `attachment` 파트·다운스케일 `lib/imageThumb.ts`, `2509210`). IPC_CONTRACT 46→49(files 1→3 + concurrency 1). **편차**: AC2 wrapper=string 병합·AC7 base=`~/.config/orca/projects`(userData 아님 — 확장 싱크 인접성 `b3426d1`). 검증 PASS(r1) 인수 9/9, 게이트 lint/typecheck(node+web+test)/test **463/463 (68 files)**(better-sqlite3 Node ABI 재빌드 후 green), 레이어 경계 0, 신규 의존성 0. 사람 확인 대기: AC2/AC7 편차+영속 범위 설계 수용·첨부 UX/시각 검증·실환경(이미지 전송·동시 query 경고·cwd 싱크)·PR 머지. | **완료 (커밋 `f55c440`)** |
| **새-채팅 동시성 + init 등록/제목 (handoff `0040-new-chat-race-early-registration`)** | **"전송→새대화→전송"(첫 `session.updated` 전) 시 2번째 세션 소멸·버블 소실 버그 해소 = 직렬 디스패치 게이트 + 낙관적 멀티 엔트리.** 렌더러 `chatStore` 가 미승격 새-채팅 main 턴을 항상 ≤1개로 직렬화(세마포어(1) `pendingNewChatKey` + FIFO `newChatQueue` payload 스냅샷): 화면엔 `draft:<uuid>` 로컬 키로 멀티턴 즉시 표시(낙관적), main 진입은 단일 진입로 — 1번째 `session.updated` 후 큐 release(`promotePendingNewChat`/`releaseNewChatGate`, side-effect 경계 분리). send 가 `__new__`→`draft` 원자 re-key 해 `newChat()` 의 슬롯 덮어쓰기(소멸 원인) 차단. sessionId 없는 터미널은 `pendingNewChatKey` 라우팅+release 로 데드락 0. **main 변경 2건 한정**(pending 모델·IPC 무변경): ① `TitleGenerator` 주입+`send.ts` `session.updated` 시 `titles.maybeStart`(init 제목, turn-end 안전망 멱등 유지), ② `turn-registry.promote(turn, sessionId)` 신원가드(resume 도 `session.updated` 방출 → 같은 owner pending 새-채팅 오승격하던 **latent 버그** 차단, Codex 검토 Issue 1). R2 등록=`recentsEpoch`→셸 훅 `useChatSessionsSync` refresh. "연결 대기" Notice(`useNewChatPending`). 검증 PASS(r1) 인수 **12/12**, 게이트 lint/typecheck/test **471/471 (68 files)**(better-sqlite3 Node ABI 재빌드 후 green), 레이어 경계 0, IPC/pending 모델·신규 의존성 0. 사람 확인 대기: n연속 전송 화면 체감·"연결 대기" 타이밍·사이드바 init 등장/제목 시각 검증·PR 머지. | **완료 (커밋 `aef1d82`)** |
| **AskUserQuestion 시각 정제 (handoff `0041-ask-ui-refine`)** | **컴포저 Ask 카드 + transcript 인라인 Q&A 를 참고 양식(중립 톤)으로 정렬 — 순수 표현 계층, 비기능 = Claude 직접 구현.** 라운드1~3: ① 옵션 선택 톤 중립화(`border-rust bg-rust-soft`→`border-border-strong bg-t3`, accent=라디오/체크 인디케이터에만·단일=원형/다중=사각) ② 헤더 chip 무채도·sentence-case(`bg-t3 text-t7`) ③ 번호배지 수직중앙·verbose 키보드 힌트줄 제거(Enter 힌트는 primary 버튼) ④ 헤더 `1/N` 카운터 배지 + multiQuestion 한정 이전/다음 화살표(`goPrev`/`goNext`·경계 disabled)·primary 가 전진/제출 겸함 ⑤ `AskExchange` 단일 좌측 결합 버블(`질문(text-ink2) 답변(굵게)`, `bg-bubble-user` 유지) ⑥ 카드 `mb-2` 제거(컴포저 `gap-2` 이중간격 해소) ⑦ 키보드(1-9/↑↓/←→/Space/Enter/Esc)·단일 자동진행·기타 상호배타·a11y(`role=listbox/option`·`aria-selected`) 무회귀 ⑧ themed 토큰만(신규 토큰 0). 라운드3 부수: `parseAsk` 키 의존 제거(키매칭→인덱스 폴백, questions 비면 answers 엔트리 직접 구성) + 단위테스트. 검증 PASS(r1) 인수 **8/8** 코드 충족, 게이트 lint/typecheck/test(실행분 476 green·main 2파일은 electron 미설치 환경 차단, 렌더러 전용 무관), 레이어 경계 0, IPC/reducer/파싱 무변경. 사람 확인 대기: UI 시각 검증(3 테마·cool=파랑 선택 톤·픽셀 일치)·PR 머지. | **완료 (커밋 `96c8c5d`)** |
| **서브에이전트(Task) transcript 표면 (handoff `0042-subagent-transcript`)** | **서브에이전트(Task) 도구호출의 중첩 트랜스크립트 캡처(백엔드) + transcript 카드 + 우측 패널 child 트랜스크립트(이미지 6~8) — 구현 주체 Codex(+Claude 보정).** **Part A 백엔드**: `claude-map` 가 SDK 메시지 `parent_tool_use_id` 캡처(`readParentToolRunId`, 공백 가드) → `NormalizedEvent`(`tool.call.started/completed`)·`AppMessagePart`(`tool_call`/`tool_result`)에 optional `parentToolRunId` 전파(persist 보존·복원 payload spread). `lib/parts.ts` `subagentTasksFromMessages`(최상위만 카운트·child 분리)·`childMessageForParentToolRunId`(parentToolRunId 벗겨 child 안 최상위 취급)로 부모 Task 키잉 분기, `messageSegments`/`partsToolCalls` 가 child 를 메인에서 제외(무회귀 — 필드 부재 시 평면 동일). **Part B 렌더러**: Task 전용 행/카드(`agent_task` kind·호출/완료/중단 상태) + 우측 `subagent` 타일이 메인 `AssistantMessage`/`ToolGroup`/`ToolCard` 재사용으로 child 트랜스크립트 렌더 + 뒤로가기 리스트↔상세 2-뷰. 1단 중첩만(다단·opencode 비범위). 검증 PASS(r1) 인수 10/10, 게이트 lint/typecheck(node+web+test)/test **502/502 실행분**(Node ABI 재빌드 후 green·electron 미설치 2파일 환경 제한 제외), 레이어 경계 0, 신규 의존성 0, IPC_CONTRACT §3·§6 동기화. 사람 확인 대기: 이미지 6~8 시각 검증·실환경 캡처·PR 머지. | **완료 (커밋 `a674588`)** |
| **서브에이전트 UI 피드백 (handoff `0043-subagent-ui-feedback`)** | **0042 후속 — 스크린샷 피드백 양식 보정 + child 텍스트 캡처(구현 주체 Claude).** 메인 transcript 양식: 그룹 헤드 `실행 중 에이전트 N개`(진행 중)·그룹 항목 `에이전트 실행 중 <model> <title> <elapsed>`·단일 `에이전트 실행 중 <model> · <현재도구> · <도구수>` — 신규 `transcript/AgentTaskRow.tsx`(store messages 구독 child 메타 + 로컬 시작시각 앵커 + 공유 훅 `shared/ui/elapsed.ts` `useElapsed`/`formatElapsed`, StatusLine 재사용) + `ToolGroup` 헤더 분기. **백엔드 child 텍스트**: `claude-map` `message.completed` 에 `parentToolRunId` 전파 → text/reasoning `AppMessagePart` 보존(메인 preview/세그먼트 제외) → `childMessageForParentToolRunId` 가 child text/reasoning 수집·우측 상세 답변 출력(+result summary 폴백) + 상세 간격 `--chat-turn-gap`(메인 양식). mock: 전 subagent 시나리오 `agentLabel`·child `message.completed` 추가·`subagent_task_multi` 동시실행화·신규 `subagent_task_running`(MockScenarioId 12→13). 검증 PASS(r1) 인수 9/9, 게이트 lint/typecheck/test **502/502 실행분**(Node ABI 재빌드 후 green·환경 제한 제외), 레이어 경계 0, 신규 의존성 0, IPC_CONTRACT 동기화. 사람 확인 대기: 양식·경과시간·답변 시각 검증·실환경 child 캡처·PR 머지. | **완료 (커밋 `a674588`)** |
| **서브에이전트 메타/중단 (handoff `0044-subagent-meta-stop`)** | **0043 후속 — 실환경 SDK 통합(모델/답변/시간/중단, 구현 주체 Claude).** 근본 원인: claude-map 이 SDK `task_*` 시스템 이벤트를 버려 시간·도구수 미추적, 모델 소스 부재로 'Explore'(subagent_type) 표시, `forwardSubagentText` 미설정으로 답변 미forward. **구현**: ① `forwardSubagentText:true`(`claude.ts`) → child 답변 캡처(0043 경로 재사용). ② `claude-map` 이 `task_started/progress/notification` → 신규 `subagent.task` NormalizedEvent(taskId·subagentType·duration_ms·tool_uses·last_tool_name·task_id↔toolUseId 복원) + child assistant `message.model` 캡처 + ctx 누산(`accrueSubagentMeta`) → 부모 Task tool_result `subagentMeta`(model/duration/toolUses) 영속·재로드 복원(`subagentTasksFromMessages` subagentMeta 우선). ③ store transient `subagentMeta`(toolUseId 키·reducer 미경유) + `useSubagentMeta` → `AgentTaskRow` 가 `modelDisplayLabel` 매핑·라이브 경과시간(단일·그룹)·현재도구·도구수 표시. ④ 우측 running 카드 '대화록 보기' 우측 네모·라운드·채움없음 중단 버튼(`stop` 아이콘, 카드 `<button>`→`<div role>` 재구성·stopPropagation). ⑤ `orca:chat:stopSubagent`(채널 49→50) → `subagent-settlement.ts` 정착 + `LiveTurn.stopTask`(foreground 거부 시 `backgroundTask` 후 stop, `ORCA_SUBAGENT_BACKGROUND` 플래그 게이트) — turn 전체 아닌 Task 단위. mock subagent.task 라이프사이클+running 진행. 검증 PASS(r1) 인수 9/9, 게이트 lint/typecheck/test **502/502 실행분**(Node ABI 재빌드 후 green·환경 제한 제외), 레이어 경계 0, 신규 의존성 0, IPC_CONTRACT 동기화. 사람 실환경 검증 대기: forwardSubagentText 답변·task_* 메타·stopTask 실제 중단(foreground/background fallback)·시각 검증·PR 머지. | **완료 (커밋 `a674588`)** |

| **승인 대기 중 idle abort 수정 + 종료/재시작 정리 (handoff `0046-approval-idle-pause`)** | **승인 카드(plan/ask/위험도구) 대기 중 idle 타이머(120s)가 턴을 abort 하던 버그수정 + 앱 종료/크래시 정리(구현 주체 Claude).** 0033 idle 타이머 미흡점: `canUseTool` await 동안 SDK 이벤트가 끊겨 idle 미reset → 120s 후 `controller.abort()` → broker 가 보류 승인을 deny 로 강제 해소(카드 auto-deny) + 늦은 클릭 무시. **수정**: ① `createIdleTimer` 에 pause/resume(=clear/reset) 추가 + `send.ts` 가 승인 보류(`await approvals.register`) 동안 idle pause(세션 자동허용 early-return 제외, `pendingApprovals` 카운터로 동시 보류 0↔1 게이트) — provider stall guard(120s)는 보존. ② **owner-gone 가드**: owner `webContents` `destroyed`/`render-process-gone` 시 진행 턴 abort(idle 완전 멈춤으로 잃는 자가치유를 타이머 아닌 이벤트로 대체, finally 해제). ③ **[Q1] 정상 종료 정리**: `IpcRouter.shutdown()`(turns/persistence 필드 승격 + `TurnRegistry.all()`)가 진행 턴의 열린 도구를 `settleOpenToolRuns('aborted')` + `controller.abort()`, `index.ts` will-quit 가 `shutdown()`→`closeDb()` 순서 호출. ④ **[Q2] 크래시 백스톱**: `lib/parts.ts` `settleOrphanToolParts` 가 로드 시 `incomplete` 메시지의 tool_result 없는 tool_call 에 합성 aborted tool_result 부착(LOAD_SESSION 한정 — 라이브 무영향) → "실행 중" 무한 렌더 제거. **불변식**: 재시작 후 승인 UI 재현 안 함(중단됨만, 사용자 결정 — `canUseTool` 죽어 이어받기 불가). 검증 PASS(r1) 인수 7/7, 게이트 typecheck(node+web+test)/lint(boundaries 0)/test **508/508 실행분**(Node ABI 재빌드 후 green; 2 suites `persist`·`send.runtime-resilience` 는 electron 바이너리 미설치 환경 제한 — 0033/0041~0044 동일), 신규 의존성 0, IPC/DB 무변경. 사람 확인 대기: GUI(2분 대기·stall·창닫기·Cmd+Q/kill -9 재시작) 실기. **라운드 2(PR #137 GUI 피드백)**: 동시 도구 승인 큐화 — 서브에이전트·병렬 tool_use 의 동시 `permission.requested(tool_approval)` 가 렌더러 `pendingToolApproval` 단일 슬롯을 덮어써 카드 소실→broker 영구 보류(inflight 고착)+idle 영구 pause 던 것을 `pendingToolApprovals` 큐(append·approvalId별 제거·Composer 스택 렌더·ToolApprovalBody prop화)로 수정. 렌더러 전용·백엔드 무변경. test 509/509 실행분. **라운드 4(동시 서브에이전트 승인)**: 단건 정상/3개 동시 무한 inflight 의 근본원인 = `send.ts` 이벤트 루프가 매 이벤트 `idle.reset()` → 다른 서브에이전트 child 이벤트가 승인 pause(clear)를 재무장(무력화). 수정: `createIdleTimer` `paused` 플래그(보류 중 reset no-op) + `options.signal` 존중(SDK 취소→broker deny·`permission.resolved` 카드 정리, `TurnRequest.requestApproval` signal 인자) + `[approval]` 진단 로그(wireLog 게이트). test 509/509 실행분. | **완료 (커밋 push 후)** |

| **계획 패널 인라인 코멘트 + 승인 카드 (handoff `0047-plan-panel-comments`)** | **Claude Code(웹) 우측 계획 패널처럼 ExitPlanMode 계획 본문을 드래그→인라인 코멘트로 남기고 수정 요청으로 묶어 보내는 기능 + composer 승인 카드 정제(구현 주체 Claude).** 본문 드래그→작성 팝오버(contentRef 상대 absolute·bottom 기본/top flip·document mouseup 확정) + 밑줄 하이라이트 오버레이(텍스트 노드별 rect·React 비침습) + composer 코멘트 칩(편집/삭제) + 휘발성 reducer 상태(`planComments`/`activePlanCommentId`, RESOLVE_PLAN/NEW_CHAT 클리어). 전송=계획 revise 흐름에 묶고 `ApprovalResolution.deny.planFeedback`(IPC 페이로드 확장·채널 수 불변)로 구조화 데이터 전달 → main `prompts/plan-feedback.ts` 가 `attachment.ts` 컨벤션 sentinel 태그(`ORCA_PLAN_FEEDBACK`)로 직렬화(영문 instruction·escaping·neutralize) → 어댑터 ExitPlanMode deny message. **후속 6라운드(팝오버 위치/좌표계/auto-grow/디자인 정합·내부 클릭 소멸 버그 수정) + 7라운드(승인 카드 정제)**: ① `수정` 펼침 시 좌측 `거부`/`수락` 버튼 제거(좌측 그룹 게이트 `!reviseExpanded`) ② **`거부` deny 미전달 버그 수정** — `rejectPlan` 의 `{deny, interrupt:true}`+`CANCEL_CHAT` 이 main `approvals.respond` 의 동기 `controller.abort()` 와 레이스(broker.resolve=마이크로태스크)로 deny 가 SDK 전파 전 쿼리를 죽여 `PLAN_REJECT_MESSAGE` 유실 → clean deny(`{deny}`)+`RESOLVE_PLAN` 만으로 변경(사용자 결정=deny 전달 후 자연 종료)·회귀 테스트 2건. 검증 PASS(r1) 인수 7/7, 게이트 typecheck/lint(boundaries 0)/test **531/531 실행분**(Node ABI 재빌드 후 green; 2 suites `persist`·`send.runtime-resilience` 는 electron 바이너리 미설치 환경 제한=0033/0046 계열·변경 무관), 신규 의존성 0, IPC 채널 수 불변. 사용자 "정상동작 확인". 사람 잔여(차단 아님): 실환경 거부 후 모델 응답·턴 자연 종료·팝오버 픽셀 정합 최종 시각 확인. | **완료 (커밋 `ff5f3a3`)** |

| **테마 팔레트 화이트 대체 (handoff `0048-theme-palette-white`)** | **테마 팔레트에서 classic(아이보리)·cool(블루) 제거 → Claude Code 웹 참고 뉴트럴 니어화이트 `white` 1종으로 대체(구현 주체 Claude, 비기능 UI 스타일링).** 결과 팔레트 = white(루트 `@theme` 기본) + dark 2종. `ThemeId`/`ThemePref`/zod enum 4곳 `'white' \| 'dark'` 통일 + `SettingsSchema.theme` `.catch('white')` 로 옛 값(classic/cool) 무손실 강등(`readSafe` 전체-리셋 회피 → density/sidebarWidth 보존) + `tokens.css` 루트 팔레트 뉴트럴화(bg `#ffffff`·사이드바/카드 중립 그레이·t3 `#e8e8e6`·코랄 강조 `#c96442` 유지)·`[data-theme='cool']` 블록 삭제·dark 유지 + `DebugPanel` 라디오 화이트/다크 + `CodeBlock` cool→one-light 매핑·`THEMES` 항목 제거(white=github-light) + 주석/`app/AGENTS.md`(세→두 테마) 정합. 검증 PASS(r1) 인수 6/6, 게이트 typecheck(node+web+test)/lint(boundaries 0)/test **531/531 실행분**(Node ABI 빌드 후 green; 2 suites `persist`·`send.runtime-resilience` 는 electron 바이너리 미설치 환경 제한=0033/0046 계열·변경 무관), 신규 의존성 0, IPC 채널/DB 스키마 무변경. 사람 확인 대기: GUI 시각 검증(화이트 톤·코드블럭·테마 토글)·`tokens.css` hex 미세조정·PR #143 머지. | **완료 (커밋 `8d79db8`)** |
| **진입 UI 정리 + 타이틀바 + 디자인 토큰 정합 (handoff `0049-design-token-ui-fixes`)** | **앱 진입 UI 보류 + 채팅 타이틀바 3버튼 + 엔진/스킬 비테마 색 정합(구현 주체 Claude, 비기능 UI).** ① 부팅 InstallerDialog 자동 오픈 보류(`bootstrapBackend` 미설치 분기 제거·상태 조회만, `setInstallerOpen` 액션 보존→기능 완성 후 재활성) ② nav 사이드바 footer `BackendStatus` 미렌더(`useSidebarSlots.footerSlot=null` + `Sidebar` 빈 풋터 컨테이너 가드) ③ `ChatTitleBar` 3버튼 raw 통일·상태 클래스(`ICON_BTN_BASE/IDLE/PRESSED/DISABLED`): 케밥 press 색 warm `fill-selected`→press 표면 토큰 `bg-t3 text-t8`(우측 패널 활성 `activeTiles>0` 또는 메뉴 열림 시 `aria-pressed`)·복사 버튼=전체 대화 마크다운 클립보드 복사(`getActiveChatSession().messages`→`partsText`→`navigator.clipboard.writeText`+check 피드백)·검색(돋보기)=`disabled`+빗금(border 토큰 사선) ④ 엔진&모델 페이지·엔진추가 다이얼로그·스킬&MCP 하위 페이지의 비테마 `cream-50`(다크 override 없음→화이트 뉴트럴/다크 모두 어긋남)→themed `bg2`("subtle inset/card fill", engine 3 + skills 5 파일). **라운드 2(사용자 후속 5건) — 강조 상태 전반 중립화 + Composer 아이콘**: warm rust/rust-soft 강조 잔재(0048/0041 이후 미정합)를 중립 press 토큰으로 — 케밥 패널 활성 시 강조 제거(`open` 만 `bg-t3`) · Composer 전송=신규 `enter`(↵)/중단=`stop`(투명 라운드 네모, 둘 다 원형) · 엔진 'default' 배지·추가 다이얼로그 선택(`border-border-strong bg-t3`)·스텝퍼(`bg-ink text-bg`/`bg-t3 text-t8`)·선택됨(`text-t8`)·focus(`border-border-strong`)·오류(`bad` 토큰) · 스킬 1depth `CustomizeRail` 활성(`bg-t3 text-t8`)·3depth 본문 토글(`text-t9`)·공용 `Toggle` on(`bg-ink`) · 프로젝트 CTA raw `#fff` 제거. rust 는 주요 CTA(만들기/추가)·danger 만 유지(사용자 Q2). **라운드 3(사용자 후속) — Primary 버튼 잉크화 + 전송 투명 + 도넛 색**: r2 의 "rust=주요 CTA 유지"를 사용자가 뒤집어 모노크롬 전환 — `Button` primary 변형 `bg-accent`→`bg-ink text-bg`(AskUserQuestion 제출·도구카드 허용/수락/수정 일괄) + `Modal.ModalActions`·raw `bg-rust text-white` CTA(프로젝트/엔진/스킬MCP/백엔드 다이얼로그)·AskUserQuestion 라디오 checked 모두 잉크화 + 전송 버튼 투명(`uncontained`) + MCP transport 세그먼트 선택 `bg-t3 text-t8` + ApprovalCard '플랜 열기'·수정 textarea(`bg-white`→`bg-bg`)·스킬 모달 검증/에러 토큰 정합(`text-bad`) + 도넛 `UsageCircle` progress 초록(`good`<0.6)→노랑(`warn`<0.85)→빨강(`bad`≥0.85/warn). rust 잔존=danger(삭제)·`CameraView`(Future). 검증 PASS(r1+r2+r3) 인수 18/18, 게이트 lint/typecheck(node+web+test)/test **540/540**(better-sqlite3 Node ABI 재빌드 후 green; 1차 12-red `db/queries.test.ts`=0019 ABI 환경·렌더러 변경 무관), 레이어 경계 0, 신규 의존성 0, IPC/DB 무변경. 사람 확인 대기: 3 테마 시각 검증·복사 실기·잉크 버튼/도넛 색·PR #144 머지. | **완료 (커밋 push 후)** |
| **라이프사이클·오케스트레이션 구조 재설계 P0 (handoff `0050-lifecycle-orchestration-redesign`)** | **세션-스코프 `SessionRuntime` 1급 추출 + 앱-레벨 2축 모듈(`lifecycle/`·`orchestration/`) 신설 + uv Python runtime 폐기(구현 주체 Codex, 검증 중 갭보강 Claude).** 설계 정본 = `etc/orca_lifecycle_orchestration_design_draft_ko.md`(엔지니어링 리뷰 8건). **C1~C2** `OneShotSessionRuntime`(`lifecycle/session-runtime.ts`, 핸들 수명=세션·OneShot close-on-terminal·정규화 terminal 트리거) + coarse 상태머신 단일 소유(`session-state.ts` cold/live/busy/interrupting/error/closed + abort cause user_cancelled/stall/retry, `turn.cancelled/timedOut` 필드 제거→런타임 파생 읽기) + `LiveTurn.close()` 계약(전 어댑터) + `turn-registry`→`session-registry`(pendingByOwner/promote·evictIdle 훅 예약)+`turn-context` 분할 + `createIdleTimer`→`StallTimer`/`IdleCloseTimer`(stub) + `concurrency-registry`→`orchestration/`. **C3** 부팅·resume dangling tool 마감(`recovery.ts` DB-only·message-scoped `upsertToolResultPartScoped`·`markMessageComplete`·live session skip·`findDanglingToolCalls` complete=0/NOT EXISTS). **C4** uv Python runtime 폐기(`runtime/**`·`fetch-uv`·extraResources·배선 삭제 + 유일 정적 정책 `python-runtime.md` 제거→`POLICY_REGISTRY=[]`·prompt 테스트 갱신, IPC 무변경). P0 테스트 4종(상태·recovery·mode-invariance(FakeSessionRuntime)·StallTimer). P1 연기: Persistent 핸들·steer/queue·핸들 cap+LRU·IdleCloseTimer 구현·TurnCoordinator 1급화. 검증 PASS(r2 final) 인수 **11/11**, 게이트 lint/typecheck(node+web+test)/test **542 passed**(electron 의존 `send.runtime-resilience`·`persist` 2파일은 바이너리 미설치 환경 제약=0048 계열·6d 는 `timers.test.ts` 대체 커버), 레이어 경계 0(`lifecycle`/`orchestration`=L1·`ports.ts` 의존역전), 신규 의존성 0. 사람 확인 대기: 실환경 GUI 회귀(cold→live·취소·StallTimer·크래시 복구)·uv 제거 후 Python MCP 폴백·electron 게이트 정상환경 재실행·PR 머지. | **완료 (커밋 `d8ec70d`+`0535be9`)** |

| **TurnCoordinator 1급화 (handoff `0052-turn-coordinator-firstclassing`)** | **0051 §A 가로축 구동체 TurnCoordinator 1급화 (P1 키스톤, 비기능 = Claude 직접 plan→impl→verify).** §A 가로축(stream→reduce→persist∥forward + 권한 재진입)을 `send.ts:handleChatSend`(~390줄)/25-필드 `InflightTurn` 에서 **L1 `lifecycle/turn-coordinator.ts`** 로 동작 보존 추출. 코디네이터(L1)=retry+consume·reduce·persist∥forward fan-out·terminal 합성·error/retry·stall 소유 + `beginApprovalPause()` + **의존 역전**(sink 인터페이스 `turn-sinks.ts` 만 주입, 기존 L3 클래스 무변경) + settle 3종(`settle.ts`)·순수 subagent 빌더 L1 이동(무회귀 배럴 re-export). 가로축 첫 단위테스트(turn-coordinator 5·settle 6·resilience 7). 게이트 typecheck/lint(boundaries L1→L3 0)/build ✅·test 541 passed. 비범위(후속): InflightTurn 풀분리·Persistent·cap/LRU·steer/queue admission·`orchestration/` 리네임. 검증 PASS(r1) 인수 충족. | **완료 (커밋 `37da79e`)** |
| **Runtime Supervisor 척추 (handoff `0053-runtime-supervisor-spine`)** | **0051 §A staging P1 — Runtime Supervisor(세로축 unit #3) 척추 추출 + 단일 멱등 close/abort (비기능 = Claude 직접 plan→impl→verify).** 척추만 먼저(정책 cap/LRU/IdleClose 는 Persistent 전까지 死코드라 0054 분리). ① `lifecycle/supervisor.ts` 신설 — `RuntimeSupervisor` 가 `SessionRuntimeRegistry` 소유(조회/등록/승격 위임) + `release(turn)`(WeakSet 멱등 teardown) + 0054 seam 주석. ② `abortTurn(turn,cause)` standalone(chatCancel·stall 공유). ③ 컴포지션 루트(`router.ts`) supervisor 주입, `ChatDeps.turns`→`supervisor`. ④ dead `TurnRegistry` alias·value re-export 제거. 동작 보존(이벤트·DB·UX 무변경, 정책 0). 게이트 typecheck/lint(boundaries·no-cycle 0) ✅·신규 `supervisor.test.ts` 7 green. 검증 PASS(r1) 인수 충족. | **완료 (커밋 `80b5fec`)** |
| **Persistent SessionRuntime + IdleCloseTimer (handoff `0054-persistent-runtime-idle-close`)** | **0051 §A staging P1 키스톤 — Persistent SessionRuntime(close-policy) + IdleCloseTimer 실구현 + Supervisor 거버넌스 (비기능 = Claude 직접 plan→impl→verify).** 0053 척추 위에 정책의 전제(long-lived 핸들)를 세운다. ① close 정책 파라미터화(decision ⑳) — `SessionRuntime(adapter, closePolicy)` 단일 클래스 + `reusable` getter, `OneShotSessionRuntime` 무회귀 alias(동작 보존). ② IdleCloseTimer 실구현(`timers.ts:createIdleCloseTimer`, IDLE_CLOSE_TIMEOUT_MS=300s). ③ `lifecycle/runtime-pool.ts` 신설(세션 키 idle 보존 + IdleCloseTimer 소유). ④ Supervisor 거버넌스(`acquireRuntime`/`releaseRuntime`/`closeIdleRuntimes`; turn teardown≠runtime close 분리). ⑤ 게이트 `ORCA_PERSISTENT_RUNTIME=1` — **기본 OneShot → 게이트 OFF 동작·이벤트·DB·UX 무변경**. ⑥ 순환 차단(`abort.ts` 분리). 게이트 typecheck/lint(boundaries·no-cycle 0)/test **578 passed**(신규 runtime-pool 7·timers·supervisor·session-runtime). 비범위(0055): cap admission·LRU/idle eviction·ConcurrencyRegistry 이관·steer/queue. 검증 PASS(r1) 인수 충족. | **완료 (커밋 `461f117`)** |
| **자원 거버넌스 — cap/LRU/idle eviction (handoff `0055-runtime-resource-governance`)** | **0051 §A staging P1 — 자원 거버넌스(cap/LRU/idle eviction + ConcurrencyRegistry 소유 이관).** 0054(Persistent 전제) 위에 정책 계층을 mechanism↔policy 분리로 채운다: `EvictionPolicy`(순수 LRU·victim 키 반환)·`RuntimeCapPolicy`(기본 무제한·union `accept\|evict-idle`) 주입, ConcurrencyRegistry **단일 진실원**(소유 Supervisor 이관+getter·파일 미이동=결정 2 준수), idempotent close 단일 helper(기존 4경로 합류). count=active+idle·eviction victim=idle only·`getRuntimePopulation()`. **기본값 게이트 OFF/ON 동작·이벤트·DB·UX 0 변경**. 검증 PASS(r1) 인수 **8/8**, 게이트 lint/typecheck(node+web+test)/test **588/588 로드분** green(신규 eviction-policy 3·runtime-pool 3·supervisor 4), 레이어 경계 0, 신규 의존성 0, IPC 무변경. Open Question(사용자): cap 수치·평가세션 cap 회계(over-cap reject/queue 는 0056 이관). 사람 확인 대기: 게이트 ON 실환경 GUI 회귀·PR 머지. | **완료 (커밋 `dfe447a`)** |
| **턴 admission 추상화 (handoff `0056-turn-admission-steer-queue`)** | **0051 §A staging P1 — 턴 admission 추상화(framework only).** busy 같은-세션 입력의 admission 을 `send.ts:163-179` 인라인 hard-reject 가드에서 `AdmissionController`(L1 순수 결정)+`AdmissionPolicy`(주입)로 1급화 — **이번 범위 = 추상화 seam + 현행 default 무회귀 보존만**. 기본 `RejectDuplicatePolicy` 가 가드 1:1 재현(인라인 제거→controller 위임·이중게이트 0), `AdmissionDecision=accept\|reject\|queue\|steer` 중 **queue/steer 는 예약 seam(enactment 0)**, enactment 레이어 분리(L1 결정·L3 reject enact). OQ 결정(2026-06-30 사용자): 의도 default=steer(streaming-input 포트 부재로 후속 종속)·framework only·cap 축 accept inert. 검증 PASS(r1) 인수 **8/8**, 게이트 lint/typecheck(node+web+test)/test **592/592** green(신규 `admission-controller.test.ts` 4/4·Node ABI 재빌드 후), 레이어 경계 0, 신규 의존성 0, IPC 무변경. 환경제약: electron 미설치 2 suite import 불가(0050~0055 동일 계열). 비범위(후속): steer/queue 메커니즘·cap reject/queue·`orchestration/` 리네임. 사람 확인 대기: 후속 steer/queue UX 시각검증·실환경 중복-send reject 실기·PR 머지. | **완료 (커밋 `84354a3`)** |
| **Orca 플러그인 재구성 (handoff `0058-orca-plugin-restructure`)** | **claude dist 확장 배포 계층을 Claude Code 플러그인(`plugins/orca/`)으로 재구성 + 런타임 `options.plugins` 로드 + dist→cwd 파일 싱크 제거.** ① dist 레이아웃을 `dist/claude/plugins/orca/`(`.claude-plugin/plugin.json`[name:orca]·`skills/`·`agents/`·`hooks/`·`.mcp.json`)로 재구성 — `deploy/claude-plugin-package.ts`(레이아웃 렌더러: skills 복사·plugin.json·`.mcp.json` `chmod 0600`·agents/hooks 스캐폴드) + `ExtensionDeploymentService`(멱등 배포·boot 실패 재시도) 분리, `deployer.ts` 는 backup/검증/마커 유지, `paths.ts` `distOrcaPluginDir` 계열. ② `adapters/claude-adapt.ts` `adaptPlugins`(매니페스트 존재 시만 `options.plugins:[{type:'local',path}]`) + `adaptSkills` 가 Orca skill 을 `orca:<name>` 네임스페이스로(어댑터 skill 은 bare, 스캔 0→`'all'`), `adapters/claude.ts` sendMessage query 에 주입(complete 1-shot 은 미주입). ③ `deploy/workspace-sync.ts`·`syncedCwds` 삭제(cwd 오염 폐기) — skill 스캔은 sources 직접 읽어 무영향. ④ deploy 를 매 query 전 선행 보장(boot 1회+skill/mcp CRUD 재배포+턴 진입 `ensureDeployed` 멱등). MCP 는 plugin `.mcp.json`(query 전 `${VAR}`/secret 확장·키검증) 로 로드, `options.mcpServers`/`adaptMcp` 는 레거시 유지(기본 경로 미호출). 구현 주체=Codex(실체=impl `98a74c0`). 검증 PASS(r1) 인수 **11/11**, 게이트 lint/typecheck(node+web+test)/test **606/606** green(better-sqlite3 Node ABI 재빌드 후·신규 `adaptPlugins` 3/`adaptSkills` 네임스페이스/`deployer` plugin 레이아웃 11 포함), 레이어 경계 0, 신규 의존성 0, IPC 무변. **verify 전 수석엔지니어 리뷰 선조치 3건(Claude 비기능)**: F3(`adaptPlugins` 매니페스트 존재 가드 복원 — AC#5·deploy 실패 시 미존재 경로 주입 방지)·F4(dead `markDirty` 제거)·F-lint(impl 이 남긴 `ClaudeAdapter` 빈 생성자 lint 에러). 환경제약: electron 미설치 2 suite(`persist`·`send.runtime-resilience`) import 불가(0050~0057 계열, 정상환경 green). 보안 trade-off: plugin `.mcp.json`·`.bak` 에 평문 비밀 잔존 가능(사용자 승인·`chmod 0600` best-effort·standardization.md 기록). 사람 확인 대기: plugin `.mcp.json` SDK 실로드·`orca:` skill init 노출·실환경 턴·PR 머지. | **완료 (impl `98a74c0` + verify선조치 `07b0c6b`)** |
| **Composer cwd 진입점 (handoff `0057-composer-cwd-panel`)** | **검증 엔지니어가 채팅 전 작업 폴더(cwd)를 고르고 세션 중엔 OS 탐색기로 여는 cwd 진입점.** 요구 변경: **세션 화면 Composer 패널 노출 취소→세션 타이틀 영역 `📂 basename / <제목>` 이동**, 랜딩(`/new`·프로젝트)은 Composer 패널에 cwd 버튼 노출(투명 표면). baseline=default·새 대화마다 리셋, 선택 cwd 는 새 세션 출생 시 고정·영속(`sessions.cwd` 마이그레이션 `0010`+insert/load/resume 해석), 세션 후 클릭=`shell.openPath`. 신규 IPC `files:pickDirectory`/`files:openPath`·`SendChatMessage.cwd`·basename 순수함수·IPC_CONTRACT(50→52). 구현 주체=Codex(기능). 검증 PASS(r1) 인수 **9/9**(요구 변경 #1·#7 재해석), 게이트 lint/typecheck/test **602 passed**, 레이어 경계 0, 신규 의존성 0. **verify 전 수석엔지니어 리뷰 선조치(Claude 비기능)**: `openPath` 경로 화이트리스트(실재 디렉토리+projects루트하위∨실재 세션cwd — 임의 실행파일 오픈 차단)·resume `getSessionById` 이중호출 정리·이모지 `📁→📂`. **알려진 한계(사용자 결정 현행 유지)**: 프로젝트 랜딩 미선택 라벨=`default`↔실 출생 cwd=프로젝트 파생폴더 불일치(세션 시작 후 init 자동 보정·cwd 의미론은 후속 분리). 환경제약: electron 미설치 2 suite import 불가(정상환경 606). 사람 확인 대기: 타이틀/랜딩 패널·이모지 톤·실환경 openPath 시각검증·PR 머지. | **완료 (impl `0058095` + verify선조치 `0c1921a`)** |
| **피드백 끼어들기 — steer/queue enactment (handoff `0059-steer-queue-feedback`)** | **0051 §A staging P1 잔여 — 0056 이 framework-only 로 남긴 steer/queue 를 실제 동작(피드백 끼어들기)으로 enact.** backend 기전 + renderer UX 통합. steer/queue 를 형제 결정이 아니라 "pending 큐 → 끼어들기 flush" 단일 흐름으로 재정의: inflight 중 피드백은 내부 FIFO 에 enqueue→pending 버블(연회색/기울임), flush 시 단일 user 메시지로 영속. ① streaming-input `push` + 신규 L1 순수 `lifecycle/steer-queue.ts`(enqueue/cancel/pending/`drainForFlush` multi→single `\n\n` 병합) ② TurnCoordinator `consumeSteerForInput`(drain→persist∥forward) ③ explicit `chat:steer`/`chat:steerCancel` IPC + `steer.queued/flushed/cancelled` variant(IPC_CONTRACT 52→54·chat 6) ④ renderer feedbackMode 3상태 버튼·`PendingSteerTurn`(hover 취소→draft 재주입). 구현 주체=Codex(기능). **설계전환**: plan AC5 `SteerQueuePolicy` admission swap → explicit `chat:steer` action path 로 대체(보류 기록). **파생 D1(Claude `2feab72`)**: `flushedSteer` 오버레이 제거→`steer.flushed`=즉시 일반 커밋 user 메시지 + main flush 경계 어시스턴트 메시지 마감으로 DB↔라이브 정렬 일치. 검증 PASS(r1) 인수 **12/14 충족 + 2 설계전환 보류**, 게이트 lint/typecheck(node+web+test)/test **618 passed**(신규 steer 3파일 19 green; electron 미설치 2 suite 제외=0050~0058 계열), 레이어 경계 0, 신규 의존성 0. verify 선조치: IPC_CONTRACT 채널 카운트 정정. 사람 확인 대기: 설계전환 승인·renderer UX 시각검증·PR 머지. | **완료 (impl `a72e2bc` + D1 `2feab72`)** |
| **steer flush 경계 교정 (handoff `0060-steer-flush-boundary`)** | **0059 후속 버그수정(비기능 = Claude 직접 구현) — steer pending 이 입력 즉시 flush 되던 문제.** 0059 가 flush 신호를 producer-pull(`onConsume`/`nextInjectedInput`)로 골랐으나 SDK 는 입력 AsyncIterable 을 eager drain 하므로 pull ≠ turn 경계였다(입력 push 즉시 커밋 버그). 수정: flush 를 입력 push 에서 분리(streaming-input `push`=리터럴 전달만)하고 TurnCoordinator 가 관찰하는 turn 경계 `isSteerFlushBoundary`(최상위 `tool.call.completed` 배치 settle=서브에이전트 취합 / telemetry)에서 `consumeSteerForInput` 구동 — 서브에이전트 내부 도구(`parentToolRunId`)는 부모 경계 아님, 병렬 최상위 도구 전부 settle 후 flush. dead 배선(`onConsume`/`nextInjectedInput`/`consumeInjectedInput`/`onInputConsumed`) 제거. renderer/persist 무변경(호출 시점만 이동). 검증 PASS(r1) 인수 **7/7 충족**(경계 판정 5케이스 단위테스트), 게이트 lint/typecheck(node+web+test)/test **618 passed**(electron 미설치 2 suite 제외), 레이어 경계·순환 0, 신규 의존성 0, IPC 무변경. **파생 이슈 r2(2026-07-02, `90e49f5`)**: 주입 명세 v2(CLI v2.1.198 추출 검증)가 r1 의 user echo 가정을 [V] 확증 → **D1**: steer stdin 메시지 `uuid`+`priority:'next'` 명시 + claude-map `input.echo`(main 내부 variant) 승격 + 커밋 신호를 echo 매칭(uuid 1차/text 폴백)으로 전환(`isSteerFlushBoundary` 경계 근사 제거 — 허위 커밋 race 해소). **D2**: 턴 종료 무조건 flush 제거(소비분만 커밋) + 미소비 pending 은 다음 `chat:send` 이월(steer row 선영속·프롬프트 병합·renderer send 시점 로컬 커밋) — 모델-미전달 텍스트의 committed 영속 해소. D3(취소 비가역)·D4(병합 표시)=Open Question. 게이트 **630 passed**. 사람 실측 대기: echo uuid 보존·C1/C2/C5 실기·carryover 통합·PR 머지. | **완료 (커밋 `7d481ce` + 파생 `90e49f5`)** |
| **concurrency→lifecycle fold + active-turn 리네임 (handoff `0061-concurrency-lifecycle-fold`)** | **0051 §A.2 결정 2 해소(비기능 리팩터 = Claude 직접 구현) — 동시성=라이프사이클 코드 정합.** 0051 이 리스크회피로 유지한 `orchestration/` 코드명을 접는다: `orchestration/concurrency.ts`(순수 leaf, 소유자 Supervisor 는 이미 lifecycle)를 `lifecycle/` 로 이동(+test)·빈 `orchestration/` 제거·import 3/주석 2/`app/src/main/AGENTS.md` L1 목록 갱신·설계서 §A.2 "fold+이름예약"으로 정합. `orchestration/` 이름은 Future handoff/fork/continuity 착지 시 재생성(YAGNI). **verify 선조치(F2, 사용자 결정)**: fold 후에도 클래스/파일명이 `Concurrency`(실체=프로젝트별 active turn 카운터, 객체보관 안 함→`Registry` 부정확)라 `runtime-cap-policy` 와 이름충돌 → `ConcurrencyRegistry`→**`ActiveTurnTracker`**·`concurrency.ts`→`active-turn-tracker.ts`·`ConcurrencyGate`→`ActiveTurnGate`·supervisor getter `activeTurns` 로 리네임(내부 식별자만, **IPC/UX 경계 어휘 `concurrencyEvent`/`concurrencyApi` 보존**=동시실행 경고=경계에선 옳은 말 → IPC 무변경). 검증 PASS(r1) 인수 **5/5 + F2 해소**, 게이트 lint/typecheck(node+web+test)/test **618 passed**(electron 미설치 2 suite 제외, better-sqlite3 Node ABI 재빌드 후), 레이어 경계·순환 0, 신규 의존성 0, 동작·이벤트·DB·IPC·UX 무변경(순수 이동+리네임). 위생: fold 커밋 `188526d` `Agent: codex` trailer=오기(비기능=Claude), 검증 커밋 본문서 정정. 사람 확인 대기: 동시 query 경고 배지 시각검증·PR 머지. | **완료 (fold `188526d` + verify 리네임)** |
| **main feature 수직 슬라이스 재구성 (handoff `0062-main-feature-slices`)** | **main 프로세스 아키텍처 스펙 정렬(비기능 리팩터 = Claude 직접 구현) — feature 수직 슬라이스 + adapters 한정 ports&adapters + 얇은 infra + app composition root.** 0017 의 평면 L0→L3(도메인 17개 L1 한 바구니, `ipc/router.ts` God-object·`send.ts` 612LoC)를 스펙 4계층으로 재배치. **결과 `src/main` 최상위 = `{app,contracts,adapters,features,infra}` + `index.ts`/`env.d.ts`.** ① infra/bus 단일 `turn.event` 파이프라인(history/usage/title/relay 를 구독자로, 등록 순서 usage→history→title→relay SSOT=`app/bootstrap.ts`, critical/격리 구분) ② lifecycle→`contracts/`(turn·bus-events·ports·session-state)+`features/{sessions,chat}` ③ ipc→`app/{bootstrap,chat-turn,handlers,context}`+`features/{history/writer,approvals/coordinator,chat/title-generation}`+`infra/ipc/{handle,send,dto}` ④ **feature 교차 import 0**(boundaries v6 강제·위반 샘플 error 확인 — abort+stall→chat / idle-close→sessions 분리, title→provider-config 포트·registry/supervisor 구조적 포트로 절단) ⑤ 데드코드 제거(prompts 정책 체인·`Installer` 인라인·opencode 변환·RevertManager 등) + 네이밍(`IpcRouter`→`Bootstrap`·`TurnPersistence`→`HistoryWriter`·`CostTracker`→`UsageTracker`·`InteractionBroker`→`ApprovalBroker`·`InflightTurn`→`TurnContext`) ⑥ `src/main/AGENTS.md` 재작성·`app/AGENTS.md` 표 갱신. 검증 PASS(r1) 인수 **16/16**, 게이트 lint/typecheck(node+web+test)/test **640**/build green(electron path 스텁+better-sqlite3 Node ABI 재빌드 후), 레이어/슬라이스 경계·순환 0, 신규 의존성 0, **shared/preload/renderer diff 0**(IPC/DTO/zod 무변경). 이연(비-AC): `contracts/ports` Runtime\* 구조적 중복 통합(의미적 타입 리스크로 후속). 사람 확인 대기: 실환경 채팅/승인/취소/설치/멀티세션 GUI 회귀(순수 이동의 런타임 동일성). | **완료 (C13 `6014124`·C14 `10b6d80`·C15 `6577a6a`·C16 `d3a3b7c`)** |
| **contracts/ports 어댑터 alias 정리 (handoff `0063-ports-adapter-alias`)** | **0062 유일 이연 항목 해소(비기능 = Claude 직접).** `contracts/ports.ts` 의 `Runtime*` 타입이 adapters ports&adapters 정본(`LiveTurn`·`SessionAdapter`·`CompleteRequest`)을 구조적으로 재선언하던 중복을 alias/extends 로 정리: `RuntimeLiveTurn extends LiveTurn`(거버넌스 훅 `markAborted?`·`cancelled?`·`timedOut?` optional 추가 — raw LiveTurn 대입 가능)·`RuntimeCompleteRequest=CompleteRequest`·`RuntimeTitleAdapter=Pick<SessionAdapter,'id'|'complete'>`·`RuntimeSessionAdapter=Pick<SessionAdapter,'id'|'complete'|'sendMessage'|'classifyError'>`(**턴 실행 부분집합** — 설치 수명주기 describe/isInstalled/install 제외로 Interface Segregation 보존). `SessionRuntime.live`=raw `LiveTurn` 로 정확화. ports.ts 51→34 LoC. 구현 중 `RuntimeSessionAdapter=SessionAdapter`(전체) 로 잡았다가 `session-runtime.test` mock(4-메서드) typecheck 실패로 narrower port 확증→`Pick` 정정. 검증 PASS(r1) 인수 **5/5**, 게이트 lint/typecheck(node+web+test)/test **640**/build green, **타입 전용(런타임/IPC/renderer diff 0)**. | **완료 (`b57e7ae`)** |
| **Conversation Continuity — fork/handoff (handoff `0064-conversation-continuity-handoff-fork`)** | **0051 §A.4 Continuity 첫 착지 — fork/handoff·session_lineage·compact 경계/요약 surface.** fork = SDK `forkSession` 위임(전체맥락 lossless·새 id) + 도착 세션 display 복사 + `fork_boundary` 구분선, 클릭=DOM draft 만·첫 보내기 lazy 물질화(취소=no-op). handoff = hermes rebind — 클릭 즉시 물질화 + 자동 메시지 `/compact [핸드오프] …`(main 단일 출처 `buildHandoffMessage`) 첫 프롬프트로 SDK 네이티브 압축이 요약 생성(Orca 자체 요약 파이프라인 0), display 무복사. 공통: `session_lineage`(마이그 0011, CASCADE)·`[분기]/[핸드오프] <원본>` initialTitle(자동 제목 억제)·LineageBanner 출처 배너·continuity draft nav 즉시 노출·`system/compact_boundary`→`session.compacted` 정규화+`compact_boundary` 파트 영속+PostCompact hook 요약 승격(`extractCompactSummary`)+압축 후 도넛 근사(r5). IPC: `chat:send` `forkFrom?/handoffFrom?`(zod 상호배타·새 세션 전용) — 신규 invoke 채널 0. 가드 3종(mid-turn·턴 2회 미만·재클릭) + main 이중 방어. **이력**: 원 구현 r1~r5 는 구(pre-0062 재편) 구조 브랜치에서 번호 0062 로 진행 → main 의 0062/0063 선점으로 **0064 재번호 + 신구조 이식**(`276d4d3` squash — db→infra/db·persist→features/history/writer(도착 물질화=ContinuityArrivalHook 구조적 포트 주입)·send→app/chat-turn·orchestration→**features/orchestration**(신규 슬라이스)). 검증 PASS(r1) 인수 **9/9 + 포팅 정합 6/6**, 게이트 lint/typecheck(3종)/test **675**(90파일) green, 신규 의존성 0. 사람 확인 대기: fork/handoff 실기 E2E(r5 체크 3종·wire log)·핸드오프 템플릿 축약 OQ. | **완료 (`276d4d3`)** |
| **Continuity 후속 — 도넛 수정·nav draft 통일 (handoff `0065-nav-draft-unify-simplify`)** | **r5 도넛 고착 결함 실측·수정 + '새 대화' nav 즉시 노출 통일 + 단순화(비기능 = Claude 직접).** 로컬 실기 실측(CDP+wireLog·실 /compact 3턴)으로 근본 원인 확정: compact 턴 `result.usage` 는 전부 0(요약 요청 사용량은 `modelUsage` 에만 계상) → r5 근사(`input:=output`)가 0 을 산출 → 원장(hasContextTokens)·renderer(contextTokens>0) 게이트 이중 스킵 → 라이브·재로드 도넛이 압축 전 값에 고착. 수정: claude-map 근사 폴백 = **modelUsage 출력 토큰 합(=요약 크기)** + chatReducer `session.compacted` 에서 stale `lastTelemetry` 클리어(잔여 엣지=미측정 표시). 실측 확증: 도넛 8%→1% 즉시 하락·turn_usage 재로드 정합(비용 원장 무손실). '새 대화' 통일: 활성 새-대화 슬롯을 draft 행 경로(`useDraftSessionRows`)로 nav 최상단 즉시 노출 — kebab 無·이탈 시 소멸·물질화 시 DB 행 자연 교체(이탈 생존 라이프사이클은 continuity draft 전용 유지). 단순화: draft 시드 dedup·훅 이름 일반화·SessionRow 메뉴 조건부 kebab. 검증 PASS(r1) 인수 5/5, 게이트 lint/typecheck(3종)/test **677**(90파일) green, 신규 의존성·IPC·DB 0. 사람 확인 대기: 핸드오프 도착 세션 도넛·warn /compact 실기. | **완료** |
| **세션 pending message queue 개념 승격 (handoff `0066-session-pending-message-queue`)** | **steer 브랜치 마이그레이션 판정 + `SteerQueue`→`PendingMessageQueue` 개념 일원화(비기능 = Claude 직접).** 마이그레이션 판정 = **이식할 것 없음** — 대상 브랜치 `claude/handoff-59-steer-queue-kavl7q` 4 커밋은 전부 main 에 patch-equivalent 재착지(`git cherry` 전건 `-`)이고 후속 D3·D4·D5 까지 main 이 앞섬(0062 재구조화로 `features/chat/` 배치 완료). 실작업: ① 세션별 pending message queue 를 1급 개념으로 — 일반 메시지(사용자 턴)=즉시 커밋(chat:send 가 `drainAll` 로 이월 pending 선커밋 후 새 메시지 커밋), steer(어시스턴트 턴)=예약 held→PostToolBatch 게이트 flush→echo 커밋, renderer 간접 관찰(`steer.*`)·취소=held 한정 — 모듈 헤더 명문화 ② `drainForFlush`→`drainAll`·데드 `hasConsumed` 제거 ③ admission `queue`/`steer` 예약 seam 폐기(`AdmissionDecision`=accept\|reject — 0056 seam 의 실체는 explicit chat:steer+본 큐가 대체) ④ 경계 무변경(11파일 전부 `src/main/**`, `takeSteerFlush`·`SteerFlushBatch`·`steer.*` 경계 어휘 보존=0061 F2 선례). 검증 PASS(r1) 인수 **8/8**, 게이트 lint/typecheck(3종)/test **678**(90파일) green, 신규 의존성·IPC·DB 0. 사람 확인 대기: "통로" 해석 승인·steer 실기 회귀. | **완료 (`699a6c1`)** |
| **Long-lived 세션 채널 + 큐 완전 일원화 (handoff `0067-long-lived-session-queue`)** | **세션 = 장수명 query(프로그램 종료/LRU cap 5 축출까지 생존, 매 턴 resume 폐기) + pending message queue 가 모든 사용자 프롬프트의 유일한 통로(구조 전환 = Claude 직접, 사용자 확정 6건).** ① 장수명 채널: `claude.ts` result 미폐쇄+`pushTurn`(라이브 setter+push), SessionRuntime **프레임 demux**(1 스트림 다중 턴 — "1 프레임=1 턴" coordinator 모델 보존), 취소=interrupt(채널 생존, 프로세스 제어=markAborted 소유·턴 신호와 분리), 스폰 실패/채널 사망=resume 콜드 패스, mock=턴-스코프 폴백 ② 거버넌스: cap 5(LRU evict-idle)·IdleCloseTimer 폐기·`ORCA_PERSISTENT_RUNTIME` 게이트 삭제 ③ 큐 일원화: `chat:send` 단일 입구(busy=예약 held — 구 `chat:steer` 흡수, 채널 54→53 / idle=아이템 배치 즉시 flush / 채널 사망 이월=프렐류드 재전달), **커밋=echo 관측 단일 경로**(`commitUserMessage` — send 선영속 폐지), 턴 종료 held 잔여=**자동 연속 턴**, 중단 버튼=held 전량 취소+composer draft 복원, admission 삭제(0056 supersede), clientKey(draft UUID) 세션-이전 큐 키+init rekey ④ renderer pending-first: 전 사용자 메시지 연회색/기울임 pending→`message.committed` 승격(`message.queued/cancelled` 일반화), Composer 단일 send, 자동 연속 턴 inflight 이벤트 파생. 훅 2계층: 주입 제어(PostToolBatch 게이트·UserPromptSubmit 프레임 신호) / 커밋 관측(echo — uuid 보존·판정-후 방출). 검증 PASS(r1 코드) 인수 **12/12**, 게이트 lint/typecheck 3종/test **685**(88파일)/build green, 신규 의존성 0. 사람 확인 대기: wire 실측 4건(result 프레이밍·자동 연속 픽업·interrupt 생존·메모리)+UX 6종. | **완료 (W1 `d124c05`·W2 `f079945`·W3 `90d501b`·W4 `8afe764`)** |
| **턴-시작 낙관 커밋 + wire 계측 (handoff `0068-turn-open-optimistic-commit`)** | **0067 실기 버그픽스(비기능 = Claude 직접) — 랜딩→transcript 전환 지연·턴-시작 메시지의 답변 중 pending 표시/위치 점프.** 근본 원인 = pending-first 를 턴-시작 메시지까지 적용 + `messages` 커밋을 echo 관측까지 지연(0067 "echo sub-second" 전제의 실기 반증). 교정: **턴을 여는 메시지(idle·새 세션) = renderer 낙관 커밋** — `Message.clientId`(=clientRequestId=큐 아이템 id=echo 배치 ids[0]) 상관키로 `message.queued` skip·`message.committed` reducer 멱등 합류·invoke 거부 시 `DROP_UNCOMMITTED_USER` 롤백. steer 예약·핸드오프 자동 메시지는 pending-first 유지(echo 승격). 랜딩 게이트 `!inflight` 이중 방어. **main 커밋 경로(echo 단일·DB 정본)·IPC 스키마 불변.** + echo↔훅 논쟁 판정용 wire 계측: `infra/ipc/wire-log.ts` 분리(electron 비의존) + coordinator `input.echo` 로그 + `makeHookWireTap()`(UserPromptSubmit/PostToolBatch 관측 전용). 검증 PASS(r1 코드) 인수 **8/8**, 게이트 lint/typecheck 3종/test **687**(88파일)/build green, 신규 의존성·IPC 0. 사람 확인 대기: 버그 소멸 실기 + wire 판독(UserPromptSubmit 발화·echo 순서 → 커밋 신호 교체 OQ). | **완료 (impl `a881d77`)** |
| **턴-시작 배치 응답-시작 소비 (handoff `0069-turn-open-consume-on-response`)** | **0068 wire 실측 후속(비기능 = Claude 직접, 사용자 확정 a안) — 커밋 신호의 경계 확정.** 실측: UserPromptSubmit 은 push 프롬프트에 발화하나 uuid 부재+init 이전 발화(훅-커밋 불가), echo 는 uuid 보존하나 모델 출력 시작 이후 도착. 교정: **턴-시작 배치(프롬프트+프렐류드) = 첫 모델 출력(`MODEL_OUTPUT_EVENTS` 앵커)이 소비 증거** — echo 불요, user row 가 항상 첫 영속 어시스턴트 파트보다 먼저 커밋(도구-first 턴 재로드 정렬 창 소멸). **steer = echo uuid 매칭 유지**(응답 진행은 소비 증거 불가, D2). 늦은 echo 무해(consumed 매칭 실패). 부수: mock 턴 user row 영속 성립. **0068 OQ(echo→훅 교체) 종결 — 훅=주입 제어 계층 확정.** 검증 PASS(r1 코드) 인수 **5/5**, 게이트 lint/typecheck 3종/test **693**(88파일)/build green, 신규 의존성·IPC·renderer 0. 사람 확인 대기: 실기 회귀(도구-first 재로드·steer 승격·block 케이스). | **완료 (impl `fc15866`)** |
| **백엔드 아키텍처 문서 런타임 정합 (handoff `0071-backend-arch-runtime-doc-sync`)** | **백엔드 arch 문서를 0062 main 재편·0066~0070 런타임 재작업에 사후 정식화(비기능=문서=Claude 직접, 사용자 2-part 요청).** `runtime-ipc.md` §1 "단일 inflight" 폐기→멀티세션+세션별 pending message queue(held/flushed/consumed, echo 관측 단일 커밋)+장수명 세션 채널(프레임 demux)+RuntimeSupervisor(cap 5 LRU)+TurnCoordinator(2 병렬 sink·retry·settle) 재작성, §2 채널 총계 SSOT 인용(31→53)·`infra/ipc/handle` 헬퍼·`turn.event` 버스 파이프라인(usage→history→title→relay). `overview.md` §3 5-슬라이스 트리·§3.1 Bootstrap 부트·§4 상태표 정정(멀티세션 ✅·`ClaudeAdapter`·`ExtensionBuilder`·마이그레이션 11·Python런타임 행 삭제). `adapters.md`/`terms.md` `CapabilityBuilder`→`ExtensionBuilder`(구명 병기). 참조 문서: `docs/AGENTS.md`(40→53 채널)·`ARCHITECTURE.md`·`arch/frontend/ux-domains.md`(채널 SSOT+상대링크)·`TRD.md`(멀티세션 anchor). 검증 PASS(r1) 인수 **7/7**, 코드 게이트 N/A(문서)·정합 grep 0·링크 유효. 후속(비범위·D1/D2): 빈 레거시 디렉토리 코드 정리·`claude-code`/`ChatEvent`/`OrcaCapabilities` 전역 개명 정합. | **완료 (문서)** |

| **로그인 SSO 게이트 (handoff `0072-login-sso-gate`)** | **로그인 랜딩 + 부팅 게이트 신규 UI(라이브 Codex 부재 → Claude 직접 구현, 사용자 확정 4건 + 추가 UX 3건).** 앱 부팅 시 `RootGate`(라우터 앞) 가 미인증·bypass off 면 사이드바 없는 풀-프레임 `LoginFrame`(슬림 타이틀바 + 중앙 오르카 이미지=제목 대체 + 카드 검정 `SSO로 로그인` 1버튼) 렌더. `features/login` Zustand store — `runSso` **항상 실패**(~800ms throw) → 버튼 위 빨간 메시지, inflight="로그인 중"+`animate-spin`, 성공 경로(`authenticated`+`/new`) 완비(향후 실로직 교체). 디버그 패널 "SSO 로그인" 그룹(신규 `PanelButton`): **bypass 토글**(`Settings.ssoBypass` 영속 — debugMock 비영속이라 이전, store 공유로 게이트 즉시 반응) + **SSO 개발 버튼**(console.log). 경계 해소 = `DebugPanel ssoSection?` 슬롯을 app 레이어 주입. 신규 IPC 채널 0(Settings 필드만). 검증 PASS(r1) 인수 8/8, lint 0·typecheck 3종·test 682·build green. 사람 확인 대기: 실 `npm run dev` 시각/드래그·오르카 원본 교체. | **완료 (push 후)** |

## Future Scope

CI 워크플로우 (`.github/workflows/`), Playwright 테스트, opencode 어댑터(변환기는 구현됨 — 어댑터/라이프사이클만 남음), `V1Captures` 실 구현 (캡처 RAW 보관 + AI 분석), 다국어 (`src/shared/i18n/ko.ts`), 세션 휴지통 30일 보존 (soft delete), 세션 메타 LRU 캐시 cap, 자동 제목 생성 (요약), `pages/RoutinesPage.tsx` placeholder + `/routines` 라우트 등록, **렌더링 P1 잔여**(TelemetryPanel·스트리밍 throttle·auto-scroll pin 완료 — AppMeasured 전체 집계(eventCount/bytesStreamed/errorRate)·DB 영속·세션 통계 화면은 후속 / Streaming backpressure 잔여 — dedup(eventId)·재연결(ReconnectPolicy)·긴 tool 출력 절단(StreamBufferPolicy) / SearchCard·AgentTaskCard 등 OpenCode 카드), **AppMessagePart 후속**(blob 분리 — 무거운 stdout/파일 본문 / file·diff·structured_output 실소스 = OpenCode 어댑터), **Phase 4 잔여 — 멀티세션 UX**(사이드바 배지·세션 탭·세션별 스크롤 기억 — 구조(0011/0013 멀티세션 외피)는 완료, [arch/frontend/state.md](arch/frontend/state.md) §2), Tweaks/Skills/Agents 훅의 store 흡수.
